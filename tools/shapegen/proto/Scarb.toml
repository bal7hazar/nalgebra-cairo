[package]
name = "shapegen_proto"
version = "0.1.0"
edition = "2024_07"
cairo-version = "2.19.4"
publish = false

[dependencies]
simba = "0.2.0"

[dev-dependencies]
fixed = "0.4.0"
nalgebra = { path = "../../../crates/nalgebra" }
nalgebra_testing = { path = "../../../crates/testing" }
snforge_std = "0.61.0"
assert_macros = "2.19.4"

[tool.fmt]
sort-module-level-items = true
max-line-length = 100

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]
