// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `Matrix2`: a statically sized 2x2 matrix (upstream `nalgebra::Matrix2`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `Matrix2Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, MulAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix2x3::Matrix2x3;
use super::matrix_mul::MatrixMul;
use super::vector2::Vector2;

/// A 2x2 matrix. `mRC` is the component at row `R`, column `C`. Fields are declared in column-major
/// order, so `Serde` matches upstream's storage order, while `new` takes its arguments in row-major
/// order like upstream.
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Matrix2<T> {
    pub m11: T,
    pub m21: T,
    pub m12: T,
    pub m22: T,
}

/// Methods of `Matrix2<T>` for any `Real` scalar.
#[generate_trait]
pub impl Matrix2Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix2Trait<T> {
    /// The 2x2 matrix with the given components, in ROW-major order. Upstream: `Matrix2::new`.
    #[inline(always)]
    fn new(m11: T, m12: T, m21: T, m22: T) -> Matrix2<T> {
        Matrix2 { m11, m21, m12, m22 }
    }

    /// The zero 2x2 matrix. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> Matrix2<T> {
        Matrix2 { m11: R::ZERO, m21: R::ZERO, m12: R::ZERO, m22: R::ZERO }
    }

    /// The 2x2 matrix whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Matrix2<T> {
        Matrix2 { m11: elem, m21: elem, m12: elem, m22: elem }
    }

    /// The 2x2 matrix whose components all equal `elem`. Alias of `from_element`. Upstream:
    /// `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Matrix2<T> {
        Matrix2 { m11: elem, m21: elem, m12: elem, m22: elem }
    }

    /// The identity matrix. Upstream: `Matrix2::identity`.
    #[inline(always)]
    fn identity() -> Matrix2<T> {
        Matrix2 { m11: R::ONE, m21: R::ZERO, m12: R::ZERO, m22: R::ONE }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Matrix2<T>, k: T) -> Matrix2<T> {
        Matrix2 { m11: self.m11 * k, m21: self.m21 * k, m12: self.m12 * k, m22: self.m22 * k }
    }

    /// The transpose, a `Matrix2`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Matrix2<T>) -> Matrix2<T> {
        Matrix2 { m11: self.m11, m21: self.m12, m12: self.m21, m22: self.m22 }
    }

    /// `self * v` (one `sum_prod2` per component (one rounding each)): the pre-8.1 name of
    /// `mul_mat` for a vector, same kernel. Panics on overflow. Upstream: `self * v`.
    #[inline(always)]
    fn mul_vec(self: Matrix2<T>, v: Vector2<T>) -> Vector2<T> {
        MatrixMul::mul_mat(self, v)
    }

    /// Dot product (sum of the component-wise products), fused (`Real::sum_prod4`): the exact sum
    /// is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: Matrix2<T>, rhs: Matrix2<T>) -> T {
        R::sum_prod4(self.m11, rhs.m11, self.m21, rhs.m21, self.m12, rhs.m12, self.m22, rhs.m22)
    }

    /// Squared Frobenius norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: Matrix2<T>) -> T {
        R::norm_squared4(self.m11, self.m21, self.m12, self.m22)
    }

    /// `m11 * m22 - m12 * m21` with a single rounding (`diff_prod`): the exact floor of the true
    /// determinant. Panics on overflow. Upstream: `determinant`.
    #[inline(always)]
    fn determinant(self: Matrix2<T>) -> T {
        R::diff_prod(self.m11, self.m22, self.m12, self.m21)
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Matrix2Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Matrix2<T>> {
    #[inline(always)]
    fn add(lhs: Matrix2<T>, rhs: Matrix2<T>) -> Matrix2<T> {
        Matrix2 {
            m11: lhs.m11 + rhs.m11,
            m21: lhs.m21 + rhs.m21,
            m12: lhs.m12 + rhs.m12,
            m22: lhs.m22 + rhs.m22,
        }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Matrix2AddAssign<T, +Add<T>, +Copy<T>, +Drop<T>> of AddAssign<Matrix2<T>, Matrix2<T>> {
    #[inline(always)]
    fn add_assign(ref self: Matrix2<T>, rhs: Matrix2<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Matrix2Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Matrix2<T>> {
    #[inline(always)]
    fn sub(lhs: Matrix2<T>, rhs: Matrix2<T>) -> Matrix2<T> {
        Matrix2 {
            m11: lhs.m11 - rhs.m11,
            m21: lhs.m21 - rhs.m21,
            m12: lhs.m12 - rhs.m12,
            m22: lhs.m22 - rhs.m22,
        }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Matrix2SubAssign<T, +Sub<T>, +Copy<T>, +Drop<T>> of SubAssign<Matrix2<T>, Matrix2<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Matrix2<T>, rhs: Matrix2<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Matrix2Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Matrix2<T>> {
    #[inline(always)]
    fn neg(a: Matrix2<T>) -> Matrix2<T> {
        Matrix2 { m11: -a.m11, m21: -a.m21, m12: -a.m12, m22: -a.m22 }
    }
}

/// `self * rhs`, a `Vector2`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Vector2> for Matrix2`.
pub impl Matrix2MulVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix2<T>, Vector2<T>> {
    type Output = Vector2<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix2<T>, rhs: Vector2<T>) -> Vector2<T> {
        Vector2 {
            x: R::sum_prod2(self.m11, rhs.x, self.m12, rhs.y),
            y: R::sum_prod2(self.m21, rhs.x, self.m22, rhs.y),
        }
    }
}

/// `a * b` (matrix product): one `sum_prod2` per component (one rounding each). Panics on overflow.
/// Upstream: `Mul`.
pub impl Matrix2Mul<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of Mul<Matrix2<T>> {
    fn mul(lhs: Matrix2<T>, rhs: Matrix2<T>) -> Matrix2<T> {
        Matrix2 {
            m11: R::sum_prod2(lhs.m11, rhs.m11, lhs.m12, rhs.m21),
            m21: R::sum_prod2(lhs.m21, rhs.m11, lhs.m22, rhs.m21),
            m12: R::sum_prod2(lhs.m11, rhs.m12, lhs.m12, rhs.m22),
            m22: R::sum_prod2(lhs.m21, rhs.m12, lhs.m22, rhs.m22),
        }
    }
}

/// `a *= b` (matrix product, `a = a * b`). Upstream: `MulAssign`.
pub impl Matrix2MulAssign<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MulAssign<Matrix2<T>, Matrix2<T>> {
    #[inline(always)]
    fn mul_assign(ref self: Matrix2<T>, rhs: Matrix2<T>) {
        self = self * rhs;
    }
}

/// `self * rhs` through `MatrixMul`: the `*` operator, for code generic over the shapes.
pub impl Matrix2MulMatrix2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix2<T>, Matrix2<T>> {
    type Output = Matrix2<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix2<T>, rhs: Matrix2<T>) -> Matrix2<T> {
        self * rhs
    }
}

/// `self * rhs`, a `Matrix2x3`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Matrix2x3> for Matrix2`.
pub impl Matrix2MulMatrix2x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix2<T>, Matrix2x3<T>> {
    type Output = Matrix2x3<T>;
    fn mul_mat(self: Matrix2<T>, rhs: Matrix2x3<T>) -> Matrix2x3<T> {
        Matrix2x3 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m13: R::sum_prod2(self.m11, rhs.m13, self.m12, rhs.m23),
            m23: R::sum_prod2(self.m21, rhs.m13, self.m22, rhs.m23),
        }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 4`. Upstream: `Index<usize>`.
pub impl Matrix2IndexLinear<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix2<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix2<T>, index: usize) -> T {
        match index {
            0 => *self.m11,
            1 => *self.m21,
            2 => *self.m12,
            3 => *self.m22,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 2` or `j >= 2`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 2` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl Matrix2Index<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix2<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix2<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.m11,
                1 => *self.m21,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.m12,
                1 => *self.m22,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
