// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `Matrix3`: a statically sized 3x3 matrix (upstream `nalgebra::Matrix3`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `Matrix3Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, MulAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix3x2::Matrix3x2;
use super::matrix_mul::MatrixMul;
use super::vector3::Vector3;

/// A 3x3 matrix. `mRC` is the component at row `R`, column `C`. Fields are declared in column-major
/// order, so `Serde` matches upstream's storage order, while `new` takes its arguments in row-major
/// order like upstream.
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Matrix3<T> {
    pub m11: T,
    pub m21: T,
    pub m31: T,
    pub m12: T,
    pub m22: T,
    pub m32: T,
    pub m13: T,
    pub m23: T,
    pub m33: T,
}

/// Methods of `Matrix3<T>` for any `Real` scalar.
#[generate_trait]
pub impl Matrix3Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix3Trait<T> {
    /// The 3x3 matrix with the given components, in ROW-major order. Upstream: `Matrix3::new`.
    #[inline(always)]
    fn new(m11: T, m12: T, m13: T, m21: T, m22: T, m23: T, m31: T, m32: T, m33: T) -> Matrix3<T> {
        Matrix3 { m11, m21, m31, m12, m22, m32, m13, m23, m33 }
    }

    /// The zero 3x3 matrix. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> Matrix3<T> {
        Matrix3 {
            m11: R::ZERO,
            m21: R::ZERO,
            m31: R::ZERO,
            m12: R::ZERO,
            m22: R::ZERO,
            m32: R::ZERO,
            m13: R::ZERO,
            m23: R::ZERO,
            m33: R::ZERO,
        }
    }

    /// The 3x3 matrix whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Matrix3<T> {
        Matrix3 {
            m11: elem,
            m21: elem,
            m31: elem,
            m12: elem,
            m22: elem,
            m32: elem,
            m13: elem,
            m23: elem,
            m33: elem,
        }
    }

    /// The 3x3 matrix whose components all equal `elem`. Alias of `from_element`. Upstream:
    /// `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Matrix3<T> {
        Matrix3 {
            m11: elem,
            m21: elem,
            m31: elem,
            m12: elem,
            m22: elem,
            m32: elem,
            m13: elem,
            m23: elem,
            m33: elem,
        }
    }

    /// The identity matrix. Upstream: `Matrix3::identity`.
    #[inline(always)]
    fn identity() -> Matrix3<T> {
        Matrix3 {
            m11: R::ONE,
            m21: R::ZERO,
            m31: R::ZERO,
            m12: R::ZERO,
            m22: R::ONE,
            m32: R::ZERO,
            m13: R::ZERO,
            m23: R::ZERO,
            m33: R::ONE,
        }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Matrix3<T>, k: T) -> Matrix3<T> {
        Matrix3 {
            m11: self.m11 * k,
            m21: self.m21 * k,
            m31: self.m31 * k,
            m12: self.m12 * k,
            m22: self.m22 * k,
            m32: self.m32 * k,
            m13: self.m13 * k,
            m23: self.m23 * k,
            m33: self.m33 * k,
        }
    }

    /// The transpose, a `Matrix3`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Matrix3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: self.m11,
            m21: self.m12,
            m31: self.m13,
            m12: self.m21,
            m22: self.m22,
            m32: self.m23,
            m13: self.m31,
            m23: self.m32,
            m33: self.m33,
        }
    }

    /// `self * v` (one `sum_prod3` per component (one rounding each)): the pre-8.1 name of
    /// `mul_mat` for a vector, same kernel. Panics on overflow. Upstream: `self * v`.
    #[inline(always)]
    fn mul_vec(self: Matrix3<T>, v: Vector3<T>) -> Vector3<T> {
        MatrixMul::mul_mat(self, v)
    }

    /// Dot product (sum of the component-wise products), fused (the `Real::Wide` accumulator): the
    /// exact sum is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: Matrix3<T>, rhs: Matrix3<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, rhs.m11);
        let w = R::wide_add_prod(w, self.m21, rhs.m21);
        let w = R::wide_add_prod(w, self.m31, rhs.m31);
        let w = R::wide_add_prod(w, self.m12, rhs.m12);
        let w = R::wide_add_prod(w, self.m22, rhs.m22);
        let w = R::wide_add_prod(w, self.m32, rhs.m32);
        let w = R::wide_add_prod(w, self.m13, rhs.m13);
        let w = R::wide_add_prod(w, self.m23, rhs.m23);
        R::wide_rescale(R::wide_add_prod(w, self.m33, rhs.m33))
    }

    /// Squared Frobenius norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: Matrix3<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, self.m11);
        let w = R::wide_add_prod(w, self.m21, self.m21);
        let w = R::wide_add_prod(w, self.m31, self.m31);
        let w = R::wide_add_prod(w, self.m12, self.m12);
        let w = R::wide_add_prod(w, self.m22, self.m22);
        let w = R::wide_add_prod(w, self.m32, self.m32);
        let w = R::wide_add_prod(w, self.m13, self.m13);
        let w = R::wide_add_prod(w, self.m23, self.m23);
        R::wide_rescale(R::wide_add_prod(w, self.m33, self.m33))
    }

    /// The determinant, by cofactor expansion along the first row: 3 `diff_prod` (2x2 minors,
    /// one rounding each) then one `sum_prod3` (second rounding). The error against the exact
    /// floor is at most `|m11| + |m12| + |m13| + 1` ulp (values, not raw: 4 ulp for a rotation).
    /// Panics on overflow. Upstream: `determinant`.
    fn determinant(self: Matrix3<T>) -> T {
        R::sum_prod3(
            self.m11,
            R::diff_prod(self.m22, self.m33, self.m23, self.m32),
            self.m12,
            R::diff_prod(self.m23, self.m31, self.m21, self.m33),
            self.m13,
            R::diff_prod(self.m21, self.m32, self.m22, self.m31),
        )
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Matrix3Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Matrix3<T>> {
    #[inline(always)]
    fn add(lhs: Matrix3<T>, rhs: Matrix3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: lhs.m11 + rhs.m11,
            m21: lhs.m21 + rhs.m21,
            m31: lhs.m31 + rhs.m31,
            m12: lhs.m12 + rhs.m12,
            m22: lhs.m22 + rhs.m22,
            m32: lhs.m32 + rhs.m32,
            m13: lhs.m13 + rhs.m13,
            m23: lhs.m23 + rhs.m23,
            m33: lhs.m33 + rhs.m33,
        }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Matrix3AddAssign<T, +Add<T>, +Copy<T>, +Drop<T>> of AddAssign<Matrix3<T>, Matrix3<T>> {
    #[inline(always)]
    fn add_assign(ref self: Matrix3<T>, rhs: Matrix3<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Matrix3Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Matrix3<T>> {
    #[inline(always)]
    fn sub(lhs: Matrix3<T>, rhs: Matrix3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: lhs.m11 - rhs.m11,
            m21: lhs.m21 - rhs.m21,
            m31: lhs.m31 - rhs.m31,
            m12: lhs.m12 - rhs.m12,
            m22: lhs.m22 - rhs.m22,
            m32: lhs.m32 - rhs.m32,
            m13: lhs.m13 - rhs.m13,
            m23: lhs.m23 - rhs.m23,
            m33: lhs.m33 - rhs.m33,
        }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Matrix3SubAssign<T, +Sub<T>, +Copy<T>, +Drop<T>> of SubAssign<Matrix3<T>, Matrix3<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Matrix3<T>, rhs: Matrix3<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Matrix3Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Matrix3<T>> {
    #[inline(always)]
    fn neg(a: Matrix3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: -a.m11,
            m21: -a.m21,
            m31: -a.m31,
            m12: -a.m12,
            m22: -a.m22,
            m32: -a.m32,
            m13: -a.m13,
            m23: -a.m23,
            m33: -a.m33,
        }
    }
}

/// `self * rhs`, a `Vector3`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Vector3> for Matrix3`.
pub impl Matrix3MulVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix3<T>, Vector3<T>> {
    type Output = Vector3<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix3<T>, rhs: Vector3<T>) -> Vector3<T> {
        Vector3 {
            x: R::sum_prod3(self.m11, rhs.x, self.m12, rhs.y, self.m13, rhs.z),
            y: R::sum_prod3(self.m21, rhs.x, self.m22, rhs.y, self.m23, rhs.z),
            z: R::sum_prod3(self.m31, rhs.x, self.m32, rhs.y, self.m33, rhs.z),
        }
    }
}

/// `self * rhs`, a `Matrix3x2`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Matrix3x2> for Matrix3`.
pub impl Matrix3MulMatrix3x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix3<T>, Matrix3x2<T>> {
    type Output = Matrix3x2<T>;
    fn mul_mat(self: Matrix3<T>, rhs: Matrix3x2<T>) -> Matrix3x2<T> {
        Matrix3x2 {
            m11: R::sum_prod3(self.m11, rhs.m11, self.m12, rhs.m21, self.m13, rhs.m31),
            m21: R::sum_prod3(self.m21, rhs.m11, self.m22, rhs.m21, self.m23, rhs.m31),
            m31: R::sum_prod3(self.m31, rhs.m11, self.m32, rhs.m21, self.m33, rhs.m31),
            m12: R::sum_prod3(self.m11, rhs.m12, self.m12, rhs.m22, self.m13, rhs.m32),
            m22: R::sum_prod3(self.m21, rhs.m12, self.m22, rhs.m22, self.m23, rhs.m32),
            m32: R::sum_prod3(self.m31, rhs.m12, self.m32, rhs.m22, self.m33, rhs.m32),
        }
    }
}

/// `a * b` (matrix product): one `sum_prod3` per component (one rounding each). Panics on overflow.
/// Upstream: `Mul`.
pub impl Matrix3Mul<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of Mul<Matrix3<T>> {
    fn mul(lhs: Matrix3<T>, rhs: Matrix3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: R::sum_prod3(lhs.m11, rhs.m11, lhs.m12, rhs.m21, lhs.m13, rhs.m31),
            m21: R::sum_prod3(lhs.m21, rhs.m11, lhs.m22, rhs.m21, lhs.m23, rhs.m31),
            m31: R::sum_prod3(lhs.m31, rhs.m11, lhs.m32, rhs.m21, lhs.m33, rhs.m31),
            m12: R::sum_prod3(lhs.m11, rhs.m12, lhs.m12, rhs.m22, lhs.m13, rhs.m32),
            m22: R::sum_prod3(lhs.m21, rhs.m12, lhs.m22, rhs.m22, lhs.m23, rhs.m32),
            m32: R::sum_prod3(lhs.m31, rhs.m12, lhs.m32, rhs.m22, lhs.m33, rhs.m32),
            m13: R::sum_prod3(lhs.m11, rhs.m13, lhs.m12, rhs.m23, lhs.m13, rhs.m33),
            m23: R::sum_prod3(lhs.m21, rhs.m13, lhs.m22, rhs.m23, lhs.m23, rhs.m33),
            m33: R::sum_prod3(lhs.m31, rhs.m13, lhs.m32, rhs.m23, lhs.m33, rhs.m33),
        }
    }
}

/// `a *= b` (matrix product, `a = a * b`). Upstream: `MulAssign`.
pub impl Matrix3MulAssign<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MulAssign<Matrix3<T>, Matrix3<T>> {
    #[inline(always)]
    fn mul_assign(ref self: Matrix3<T>, rhs: Matrix3<T>) {
        self = self * rhs;
    }
}

/// `self * rhs` through `MatrixMul`: the `*` operator, for code generic over the shapes.
pub impl Matrix3MulMatrix3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix3<T>, Matrix3<T>> {
    type Output = Matrix3<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix3<T>, rhs: Matrix3<T>) -> Matrix3<T> {
        self * rhs
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 9`. Upstream: `Index<usize>`.
pub impl Matrix3IndexLinear<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix3<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix3<T>, index: usize) -> T {
        match index {
            0 => *self.m11,
            1 => *self.m21,
            2 => *self.m31,
            3 => *self.m12,
            4 => *self.m22,
            5 => *self.m32,
            6 => *self.m13,
            7 => *self.m23,
            8 => *self.m33,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 3` or `j >= 3`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 3` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl Matrix3Index<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix3<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix3<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.m11,
                1 => *self.m21,
                2 => *self.m31,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.m12,
                1 => *self.m22,
                2 => *self.m32,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match i {
                0 => *self.m13,
                1 => *self.m23,
                2 => *self.m33,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
