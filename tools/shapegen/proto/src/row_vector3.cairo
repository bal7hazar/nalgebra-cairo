// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `RowVector3`: a statically sized 3-dimensional row vector (upstream `nalgebra::RowVector3`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `RowVector3Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix1::Matrix1;
use super::matrix3::Matrix3;
use super::matrix3x2::Matrix3x2;
use super::matrix_mul::MatrixMul;
use super::row_vector2::RowVector2;
use super::vector3::Vector3;

/// A 3-dimensional row vector. Components are named like upstream's `Deref` targets (`x, y, z, w,
/// a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct RowVector3<T> {
    pub x: T,
    pub y: T,
    pub z: T,
}

/// Upstream alias `Matrix1x3`: the same type as `RowVector3`.
pub type Matrix1x3<T> = RowVector3<T>;

/// Methods of `RowVector3<T>` for any `Real` scalar.
#[generate_trait]
pub impl RowVector3Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector3Trait<T> {
    /// The 3-dimensional row vector with the given components. Upstream: `RowVector3::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T) -> RowVector3<T> {
        RowVector3 { x, y, z }
    }

    /// The zero 3-dimensional row vector. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> RowVector3<T> {
        RowVector3 { x: R::ZERO, y: R::ZERO, z: R::ZERO }
    }

    /// The 3-dimensional row vector whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> RowVector3<T> {
        RowVector3 { x: elem, y: elem, z: elem }
    }

    /// The 3-dimensional row vector whose components all equal `elem`. Alias of `from_element`.
    /// Upstream: `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> RowVector3<T> {
        RowVector3 { x: elem, y: elem, z: elem }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: RowVector3<T>, k: T) -> RowVector3<T> {
        RowVector3 { x: self.x * k, y: self.y * k, z: self.z * k }
    }

    /// The transpose, a `Vector3`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: RowVector3<T>) -> Vector3<T> {
        Vector3 { x: self.x, y: self.y, z: self.z }
    }

    /// Dot product (sum of the component-wise products), fused (`Real::sum_prod3`): the exact sum
    /// is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: RowVector3<T>, rhs: RowVector3<T>) -> T {
        R::sum_prod3(self.x, rhs.x, self.y, rhs.y, self.z, rhs.z)
    }

    /// Squared Euclidean norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: RowVector3<T>) -> T {
        R::norm_squared3(self.x, self.y, self.z)
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl RowVector3Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<RowVector3<T>> {
    #[inline(always)]
    fn add(lhs: RowVector3<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: lhs.x + rhs.x, y: lhs.y + rhs.y, z: lhs.z + rhs.z }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl RowVector3AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<RowVector3<T>, RowVector3<T>> {
    #[inline(always)]
    fn add_assign(ref self: RowVector3<T>, rhs: RowVector3<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl RowVector3Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<RowVector3<T>> {
    #[inline(always)]
    fn sub(lhs: RowVector3<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: lhs.x - rhs.x, y: lhs.y - rhs.y, z: lhs.z - rhs.z }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl RowVector3SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<RowVector3<T>, RowVector3<T>> {
    #[inline(always)]
    fn sub_assign(ref self: RowVector3<T>, rhs: RowVector3<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl RowVector3Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<RowVector3<T>> {
    #[inline(always)]
    fn neg(a: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: -a.x, y: -a.y, z: -a.z }
    }
}

/// `self * rhs`, a `Matrix1`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Vector3> for RowVector3`.
pub impl RowVector3MulVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Vector3<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Vector3<T>) -> Matrix1<T> {
        Matrix1 { x: R::sum_prod3(self.x, rhs.x, self.y, rhs.y, self.z, rhs.z) }
    }
}

/// `self * rhs`, a `RowVector2`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Matrix3x2> for RowVector3`.
pub impl RowVector3MulMatrix3x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3x2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3x2<T>) -> RowVector2<T> {
        RowVector2 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
        }
    }
}

/// `self * rhs`, a `RowVector3`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Matrix3> for RowVector3`.
pub impl RowVector3MulMatrix3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3<T>) -> RowVector3<T> {
        RowVector3 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
            z: R::sum_prod3(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33),
        }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 3`. Upstream: `Index<usize>`.
pub impl RowVector3IndexLinear<
    T, +Copy<T>, +Drop<T>,
> of core::ops::IndexView<RowVector3<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @RowVector3<T>, index: usize) -> T {
        match index {
            0 => *self.x,
            1 => *self.y,
            2 => *self.z,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 1` or `j >= 3`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 1` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl RowVector3Index<
    T, +Copy<T>, +Drop<T>,
> of core::ops::IndexView<RowVector3<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @RowVector3<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.x,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.y,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match i {
                0 => *self.z,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
