// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `RowVector6`: a statically sized 6-dimensional row vector (upstream `nalgebra::RowVector6`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `RowVector6Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::kernels::Fused;
use super::matrix1::Matrix1;
use super::matrix_mul::MatrixMul;
use super::vector6::Vector6;

/// A 6-dimensional row vector. Components are named like upstream's `Deref` targets (`x, y, z, w,
/// a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct RowVector6<T> {
    pub x: T,
    pub y: T,
    pub z: T,
    pub w: T,
    pub a: T,
    pub b: T,
}

/// Upstream alias `Matrix1x6`: the same type as `RowVector6`.
pub type Matrix1x6<T> = RowVector6<T>;

/// Methods of `RowVector6<T>` for any `Real` scalar.
#[generate_trait]
pub impl RowVector6Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector6Trait<T> {
    /// The 6-dimensional row vector with the given components. Upstream: `RowVector6::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T, w: T, a: T, b: T) -> RowVector6<T> {
        RowVector6 { x, y, z, w, a, b }
    }

    /// The zero 6-dimensional row vector. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> RowVector6<T> {
        RowVector6 { x: R::ZERO, y: R::ZERO, z: R::ZERO, w: R::ZERO, a: R::ZERO, b: R::ZERO }
    }

    /// The 6-dimensional row vector whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> RowVector6<T> {
        RowVector6 { x: elem, y: elem, z: elem, w: elem, a: elem, b: elem }
    }

    /// The 6-dimensional row vector whose components all equal `elem`. Alias of `from_element`.
    /// Upstream: `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> RowVector6<T> {
        RowVector6 { x: elem, y: elem, z: elem, w: elem, a: elem, b: elem }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: RowVector6<T>, k: T) -> RowVector6<T> {
        RowVector6 {
            x: self.x * k,
            y: self.y * k,
            z: self.z * k,
            w: self.w * k,
            a: self.a * k,
            b: self.b * k,
        }
    }

    /// The transpose, a `Vector6`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: RowVector6<T>) -> Vector6<T> {
        Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }
    }

    /// Dot product (sum of the component-wise products), fused (the `Real::Wide` accumulator): the
    /// exact sum is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: RowVector6<T>, rhs: RowVector6<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.x, rhs.x);
        let w = R::wide_add_prod(w, self.y, rhs.y);
        let w = R::wide_add_prod(w, self.z, rhs.z);
        let w = R::wide_add_prod(w, self.w, rhs.w);
        let w = R::wide_add_prod(w, self.a, rhs.a);
        R::wide_rescale(R::wide_add_prod(w, self.b, rhs.b))
    }

    /// Squared Euclidean norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: RowVector6<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.x, self.x);
        let w = R::wide_add_prod(w, self.y, self.y);
        let w = R::wide_add_prod(w, self.z, self.z);
        let w = R::wide_add_prod(w, self.w, self.w);
        let w = R::wide_add_prod(w, self.a, self.a);
        R::wide_rescale(R::wide_add_prod(w, self.b, self.b))
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl RowVector6Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<RowVector6<T>> {
    #[inline(always)]
    fn add(lhs: RowVector6<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: lhs.x + rhs.x,
            y: lhs.y + rhs.y,
            z: lhs.z + rhs.z,
            w: lhs.w + rhs.w,
            a: lhs.a + rhs.a,
            b: lhs.b + rhs.b,
        }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl RowVector6AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn add_assign(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl RowVector6Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<RowVector6<T>> {
    #[inline(always)]
    fn sub(lhs: RowVector6<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: lhs.x - rhs.x,
            y: lhs.y - rhs.y,
            z: lhs.z - rhs.z,
            w: lhs.w - rhs.w,
            a: lhs.a - rhs.a,
            b: lhs.b - rhs.b,
        }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl RowVector6SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn sub_assign(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl RowVector6Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<RowVector6<T>> {
    #[inline(always)]
    fn neg(a: RowVector6<T>) -> RowVector6<T> {
        RowVector6 { x: -a.x, y: -a.y, z: -a.z, w: -a.w, a: -a.a, b: -a.b }
    }
}

/// `self * rhs`, a `Matrix1`: the 6 products of each component accumulated in `Real::Wide`,
/// rescaled once. Panics on overflow. Upstream: `Mul<Vector6> for RowVector6`.
pub impl RowVector6MulVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Vector6<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector6<T>, rhs: Vector6<T>) -> Matrix1<T> {
        Matrix1 {
            x: Fused::sum_prod6(
                self.x,
                rhs.x,
                self.y,
                rhs.y,
                self.z,
                rhs.z,
                self.w,
                rhs.w,
                self.a,
                rhs.a,
                self.b,
                rhs.b,
            ),
        }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 6`. Upstream: `Index<usize>`.
pub impl RowVector6IndexLinear<
    T, +Copy<T>, +Drop<T>,
> of core::ops::IndexView<RowVector6<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @RowVector6<T>, index: usize) -> T {
        match index {
            0 => *self.x,
            1 => *self.y,
            2 => *self.z,
            3 => *self.w,
            4 => *self.a,
            5 => *self.b,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 1` or `j >= 6`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 1` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl RowVector6Index<
    T, +Copy<T>, +Drop<T>,
> of core::ops::IndexView<RowVector6<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @RowVector6<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.x,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.y,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match i {
                0 => *self.z,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match i {
                0 => *self.w,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            4 => match i {
                0 => *self.a,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            5 => match i {
                0 => *self.b,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
