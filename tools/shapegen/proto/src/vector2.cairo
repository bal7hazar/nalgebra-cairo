// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `Vector2`: a statically sized 2-dimensional column vector (upstream `nalgebra::Vector2`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `Vector2Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix1::Matrix1;
use super::matrix2::Matrix2;
use super::matrix2x3::Matrix2x3;
use super::matrix_mul::MatrixMul;
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;

/// A 2-dimensional column vector. Components are named like upstream's `Deref` targets (`x, y, z,
/// w, a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Vector2<T> {
    pub x: T,
    pub y: T,
}

/// Upstream alias `Matrix2x1`: the same type as `Vector2`.
pub type Matrix2x1<T> = Vector2<T>;

/// Methods of `Vector2<T>` for any `Real` scalar.
#[generate_trait]
pub impl Vector2Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Vector2Trait<T> {
    /// The 2-dimensional column vector with the given components. Upstream: `Vector2::new`.
    #[inline(always)]
    fn new(x: T, y: T) -> Vector2<T> {
        Vector2 { x, y }
    }

    /// The zero 2-dimensional column vector. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> Vector2<T> {
        Vector2 { x: R::ZERO, y: R::ZERO }
    }

    /// The 2-dimensional column vector whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Vector2<T> {
        Vector2 { x: elem, y: elem }
    }

    /// The 2-dimensional column vector whose components all equal `elem`. Alias of `from_element`.
    /// Upstream: `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Vector2<T> {
        Vector2 { x: elem, y: elem }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Vector2<T>, k: T) -> Vector2<T> {
        Vector2 { x: self.x * k, y: self.y * k }
    }

    /// The transpose, a `RowVector2`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Vector2<T>) -> RowVector2<T> {
        RowVector2 { x: self.x, y: self.y }
    }

    /// Dot product (sum of the component-wise products), fused (`Real::sum_prod2`): the exact sum
    /// is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: Vector2<T>, rhs: Vector2<T>) -> T {
        R::sum_prod2(self.x, rhs.x, self.y, rhs.y)
    }

    /// Squared Euclidean norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: Vector2<T>) -> T {
        R::norm_squared2(self.x, self.y)
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Vector2Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Vector2<T>> {
    #[inline(always)]
    fn add(lhs: Vector2<T>, rhs: Vector2<T>) -> Vector2<T> {
        Vector2 { x: lhs.x + rhs.x, y: lhs.y + rhs.y }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Vector2AddAssign<T, +Add<T>, +Copy<T>, +Drop<T>> of AddAssign<Vector2<T>, Vector2<T>> {
    #[inline(always)]
    fn add_assign(ref self: Vector2<T>, rhs: Vector2<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Vector2Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Vector2<T>> {
    #[inline(always)]
    fn sub(lhs: Vector2<T>, rhs: Vector2<T>) -> Vector2<T> {
        Vector2 { x: lhs.x - rhs.x, y: lhs.y - rhs.y }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Vector2SubAssign<T, +Sub<T>, +Copy<T>, +Drop<T>> of SubAssign<Vector2<T>, Vector2<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Vector2<T>, rhs: Vector2<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Vector2Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Vector2<T>> {
    #[inline(always)]
    fn neg(a: Vector2<T>) -> Vector2<T> {
        Vector2 { x: -a.x, y: -a.y }
    }
}

/// `self * rhs`, a `Vector2`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<Vector1> for Vector2`.
pub impl Vector2MulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Vector2<T>, Matrix1<T>> {
    type Output = Vector2<T>;
    #[inline(always)]
    fn mul_mat(self: Vector2<T>, rhs: Matrix1<T>) -> Vector2<T> {
        Vector2 { x: self.x * rhs.x, y: self.y * rhs.x }
    }
}

/// `self * rhs`, a `Matrix2`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<RowVector2> for Vector2`.
pub impl Vector2MulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Vector2<T>, RowVector2<T>> {
    type Output = Matrix2<T>;
    fn mul_mat(self: Vector2<T>, rhs: RowVector2<T>) -> Matrix2<T> {
        Matrix2 {
            m11: self.x * rhs.x, m21: self.y * rhs.x, m12: self.x * rhs.y, m22: self.y * rhs.y,
        }
    }
}

/// `self * rhs`, a `Matrix2x3`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<RowVector3> for Vector2`.
pub impl Vector2MulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Vector2<T>, RowVector3<T>> {
    type Output = Matrix2x3<T>;
    fn mul_mat(self: Vector2<T>, rhs: RowVector3<T>) -> Matrix2x3<T> {
        Matrix2x3 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
        }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 2`. Upstream: `Index<usize>`.
pub impl Vector2IndexLinear<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Vector2<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Vector2<T>, index: usize) -> T {
        match index {
            0 => *self.x,
            1 => *self.y,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 2` or `j >= 1`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 2` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl Vector2Index<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Vector2<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Vector2<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.x,
                1 => *self.y,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
