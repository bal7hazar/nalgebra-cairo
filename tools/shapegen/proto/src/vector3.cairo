// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `Vector3`: a statically sized 3-dimensional column vector (upstream `nalgebra::Vector3`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `Vector3Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix1::Matrix1;
use super::matrix3::Matrix3;
use super::matrix3x2::Matrix3x2;
use super::matrix_mul::MatrixMul;
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;

/// A 3-dimensional column vector. Components are named like upstream's `Deref` targets (`x, y, z,
/// w, a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Vector3<T> {
    pub x: T,
    pub y: T,
    pub z: T,
}

/// Upstream alias `Matrix3x1`: the same type as `Vector3`.
pub type Matrix3x1<T> = Vector3<T>;

/// Methods of `Vector3<T>` for any `Real` scalar.
#[generate_trait]
pub impl Vector3Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Vector3Trait<T> {
    /// The 3-dimensional column vector with the given components. Upstream: `Vector3::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T) -> Vector3<T> {
        Vector3 { x, y, z }
    }

    /// The zero 3-dimensional column vector. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> Vector3<T> {
        Vector3 { x: R::ZERO, y: R::ZERO, z: R::ZERO }
    }

    /// The 3-dimensional column vector whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Vector3<T> {
        Vector3 { x: elem, y: elem, z: elem }
    }

    /// The 3-dimensional column vector whose components all equal `elem`. Alias of `from_element`.
    /// Upstream: `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Vector3<T> {
        Vector3 { x: elem, y: elem, z: elem }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Vector3<T>, k: T) -> Vector3<T> {
        Vector3 { x: self.x * k, y: self.y * k, z: self.z * k }
    }

    /// The transpose, a `RowVector3`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Vector3<T>) -> RowVector3<T> {
        RowVector3 { x: self.x, y: self.y, z: self.z }
    }

    /// Dot product (sum of the component-wise products), fused (`Real::sum_prod3`): the exact sum
    /// is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: Vector3<T>, rhs: Vector3<T>) -> T {
        R::sum_prod3(self.x, rhs.x, self.y, rhs.y, self.z, rhs.z)
    }

    /// Squared Euclidean norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: Vector3<T>) -> T {
        R::norm_squared3(self.x, self.y, self.z)
    }

    /// Cross product, each component fused (`Real::diff_prod`): the exact difference of products
    /// is floored once. Panics on overflow of a result component. Upstream: `cross`.
    #[inline(always)]
    fn cross(self: Vector3<T>, rhs: Vector3<T>) -> Vector3<T> {
        Vector3 {
            x: R::diff_prod(self.y, rhs.z, self.z, rhs.y),
            y: R::diff_prod(self.z, rhs.x, self.x, rhs.z),
            z: R::diff_prod(self.x, rhs.y, self.y, rhs.x),
        }
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Vector3Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Vector3<T>> {
    #[inline(always)]
    fn add(lhs: Vector3<T>, rhs: Vector3<T>) -> Vector3<T> {
        Vector3 { x: lhs.x + rhs.x, y: lhs.y + rhs.y, z: lhs.z + rhs.z }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Vector3AddAssign<T, +Add<T>, +Copy<T>, +Drop<T>> of AddAssign<Vector3<T>, Vector3<T>> {
    #[inline(always)]
    fn add_assign(ref self: Vector3<T>, rhs: Vector3<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Vector3Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Vector3<T>> {
    #[inline(always)]
    fn sub(lhs: Vector3<T>, rhs: Vector3<T>) -> Vector3<T> {
        Vector3 { x: lhs.x - rhs.x, y: lhs.y - rhs.y, z: lhs.z - rhs.z }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Vector3SubAssign<T, +Sub<T>, +Copy<T>, +Drop<T>> of SubAssign<Vector3<T>, Vector3<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Vector3<T>, rhs: Vector3<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Vector3Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Vector3<T>> {
    #[inline(always)]
    fn neg(a: Vector3<T>) -> Vector3<T> {
        Vector3 { x: -a.x, y: -a.y, z: -a.z }
    }
}

/// `self * rhs`, a `Vector3`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<Vector1> for Vector3`.
pub impl Vector3MulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Vector3<T>, Matrix1<T>> {
    type Output = Vector3<T>;
    #[inline(always)]
    fn mul_mat(self: Vector3<T>, rhs: Matrix1<T>) -> Vector3<T> {
        Vector3 { x: self.x * rhs.x, y: self.y * rhs.x, z: self.z * rhs.x }
    }
}

/// `self * rhs`, a `Matrix3x2`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<RowVector2> for Vector3`.
pub impl Vector3MulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Vector3<T>, RowVector2<T>> {
    type Output = Matrix3x2<T>;
    fn mul_mat(self: Vector3<T>, rhs: RowVector2<T>) -> Matrix3x2<T> {
        Matrix3x2 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
        }
    }
}

/// `self * rhs`, a `Matrix3`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<RowVector3> for Vector3`.
pub impl Vector3MulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Vector3<T>, RowVector3<T>> {
    type Output = Matrix3<T>;
    fn mul_mat(self: Vector3<T>, rhs: RowVector3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
        }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 3`. Upstream: `Index<usize>`.
pub impl Vector3IndexLinear<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Vector3<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Vector3<T>, index: usize) -> T {
        match index {
            0 => *self.x,
            1 => *self.y,
            2 => *self.z,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 3` or `j >= 1`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 3` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl Vector3Index<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Vector3<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Vector3<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.x,
                1 => *self.y,
                2 => *self.z,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
