// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `Matrix1`: a statically sized 1x1 matrix (upstream `nalgebra::Vector1`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `Matrix1Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, MulAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix_mul::MatrixMul;
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;

/// A 1x1 matrix. Components are named like upstream's `Deref` targets (`x, y, z, w, a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Matrix1<T> {
    pub x: T,
}

/// Upstream alias `Vector1`: the same type as `Matrix1`.
pub type Vector1<T> = Matrix1<T>;

/// Upstream alias `RowVector1`: the same type as `Matrix1`.
pub type RowVector1<T> = Matrix1<T>;

/// Methods of `Matrix1<T>` for any `Real` scalar.
#[generate_trait]
pub impl Matrix1Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix1Trait<T> {
    /// The 1x1 matrix with the given components. Upstream: `Vector1::new`.
    #[inline(always)]
    fn new(x: T) -> Matrix1<T> {
        Matrix1 { x }
    }

    /// The zero 1x1 matrix. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> Matrix1<T> {
        Matrix1 { x: R::ZERO }
    }

    /// The 1x1 matrix whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Matrix1<T> {
        Matrix1 { x: elem }
    }

    /// The 1x1 matrix whose components all equal `elem`. Alias of `from_element`. Upstream:
    /// `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Matrix1<T> {
        Matrix1 { x: elem }
    }

    /// The identity matrix. Upstream: `Vector1::identity`.
    #[inline(always)]
    fn identity() -> Matrix1<T> {
        Matrix1 { x: R::ONE }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Matrix1<T>, k: T) -> Matrix1<T> {
        Matrix1 { x: self.x * k }
    }

    /// The transpose, a `Matrix1`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: self.x }
    }

    /// Dot product (sum of the component-wise products), fused (one floored product): the exact sum
    /// is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: Matrix1<T>, rhs: Matrix1<T>) -> T {
        self.x * rhs.x
    }

    /// Squared Euclidean norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: Matrix1<T>) -> T {
        self.x * self.x
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Matrix1Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Matrix1<T>> {
    #[inline(always)]
    fn add(lhs: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: lhs.x + rhs.x }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Matrix1AddAssign<T, +Add<T>, +Copy<T>, +Drop<T>> of AddAssign<Matrix1<T>, Matrix1<T>> {
    #[inline(always)]
    fn add_assign(ref self: Matrix1<T>, rhs: Matrix1<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Matrix1Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Matrix1<T>> {
    #[inline(always)]
    fn sub(lhs: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: lhs.x - rhs.x }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Matrix1SubAssign<T, +Sub<T>, +Copy<T>, +Drop<T>> of SubAssign<Matrix1<T>, Matrix1<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Matrix1<T>, rhs: Matrix1<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Matrix1Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Matrix1<T>> {
    #[inline(always)]
    fn neg(a: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: -a.x }
    }
}

/// `a * b` (matrix product): one floored product per component. Panics on overflow. Upstream:
/// `Mul`.
pub impl Matrix1Mul<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of Mul<Matrix1<T>> {
    #[inline(always)]
    fn mul(lhs: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: lhs.x * rhs.x }
    }
}

/// `a *= b` (matrix product, `a = a * b`). Upstream: `MulAssign`.
pub impl Matrix1MulAssign<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MulAssign<Matrix1<T>, Matrix1<T>> {
    #[inline(always)]
    fn mul_assign(ref self: Matrix1<T>, rhs: Matrix1<T>) {
        self = self * rhs;
    }
}

/// `self * rhs` through `MatrixMul`: the `*` operator, for code generic over the shapes.
pub impl Matrix1MulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, Matrix1<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        self * rhs
    }
}

/// `self * rhs`, a `RowVector2`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<RowVector2> for Vector1`.
pub impl Matrix1MulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: RowVector2<T>) -> RowVector2<T> {
        RowVector2 { x: self.x * rhs.x, y: self.x * rhs.y }
    }
}

/// `self * rhs`, a `RowVector3`: one floored product per component. Panics on overflow. Upstream:
/// `Mul<RowVector3> for Vector1`.
pub impl Matrix1MulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: self.x * rhs.x, y: self.x * rhs.y, z: self.x * rhs.z }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 1`. Upstream: `Index<usize>`.
pub impl Matrix1IndexLinear<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix1<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix1<T>, index: usize) -> T {
        match index {
            0 => *self.x,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 1` or `j >= 1`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 1` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl Matrix1Index<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix1<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix1<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.x,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
