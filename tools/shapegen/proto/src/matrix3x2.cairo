// Generated by tools/shapegen/shapegen.py: do not edit by hand.
//! `Matrix3x2`: a statically sized 3x2 matrix (upstream `nalgebra::Matrix3x2`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one
//! overflow check per output scalar. Operators live in this module, so they need no import; the
//! other operations are methods of `Matrix3x2Trait`, products between shapes are
//! `MatrixMul::mul_mat`.

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::errors;
use super::matrix2::Matrix2;
use super::matrix2x3::Matrix2x3;
use super::matrix3::Matrix3;
use super::matrix_mul::MatrixMul;
use super::vector2::Vector2;
use super::vector3::Vector3;

/// A 3x2 matrix. `mRC` is the component at row `R`, column `C`. Fields are declared in column-major
/// order, so `Serde` matches upstream's storage order, while `new` takes its arguments in row-major
/// order like upstream.
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Matrix3x2<T> {
    pub m11: T,
    pub m21: T,
    pub m31: T,
    pub m12: T,
    pub m22: T,
    pub m32: T,
}

/// Methods of `Matrix3x2<T>` for any `Real` scalar.
#[generate_trait]
pub impl Matrix3x2Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix3x2Trait<T> {
    /// The 3x2 matrix with the given components, in ROW-major order. Upstream: `Matrix3x2::new`.
    #[inline(always)]
    fn new(m11: T, m12: T, m21: T, m22: T, m31: T, m32: T) -> Matrix3x2<T> {
        Matrix3x2 { m11, m21, m31, m12, m22, m32 }
    }

    /// The zero 3x2 matrix. Upstream: `zeros`.
    #[inline(always)]
    fn zeros() -> Matrix3x2<T> {
        Matrix3x2 {
            m11: R::ZERO, m21: R::ZERO, m31: R::ZERO, m12: R::ZERO, m22: R::ZERO, m32: R::ZERO,
        }
    }

    /// The 3x2 matrix whose components all equal `elem`. Upstream: `from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Matrix3x2<T> {
        Matrix3x2 { m11: elem, m21: elem, m31: elem, m12: elem, m22: elem, m32: elem }
    }

    /// The 3x2 matrix whose components all equal `elem`. Alias of `from_element`. Upstream:
    /// `repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Matrix3x2<T> {
        Matrix3x2 { m11: elem, m21: elem, m31: elem, m12: elem, m22: elem, m32: elem }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Matrix3x2<T>, k: T) -> Matrix3x2<T> {
        Matrix3x2 {
            m11: self.m11 * k,
            m21: self.m21 * k,
            m31: self.m31 * k,
            m12: self.m12 * k,
            m22: self.m22 * k,
            m32: self.m32 * k,
        }
    }

    /// The transpose, a `Matrix2x3`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Matrix3x2<T>) -> Matrix2x3<T> {
        Matrix2x3 {
            m11: self.m11,
            m21: self.m12,
            m12: self.m21,
            m22: self.m22,
            m13: self.m31,
            m23: self.m32,
        }
    }

    /// Dot product (sum of the component-wise products), fused (the `Real::Wide` accumulator): the
    /// exact sum is floored once. Only the result must fit: panics on overflow. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: Matrix3x2<T>, rhs: Matrix3x2<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, rhs.m11);
        let w = R::wide_add_prod(w, self.m21, rhs.m21);
        let w = R::wide_add_prod(w, self.m31, rhs.m31);
        let w = R::wide_add_prod(w, self.m12, rhs.m12);
        let w = R::wide_add_prod(w, self.m22, rhs.m22);
        R::wide_rescale(R::wide_add_prod(w, self.m32, rhs.m32))
    }

    /// Squared Frobenius norm: the exact sum of squares floored once. Panics on overflow. Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: Matrix3x2<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, self.m11);
        let w = R::wide_add_prod(w, self.m21, self.m21);
        let w = R::wide_add_prod(w, self.m31, self.m31);
        let w = R::wide_add_prod(w, self.m12, self.m12);
        let w = R::wide_add_prod(w, self.m22, self.m22);
        R::wide_rescale(R::wide_add_prod(w, self.m32, self.m32))
    }
}

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Matrix3x2Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Matrix3x2<T>> {
    #[inline(always)]
    fn add(lhs: Matrix3x2<T>, rhs: Matrix3x2<T>) -> Matrix3x2<T> {
        Matrix3x2 {
            m11: lhs.m11 + rhs.m11,
            m21: lhs.m21 + rhs.m21,
            m31: lhs.m31 + rhs.m31,
            m12: lhs.m12 + rhs.m12,
            m22: lhs.m22 + rhs.m22,
            m32: lhs.m32 + rhs.m32,
        }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Matrix3x2AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<Matrix3x2<T>, Matrix3x2<T>> {
    #[inline(always)]
    fn add_assign(ref self: Matrix3x2<T>, rhs: Matrix3x2<T>) {
        self = self + rhs;
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Matrix3x2Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Matrix3x2<T>> {
    #[inline(always)]
    fn sub(lhs: Matrix3x2<T>, rhs: Matrix3x2<T>) -> Matrix3x2<T> {
        Matrix3x2 {
            m11: lhs.m11 - rhs.m11,
            m21: lhs.m21 - rhs.m21,
            m31: lhs.m31 - rhs.m31,
            m12: lhs.m12 - rhs.m12,
            m22: lhs.m22 - rhs.m22,
            m32: lhs.m32 - rhs.m32,
        }
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Matrix3x2SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<Matrix3x2<T>, Matrix3x2<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Matrix3x2<T>, rhs: Matrix3x2<T>) {
        self = self - rhs;
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Matrix3x2Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Matrix3x2<T>> {
    #[inline(always)]
    fn neg(a: Matrix3x2<T>) -> Matrix3x2<T> {
        Matrix3x2 { m11: -a.m11, m21: -a.m21, m31: -a.m31, m12: -a.m12, m22: -a.m22, m32: -a.m32 }
    }
}

/// `self * rhs`, a `Vector3`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Vector2> for Matrix3x2`.
pub impl Matrix3x2MulVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix3x2<T>, Vector2<T>> {
    type Output = Vector3<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix3x2<T>, rhs: Vector2<T>) -> Vector3<T> {
        Vector3 {
            x: R::sum_prod2(self.m11, rhs.x, self.m12, rhs.y),
            y: R::sum_prod2(self.m21, rhs.x, self.m22, rhs.y),
            z: R::sum_prod2(self.m31, rhs.x, self.m32, rhs.y),
        }
    }
}

/// `self * rhs`, a `Matrix3x2`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Matrix2> for Matrix3x2`.
pub impl Matrix3x2MulMatrix2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix3x2<T>, Matrix2<T>> {
    type Output = Matrix3x2<T>;
    fn mul_mat(self: Matrix3x2<T>, rhs: Matrix2<T>) -> Matrix3x2<T> {
        Matrix3x2 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
        }
    }
}

/// `self * rhs`, a `Matrix3`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Mul<Matrix2x3> for Matrix3x2`.
pub impl Matrix3x2MulMatrix2x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix3x2<T>, Matrix2x3<T>> {
    type Output = Matrix3<T>;
    fn mul_mat(self: Matrix3x2<T>, rhs: Matrix2x3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
            m13: R::sum_prod2(self.m11, rhs.m13, self.m12, rhs.m23),
            m23: R::sum_prod2(self.m21, rhs.m13, self.m22, rhs.m23),
            m33: R::sum_prod2(self.m31, rhs.m13, self.m32, rhs.m23),
        }
    }
}

/// `m[i]`: the `i`-th component in column-major order (the storage order), like upstream. Panics
/// with `'Matrix index out of bounds'` when `i >= 6`. Upstream: `Index<usize>`.
pub impl Matrix3x2IndexLinear<T, +Copy<T>, +Drop<T>> of core::ops::IndexView<Matrix3x2<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix3x2<T>, index: usize) -> T {
        match index {
            0 => *self.m11,
            1 => *self.m21,
            2 => *self.m31,
            3 => *self.m12,
            4 => *self.m22,
            5 => *self.m32,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at row `i`, column `j` (0-based). Panics with `'Matrix index out of
/// bounds'` when `i >= 3` or `j >= 2`. Upstream: `Index<(usize, usize)>`.
///
/// A `match` on the column then on the row: the linear form (`i + j * 3` then one `match`, with a
/// row check) is dearer (`bench_*_index2__alt_linear`).
pub impl Matrix3x2Index<
    T, +Copy<T>, +Drop<T>,
> of core::ops::IndexView<Matrix3x2<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix3x2<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.m11,
                1 => *self.m21,
                2 => *self.m31,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.m12,
                1 => *self.m22,
                2 => *self.m32,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}
