[package]
name = "nalgebra_tests_sparse"
description = "Tests and gas benchmarks of the sparse matrices, the Matrix Market parser and the vector convolutions (WP 8.6-P20, P18; not published)."
version.workspace = true
edition.workspace = true
cairo-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
publish = false

[dependencies]
nalgebra = { path = "../nalgebra", default-features = false, features = ["dynamic", "sparse", "io"] }
simba.workspace = true
fixed.workspace = true

[dev-dependencies]
nalgebra_tests_utils = { path = "../tests_utils" }
nalgebra_testing = { path = "../testing" }
snforge_std.workspace = true
assert_macros.workspace = true

[tool]
fmt.workspace = true
scarb.workspace = true
