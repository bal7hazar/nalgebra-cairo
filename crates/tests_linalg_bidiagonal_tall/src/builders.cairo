// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! Builders of the static shapes from the raw oracle layout (ROW-major rows,
//! component tuples), the raw-unit error measures of the tests and the test-only
//! `PartialEq` of the decompositions (upstream has none).

use fixed::Fixed;
use nalgebra::{
    Matrix1, Matrix2, Matrix3, Matrix3x2, Matrix4, Matrix4x2, Matrix4x3, Matrix5, Matrix5x2,
    Matrix5x3, Matrix5x4, Matrix6x2, Matrix6x3, Matrix6x4, Matrix6x5, Vector2, Vector3, Vector4,
    Vector5, Vector6,
};
use nalgebra_tests_utils::{abs_raw, fx, ulp_diff};

/// `Matrix1` from raw ROW-major rows.
pub fn mat1x1(rows: [[i64; 1]; 1]) -> Matrix1<Fixed> {
    let [r0] = rows;
    let [a00] = r0;
    Matrix1 { x: fx(a00) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_1x1(a: Matrix1<Fixed>, b: Matrix1<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_1x1(a: Matrix1<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_1x1(a: Matrix1<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e = core::cmp::max(e, dev(w(a.x) * w(a.x), 0x100000000));
    e
}

/// `Vector2` from raw ROW-major rows.
pub fn mat2x1(rows: [[i64; 1]; 2]) -> Vector2<Fixed> {
    let [r0, r1] = rows;
    let [a00] = r0;
    let [a10] = r1;
    Vector2 { x: fx(a00), y: fx(a10) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_2x1(a: Vector2<Fixed>, b: Vector2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_2x1(a: Vector2<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_2x1(a: Vector2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e = core::cmp::max(e, dev(w(a.x) * w(a.x) + w(a.y) * w(a.y), 0x100000000));
    e
}

/// `Matrix2` from raw ROW-major rows.
pub fn mat2x2(rows: [[i64; 2]; 2]) -> Matrix2<Fixed> {
    let [r0, r1] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    Matrix2 { m11: fx(a00), m21: fx(a10), m12: fx(a01), m22: fx(a11) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_2x2(a: Matrix2<Fixed>, b: Matrix2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_2x2(a: Matrix2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_2x2(a: Matrix2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m11) + w(a.m21) * w(a.m21), 0x100000000));
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m12) + w(a.m21) * w(a.m22), 0));
    e = core::cmp::max(e, dev(w(a.m12) * w(a.m12) + w(a.m22) * w(a.m22), 0x100000000));
    e
}

/// `Vector3` from raw ROW-major rows.
pub fn mat3x1(rows: [[i64; 1]; 3]) -> Vector3<Fixed> {
    let [r0, r1, r2] = rows;
    let [a00] = r0;
    let [a10] = r1;
    let [a20] = r2;
    Vector3 { x: fx(a00), y: fx(a10), z: fx(a20) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_3x1(a: Vector3<Fixed>, b: Vector3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e = core::cmp::max(e, ulp_diff(a.z, b.z));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_3x1(a: Vector3<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = core::cmp::max(e, abs_raw(a.z));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_3x1(a: Vector3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e = core::cmp::max(e, dev(w(a.x) * w(a.x) + w(a.y) * w(a.y) + w(a.z) * w(a.z), 0x100000000));
    e
}

/// `Matrix3x2` from raw ROW-major rows.
pub fn mat3x2(rows: [[i64; 2]; 3]) -> Matrix3x2<Fixed> {
    let [r0, r1, r2] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    let [a20, a21] = r2;
    Matrix3x2 { m11: fx(a00), m21: fx(a10), m31: fx(a20), m12: fx(a01), m22: fx(a11), m32: fx(a21) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_3x2(a: Matrix3x2<Fixed>, b: Matrix3x2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_3x2(a: Matrix3x2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_3x2(a: Matrix3x2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e, dev(w(a.m11) * w(a.m11) + w(a.m21) * w(a.m21) + w(a.m31) * w(a.m31), 0x100000000),
        );
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m12) + w(a.m21) * w(a.m22) + w(a.m31) * w(a.m32), 0));
    e =
        core::cmp::max(
            e, dev(w(a.m12) * w(a.m12) + w(a.m22) * w(a.m22) + w(a.m32) * w(a.m32), 0x100000000),
        );
    e
}

/// `Matrix3` from raw ROW-major rows.
pub fn mat3x3(rows: [[i64; 3]; 3]) -> Matrix3<Fixed> {
    let [r0, r1, r2] = rows;
    let [a00, a01, a02] = r0;
    let [a10, a11, a12] = r1;
    let [a20, a21, a22] = r2;
    Matrix3 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_3x3(a: Matrix3<Fixed>, b: Matrix3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_3x3(a: Matrix3<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_3x3(a: Matrix3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e, dev(w(a.m11) * w(a.m11) + w(a.m21) * w(a.m21) + w(a.m31) * w(a.m31), 0x100000000),
        );
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m12) + w(a.m21) * w(a.m22) + w(a.m31) * w(a.m32), 0));
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m13) + w(a.m21) * w(a.m23) + w(a.m31) * w(a.m33), 0));
    e =
        core::cmp::max(
            e, dev(w(a.m12) * w(a.m12) + w(a.m22) * w(a.m22) + w(a.m32) * w(a.m32), 0x100000000),
        );
    e = core::cmp::max(e, dev(w(a.m12) * w(a.m13) + w(a.m22) * w(a.m23) + w(a.m32) * w(a.m33), 0));
    e =
        core::cmp::max(
            e, dev(w(a.m13) * w(a.m13) + w(a.m23) * w(a.m23) + w(a.m33) * w(a.m33), 0x100000000),
        );
    e
}

/// `Vector4` from raw ROW-major rows.
pub fn mat4x1(rows: [[i64; 1]; 4]) -> Vector4<Fixed> {
    let [r0, r1, r2, r3] = rows;
    let [a00] = r0;
    let [a10] = r1;
    let [a20] = r2;
    let [a30] = r3;
    Vector4 { x: fx(a00), y: fx(a10), z: fx(a20), w: fx(a30) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_4x1(a: Vector4<Fixed>, b: Vector4<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e = core::cmp::max(e, ulp_diff(a.z, b.z));
    e = core::cmp::max(e, ulp_diff(a.w, b.w));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_4x1(a: Vector4<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = core::cmp::max(e, abs_raw(a.z));
    e = core::cmp::max(e, abs_raw(a.w));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_4x1(a: Vector4<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(w(a.x) * w(a.x) + w(a.y) * w(a.y) + w(a.z) * w(a.z) + w(a.w) * w(a.w), 0x100000000),
        );
    e
}

/// `Matrix4x2` from raw ROW-major rows.
pub fn mat4x2(rows: [[i64; 2]; 4]) -> Matrix4x2<Fixed> {
    let [r0, r1, r2, r3] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    let [a20, a21] = r2;
    let [a30, a31] = r3;
    Matrix4x2 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_4x2(a: Matrix4x2<Fixed>, b: Matrix4x2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_4x2(a: Matrix4x2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_4x2(a: Matrix4x2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42),
                0x100000000,
            ),
        );
    e
}

/// `Matrix4x3` from raw ROW-major rows.
pub fn mat4x3(rows: [[i64; 3]; 4]) -> Matrix4x3<Fixed> {
    let [r0, r1, r2, r3] = rows;
    let [a00, a01, a02] = r0;
    let [a10, a11, a12] = r1;
    let [a20, a21, a22] = r2;
    let [a30, a31, a32] = r3;
    Matrix4x3 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_4x3(a: Matrix4x3<Fixed>, b: Matrix4x3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_4x3(a: Matrix4x3<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_4x3(a: Matrix4x3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43),
                0x100000000,
            ),
        );
    e
}

/// `Matrix4` from raw ROW-major rows.
pub fn mat4x4(rows: [[i64; 4]; 4]) -> Matrix4<Fixed> {
    let [r0, r1, r2, r3] = rows;
    let [a00, a01, a02, a03] = r0;
    let [a10, a11, a12, a13] = r1;
    let [a20, a21, a22, a23] = r2;
    let [a30, a31, a32, a33] = r3;
    Matrix4 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m14: fx(a03),
        m24: fx(a13),
        m34: fx(a23),
        m44: fx(a33),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_4x4(a: Matrix4<Fixed>, b: Matrix4<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m34, b.m34));
    e = core::cmp::max(e, ulp_diff(a.m44, b.m44));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_4x4(a: Matrix4<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m34));
    e = core::cmp::max(e, abs_raw(a.m44));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_4x4(a: Matrix4<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m14)
                    + w(a.m21) * w(a.m24)
                    + w(a.m31) * w(a.m34)
                    + w(a.m41) * w(a.m44),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m14)
                    + w(a.m22) * w(a.m24)
                    + w(a.m32) * w(a.m34)
                    + w(a.m42) * w(a.m44),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m14)
                    + w(a.m23) * w(a.m24)
                    + w(a.m33) * w(a.m34)
                    + w(a.m43) * w(a.m44),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m14)
                    + w(a.m24) * w(a.m24)
                    + w(a.m34) * w(a.m34)
                    + w(a.m44) * w(a.m44),
                0x100000000,
            ),
        );
    e
}

/// `Vector5` from raw ROW-major rows.
pub fn mat5x1(rows: [[i64; 1]; 5]) -> Vector5<Fixed> {
    let [r0, r1, r2, r3, r4] = rows;
    let [a00] = r0;
    let [a10] = r1;
    let [a20] = r2;
    let [a30] = r3;
    let [a40] = r4;
    Vector5 { x: fx(a00), y: fx(a10), z: fx(a20), w: fx(a30), a: fx(a40) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_5x1(a: Vector5<Fixed>, b: Vector5<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e = core::cmp::max(e, ulp_diff(a.z, b.z));
    e = core::cmp::max(e, ulp_diff(a.w, b.w));
    e = core::cmp::max(e, ulp_diff(a.a, b.a));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_5x1(a: Vector5<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = core::cmp::max(e, abs_raw(a.z));
    e = core::cmp::max(e, abs_raw(a.w));
    e = core::cmp::max(e, abs_raw(a.a));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_5x1(a: Vector5<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.x) * w(a.x)
                    + w(a.y) * w(a.y)
                    + w(a.z) * w(a.z)
                    + w(a.w) * w(a.w)
                    + w(a.a) * w(a.a),
                0x100000000,
            ),
        );
    e
}

/// `Matrix5x2` from raw ROW-major rows.
pub fn mat5x2(rows: [[i64; 2]; 5]) -> Matrix5x2<Fixed> {
    let [r0, r1, r2, r3, r4] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    let [a20, a21] = r2;
    let [a30, a31] = r3;
    let [a40, a41] = r4;
    Matrix5x2 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_5x2(a: Matrix5x2<Fixed>, b: Matrix5x2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_5x2(a: Matrix5x2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_5x2(a: Matrix5x2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52),
                0x100000000,
            ),
        );
    e
}

/// `Matrix5x3` from raw ROW-major rows.
pub fn mat5x3(rows: [[i64; 3]; 5]) -> Matrix5x3<Fixed> {
    let [r0, r1, r2, r3, r4] = rows;
    let [a00, a01, a02] = r0;
    let [a10, a11, a12] = r1;
    let [a20, a21, a22] = r2;
    let [a30, a31, a32] = r3;
    let [a40, a41, a42] = r4;
    Matrix5x3 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_5x3(a: Matrix5x3<Fixed>, b: Matrix5x3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_5x3(a: Matrix5x3<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_5x3(a: Matrix5x3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53),
                0x100000000,
            ),
        );
    e
}

/// `Matrix5x4` from raw ROW-major rows.
pub fn mat5x4(rows: [[i64; 4]; 5]) -> Matrix5x4<Fixed> {
    let [r0, r1, r2, r3, r4] = rows;
    let [a00, a01, a02, a03] = r0;
    let [a10, a11, a12, a13] = r1;
    let [a20, a21, a22, a23] = r2;
    let [a30, a31, a32, a33] = r3;
    let [a40, a41, a42, a43] = r4;
    Matrix5x4 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
        m14: fx(a03),
        m24: fx(a13),
        m34: fx(a23),
        m44: fx(a33),
        m54: fx(a43),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_5x4(a: Matrix5x4<Fixed>, b: Matrix5x4<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m34, b.m34));
    e = core::cmp::max(e, ulp_diff(a.m44, b.m44));
    e = core::cmp::max(e, ulp_diff(a.m54, b.m54));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_5x4(a: Matrix5x4<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m34));
    e = core::cmp::max(e, abs_raw(a.m44));
    e = core::cmp::max(e, abs_raw(a.m54));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_5x4(a: Matrix5x4<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m14)
                    + w(a.m21) * w(a.m24)
                    + w(a.m31) * w(a.m34)
                    + w(a.m41) * w(a.m44)
                    + w(a.m51) * w(a.m54),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m14)
                    + w(a.m22) * w(a.m24)
                    + w(a.m32) * w(a.m34)
                    + w(a.m42) * w(a.m44)
                    + w(a.m52) * w(a.m54),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m14)
                    + w(a.m23) * w(a.m24)
                    + w(a.m33) * w(a.m34)
                    + w(a.m43) * w(a.m44)
                    + w(a.m53) * w(a.m54),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m14)
                    + w(a.m24) * w(a.m24)
                    + w(a.m34) * w(a.m34)
                    + w(a.m44) * w(a.m44)
                    + w(a.m54) * w(a.m54),
                0x100000000,
            ),
        );
    e
}

/// `Matrix5` from raw ROW-major rows.
pub fn mat5x5(rows: [[i64; 5]; 5]) -> Matrix5<Fixed> {
    let [r0, r1, r2, r3, r4] = rows;
    let [a00, a01, a02, a03, a04] = r0;
    let [a10, a11, a12, a13, a14] = r1;
    let [a20, a21, a22, a23, a24] = r2;
    let [a30, a31, a32, a33, a34] = r3;
    let [a40, a41, a42, a43, a44] = r4;
    Matrix5 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
        m14: fx(a03),
        m24: fx(a13),
        m34: fx(a23),
        m44: fx(a33),
        m54: fx(a43),
        m15: fx(a04),
        m25: fx(a14),
        m35: fx(a24),
        m45: fx(a34),
        m55: fx(a44),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_5x5(a: Matrix5<Fixed>, b: Matrix5<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m34, b.m34));
    e = core::cmp::max(e, ulp_diff(a.m44, b.m44));
    e = core::cmp::max(e, ulp_diff(a.m54, b.m54));
    e = core::cmp::max(e, ulp_diff(a.m15, b.m15));
    e = core::cmp::max(e, ulp_diff(a.m25, b.m25));
    e = core::cmp::max(e, ulp_diff(a.m35, b.m35));
    e = core::cmp::max(e, ulp_diff(a.m45, b.m45));
    e = core::cmp::max(e, ulp_diff(a.m55, b.m55));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_5x5(a: Matrix5<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m34));
    e = core::cmp::max(e, abs_raw(a.m44));
    e = core::cmp::max(e, abs_raw(a.m54));
    e = core::cmp::max(e, abs_raw(a.m15));
    e = core::cmp::max(e, abs_raw(a.m25));
    e = core::cmp::max(e, abs_raw(a.m35));
    e = core::cmp::max(e, abs_raw(a.m45));
    e = core::cmp::max(e, abs_raw(a.m55));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_5x5(a: Matrix5<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m14)
                    + w(a.m21) * w(a.m24)
                    + w(a.m31) * w(a.m34)
                    + w(a.m41) * w(a.m44)
                    + w(a.m51) * w(a.m54),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m15)
                    + w(a.m21) * w(a.m25)
                    + w(a.m31) * w(a.m35)
                    + w(a.m41) * w(a.m45)
                    + w(a.m51) * w(a.m55),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m14)
                    + w(a.m22) * w(a.m24)
                    + w(a.m32) * w(a.m34)
                    + w(a.m42) * w(a.m44)
                    + w(a.m52) * w(a.m54),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m15)
                    + w(a.m22) * w(a.m25)
                    + w(a.m32) * w(a.m35)
                    + w(a.m42) * w(a.m45)
                    + w(a.m52) * w(a.m55),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m14)
                    + w(a.m23) * w(a.m24)
                    + w(a.m33) * w(a.m34)
                    + w(a.m43) * w(a.m44)
                    + w(a.m53) * w(a.m54),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m15)
                    + w(a.m23) * w(a.m25)
                    + w(a.m33) * w(a.m35)
                    + w(a.m43) * w(a.m45)
                    + w(a.m53) * w(a.m55),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m14)
                    + w(a.m24) * w(a.m24)
                    + w(a.m34) * w(a.m34)
                    + w(a.m44) * w(a.m44)
                    + w(a.m54) * w(a.m54),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m15)
                    + w(a.m24) * w(a.m25)
                    + w(a.m34) * w(a.m35)
                    + w(a.m44) * w(a.m45)
                    + w(a.m54) * w(a.m55),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m15) * w(a.m15)
                    + w(a.m25) * w(a.m25)
                    + w(a.m35) * w(a.m35)
                    + w(a.m45) * w(a.m45)
                    + w(a.m55) * w(a.m55),
                0x100000000,
            ),
        );
    e
}

/// `Vector6` from raw ROW-major rows.
pub fn mat6x1(rows: [[i64; 1]; 6]) -> Vector6<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00] = r0;
    let [a10] = r1;
    let [a20] = r2;
    let [a30] = r3;
    let [a40] = r4;
    let [a50] = r5;
    Vector6 { x: fx(a00), y: fx(a10), z: fx(a20), w: fx(a30), a: fx(a40), b: fx(a50) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x1(a: Vector6<Fixed>, b: Vector6<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e = core::cmp::max(e, ulp_diff(a.z, b.z));
    e = core::cmp::max(e, ulp_diff(a.w, b.w));
    e = core::cmp::max(e, ulp_diff(a.a, b.a));
    e = core::cmp::max(e, ulp_diff(a.b, b.b));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x1(a: Vector6<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = core::cmp::max(e, abs_raw(a.z));
    e = core::cmp::max(e, abs_raw(a.w));
    e = core::cmp::max(e, abs_raw(a.a));
    e = core::cmp::max(e, abs_raw(a.b));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x1(a: Vector6<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.x) * w(a.x)
                    + w(a.y) * w(a.y)
                    + w(a.z) * w(a.z)
                    + w(a.w) * w(a.w)
                    + w(a.a) * w(a.a)
                    + w(a.b) * w(a.b),
                0x100000000,
            ),
        );
    e
}

/// `Matrix6x2` from raw ROW-major rows.
pub fn mat6x2(rows: [[i64; 2]; 6]) -> Matrix6x2<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    let [a20, a21] = r2;
    let [a30, a31] = r3;
    let [a40, a41] = r4;
    let [a50, a51] = r5;
    Matrix6x2 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m61: fx(a50),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m62: fx(a51),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x2(a: Matrix6x2<Fixed>, b: Matrix6x2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m61, b.m61));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m62, b.m62));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x2(a: Matrix6x2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m61));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m62));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x2(a: Matrix6x2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51)
                    + w(a.m61) * w(a.m61),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52)
                    + w(a.m61) * w(a.m62),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52)
                    + w(a.m62) * w(a.m62),
                0x100000000,
            ),
        );
    e
}

/// `Matrix6x3` from raw ROW-major rows.
pub fn mat6x3(rows: [[i64; 3]; 6]) -> Matrix6x3<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00, a01, a02] = r0;
    let [a10, a11, a12] = r1;
    let [a20, a21, a22] = r2;
    let [a30, a31, a32] = r3;
    let [a40, a41, a42] = r4;
    let [a50, a51, a52] = r5;
    Matrix6x3 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m61: fx(a50),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m62: fx(a51),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
        m63: fx(a52),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x3(a: Matrix6x3<Fixed>, b: Matrix6x3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m61, b.m61));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m62, b.m62));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e = core::cmp::max(e, ulp_diff(a.m63, b.m63));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x3(a: Matrix6x3<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m61));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m62));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = core::cmp::max(e, abs_raw(a.m63));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x3(a: Matrix6x3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51)
                    + w(a.m61) * w(a.m61),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52)
                    + w(a.m61) * w(a.m62),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53)
                    + w(a.m61) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52)
                    + w(a.m62) * w(a.m62),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53)
                    + w(a.m62) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53)
                    + w(a.m63) * w(a.m63),
                0x100000000,
            ),
        );
    e
}

/// `Matrix6x4` from raw ROW-major rows.
pub fn mat6x4(rows: [[i64; 4]; 6]) -> Matrix6x4<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00, a01, a02, a03] = r0;
    let [a10, a11, a12, a13] = r1;
    let [a20, a21, a22, a23] = r2;
    let [a30, a31, a32, a33] = r3;
    let [a40, a41, a42, a43] = r4;
    let [a50, a51, a52, a53] = r5;
    Matrix6x4 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m61: fx(a50),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m62: fx(a51),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
        m63: fx(a52),
        m14: fx(a03),
        m24: fx(a13),
        m34: fx(a23),
        m44: fx(a33),
        m54: fx(a43),
        m64: fx(a53),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x4(a: Matrix6x4<Fixed>, b: Matrix6x4<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m61, b.m61));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m62, b.m62));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e = core::cmp::max(e, ulp_diff(a.m63, b.m63));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m34, b.m34));
    e = core::cmp::max(e, ulp_diff(a.m44, b.m44));
    e = core::cmp::max(e, ulp_diff(a.m54, b.m54));
    e = core::cmp::max(e, ulp_diff(a.m64, b.m64));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x4(a: Matrix6x4<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m61));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m62));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = core::cmp::max(e, abs_raw(a.m63));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m34));
    e = core::cmp::max(e, abs_raw(a.m44));
    e = core::cmp::max(e, abs_raw(a.m54));
    e = core::cmp::max(e, abs_raw(a.m64));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x4(a: Matrix6x4<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51)
                    + w(a.m61) * w(a.m61),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52)
                    + w(a.m61) * w(a.m62),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53)
                    + w(a.m61) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m14)
                    + w(a.m21) * w(a.m24)
                    + w(a.m31) * w(a.m34)
                    + w(a.m41) * w(a.m44)
                    + w(a.m51) * w(a.m54)
                    + w(a.m61) * w(a.m64),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52)
                    + w(a.m62) * w(a.m62),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53)
                    + w(a.m62) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m14)
                    + w(a.m22) * w(a.m24)
                    + w(a.m32) * w(a.m34)
                    + w(a.m42) * w(a.m44)
                    + w(a.m52) * w(a.m54)
                    + w(a.m62) * w(a.m64),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53)
                    + w(a.m63) * w(a.m63),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m14)
                    + w(a.m23) * w(a.m24)
                    + w(a.m33) * w(a.m34)
                    + w(a.m43) * w(a.m44)
                    + w(a.m53) * w(a.m54)
                    + w(a.m63) * w(a.m64),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m14)
                    + w(a.m24) * w(a.m24)
                    + w(a.m34) * w(a.m34)
                    + w(a.m44) * w(a.m44)
                    + w(a.m54) * w(a.m54)
                    + w(a.m64) * w(a.m64),
                0x100000000,
            ),
        );
    e
}

/// `Matrix6x5` from raw ROW-major rows.
pub fn mat6x5(rows: [[i64; 5]; 6]) -> Matrix6x5<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00, a01, a02, a03, a04] = r0;
    let [a10, a11, a12, a13, a14] = r1;
    let [a20, a21, a22, a23, a24] = r2;
    let [a30, a31, a32, a33, a34] = r3;
    let [a40, a41, a42, a43, a44] = r4;
    let [a50, a51, a52, a53, a54] = r5;
    Matrix6x5 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m61: fx(a50),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m62: fx(a51),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
        m63: fx(a52),
        m14: fx(a03),
        m24: fx(a13),
        m34: fx(a23),
        m44: fx(a33),
        m54: fx(a43),
        m64: fx(a53),
        m15: fx(a04),
        m25: fx(a14),
        m35: fx(a24),
        m45: fx(a34),
        m55: fx(a44),
        m65: fx(a54),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x5(a: Matrix6x5<Fixed>, b: Matrix6x5<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m61, b.m61));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m62, b.m62));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e = core::cmp::max(e, ulp_diff(a.m63, b.m63));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m34, b.m34));
    e = core::cmp::max(e, ulp_diff(a.m44, b.m44));
    e = core::cmp::max(e, ulp_diff(a.m54, b.m54));
    e = core::cmp::max(e, ulp_diff(a.m64, b.m64));
    e = core::cmp::max(e, ulp_diff(a.m15, b.m15));
    e = core::cmp::max(e, ulp_diff(a.m25, b.m25));
    e = core::cmp::max(e, ulp_diff(a.m35, b.m35));
    e = core::cmp::max(e, ulp_diff(a.m45, b.m45));
    e = core::cmp::max(e, ulp_diff(a.m55, b.m55));
    e = core::cmp::max(e, ulp_diff(a.m65, b.m65));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x5(a: Matrix6x5<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m61));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m62));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = core::cmp::max(e, abs_raw(a.m63));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m34));
    e = core::cmp::max(e, abs_raw(a.m44));
    e = core::cmp::max(e, abs_raw(a.m54));
    e = core::cmp::max(e, abs_raw(a.m64));
    e = core::cmp::max(e, abs_raw(a.m15));
    e = core::cmp::max(e, abs_raw(a.m25));
    e = core::cmp::max(e, abs_raw(a.m35));
    e = core::cmp::max(e, abs_raw(a.m45));
    e = core::cmp::max(e, abs_raw(a.m55));
    e = core::cmp::max(e, abs_raw(a.m65));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x5(a: Matrix6x5<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51)
                    + w(a.m61) * w(a.m61),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52)
                    + w(a.m61) * w(a.m62),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53)
                    + w(a.m61) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m14)
                    + w(a.m21) * w(a.m24)
                    + w(a.m31) * w(a.m34)
                    + w(a.m41) * w(a.m44)
                    + w(a.m51) * w(a.m54)
                    + w(a.m61) * w(a.m64),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m15)
                    + w(a.m21) * w(a.m25)
                    + w(a.m31) * w(a.m35)
                    + w(a.m41) * w(a.m45)
                    + w(a.m51) * w(a.m55)
                    + w(a.m61) * w(a.m65),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52)
                    + w(a.m62) * w(a.m62),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53)
                    + w(a.m62) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m14)
                    + w(a.m22) * w(a.m24)
                    + w(a.m32) * w(a.m34)
                    + w(a.m42) * w(a.m44)
                    + w(a.m52) * w(a.m54)
                    + w(a.m62) * w(a.m64),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m15)
                    + w(a.m22) * w(a.m25)
                    + w(a.m32) * w(a.m35)
                    + w(a.m42) * w(a.m45)
                    + w(a.m52) * w(a.m55)
                    + w(a.m62) * w(a.m65),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53)
                    + w(a.m63) * w(a.m63),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m14)
                    + w(a.m23) * w(a.m24)
                    + w(a.m33) * w(a.m34)
                    + w(a.m43) * w(a.m44)
                    + w(a.m53) * w(a.m54)
                    + w(a.m63) * w(a.m64),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m15)
                    + w(a.m23) * w(a.m25)
                    + w(a.m33) * w(a.m35)
                    + w(a.m43) * w(a.m45)
                    + w(a.m53) * w(a.m55)
                    + w(a.m63) * w(a.m65),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m14)
                    + w(a.m24) * w(a.m24)
                    + w(a.m34) * w(a.m34)
                    + w(a.m44) * w(a.m44)
                    + w(a.m54) * w(a.m54)
                    + w(a.m64) * w(a.m64),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m14) * w(a.m15)
                    + w(a.m24) * w(a.m25)
                    + w(a.m34) * w(a.m35)
                    + w(a.m44) * w(a.m45)
                    + w(a.m54) * w(a.m55)
                    + w(a.m64) * w(a.m65),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m15) * w(a.m15)
                    + w(a.m25) * w(a.m25)
                    + w(a.m35) * w(a.m35)
                    + w(a.m45) * w(a.m45)
                    + w(a.m55) * w(a.m55)
                    + w(a.m65) * w(a.m65),
                0x100000000,
            ),
        );
    e
}

/// The raw value, widened.
fn w(a: Fixed) -> i128 {
    a.raw.into()
}

/// `|floor(p / 2^32) - target|` for an exact wide sum of raw products `p`.
fn dev(p: i128, target: i128) -> u128 {
    let q = if p >= 0 {
        p / 0x100000000
    } else {
        -((-p + 0xffffffff) / 0x100000000)
    };
    let d = q - target;
    let d = if d < 0 {
        -d
    } else {
        d
    };
    d.try_into().unwrap()
}
