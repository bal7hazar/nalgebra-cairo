//! Package `nalgebra_tests_dynamic` (WP 8.5-P13): tests and gas benchmarks of the dynamic
//! matrices `DMatrix` / `DVector` / `RowDVector` (DESIGN D5), through the public API, and the
//! benchmarks of the storage layouts D5 was decided on (`layout`).

#[cfg(test)]
mod layout;
