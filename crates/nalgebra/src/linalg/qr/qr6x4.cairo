// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Qr6x4`: the QR factorisation of a `Matrix6x4` (upstream `nalgebra::linalg::QR<T, U6, U4>`) by
//! modified Gram-Schmidt, the algorithm of `Qr3` / `Qr4` (see `linalg::qr` for the study of the
//! alternatives), generated for the shapes P14a did not cover (WP 8.5-P14b).

use simba::scalar::Real;
use crate::base::matrix4::Matrix4;
use crate::base::matrix6x4::Matrix6x4;
use crate::base::vector6::Vector6;

/// The QR factorisation `A = q * r` of a `Matrix6x4<T>`: `q` is 6x4 (thin: 6x4, orthonormal
/// COLUMNS), `r` is 4x4 upper triangular (trapezoidal) with a non-negative diagonal and an exactly
/// zero strict lower part — upstream's unpacked `q()` / `r()`. A column that collapses to exactly
/// zero gives a zero column of `q` and a zero diagonal entry of `r` (`q * r = A` still holds; see
/// `linalg::qr`). Upstream: `nalgebra::linalg::QR`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Qr6x4<T> {
    /// The orthonormal factor (6x4).
    pub q: Matrix6x4<T>,
    /// The upper triangular factor (4x4), `r_ii >= 0`.
    pub r: Matrix4<T>,
}

/// Test-only field-wise equality (upstream `QR` has no `PartialEq`).
#[cfg(test)]
impl Qr6x4PartialEq<T, +PartialEq<T>> of PartialEq<Qr6x4<T>> {
    fn eq(lhs: @Qr6x4<T>, rhs: @Qr6x4<T>) -> bool {
        lhs.q == rhs.q && lhs.r == rhs.r
    }
}

/// Methods of `Qr6x4<T>` for any `Real` scalar.
#[generate_trait]
pub impl Qr6x4Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Qr6x4Trait<T> {
    /// The QR factorisation of `matrix` by modified Gram-Schmidt, fully unrolled: for each of
    /// the 4 leading columns, `r_ii = |a_i|` (floored norm of the exact sum of squares), `q_i =
    /// a_i / r_ii` (one prepared divisor), then `r_ij = <q_i, a_j>` (fused) and `a_j -= r_ij q_i`
    /// (one `mul_add` per component) for the later columns. Panics on overflow of a norm.
    /// Upstream: `matrix.qr()` / `QR::new(matrix)` (Householder, same unpacked factors).
    fn new(matrix: Matrix6x4<T>) -> Qr6x4<T> {
        let a0 = Vector6 {
            x: matrix.m11,
            y: matrix.m21,
            z: matrix.m31,
            w: matrix.m41,
            a: matrix.m51,
            b: matrix.m61,
        };
        let a1 = Vector6 {
            x: matrix.m12,
            y: matrix.m22,
            z: matrix.m32,
            w: matrix.m42,
            a: matrix.m52,
            b: matrix.m62,
        };
        let a2 = Vector6 {
            x: matrix.m13,
            y: matrix.m23,
            z: matrix.m33,
            w: matrix.m43,
            a: matrix.m53,
            b: matrix.m63,
        };
        let a3 = Vector6 {
            x: matrix.m14,
            y: matrix.m24,
            z: matrix.m34,
            w: matrix.m44,
            a: matrix.m54,
            b: matrix.m64,
        };
        let r0_0 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), a0.x, a0.x), a0.y, a0.y,
                            ),
                            a0.z,
                            a0.z,
                        ),
                        a0.w,
                        a0.w,
                    ),
                    a0.a,
                    a0.a,
                ),
                a0.b,
                a0.b,
            ),
        );
        let q0 = if r0_0 == R::zero() {
            Vector6 {
                x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
            }
        } else {
            {
                let (q0, q1, q2, q3, q4, q5) = R::div6(a0.x, a0.y, a0.z, a0.w, a0.a, a0.b, r0_0);
                Vector6 { x: q0, y: q1, z: q2, w: q3, a: q4, b: q5 }
            }
        };
        let r0_1 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), q0.x, a1.x), q0.y, a1.y,
                            ),
                            q0.z,
                            a1.z,
                        ),
                        q0.w,
                        a1.w,
                    ),
                    q0.a,
                    a1.a,
                ),
                q0.b,
                a1.b,
            ),
        );
        let r0_2 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), q0.x, a2.x), q0.y, a2.y,
                            ),
                            q0.z,
                            a2.z,
                        ),
                        q0.w,
                        a2.w,
                    ),
                    q0.a,
                    a2.a,
                ),
                q0.b,
                a2.b,
            ),
        );
        let r0_3 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), q0.x, a3.x), q0.y, a3.y,
                            ),
                            q0.z,
                            a3.z,
                        ),
                        q0.w,
                        a3.w,
                    ),
                    q0.a,
                    a3.a,
                ),
                q0.b,
                a3.b,
            ),
        );
        let a1 = Vector6 {
            x: R::mul_add(-r0_1, q0.x, a1.x),
            y: R::mul_add(-r0_1, q0.y, a1.y),
            z: R::mul_add(-r0_1, q0.z, a1.z),
            w: R::mul_add(-r0_1, q0.w, a1.w),
            a: R::mul_add(-r0_1, q0.a, a1.a),
            b: R::mul_add(-r0_1, q0.b, a1.b),
        };
        let a2 = Vector6 {
            x: R::mul_add(-r0_2, q0.x, a2.x),
            y: R::mul_add(-r0_2, q0.y, a2.y),
            z: R::mul_add(-r0_2, q0.z, a2.z),
            w: R::mul_add(-r0_2, q0.w, a2.w),
            a: R::mul_add(-r0_2, q0.a, a2.a),
            b: R::mul_add(-r0_2, q0.b, a2.b),
        };
        let a3 = Vector6 {
            x: R::mul_add(-r0_3, q0.x, a3.x),
            y: R::mul_add(-r0_3, q0.y, a3.y),
            z: R::mul_add(-r0_3, q0.z, a3.z),
            w: R::mul_add(-r0_3, q0.w, a3.w),
            a: R::mul_add(-r0_3, q0.a, a3.a),
            b: R::mul_add(-r0_3, q0.b, a3.b),
        };
        let r1_1 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), a1.x, a1.x), a1.y, a1.y,
                            ),
                            a1.z,
                            a1.z,
                        ),
                        a1.w,
                        a1.w,
                    ),
                    a1.a,
                    a1.a,
                ),
                a1.b,
                a1.b,
            ),
        );
        let q1 = if r1_1 == R::zero() {
            Vector6 {
                x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
            }
        } else {
            {
                let (q0, q1, q2, q3, q4, q5) = R::div6(a1.x, a1.y, a1.z, a1.w, a1.a, a1.b, r1_1);
                Vector6 { x: q0, y: q1, z: q2, w: q3, a: q4, b: q5 }
            }
        };
        let r1_2 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), q1.x, a2.x), q1.y, a2.y,
                            ),
                            q1.z,
                            a2.z,
                        ),
                        q1.w,
                        a2.w,
                    ),
                    q1.a,
                    a2.a,
                ),
                q1.b,
                a2.b,
            ),
        );
        let r1_3 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), q1.x, a3.x), q1.y, a3.y,
                            ),
                            q1.z,
                            a3.z,
                        ),
                        q1.w,
                        a3.w,
                    ),
                    q1.a,
                    a3.a,
                ),
                q1.b,
                a3.b,
            ),
        );
        let a2 = Vector6 {
            x: R::mul_add(-r1_2, q1.x, a2.x),
            y: R::mul_add(-r1_2, q1.y, a2.y),
            z: R::mul_add(-r1_2, q1.z, a2.z),
            w: R::mul_add(-r1_2, q1.w, a2.w),
            a: R::mul_add(-r1_2, q1.a, a2.a),
            b: R::mul_add(-r1_2, q1.b, a2.b),
        };
        let a3 = Vector6 {
            x: R::mul_add(-r1_3, q1.x, a3.x),
            y: R::mul_add(-r1_3, q1.y, a3.y),
            z: R::mul_add(-r1_3, q1.z, a3.z),
            w: R::mul_add(-r1_3, q1.w, a3.w),
            a: R::mul_add(-r1_3, q1.a, a3.a),
            b: R::mul_add(-r1_3, q1.b, a3.b),
        };
        let r2_2 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), a2.x, a2.x), a2.y, a2.y,
                            ),
                            a2.z,
                            a2.z,
                        ),
                        a2.w,
                        a2.w,
                    ),
                    a2.a,
                    a2.a,
                ),
                a2.b,
                a2.b,
            ),
        );
        let q2 = if r2_2 == R::zero() {
            Vector6 {
                x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
            }
        } else {
            {
                let (q0, q1, q2, q3, q4, q5) = R::div6(a2.x, a2.y, a2.z, a2.w, a2.a, a2.b, r2_2);
                Vector6 { x: q0, y: q1, z: q2, w: q3, a: q4, b: q5 }
            }
        };
        let r2_3 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), q2.x, a3.x), q2.y, a3.y,
                            ),
                            q2.z,
                            a3.z,
                        ),
                        q2.w,
                        a3.w,
                    ),
                    q2.a,
                    a3.a,
                ),
                q2.b,
                a3.b,
            ),
        );
        let a3 = Vector6 {
            x: R::mul_add(-r2_3, q2.x, a3.x),
            y: R::mul_add(-r2_3, q2.y, a3.y),
            z: R::mul_add(-r2_3, q2.z, a3.z),
            w: R::mul_add(-r2_3, q2.w, a3.w),
            a: R::mul_add(-r2_3, q2.a, a3.a),
            b: R::mul_add(-r2_3, q2.b, a3.b),
        };
        let r3_3 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), a3.x, a3.x), a3.y, a3.y,
                            ),
                            a3.z,
                            a3.z,
                        ),
                        a3.w,
                        a3.w,
                    ),
                    a3.a,
                    a3.a,
                ),
                a3.b,
                a3.b,
            ),
        );
        let q3 = if r3_3 == R::zero() {
            Vector6 {
                x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
            }
        } else {
            {
                let (q0, q1, q2, q3, q4, q5) = R::div6(a3.x, a3.y, a3.z, a3.w, a3.a, a3.b, r3_3);
                Vector6 { x: q0, y: q1, z: q2, w: q3, a: q4, b: q5 }
            }
        };
        Qr6x4 {
            q: Matrix6x4 {
                m11: q0.x,
                m21: q0.y,
                m31: q0.z,
                m41: q0.w,
                m51: q0.a,
                m61: q0.b,
                m12: q1.x,
                m22: q1.y,
                m32: q1.z,
                m42: q1.w,
                m52: q1.a,
                m62: q1.b,
                m13: q2.x,
                m23: q2.y,
                m33: q2.z,
                m43: q2.w,
                m53: q2.a,
                m63: q2.b,
                m14: q3.x,
                m24: q3.y,
                m34: q3.z,
                m44: q3.w,
                m54: q3.a,
                m64: q3.b,
            },
            r: Matrix4 {
                m11: r0_0,
                m21: R::zero(),
                m31: R::zero(),
                m41: R::zero(),
                m12: r0_1,
                m22: r1_1,
                m32: R::zero(),
                m42: R::zero(),
                m13: r0_2,
                m23: r1_2,
                m33: r2_2,
                m43: R::zero(),
                m14: r0_3,
                m24: r1_3,
                m34: r2_3,
                m44: r3_3,
            },
        }
    }

    /// The orthonormal factor. Exact: a move. Upstream: `QR::q`.
    #[inline(always)]
    fn q(self: Qr6x4<T>) -> Matrix6x4<T> {
        self.q
    }

    /// The upper triangular factor. Exact: a move. Upstream: `QR::r`.
    #[inline(always)]
    fn r(self: Qr6x4<T>) -> Matrix4<T> {
        self.r
    }

    /// `(q, r)`. Exact: moves. Upstream: `QR::unpack`.
    #[inline(always)]
    fn unpack(self: Qr6x4<T>) -> (Matrix6x4<T>, Matrix4<T>) {
        (self.q, self.r)
    }

    /// The upper triangular factor, consuming the factorisation. Exact. Upstream: `QR::unpack_r`.
    #[inline(always)]
    fn unpack_r(self: Qr6x4<T>) -> Matrix4<T> {
        self.r
    }

    /// The factors as stored, `(q, r)` (modified Gram-Schmidt keeps both; upstream packs the
    /// reflectors). Exact. Upstream: `QR::qr_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn qr_internal(self: Qr6x4<T>) -> (Matrix6x4<T>, Matrix4<T>) {
        (self.q, self.r)
    }
}

/// `Matrix6x4` methods that go through the QR factorisation. Import `Matrix6x4QrTrait` to use them.
#[generate_trait]
pub impl Matrix6x4QrImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix6x4QrTrait<T> {
    /// The QR factorisation. Upstream: `Matrix::qr`.
    #[inline(always)]
    fn qr(self: Matrix6x4<T>) -> Qr6x4<T> {
        Qr6x4Trait::new(self)
    }
}
