// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Qr3x2`: the QR factorisation of a `Matrix3x2` (upstream `nalgebra::linalg::QR<T, U3, U2>`) by
//! modified Gram-Schmidt, the algorithm of `Qr3` / `Qr4` (see `linalg::qr` for the study of the
//! alternatives), generated for the shapes P14a did not cover (WP 8.5-P14b).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix2::Matrix2;
use crate::base::matrix3x2::Matrix3x2;
use crate::base::vector3::Vector3;

/// The QR factorisation `A = q * r` of a `Matrix3x2<T>`: `q` is 3x2 (thin: 3x2, orthonormal
/// COLUMNS), `r` is 2x2 upper triangular (trapezoidal) with a non-negative diagonal and an exactly
/// zero strict lower part — upstream's unpacked `q()` / `r()`. A column that collapses to exactly
/// zero gives a zero column of `q` and a zero diagonal entry of `r` (`q * r = A` still holds; see
/// `linalg::qr`). Upstream: `nalgebra::linalg::QR`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Qr3x2<T> {
    /// The orthonormal factor (3x2).
    pub q: Matrix3x2<T>,
    /// The upper triangular factor (2x2), `r_ii >= 0`.
    pub r: Matrix2<T>,
}

/// Test-only field-wise equality (upstream `QR` has no `PartialEq`).
#[cfg(test)]
impl Qr3x2PartialEq<T, +PartialEq<T>> of PartialEq<Qr3x2<T>> {
    fn eq(lhs: @Qr3x2<T>, rhs: @Qr3x2<T>) -> bool {
        lhs.q == rhs.q && lhs.r == rhs.r
    }
}

/// Methods of `Qr3x2<T>` for any `Real` scalar.
#[generate_trait]
pub impl Qr3x2Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Qr3x2Trait<T> {
    /// The QR factorisation of `matrix` by modified Gram-Schmidt, fully unrolled: for each of
    /// the 2 leading columns, `r_ii = |a_i|` (floored norm of the exact sum of squares), `q_i =
    /// a_i / r_ii` (one prepared divisor), then `r_ij = <q_i, a_j>` (fused) and `a_j -= r_ij q_i`
    /// (one `mul_add` per component) for the later columns. Panics on overflow of a norm.
    /// Upstream: `matrix.qr()` / `QR::new(matrix)` (Householder, same unpacked factors).
    fn new(matrix: Matrix3x2<T>) -> Qr3x2<T> {
        revoke_ap_tracking();
        let a0 = Vector3 { x: matrix.m11, y: matrix.m21, z: matrix.m31 };
        let a1 = Vector3 { x: matrix.m12, y: matrix.m22, z: matrix.m32 };
        let r0_0 = R::norm3(a0.x, a0.y, a0.z);
        let q0 = if r0_0 == R::zero() {
            Vector3 { x: R::zero(), y: R::zero(), z: R::zero() }
        } else {
            {
                let (q0, q1, q2) = R::div3(a0.x, a0.y, a0.z, r0_0);
                Vector3 { x: q0, y: q1, z: q2 }
            }
        };
        let r0_1 = R::sum_prod3(q0.x, a1.x, q0.y, a1.y, q0.z, a1.z);
        let a1 = Vector3 {
            x: R::mul_add(-r0_1, q0.x, a1.x),
            y: R::mul_add(-r0_1, q0.y, a1.y),
            z: R::mul_add(-r0_1, q0.z, a1.z),
        };
        let r1_1 = R::norm3(a1.x, a1.y, a1.z);
        let q1 = if r1_1 == R::zero() {
            Vector3 { x: R::zero(), y: R::zero(), z: R::zero() }
        } else {
            {
                let (q0, q1, q2) = R::div3(a1.x, a1.y, a1.z, r1_1);
                Vector3 { x: q0, y: q1, z: q2 }
            }
        };
        Qr3x2 {
            q: Matrix3x2 { m11: q0.x, m21: q0.y, m31: q0.z, m12: q1.x, m22: q1.y, m32: q1.z },
            r: Matrix2 { m11: r0_0, m21: R::zero(), m12: r0_1, m22: r1_1 },
        }
    }

    /// The orthonormal factor. Exact: a move. Upstream: `QR::q`.
    #[inline(always)]
    fn q(self: Qr3x2<T>) -> Matrix3x2<T> {
        self.q
    }

    /// The upper triangular factor. Exact: a move. Upstream: `QR::r`.
    #[inline(always)]
    fn r(self: Qr3x2<T>) -> Matrix2<T> {
        self.r
    }

    /// `(q, r)`. Exact: moves. Upstream: `QR::unpack`.
    #[inline(always)]
    fn unpack(self: Qr3x2<T>) -> (Matrix3x2<T>, Matrix2<T>) {
        (self.q, self.r)
    }

    /// The upper triangular factor, consuming the factorisation. Exact. Upstream: `QR::unpack_r`.
    #[inline(always)]
    fn unpack_r(self: Qr3x2<T>) -> Matrix2<T> {
        self.r
    }

    /// The factors as stored, `(q, r)` (modified Gram-Schmidt keeps both; upstream packs the
    /// reflectors). Exact. Upstream: `QR::qr_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn qr_internal(self: Qr3x2<T>) -> (Matrix3x2<T>, Matrix2<T>) {
        (self.q, self.r)
    }
}

/// `Matrix3x2` methods that go through the QR factorisation. Import `Matrix3x2QrTrait` to use them.
#[generate_trait]
pub impl Matrix3x2QrImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix3x2QrTrait<T> {
    /// The QR factorisation. Upstream: `Matrix::qr`.
    #[inline(always)]
    fn qr(self: Matrix3x2<T>) -> Qr3x2<T> {
        Qr3x2Trait::new(self)
    }
}
