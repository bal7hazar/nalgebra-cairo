// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Qr4x5`: the QR factorisation of a `Matrix4x5` (upstream `nalgebra::linalg::QR<T, U4, U5>`) by
//! modified Gram-Schmidt, the algorithm of `Qr3` / `Qr4` (see `linalg::qr` for the study of the
//! alternatives), generated for the shapes P14a did not cover (WP 8.5-P14b).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix4::Matrix4;
use crate::base::matrix4x5::Matrix4x5;
use crate::base::solve::SolveKernel;
use crate::base::vector4::Vector4;

/// The QR factorisation `A = q * r` of a `Matrix4x5<T>`: `q` is 4x4, `r` is 4x5
/// upper triangular (trapezoidal) with a non-negative diagonal and an exactly zero strict lower
/// part — upstream's unpacked `q()` / `r()`. A column that collapses to exactly zero gives a
/// zero column of `q` and a zero diagonal entry of `r` (`q * r = A` still holds; see
/// `linalg::qr`). Upstream: `nalgebra::linalg::QR`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Qr4x5<T> {
    /// The orthonormal factor (4x4).
    pub q: Matrix4<T>,
    /// The upper triangular factor (4x5), `r_ii >= 0`.
    pub r: Matrix4x5<T>,
}

/// Test-only field-wise equality (upstream `QR` has no `PartialEq`).
#[cfg(test)]
impl Qr4x5PartialEq<T, +PartialEq<T>> of PartialEq<Qr4x5<T>> {
    fn eq(lhs: @Qr4x5<T>, rhs: @Qr4x5<T>) -> bool {
        lhs.q == rhs.q && lhs.r == rhs.r
    }
}

/// Methods of `Qr4x5<T>` for any `Real` scalar.
#[generate_trait]
pub impl Qr4x5Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Qr4x5Trait<T> {
    /// The QR factorisation of `matrix` by modified Gram-Schmidt, fully unrolled: for each of
    /// the 4 leading columns, `r_ii = |a_i|` (floored norm of the exact sum of squares), `q_i =
    /// a_i / r_ii` (one prepared divisor), then `r_ij = <q_i, a_j>` (fused) and `a_j -= r_ij q_i`
    /// (one `mul_add` per component) for the later columns. Panics on overflow of a norm.
    /// Upstream: `matrix.qr()` / `QR::new(matrix)` (Householder, same unpacked factors).
    fn new(matrix: Matrix4x5<T>) -> Qr4x5<T> {
        revoke_ap_tracking();
        let a0 = Vector4 { x: matrix.m11, y: matrix.m21, z: matrix.m31, w: matrix.m41 };
        let a1 = Vector4 { x: matrix.m12, y: matrix.m22, z: matrix.m32, w: matrix.m42 };
        let a2 = Vector4 { x: matrix.m13, y: matrix.m23, z: matrix.m33, w: matrix.m43 };
        let a3 = Vector4 { x: matrix.m14, y: matrix.m24, z: matrix.m34, w: matrix.m44 };
        let a4 = Vector4 { x: matrix.m15, y: matrix.m25, z: matrix.m35, w: matrix.m45 };
        let r0_0 = R::norm4(a0.x, a0.y, a0.z, a0.w);
        let q0 = if r0_0 == R::zero() {
            Vector4 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3) = R::div4(a0.x, a0.y, a0.z, a0.w, r0_0);
                Vector4 { x: q0, y: q1, z: q2, w: q3 }
            }
        };
        let r0_1 = R::sum_prod4(q0.x, a1.x, q0.y, a1.y, q0.z, a1.z, q0.w, a1.w);
        let r0_2 = R::sum_prod4(q0.x, a2.x, q0.y, a2.y, q0.z, a2.z, q0.w, a2.w);
        let r0_3 = R::sum_prod4(q0.x, a3.x, q0.y, a3.y, q0.z, a3.z, q0.w, a3.w);
        let r0_4 = R::sum_prod4(q0.x, a4.x, q0.y, a4.y, q0.z, a4.z, q0.w, a4.w);
        let a1 = Vector4 {
            x: R::mul_add(-r0_1, q0.x, a1.x),
            y: R::mul_add(-r0_1, q0.y, a1.y),
            z: R::mul_add(-r0_1, q0.z, a1.z),
            w: R::mul_add(-r0_1, q0.w, a1.w),
        };
        let a2 = Vector4 {
            x: R::mul_add(-r0_2, q0.x, a2.x),
            y: R::mul_add(-r0_2, q0.y, a2.y),
            z: R::mul_add(-r0_2, q0.z, a2.z),
            w: R::mul_add(-r0_2, q0.w, a2.w),
        };
        let a3 = Vector4 {
            x: R::mul_add(-r0_3, q0.x, a3.x),
            y: R::mul_add(-r0_3, q0.y, a3.y),
            z: R::mul_add(-r0_3, q0.z, a3.z),
            w: R::mul_add(-r0_3, q0.w, a3.w),
        };
        let a4 = Vector4 {
            x: R::mul_add(-r0_4, q0.x, a4.x),
            y: R::mul_add(-r0_4, q0.y, a4.y),
            z: R::mul_add(-r0_4, q0.z, a4.z),
            w: R::mul_add(-r0_4, q0.w, a4.w),
        };
        let r1_1 = R::norm4(a1.x, a1.y, a1.z, a1.w);
        let q1 = if r1_1 == R::zero() {
            Vector4 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3) = R::div4(a1.x, a1.y, a1.z, a1.w, r1_1);
                Vector4 { x: q0, y: q1, z: q2, w: q3 }
            }
        };
        let r1_2 = R::sum_prod4(q1.x, a2.x, q1.y, a2.y, q1.z, a2.z, q1.w, a2.w);
        let r1_3 = R::sum_prod4(q1.x, a3.x, q1.y, a3.y, q1.z, a3.z, q1.w, a3.w);
        let r1_4 = R::sum_prod4(q1.x, a4.x, q1.y, a4.y, q1.z, a4.z, q1.w, a4.w);
        let a2 = Vector4 {
            x: R::mul_add(-r1_2, q1.x, a2.x),
            y: R::mul_add(-r1_2, q1.y, a2.y),
            z: R::mul_add(-r1_2, q1.z, a2.z),
            w: R::mul_add(-r1_2, q1.w, a2.w),
        };
        let a3 = Vector4 {
            x: R::mul_add(-r1_3, q1.x, a3.x),
            y: R::mul_add(-r1_3, q1.y, a3.y),
            z: R::mul_add(-r1_3, q1.z, a3.z),
            w: R::mul_add(-r1_3, q1.w, a3.w),
        };
        let a4 = Vector4 {
            x: R::mul_add(-r1_4, q1.x, a4.x),
            y: R::mul_add(-r1_4, q1.y, a4.y),
            z: R::mul_add(-r1_4, q1.z, a4.z),
            w: R::mul_add(-r1_4, q1.w, a4.w),
        };
        let r2_2 = R::norm4(a2.x, a2.y, a2.z, a2.w);
        let q2 = if r2_2 == R::zero() {
            Vector4 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3) = R::div4(a2.x, a2.y, a2.z, a2.w, r2_2);
                Vector4 { x: q0, y: q1, z: q2, w: q3 }
            }
        };
        let r2_3 = R::sum_prod4(q2.x, a3.x, q2.y, a3.y, q2.z, a3.z, q2.w, a3.w);
        let r2_4 = R::sum_prod4(q2.x, a4.x, q2.y, a4.y, q2.z, a4.z, q2.w, a4.w);
        let a3 = Vector4 {
            x: R::mul_add(-r2_3, q2.x, a3.x),
            y: R::mul_add(-r2_3, q2.y, a3.y),
            z: R::mul_add(-r2_3, q2.z, a3.z),
            w: R::mul_add(-r2_3, q2.w, a3.w),
        };
        let a4 = Vector4 {
            x: R::mul_add(-r2_4, q2.x, a4.x),
            y: R::mul_add(-r2_4, q2.y, a4.y),
            z: R::mul_add(-r2_4, q2.z, a4.z),
            w: R::mul_add(-r2_4, q2.w, a4.w),
        };
        let r3_3 = R::norm4(a3.x, a3.y, a3.z, a3.w);
        let q3 = if r3_3 == R::zero() {
            Vector4 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3) = R::div4(a3.x, a3.y, a3.z, a3.w, r3_3);
                Vector4 { x: q0, y: q1, z: q2, w: q3 }
            }
        };
        let r3_4 = R::sum_prod4(q3.x, a4.x, q3.y, a4.y, q3.z, a4.z, q3.w, a4.w);
        Qr4x5 {
            q: Matrix4 {
                m11: q0.x,
                m21: q0.y,
                m31: q0.z,
                m41: q0.w,
                m12: q1.x,
                m22: q1.y,
                m32: q1.z,
                m42: q1.w,
                m13: q2.x,
                m23: q2.y,
                m33: q2.z,
                m43: q2.w,
                m14: q3.x,
                m24: q3.y,
                m34: q3.z,
                m44: q3.w,
            },
            r: Matrix4x5 {
                m11: r0_0,
                m21: R::zero(),
                m31: R::zero(),
                m41: R::zero(),
                m12: r0_1,
                m22: r1_1,
                m32: R::zero(),
                m42: R::zero(),
                m13: r0_2,
                m23: r1_2,
                m33: r2_2,
                m43: R::zero(),
                m14: r0_3,
                m24: r1_3,
                m34: r2_3,
                m44: r3_3,
                m15: r0_4,
                m25: r1_4,
                m35: r2_4,
                m45: r3_4,
            },
        }
    }

    /// The orthonormal factor. Exact: a move. Upstream: `QR::q`.
    #[inline(always)]
    fn q(self: Qr4x5<T>) -> Matrix4<T> {
        self.q
    }

    /// The upper triangular factor. Exact: a move. Upstream: `QR::r`.
    #[inline(always)]
    fn r(self: Qr4x5<T>) -> Matrix4x5<T> {
        self.r
    }

    /// `(q, r)`. Exact: moves. Upstream: `QR::unpack`.
    #[inline(always)]
    fn unpack(self: Qr4x5<T>) -> (Matrix4<T>, Matrix4x5<T>) {
        (self.q, self.r)
    }

    /// The upper triangular factor, consuming the factorisation. Exact. Upstream: `QR::unpack_r`.
    #[inline(always)]
    fn unpack_r(self: Qr4x5<T>) -> Matrix4x5<T> {
        self.r
    }

    /// The factors as stored, `(q, r)` (modified Gram-Schmidt keeps both; upstream packs the
    /// reflectors). Exact. Upstream: `QR::qr_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn qr_internal(self: Qr4x5<T>) -> (Matrix4<T>, Matrix4x5<T>) {
        (self.q, self.r)
    }

    /// `rhs = Qᵀ * rhs` in place, for any `rhs` with 4 rows (a vector or a matrix): ONE fused
    /// sum of products per entry, floored once. Upstream: `QR::q_tr_mul` (the Householder
    /// reflections one after the other, a rounding each).
    fn q_tr_mul<B, impl K: SolveKernel<Matrix4<T>, B>, +Drop<B>>(self: Qr4x5<T>, ref rhs: B) {
        rhs = K::tr_mul_rhs(self.q, rhs);
    }
}

/// `Matrix4x5` methods that go through the QR factorisation. Import `Matrix4x5QrTrait` to use them.
#[generate_trait]
pub impl Matrix4x5QrImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix4x5QrTrait<T> {
    /// The QR factorisation. Upstream: `Matrix::qr`.
    #[inline(always)]
    fn qr(self: Matrix4x5<T>) -> Qr4x5<T> {
        Qr4x5Trait::new(self)
    }
}
