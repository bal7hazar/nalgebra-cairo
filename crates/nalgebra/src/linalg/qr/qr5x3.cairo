// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Qr5x3`: the QR factorisation of a `Matrix5x3` (upstream `nalgebra::linalg::QR<T, U5, U3>`) by
//! modified Gram-Schmidt, the algorithm of `Qr3` / `Qr4` (see `linalg::qr` for the study of the
//! alternatives), generated for the shapes P14a did not cover (WP 8.5-P14b).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix3::Matrix3;
use crate::base::matrix5x3::Matrix5x3;
use crate::base::vector5::Vector5;

/// The QR factorisation `A = q * r` of a `Matrix5x3<T>`: `q` is 5x3 (thin: 5x3, orthonormal
/// COLUMNS), `r` is 3x3 upper triangular (trapezoidal) with a non-negative diagonal and an exactly
/// zero strict lower part — upstream's unpacked `q()` / `r()`. A column that collapses to exactly
/// zero gives a zero column of `q` and a zero diagonal entry of `r` (`q * r = A` still holds; see
/// `linalg::qr`). Upstream: `nalgebra::linalg::QR`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Qr5x3<T> {
    /// The orthonormal factor (5x3).
    pub q: Matrix5x3<T>,
    /// The upper triangular factor (3x3), `r_ii >= 0`.
    pub r: Matrix3<T>,
}

/// Test-only field-wise equality (upstream `QR` has no `PartialEq`).
#[cfg(test)]
impl Qr5x3PartialEq<T, +PartialEq<T>> of PartialEq<Qr5x3<T>> {
    fn eq(lhs: @Qr5x3<T>, rhs: @Qr5x3<T>) -> bool {
        lhs.q == rhs.q && lhs.r == rhs.r
    }
}

/// Methods of `Qr5x3<T>` for any `Real` scalar.
#[generate_trait]
pub impl Qr5x3Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Qr5x3Trait<T> {
    /// The QR factorisation of `matrix` by modified Gram-Schmidt, fully unrolled: for each of
    /// the 3 leading columns, `r_ii = |a_i|` (floored norm of the exact sum of squares), `q_i =
    /// a_i / r_ii` (one prepared divisor), then `r_ij = <q_i, a_j>` (fused) and `a_j -= r_ij q_i`
    /// (one `mul_add` per component) for the later columns. Panics on overflow of a norm.
    /// Upstream: `matrix.qr()` / `QR::new(matrix)` (Householder, same unpacked factors).
    fn new(matrix: Matrix5x3<T>) -> Qr5x3<T> {
        revoke_ap_tracking();
        let a0 = Vector5 {
            x: matrix.m11, y: matrix.m21, z: matrix.m31, w: matrix.m41, a: matrix.m51,
        };
        let a1 = Vector5 {
            x: matrix.m12, y: matrix.m22, z: matrix.m32, w: matrix.m42, a: matrix.m52,
        };
        let a2 = Vector5 {
            x: matrix.m13, y: matrix.m23, z: matrix.m33, w: matrix.m43, a: matrix.m53,
        };
        let r0_0 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), a0.x, a0.x), a0.y, a0.y),
                        a0.z,
                        a0.z,
                    ),
                    a0.w,
                    a0.w,
                ),
                a0.a,
                a0.a,
            ),
        );
        let q0 = if r0_0 == R::zero() {
            Vector5 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3, q4) = R::div5(a0.x, a0.y, a0.z, a0.w, a0.a, r0_0);
                Vector5 { x: q0, y: q1, z: q2, w: q3, a: q4 }
            }
        };
        let r0_1 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), q0.x, a1.x), q0.y, a1.y),
                        q0.z,
                        a1.z,
                    ),
                    q0.w,
                    a1.w,
                ),
                q0.a,
                a1.a,
            ),
        );
        let r0_2 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), q0.x, a2.x), q0.y, a2.y),
                        q0.z,
                        a2.z,
                    ),
                    q0.w,
                    a2.w,
                ),
                q0.a,
                a2.a,
            ),
        );
        let a1 = Vector5 {
            x: R::mul_add(-r0_1, q0.x, a1.x),
            y: R::mul_add(-r0_1, q0.y, a1.y),
            z: R::mul_add(-r0_1, q0.z, a1.z),
            w: R::mul_add(-r0_1, q0.w, a1.w),
            a: R::mul_add(-r0_1, q0.a, a1.a),
        };
        let a2 = Vector5 {
            x: R::mul_add(-r0_2, q0.x, a2.x),
            y: R::mul_add(-r0_2, q0.y, a2.y),
            z: R::mul_add(-r0_2, q0.z, a2.z),
            w: R::mul_add(-r0_2, q0.w, a2.w),
            a: R::mul_add(-r0_2, q0.a, a2.a),
        };
        let r1_1 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), a1.x, a1.x), a1.y, a1.y),
                        a1.z,
                        a1.z,
                    ),
                    a1.w,
                    a1.w,
                ),
                a1.a,
                a1.a,
            ),
        );
        let q1 = if r1_1 == R::zero() {
            Vector5 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3, q4) = R::div5(a1.x, a1.y, a1.z, a1.w, a1.a, r1_1);
                Vector5 { x: q0, y: q1, z: q2, w: q3, a: q4 }
            }
        };
        let r1_2 = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), q1.x, a2.x), q1.y, a2.y),
                        q1.z,
                        a2.z,
                    ),
                    q1.w,
                    a2.w,
                ),
                q1.a,
                a2.a,
            ),
        );
        let a2 = Vector5 {
            x: R::mul_add(-r1_2, q1.x, a2.x),
            y: R::mul_add(-r1_2, q1.y, a2.y),
            z: R::mul_add(-r1_2, q1.z, a2.z),
            w: R::mul_add(-r1_2, q1.w, a2.w),
            a: R::mul_add(-r1_2, q1.a, a2.a),
        };
        let r2_2 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), a2.x, a2.x), a2.y, a2.y),
                        a2.z,
                        a2.z,
                    ),
                    a2.w,
                    a2.w,
                ),
                a2.a,
                a2.a,
            ),
        );
        let q2 = if r2_2 == R::zero() {
            Vector5 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3, q4) = R::div5(a2.x, a2.y, a2.z, a2.w, a2.a, r2_2);
                Vector5 { x: q0, y: q1, z: q2, w: q3, a: q4 }
            }
        };
        Qr5x3 {
            q: Matrix5x3 {
                m11: q0.x,
                m21: q0.y,
                m31: q0.z,
                m41: q0.w,
                m51: q0.a,
                m12: q1.x,
                m22: q1.y,
                m32: q1.z,
                m42: q1.w,
                m52: q1.a,
                m13: q2.x,
                m23: q2.y,
                m33: q2.z,
                m43: q2.w,
                m53: q2.a,
            },
            r: Matrix3 {
                m11: r0_0,
                m21: R::zero(),
                m31: R::zero(),
                m12: r0_1,
                m22: r1_1,
                m32: R::zero(),
                m13: r0_2,
                m23: r1_2,
                m33: r2_2,
            },
        }
    }

    /// The orthonormal factor. Exact: a move. Upstream: `QR::q`.
    #[inline(always)]
    fn q(self: Qr5x3<T>) -> Matrix5x3<T> {
        self.q
    }

    /// The upper triangular factor. Exact: a move. Upstream: `QR::r`.
    #[inline(always)]
    fn r(self: Qr5x3<T>) -> Matrix3<T> {
        self.r
    }

    /// `(q, r)`. Exact: moves. Upstream: `QR::unpack`.
    #[inline(always)]
    fn unpack(self: Qr5x3<T>) -> (Matrix5x3<T>, Matrix3<T>) {
        (self.q, self.r)
    }

    /// The upper triangular factor, consuming the factorisation. Exact. Upstream: `QR::unpack_r`.
    #[inline(always)]
    fn unpack_r(self: Qr5x3<T>) -> Matrix3<T> {
        self.r
    }

    /// The factors as stored, `(q, r)` (modified Gram-Schmidt keeps both; upstream packs the
    /// reflectors). Exact. Upstream: `QR::qr_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn qr_internal(self: Qr5x3<T>) -> (Matrix5x3<T>, Matrix3<T>) {
        (self.q, self.r)
    }
}

/// `Matrix5x3` methods that go through the QR factorisation. Import `Matrix5x3QrTrait` to use them.
#[generate_trait]
pub impl Matrix5x3QrImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix5x3QrTrait<T> {
    /// The QR factorisation. Upstream: `Matrix::qr`.
    #[inline(always)]
    fn qr(self: Matrix5x3<T>) -> Qr5x3<T> {
        Qr5x3Trait::new(self)
    }
}
