// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Qr5x1`: the QR factorisation of a `Vector5` (upstream `nalgebra::linalg::QR<T, U5, U1>`) by
//! modified Gram-Schmidt, the algorithm of `Qr3` / `Qr4` (see `linalg::qr` for the study of the
//! alternatives), generated for the shapes P14a did not cover (WP 8.5-P14b).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix1::Matrix1;
use crate::base::vector5::Vector5;

/// The QR factorisation `A = q * r` of a `Vector5<T>`: `q` is 5x1 (thin: 5x1, orthonormal COLUMNS),
/// `r` is 1x1 upper triangular (trapezoidal) with a non-negative diagonal and an exactly zero
/// strict lower part — upstream's unpacked `q()` / `r()`. A column that collapses to exactly zero
/// gives a zero column of `q` and a zero diagonal entry of `r` (`q * r = A` still holds; see
/// `linalg::qr`). Upstream: `nalgebra::linalg::QR`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Qr5x1<T> {
    /// The orthonormal factor (5x1).
    pub q: Vector5<T>,
    /// The upper triangular factor (1x1), `r_ii >= 0`.
    pub r: Matrix1<T>,
}

/// Test-only field-wise equality (upstream `QR` has no `PartialEq`).
#[cfg(test)]
impl Qr5x1PartialEq<T, +PartialEq<T>> of PartialEq<Qr5x1<T>> {
    fn eq(lhs: @Qr5x1<T>, rhs: @Qr5x1<T>) -> bool {
        lhs.q == rhs.q && lhs.r == rhs.r
    }
}

/// Methods of `Qr5x1<T>` for any `Real` scalar.
#[generate_trait]
pub impl Qr5x1Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Qr5x1Trait<T> {
    /// The QR factorisation of `matrix` by modified Gram-Schmidt, fully unrolled: for each of
    /// the 1 leading columns, `r_ii = |a_i|` (floored norm of the exact sum of squares), `q_i =
    /// a_i / r_ii` (one prepared divisor), then `r_ij = <q_i, a_j>` (fused) and `a_j -= r_ij q_i`
    /// (one `mul_add` per component) for the later columns. Panics on overflow of a norm.
    /// Upstream: `matrix.qr()` / `QR::new(matrix)` (Householder, same unpacked factors).
    fn new(matrix: Vector5<T>) -> Qr5x1<T> {
        revoke_ap_tracking();
        let a0 = Vector5 { x: matrix.x, y: matrix.y, z: matrix.z, w: matrix.w, a: matrix.a };
        let r0_0 = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_add_prod(R::wide_zero(), a0.x, a0.x), a0.y, a0.y),
                        a0.z,
                        a0.z,
                    ),
                    a0.w,
                    a0.w,
                ),
                a0.a,
                a0.a,
            ),
        );
        let q0 = if r0_0 == R::zero() {
            Vector5 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero() }
        } else {
            {
                let (q0, q1, q2, q3, q4) = R::div5(a0.x, a0.y, a0.z, a0.w, a0.a, r0_0);
                Vector5 { x: q0, y: q1, z: q2, w: q3, a: q4 }
            }
        };
        Qr5x1 { q: Vector5 { x: q0.x, y: q0.y, z: q0.z, w: q0.w, a: q0.a }, r: Matrix1 { x: r0_0 } }
    }

    /// The orthonormal factor. Exact: a move. Upstream: `QR::q`.
    #[inline(always)]
    fn q(self: Qr5x1<T>) -> Vector5<T> {
        self.q
    }

    /// The upper triangular factor. Exact: a move. Upstream: `QR::r`.
    #[inline(always)]
    fn r(self: Qr5x1<T>) -> Matrix1<T> {
        self.r
    }

    /// `(q, r)`. Exact: moves. Upstream: `QR::unpack`.
    #[inline(always)]
    fn unpack(self: Qr5x1<T>) -> (Vector5<T>, Matrix1<T>) {
        (self.q, self.r)
    }

    /// The upper triangular factor, consuming the factorisation. Exact. Upstream: `QR::unpack_r`.
    #[inline(always)]
    fn unpack_r(self: Qr5x1<T>) -> Matrix1<T> {
        self.r
    }

    /// The factors as stored, `(q, r)` (modified Gram-Schmidt keeps both; upstream packs the
    /// reflectors). Exact. Upstream: `QR::qr_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn qr_internal(self: Qr5x1<T>) -> (Vector5<T>, Matrix1<T>) {
        (self.q, self.r)
    }
}

/// `Vector5` methods that go through the QR factorisation. Import `Vector5QrTrait` to use them.
#[generate_trait]
pub impl Vector5QrImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Vector5QrTrait<T> {
    /// The QR factorisation. Upstream: `Matrix::qr`.
    #[inline(always)]
    fn qr(self: Vector5<T>) -> Qr5x1<T> {
        Qr5x1Trait::new(self)
    }
}
