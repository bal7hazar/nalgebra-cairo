// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `FullPivLu3`: the LU factorisation with full pivoting of a `Matrix3` (upstream
//! `nalgebra::linalg::FullPivLU<T, U3, U3>`), fully unrolled (WP 8.5-P15).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix3::Matrix3;
use crate::base::solve::SolveKernel;
use crate::linalg::lu::Perm3;
use crate::linalg::permutation_sequence::{PermuteColumns, PermuteRows};

/// The LU factorisation with full (row and column) pivoting of a `Matrix3<T>`: `P A Q = L U`.
///
/// `lu` packs both factors like upstream (strict lower triangle = `L` without its unit diagonal,
/// upper triangle = `U`); `p` holds the row transpositions and `q` the column transpositions, as
/// the compact sequences `Perm3` / `Perm3` — the permutations of the rows / columns of the
/// matrix, where upstream's `PermutationSequence<DimMinimum<R, C>>` holds the same `min(R, C)`
/// transpositions (a step that swaps nothing records the identity, as upstream's `append` of
/// `(i, i)`). Built by `FullPivLu3Trait::new` or `Matrix3FullPivLuTrait::full_piv_lu`.
/// Upstream: `nalgebra::linalg::FullPivLU`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct FullPivLu3<T> {
    /// `L` (strict lower triangle) and `U` (upper triangle) packed in one matrix.
    pub lu: Matrix3<T>,
    /// The row transpositions (`P`).
    pub p: Perm3,
    /// The column transpositions (`Q`).
    pub q: Perm3,
}

/// Methods of `FullPivLu3<T>` for any `Real` scalar.
#[generate_trait]
pub impl FullPivLu3Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of FullPivLu3Trait<T> {
    /// The LU factorisation of `matrix` with full pivoting, fully unrolled. Upstream:
    /// `matrix.full_piv_lu()` / `FullPivLU::new(matrix)`.
    ///
    /// Always succeeds, like upstream. At step `k` the pivot is the FIRST entry of largest `|a_ij|`
    /// of the trailing block in column-major order (upstream's `icamax_full`: strict `>`, so ties
    /// go to the lowest column, then the lowest row); its column is swapped with column `k` and its
    /// row with row `k` (whole rows and columns: upstream's `swap_columns` + `gauss_step_swap`).
    /// A trailing block that is exactly zero ends the elimination (upstream's `break`): nothing is
    /// swapped nor recorded from there on. The multipliers `l_ik = a_ik / a_kk` are correctly
    /// rounded quotients by one prepared divisor (upstream multiplies by a rounded `1 / a_kk`, as
    /// in `gauss_step`), and every update is ONE `Real::mul_add` (one floor per entry per step).
    /// Panics on overflow of an update.
    fn new(matrix: Matrix3<T>) -> FullPivLu3<T> {
        revoke_ap_tracking();
        let mut a00 = matrix.m11;
        let mut a10 = matrix.m21;
        let mut a20 = matrix.m31;
        let mut a01 = matrix.m12;
        let mut a11 = matrix.m22;
        let mut a21 = matrix.m32;
        let mut a02 = matrix.m13;
        let mut a12 = matrix.m23;
        let mut a22 = matrix.m33;
        // step 1: the first largest |a_ij| of rows 1..3, columns 1..3 (column-major scan)
        let mut piv = R::abs(a00);
        let mut rp0 = 1_u8;
        let mut cp0 = 1_u8;
        let x = R::abs(a10);
        if x > piv {
            piv = x;
            rp0 = 2;
            cp0 = 1;
        }
        let x = R::abs(a20);
        if x > piv {
            piv = x;
            rp0 = 3;
            cp0 = 1;
        }
        let x = R::abs(a01);
        if x > piv {
            piv = x;
            rp0 = 1;
            cp0 = 2;
        }
        let x = R::abs(a11);
        if x > piv {
            piv = x;
            rp0 = 2;
            cp0 = 2;
        }
        let x = R::abs(a21);
        if x > piv {
            piv = x;
            rp0 = 3;
            cp0 = 2;
        }
        let x = R::abs(a02);
        if x > piv {
            piv = x;
            rp0 = 1;
            cp0 = 3;
        }
        let x = R::abs(a12);
        if x > piv {
            piv = x;
            rp0 = 2;
            cp0 = 3;
        }
        let x = R::abs(a22);
        if x > piv {
            piv = x;
            rp0 = 3;
            cp0 = 3;
        }
        if piv != R::zero() {
            if cp0 == 2 {
                let t = a00;
                a00 = a01;
                a01 = t;
                let t = a10;
                a10 = a11;
                a11 = t;
                let t = a20;
                a20 = a21;
                a21 = t;
            } else if cp0 == 3 {
                let t = a00;
                a00 = a02;
                a02 = t;
                let t = a10;
                a10 = a12;
                a12 = t;
                let t = a20;
                a20 = a22;
                a22 = t;
            }
            if rp0 == 2 {
                let t = a00;
                a00 = a10;
                a10 = t;
                let t = a01;
                a01 = a11;
                a11 = t;
                let t = a02;
                a02 = a12;
                a12 = t;
            } else if rp0 == 3 {
                let t = a00;
                a00 = a20;
                a20 = t;
                let t = a01;
                a01 = a21;
                a21 = t;
                let t = a02;
                a02 = a22;
                a22 = t;
            }
            let (l1, l2) = (R::div(a10, a00), R::div(a20, a00));
            let nl = -l1;
            a11 = R::mul_add(nl, a01, a11);
            a12 = R::mul_add(nl, a02, a12);
            a10 = l1;
            let nl = -l2;
            a21 = R::mul_add(nl, a01, a21);
            a22 = R::mul_add(nl, a02, a22);
            a20 = l2;
        }
        // step 2: the first largest |a_ij| of rows 2..3, columns 2..3 (column-major scan)
        let mut piv = R::abs(a11);
        let mut rp1 = 2_u8;
        let mut cp1 = 2_u8;
        let x = R::abs(a21);
        if x > piv {
            piv = x;
            rp1 = 3;
            cp1 = 2;
        }
        let x = R::abs(a12);
        if x > piv {
            piv = x;
            rp1 = 2;
            cp1 = 3;
        }
        let x = R::abs(a22);
        if x > piv {
            piv = x;
            rp1 = 3;
            cp1 = 3;
        }
        if piv != R::zero() {
            if cp1 == 3 {
                let t = a01;
                a01 = a02;
                a02 = t;
                let t = a11;
                a11 = a12;
                a12 = t;
                let t = a21;
                a21 = a22;
                a22 = t;
            }
            if rp1 == 3 {
                let t = a10;
                a10 = a20;
                a20 = t;
                let t = a11;
                a11 = a21;
                a21 = t;
                let t = a12;
                a12 = a22;
                a22 = t;
            }
            let l2 = R::div(a21, a11);
            let nl = -l2;
            a22 = R::mul_add(nl, a12, a22);
            a21 = l2;
        }
        FullPivLu3 {
            lu: Matrix3 {
                m11: a00,
                m21: a10,
                m31: a20,
                m12: a01,
                m22: a11,
                m32: a21,
                m13: a02,
                m23: a12,
                m33: a22,
            },
            p: Perm3 { p1: rp0, p2: rp1 },
            q: Perm3 { p1: cp0, p2: cp1 },
        }
    }

    /// The packed factors as stored (strict lower triangle = `L`, upper triangle = `U`). Exact.
    /// Upstream: `FullPivLU::lu_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn lu_internal(self: FullPivLu3<T>) -> Matrix3<T> {
        self.lu
    }

    /// The unit lower triangular factor `L` (3x3). Exact: moves. Upstream: `FullPivLU::l`.
    #[inline(always)]
    fn l(self: FullPivLu3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: R::one(),
            m21: self.lu.m21,
            m31: self.lu.m31,
            m12: R::zero(),
            m22: R::one(),
            m32: self.lu.m32,
            m13: R::zero(),
            m23: R::zero(),
            m33: R::one(),
        }
    }

    /// The upper triangular factor `U` (3x3). Exact: moves. Upstream: `FullPivLU::u`.
    #[inline(always)]
    fn u(self: FullPivLu3<T>) -> Matrix3<T> {
        Matrix3 {
            m11: self.lu.m11,
            m21: R::zero(),
            m31: R::zero(),
            m12: self.lu.m12,
            m22: self.lu.m22,
            m32: R::zero(),
            m13: self.lu.m13,
            m23: self.lu.m23,
            m33: self.lu.m33,
        }
    }

    /// The row permutation `P`. Upstream: `FullPivLU::p`.
    #[inline(always)]
    fn p(self: FullPivLu3<T>) -> Perm3 {
        self.p
    }

    /// The column permutation `Q`. Upstream: `FullPivLU::q`.
    #[inline(always)]
    fn q(self: FullPivLu3<T>) -> Perm3 {
        self.q
    }

    /// `(P, L, U, Q)`: `(p(), l(), u(), q())`, exact. Upstream: `FullPivLU::unpack`.
    #[inline(always)]
    fn unpack(self: FullPivLu3<T>) -> (Perm3, Matrix3<T>, Matrix3<T>, Perm3) {
        (self.p, Self::l(self), Self::u(self), self.q)
    }

    /// Whether the factored matrix is invertible: the LAST pivot is exactly nonzero, like
    /// upstream's `FullPivLU::is_invertible` (full pivoting makes the pivots non-increasing in
    /// magnitude: an exactly zero trailing block leaves zeros from its first pivot to the last).
    #[inline(always)]
    fn is_invertible(self: FullPivLu3<T>) -> bool {
        self.lu.m33 != R::zero()
    }

    /// The determinant: `sign(P) sign(Q)` times the product of the 3 pivots, or exactly zero when
    /// the last pivot is (upstream's early return). The sign goes on the last pivot (an exact
    /// negation), then the chain is upstream's order — the last pivot times the others from the
    /// first — one floor per product (`Real` has no exact product of three scalars). Panics on
    /// overflow of a partial product. Upstream: `FullPivLU::determinant`.
    fn determinant(self: FullPivLu3<T>) -> T {
        if self.lu.m33 == R::zero() {
            return R::zero();
        }
        let mut neg = false;
        if self.p.p1 != 1 {
            neg = !neg;
        }
        if self.p.p2 != 2 {
            neg = !neg;
        }
        if self.q.p1 != 1 {
            neg = !neg;
        }
        if self.q.p2 != 2 {
            neg = !neg;
        }
        let d = if neg {
            -self.lu.m33
        } else {
            self.lu.m33
        };
        let d = d * self.lu.m11;
        let d = d * self.lu.m22;
        d
    }

    /// Overwrites `b` (any shape with 3 rows: a vector or a matrix) with the solution `x` of
    /// `A x = b` and returns `true`, or returns `false` and leaves `b` unchanged when the matrix
    /// is not invertible (`is_invertible`; upstream may leave `b` overwritten). Upstream's steps
    /// on every column at once: `P b` (moves), `L y = P b` (unit diagonal: one floor per
    /// component), `U z = y` (one fused numerator and one correctly rounded division per
    /// component), `x = Qᵀ z` (moves). Panics on overflow. Upstream: `FullPivLU::solve_mut`.
    fn solve_mut<B, impl P: PermuteRows<Perm3, B>, impl K: SolveKernel<Matrix3<T>, B>, +Drop<B>>(
        self: FullPivLu3<T>, ref b: B,
    ) -> bool {
        if !Self::is_invertible(self) {
            return false;
        }
        P::permute_rows(self.p, ref b);
        b = K::upper(self.lu, K::lower_unit(self.lu, b));
        P::inv_permute_rows(self.q, ref b);
        true
    }

    /// The solution of `A x = b` for any `b` with 3 rows, or `None` when the matrix is not
    /// invertible: `solve_mut` on a copy. Upstream: `FullPivLU::solve`.
    fn solve<B, impl P: PermuteRows<Perm3, B>, impl K: SolveKernel<Matrix3<T>, B>, +Drop<B>>(
        self: FullPivLu3<T>, b: B,
    ) -> Option<B> {
        let mut x = b;
        if Self::solve_mut(self, ref x) {
            Some(x)
        } else {
            None
        }
    }

    /// The inverse, or `None` when the matrix is not invertible. Upstream solves against the
    /// identity (`FullPivLU::try_inverse`); here `(L U)⁻¹` is computed on the STATIC unit
    /// columns (their zeros vanish at generation time: bit-identical to the general solve, fewer
    /// products), one prepared divisor per row of the back substitution, then the columns are
    /// permuted by `P`
    /// and the rows by `Qᵀ` (moves). Two roundings per entry of the back substitution. Panics on
    /// overflow. Upstream: `FullPivLU::try_inverse`.
    fn try_inverse(self: FullPivLu3<T>) -> Option<Matrix3<T>> {
        revoke_ap_tracking();
        if !Self::is_invertible(self) {
            return None;
        }
        let y10 = -self.lu.m21;
        let y20 = R::wide_rescale(
            R::wide_sub_prod(R::wide_sub(R::wide_zero(), self.lu.m31), self.lu.m32, y10),
        );
        let y21 = -self.lu.m32;
        let (x20, x21, x22) = R::div3(y20, y21, R::one(), self.lu.m33);
        let (x10, x11, x12) = R::div3(
            R::mul_add(-self.lu.m23, x20, y10),
            R::mul_add(-self.lu.m23, x21, R::one()),
            (-self.lu.m23) * x22,
            self.lu.m22,
        );
        let (x00, x01, x02) = R::div3(
            R::wide_rescale(
                R::wide_sub_prod(
                    R::wide_sub_prod(R::wide_add(R::wide_zero(), R::one()), self.lu.m12, x10),
                    self.lu.m13,
                    x20,
                ),
            ),
            R::wide_rescale(
                R::wide_sub_prod(
                    R::wide_sub_prod(R::wide_zero(), self.lu.m12, x11), self.lu.m13, x21,
                ),
            ),
            R::wide_rescale(
                R::wide_sub_prod(
                    R::wide_sub_prod(R::wide_zero(), self.lu.m12, x12), self.lu.m13, x22,
                ),
            ),
            self.lu.m11,
        );
        let mut x = Matrix3 {
            m11: x00,
            m21: x10,
            m31: x20,
            m12: x01,
            m22: x11,
            m32: x21,
            m13: x02,
            m23: x12,
            m33: x22,
        };
        PermuteColumns::inv_permute_columns(self.p, ref x);
        PermuteRows::inv_permute_rows(self.q, ref x);
        Some(x)
    }
}

/// `Matrix3` methods that go through the full-pivot LU factorisation. Import
/// `Matrix3FullPivLuTrait`.
#[generate_trait]
pub impl Matrix3FullPivLuImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix3FullPivLuTrait<T> {
    /// The LU factorisation with full pivoting. Upstream: `Matrix::full_piv_lu`.
    #[inline(always)]
    fn full_piv_lu(self: Matrix3<T>) -> FullPivLu3<T> {
        FullPivLu3Trait::new(self)
    }
}
