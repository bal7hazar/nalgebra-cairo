// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `FullPivLu2x1`: the LU factorisation with full pivoting of a `Vector2` (upstream
//! `nalgebra::linalg::FullPivLU<T, U2, U1>`), fully unrolled (WP 8.5-P15).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix1::Matrix1;
use crate::base::vector2::Vector2;
use crate::linalg::lu::{Perm1, Perm2};

/// The LU factorisation with full (row and column) pivoting of a `Vector2<T>`: `P A Q = L U`.
///
/// `lu` packs both factors like upstream (strict lower triangle = `L` without its unit diagonal,
/// upper triangle = `U`); `p` holds the row transpositions and `q` the column transpositions, as
/// the compact sequences `Perm2` / `Perm1` — the permutations of the rows / columns of the
/// matrix, where upstream's `PermutationSequence<DimMinimum<R, C>>` holds the same `min(R, C)`
/// transpositions (a step that swaps nothing records the identity, as upstream's `append` of
/// `(i, i)`).
///
/// Non-square: `m = min(2, 1) = 1` elimination steps; `L` is 2x1, `U` 1x1. Built by
/// `FullPivLu2x1Trait::new` or `Vector2FullPivLuTrait::full_piv_lu`.
/// Upstream: `nalgebra::linalg::FullPivLU`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct FullPivLu2x1<T> {
    /// `L` (strict lower triangle) and `U` (upper triangle) packed in one matrix.
    pub lu: Vector2<T>,
    /// The row transpositions (`P`).
    pub p: Perm2,
    /// The column transpositions (`Q`).
    pub q: Perm1,
}

/// Methods of `FullPivLu2x1<T>` for any `Real` scalar.
#[generate_trait]
pub impl FullPivLu2x1Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of FullPivLu2x1Trait<T> {
    /// The LU factorisation of `matrix` with full pivoting, fully unrolled. Upstream:
    /// `matrix.full_piv_lu()` / `FullPivLU::new(matrix)`.
    ///
    /// Always succeeds, like upstream. At step `k` the pivot is the FIRST entry of largest `|a_ij|`
    /// of the trailing block in column-major order (upstream's `icamax_full`: strict `>`, so ties
    /// go to the lowest column, then the lowest row); its column is swapped with column `k` and its
    /// row with row `k` (whole rows and columns: upstream's `swap_columns` + `gauss_step_swap`).
    /// A trailing block that is exactly zero ends the elimination (upstream's `break`): nothing is
    /// swapped nor recorded from there on. The multipliers `l_ik = a_ik / a_kk` are correctly
    /// rounded quotients by one prepared divisor (upstream multiplies by a rounded `1 / a_kk`, as
    /// in `gauss_step`), and every update is ONE `Real::mul_add` (one floor per entry per step).
    /// Panics on overflow of an update.
    fn new(matrix: Vector2<T>) -> FullPivLu2x1<T> {
        revoke_ap_tracking();
        let mut a00 = matrix.x;
        let mut a10 = matrix.y;
        // step 1: the first largest |a_ij| of rows 1..2, columns 1..1 (column-major scan)
        let mut piv = R::abs(a00);
        let mut rp0 = 1_u8;
        let x = R::abs(a10);
        if x > piv {
            piv = x;
            rp0 = 2;
        }
        if piv != R::zero() {
            if rp0 == 2 {
                let t = a00;
                a00 = a10;
                a10 = t;
            }
            let l1 = R::div(a10, a00);
            a10 = l1;
        }
        FullPivLu2x1 { lu: Vector2 { x: a00, y: a10 }, p: Perm2 { p1: rp0 }, q: Perm1 {} }
    }

    /// The packed factors as stored (strict lower triangle = `L`, upper triangle = `U`). Exact.
    /// Upstream: `FullPivLU::lu_internal` (`#[doc(hidden)]`).
    #[inline(always)]
    fn lu_internal(self: FullPivLu2x1<T>) -> Vector2<T> {
        self.lu
    }

    /// The unit lower triangular factor `L` (2x1). Exact: moves. Upstream: `FullPivLU::l`.
    #[inline(always)]
    fn l(self: FullPivLu2x1<T>) -> Vector2<T> {
        Vector2 { x: R::one(), y: self.lu.y }
    }

    /// The upper triangular factor `U` (1x1). Exact: moves. Upstream: `FullPivLU::u`.
    #[inline(always)]
    fn u(self: FullPivLu2x1<T>) -> Matrix1<T> {
        Matrix1 { x: self.lu.x }
    }

    /// The row permutation `P`. Upstream: `FullPivLU::p`.
    #[inline(always)]
    fn p(self: FullPivLu2x1<T>) -> Perm2 {
        self.p
    }

    /// The column permutation `Q`. Upstream: `FullPivLU::q`.
    #[inline(always)]
    fn q(self: FullPivLu2x1<T>) -> Perm1 {
        self.q
    }

    /// `(P, L, U, Q)`: `(p(), l(), u(), q())`, exact. Upstream: `FullPivLU::unpack`.
    #[inline(always)]
    fn unpack(self: FullPivLu2x1<T>) -> (Perm2, Vector2<T>, Matrix1<T>, Perm1) {
        (self.p, Self::l(self), Self::u(self), self.q)
    }
}

/// `Vector2` methods that go through the full-pivot LU factorisation. Import
/// `Vector2FullPivLuTrait`.
#[generate_trait]
pub impl Vector2FullPivLuImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Vector2FullPivLuTrait<T> {
    /// The LU factorisation with full pivoting. Upstream: `Matrix::full_piv_lu`.
    #[inline(always)]
    fn full_piv_lu(self: Vector2<T>) -> FullPivLu2x1<T> {
        FullPivLu2x1Trait::new(self)
    }
}
