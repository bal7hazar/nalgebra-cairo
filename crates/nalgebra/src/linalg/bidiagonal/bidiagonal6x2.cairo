// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Bidiagonal6x2`: the bidiagonalisation of a `Matrix6x2` (upstream
//! `nalgebra::linalg::Bidiagonal<T, U6, U2>`) by Householder reflections, fully unrolled
//! (WP 8.5-P16).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix1::Matrix1;
use crate::base::matrix2::Matrix2;
use crate::base::matrix6x2::Matrix6x2;
use crate::base::vector2::Vector2;
use crate::linalg::householder_kernels::HouseholderKernelTrait;

/// The bidiagonalisation `A = U D Vᵀ` of a `Matrix6x2<T>`: `D` 2x2 UPPER bidiagonal (`6 >= 2`),
/// `U` (6x2) and `Vᵀ` (2x2) with orthonormal columns / rows.
///
/// Upstream's storage: `uv` holds the Householder axes of both sides (unit vectors: the left ones
/// below the diagonal, the right ones past the superdiagonal), `diagonal` /
/// `off_diagonal` the SIGNED norms of the reflections (their absolute values are the diagonal /
/// off-diagonal of `D`).
/// Built by `Bidiagonal6x2Trait::new` or `Matrix6x2BidiagonalTrait::bidiagonalize`. Upstream:
/// `nalgebra::linalg::Bidiagonal`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Bidiagonal6x2<T> {
    /// The Householder axes of both sides, packed.
    pub uv: Matrix6x2<T>,
    /// The SIGNED diagonal (`|diagonal|` is the diagonal of `D`).
    pub diagonal: Vector2<T>,
    /// The SIGNED off-diagonal (`|off_diagonal|` is the off-diagonal of `D`).
    pub off_diagonal: Matrix1<T>,
    /// Whether `D` is upper bidiagonal (`true` for this shape: rows >= columns).
    pub upper_diagonal: bool,
}

/// Methods of `Bidiagonal6x2<T>` for any `Real` scalar.
#[generate_trait]
pub impl Bidiagonal6x2Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Bidiagonal6x2Trait<T> {
    /// The bidiagonalisation of `matrix` by Householder reflections, fully unrolled: for `i` =
    /// 0..0: `diagonal_i = clear_column_unchecked(m, i, 0)` (a reflection from the left clears
    /// column `i` below the diagonal) then `off_diagonal_i = clear_row_unchecked(m, i, 1)` (one
    /// from the right clears row `i` past the superdiagonal); last `clear_column_unchecked(m, 1,
    /// 0)`.
    /// Upstream: `Bidiagonal::new` / `matrix.bidiagonalize()`.
    ///
    /// Each step computes the axis `u` of the reflection (`reflection_axis_mut`, see
    /// `linalg::householder_kernels`), then applies `s (x - 2 (u·x) u)` with `s = sign(norm)` to
    /// the trailing columns (left reflection) or `s (x - 2 (x·u) uᵀ)` to the trailing rows
    /// (right reflection) and stores the axis in place of the cleared entries. Rounding: one fused
    /// sum per dot product (doubled exactly), one `Real::mul_add` per updated entry. A zero column
    /// /
    /// row is left as it is (norm 0). Panics on overflow.
    fn new(matrix: Matrix6x2<T>) -> Bidiagonal6x2<T> {
        revoke_ap_tracking();
        let mut a00 = matrix.m11;
        let mut a10 = matrix.m21;
        let mut a20 = matrix.m31;
        let mut a30 = matrix.m41;
        let mut a40 = matrix.m51;
        let mut a50 = matrix.m61;
        let mut a01 = matrix.m12;
        let mut a11 = matrix.m22;
        let mut a21 = matrix.m32;
        let mut a31 = matrix.m42;
        let mut a41 = matrix.m52;
        let mut a51 = matrix.m62;
        // `d0 = clear_column_unchecked(matrix, 0, 0, None)`
        let (d0, nz_d0, uc0_0, uc0_1, uc0_2, uc0_3, uc0_4, uc0_5) = HouseholderKernelTrait::<
            T,
        >::axis6(a00, a10, a20, a30, a40, a50);
        if nz_d0 {
            let neg = d0 < R::zero();
            let h = R::wide_rescale(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(
                                    R::wide_add_prod(R::wide_zero(), uc0_0, a01), uc0_1, a11,
                                ),
                                uc0_2,
                                a21,
                            ),
                            uc0_3,
                            a31,
                        ),
                        uc0_4,
                        a41,
                    ),
                    uc0_5,
                    a51,
                ),
            );
            let w = h + h;
            let nw = -w;
            a01 = if neg {
                R::mul_add(w, uc0_0, -a01)
            } else {
                R::mul_add(nw, uc0_0, a01)
            };
            a11 = if neg {
                R::mul_add(w, uc0_1, -a11)
            } else {
                R::mul_add(nw, uc0_1, a11)
            };
            a21 = if neg {
                R::mul_add(w, uc0_2, -a21)
            } else {
                R::mul_add(nw, uc0_2, a21)
            };
            a31 = if neg {
                R::mul_add(w, uc0_3, -a31)
            } else {
                R::mul_add(nw, uc0_3, a31)
            };
            a41 = if neg {
                R::mul_add(w, uc0_4, -a41)
            } else {
                R::mul_add(nw, uc0_4, a41)
            };
            a51 = if neg {
                R::mul_add(w, uc0_5, -a51)
            } else {
                R::mul_add(nw, uc0_5, a51)
            };
            a00 = uc0_0;
            a10 = uc0_1;
            a20 = uc0_2;
            a30 = uc0_3;
            a40 = uc0_4;
            a50 = uc0_5;
        }
        // `e0 = clear_row_unchecked(matrix, axis_packed, work, 0, 1)`
        let (e0, nz_e0, ur0_0) = HouseholderKernelTrait::<T>::axis1(a01);
        if nz_e0 {
            let neg = e0 < R::zero();
            let h = a11 * ur0_0;
            let w = h + h;
            let nw = -w;
            a11 = if neg {
                R::mul_add(w, ur0_0, -a11)
            } else {
                R::mul_add(nw, ur0_0, a11)
            };
            let h = a21 * ur0_0;
            let w = h + h;
            let nw = -w;
            a21 = if neg {
                R::mul_add(w, ur0_0, -a21)
            } else {
                R::mul_add(nw, ur0_0, a21)
            };
            let h = a31 * ur0_0;
            let w = h + h;
            let nw = -w;
            a31 = if neg {
                R::mul_add(w, ur0_0, -a31)
            } else {
                R::mul_add(nw, ur0_0, a31)
            };
            let h = a41 * ur0_0;
            let w = h + h;
            let nw = -w;
            a41 = if neg {
                R::mul_add(w, ur0_0, -a41)
            } else {
                R::mul_add(nw, ur0_0, a41)
            };
            let h = a51 * ur0_0;
            let w = h + h;
            let nw = -w;
            a51 = if neg {
                R::mul_add(w, ur0_0, -a51)
            } else {
                R::mul_add(nw, ur0_0, a51)
            };
            a01 = ur0_0;
        }
        // `d1 = clear_column_unchecked(matrix, 1, 0, None)`
        let (d1, nz_d1, uc1_0, uc1_1, uc1_2, uc1_3, uc1_4) = HouseholderKernelTrait::<
            T,
        >::axis5(a11, a21, a31, a41, a51);
        if nz_d1 {
            a11 = uc1_0;
            a21 = uc1_1;
            a31 = uc1_2;
            a41 = uc1_3;
            a51 = uc1_4;
        }
        Bidiagonal6x2 {
            uv: Matrix6x2 {
                m11: a00,
                m21: a10,
                m31: a20,
                m41: a30,
                m51: a40,
                m61: a50,
                m12: a01,
                m22: a11,
                m32: a21,
                m42: a31,
                m52: a41,
                m62: a51,
            },
            diagonal: Vector2 { x: d0, y: d1 },
            off_diagonal: Matrix1 { x: e0 },
            upper_diagonal: true,
        }
    }

    /// Whether `D` is upper bidiagonal (rows >= columns). Upstream:
    /// `Bidiagonal::is_upper_diagonal`.
    #[inline(always)]
    fn is_upper_diagonal(self: Bidiagonal6x2<T>) -> bool {
        self.upper_diagonal
    }

    /// `(U, D, Vᵀ)`: `(u(), d(), v_t())`, with `A = U D Vᵀ`. Upstream: `Bidiagonal::unpack`.
    #[inline(always)]
    fn unpack(self: Bidiagonal6x2<T>) -> (Matrix6x2<T>, Matrix2<T>, Matrix2<T>) {
        (Self::u(self), Self::d(self), Self::v_t(self))
    }

    /// The bidiagonal factor `D` (2x2): `|diagonal|` on the diagonal, `|off_diagonal|` on the
    /// superdiagonal. Exact. Upstream: `Bidiagonal::d`.
    #[inline(always)]
    fn d(self: Bidiagonal6x2<T>) -> Matrix2<T> {
        Matrix2 {
            m11: R::abs(self.diagonal.x),
            m21: R::zero(),
            m12: R::abs(self.off_diagonal.x),
            m22: R::abs(self.diagonal.y),
        }
    }

    /// The left factor `U` (6x2): upstream's loop, the signed left reflections applied in
    /// reverse order to the first 2 columns of the identity, on symbolic entries (its static zeros
    /// and ones cost nothing): one fused dot product per column and reflection (doubled exactly),
    /// one `Real::mul_add` per entry. A zero axis is a no-op, as upstream's `continue`. Upstream:
    /// `Bidiagonal::u`.
    fn u(self: Bidiagonal6x2<T>) -> Matrix6x2<T> {
        revoke_ap_tracking();
        let su1 = self.diagonal.y < R::zero();
        let h = self.uv.m22;
        let w = h + h;
        let nw = -w;
        let u1_11 = if su1 {
            R::mul_add(w, self.uv.m22, -R::one())
        } else {
            R::mul_add(nw, self.uv.m22, R::one())
        };
        let u1_21 = if su1 {
            w * self.uv.m32
        } else {
            nw * self.uv.m32
        };
        let u1_31 = if su1 {
            w * self.uv.m42
        } else {
            nw * self.uv.m42
        };
        let u1_41 = if su1 {
            w * self.uv.m52
        } else {
            nw * self.uv.m52
        };
        let u1_51 = if su1 {
            w * self.uv.m62
        } else {
            nw * self.uv.m62
        };
        let su0 = self.diagonal.x < R::zero();
        let h = self.uv.m11;
        let w = h + h;
        let nw = -w;
        let u0_00 = if su0 {
            R::mul_add(w, self.uv.m11, -R::one())
        } else {
            R::mul_add(nw, self.uv.m11, R::one())
        };
        let u0_10 = if su0 {
            w * self.uv.m21
        } else {
            nw * self.uv.m21
        };
        let u0_20 = if su0 {
            w * self.uv.m31
        } else {
            nw * self.uv.m31
        };
        let u0_30 = if su0 {
            w * self.uv.m41
        } else {
            nw * self.uv.m41
        };
        let u0_40 = if su0 {
            w * self.uv.m51
        } else {
            nw * self.uv.m51
        };
        let u0_50 = if su0 {
            w * self.uv.m61
        } else {
            nw * self.uv.m61
        };
        let h = R::wide_rescale(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(R::wide_zero(), self.uv.m21, u1_11),
                            self.uv.m31,
                            u1_21,
                        ),
                        self.uv.m41,
                        u1_31,
                    ),
                    self.uv.m51,
                    u1_41,
                ),
                self.uv.m61,
                u1_51,
            ),
        );
        let w = h + h;
        let nw = -w;
        let u0_01 = if su0 {
            w * self.uv.m11
        } else {
            nw * self.uv.m11
        };
        let u0_11 = if su0 {
            R::mul_add(w, self.uv.m21, -u1_11)
        } else {
            R::mul_add(nw, self.uv.m21, u1_11)
        };
        let u0_21 = if su0 {
            R::mul_add(w, self.uv.m31, -u1_21)
        } else {
            R::mul_add(nw, self.uv.m31, u1_21)
        };
        let u0_31 = if su0 {
            R::mul_add(w, self.uv.m41, -u1_31)
        } else {
            R::mul_add(nw, self.uv.m41, u1_31)
        };
        let u0_41 = if su0 {
            R::mul_add(w, self.uv.m51, -u1_41)
        } else {
            R::mul_add(nw, self.uv.m51, u1_41)
        };
        let u0_51 = if su0 {
            R::mul_add(w, self.uv.m61, -u1_51)
        } else {
            R::mul_add(nw, self.uv.m61, u1_51)
        };
        Matrix6x2 {
            m11: u0_00,
            m21: u0_10,
            m31: u0_20,
            m41: u0_30,
            m51: u0_40,
            m61: u0_50,
            m12: u0_01,
            m22: u0_11,
            m32: u0_21,
            m42: u0_31,
            m52: u0_41,
            m62: u0_51,
        }
    }

    /// The right factor `Vᵀ` (2x2): the signed right reflections applied in reverse order to
    /// the first 2 rows of the identity (same rounding as `u`). Upstream: `Bidiagonal::v_t`.
    fn v_t(self: Bidiagonal6x2<T>) -> Matrix2<T> {
        revoke_ap_tracking();
        let sv0 = self.off_diagonal.x < R::zero();
        let h = self.uv.m12;
        let w = h + h;
        let nw = -w;
        let v0_11 = if sv0 {
            R::mul_add(w, self.uv.m12, -R::one())
        } else {
            R::mul_add(nw, self.uv.m12, R::one())
        };
        Matrix2 { m11: R::one(), m21: R::zero(), m12: R::zero(), m22: v0_11 }
    }

    /// The diagonal of `D` (`|diagonal|`). Exact. Upstream: `Bidiagonal::diagonal`.
    #[inline(always)]
    fn diagonal(self: Bidiagonal6x2<T>) -> Vector2<T> {
        Vector2 { x: R::abs(self.diagonal.x), y: R::abs(self.diagonal.y) }
    }

    /// The off-diagonal of `D` (`|off_diagonal|`). Exact. Upstream:
    /// `Bidiagonal::off_diagonal`.
    #[inline(always)]
    fn off_diagonal(self: Bidiagonal6x2<T>) -> Matrix1<T> {
        Matrix1 { x: R::abs(self.off_diagonal.x) }
    }

    /// The packed storage (the Householder axes). Exact. Upstream: `Bidiagonal::uv_internal`
    /// (`#[doc(hidden)]`).
    #[inline(always)]
    fn uv_internal(self: Bidiagonal6x2<T>) -> Matrix6x2<T> {
        self.uv
    }
}

/// `Matrix6x2` methods that go through the bidiagonalisation. Import `Matrix6x2BidiagonalTrait`.
#[generate_trait]
pub impl Matrix6x2BidiagonalImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix6x2BidiagonalTrait<T> {
    /// The bidiagonalisation. Upstream: `Matrix::bidiagonalize`.
    #[inline(always)]
    fn bidiagonalize(self: Matrix6x2<T>) -> Bidiagonal6x2<T> {
        Bidiagonal6x2Trait::new(self)
    }
}
