// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Svd4`: the singular value decomposition `M = U · Σ · Vᵀ` of a `Matrix4` (upstream
//! `nalgebra::linalg::SVD<T, U4, U4>`), its pseudo-inverse, least-squares solve, rank and polar
//! decomposition (WP 8.5-P14b, DESIGN D6).

use core::internal::revoke_ap_tracking;
use simba::scalar::Real;
use crate::base::matrix4::Matrix4;
use crate::base::vector4::Vector4;
use crate::base::{MatrixMul, MatrixTrMul};
use crate::linalg::symmetric_eigen4::Sym4;
use super::kernels::{SvdComplete4Impl, SvdRightImpl};

/// The singular value decomposition `M = u · diag(singular_values) · v_t` of a `Matrix4<T>`:
/// `u` is 4x4 with orthonormal columns, `v_t` is 4x4 with orthonormal rows, the 4
/// singular values are non-negative and sorted DESCENDING. Unlike upstream, where `u` and `v_t`
/// are `Option`s selected by the `compute_u` / `compute_v` flags, both are always present.
///
/// Sign and order convention: the rows of `v_t` are the eigenvectors of the Gram matrix
/// `MᵀM` as the eigen decomposition returns them, reordered by descending singular
/// value (ties keep the eigen order, so the decomposition of the identity is the identity), and
/// `u_k` points along `M v_k`. A deterministic function of the raw components.
///
/// Upstream: `SVD { u: Option<OMatrix>, v_t: Option<OMatrix>, singular_values: OVector }`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Svd4<T> {
    /// The left singular vectors, as columns (4x4).
    pub u: Matrix4<T>,
    /// The 4 singular values, descending, non-negative.
    pub singular_values: Vector4<T>,
    /// The TRANSPOSE of the right singular vectors (4x4): `v_i` is row `i`.
    pub v_t: Matrix4<T>,
}

/// Test-only field-wise equality (upstream `SVD` has no `PartialEq`).
#[cfg(test)]
impl Svd4PartialEq<T, +PartialEq<T>> of PartialEq<Svd4<T>> {
    fn eq(lhs: @Svd4<T>, rhs: @Svd4<T>) -> bool {
        lhs.u == rhs.u && lhs.singular_values == rhs.singular_values && lhs.v_t == rhs.v_t
    }
}

/// Methods of `Svd4<T>` for any `Real` scalar.
#[generate_trait]
pub impl Svd4Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Svd4Trait<T> {
    /// The singular value decomposition of `matrix`. Upstream: `SVD::new(matrix, true, true)`
    /// (both factors are always computed here: the `compute_u` / `compute_v` flags are dropped,
    /// like `Svd2` / `Svd3`).
    ///
    /// ```text
    /// S   = MᵀM                    (10 fused sums)
    /// V   = eigenvectors of S      (SymmetricEigen4, 5 Jacobi sweeps)
    /// w_i = M v_i,  σ_i = |w_i|    (fused sums, floored norms), sorted DESCENDING
    /// u_k = Gram-Schmidt of w_k against u_1 .. u_(k-1), twice, normalised
    /// ```
    ///
    /// DESIGN D6: the symmetric eigen decomposition of the Gram matrix instead of upstream's
    /// bidiagonalisation and implicitly shifted QR (a data-dependent loop). The singular values
    /// are `|M v_i|`, not `sqrt(λ_i)` (see `Svd3`: more accurate, and never the square root of a
    /// negative rounding). Rank deficiency: a left vector whose Gram-Schmidt residual is EXACTLY
    /// zero is completed by an axis (`U` stays orthonormal); tiny singular values are not treated
    /// as zero, use `rank(eps)` / `pseudo_inverse(eps)` / `solve(b, eps)` for rank decisions.
    /// Cost: constant. The Gram matrix is formed from `M / max |m_ij|` (see `normalised`), so it
    /// cannot overflow; `M V` must fit (it does whenever `σ_1 = |M|₂` does).
    fn new(matrix: Matrix4<T>) -> Svd4<T> {
        Svd4InternalTrait::from_right(matrix, Svd4InternalTrait::right(matrix))
    }

    /// `new`, or `None` when the eigen decomposition of `MᵀM` did not converge within `eps`
    /// (every off-diagonal entry of the final Jacobi state within `eps * (|s_ii| + |s_jj|)`;
    /// see `SymmetricEigen4Trait::try_new`). `max_niter` is accepted for
    /// signature parity and ignored: the iteration budget is a constant of the type. Upstream:
    /// `SVD::try_new(matrix, true, true, eps, max_niter)`.
    fn try_new(matrix: Matrix4<T>, eps: T, max_niter: usize) -> Option<Svd4<T>> {
        let _ = max_niter;
        match Svd4InternalTrait::try_right(matrix, eps) {
            Some(v) => Some(Svd4InternalTrait::from_right(matrix, v)),
            None => None,
        }
    }

    /// `new`: the decomposition is built in descending order (the Gram-Schmidt of the left
    /// vectors needs it), so the unordered form costs the same and is sorted too — a valid
    /// "unordered" result. Upstream: `SVD::new_unordered`.
    #[inline(always)]
    fn new_unordered(matrix: Matrix4<T>) -> Svd4<T> {
        Self::new(matrix)
    }

    /// `try_new`, see `new_unordered`. Upstream: `SVD::try_new_unordered`.
    #[inline(always)]
    fn try_new_unordered(matrix: Matrix4<T>, eps: T, max_niter: usize) -> Option<Svd4<T>> {
        Self::try_new(matrix, eps, max_niter)
    }

    /// The number of singular values strictly greater than `eps`. Upstream: `SVD::rank`, which
    /// also asserts `eps >= 0` (a negative `eps` counts every singular value here, like `Svd2/3`).
    fn rank(self: Svd4<T>, eps: T) -> usize {
        let mut n: usize = 0;
        if self.singular_values.x > eps {
            n += 1;
        }
        if self.singular_values.y > eps {
            n += 1;
        }
        if self.singular_values.z > eps {
            n += 1;
        }
        if self.singular_values.w > eps {
            n += 1;
        }
        n
    }

    /// `U · diag(singular_values) · v_t`: the columns of `U` scaled (one floored product each),
    /// then `MatrixMul::mul_mat` (one fused sum of 4 products per entry). Panics on overflow.
    /// Upstream: `SVD::recompose` (a `Result` there because `u` / `v_t` may be missing; never
    /// here).
    fn recompose(self: Svd4<T>) -> Matrix4<T> {
        revoke_ap_tracking();
        Matrix4 {
            m11: self.u.m11 * self.singular_values.x,
            m21: self.u.m21 * self.singular_values.x,
            m31: self.u.m31 * self.singular_values.x,
            m41: self.u.m41 * self.singular_values.x,
            m12: self.u.m12 * self.singular_values.y,
            m22: self.u.m22 * self.singular_values.y,
            m32: self.u.m32 * self.singular_values.y,
            m42: self.u.m42 * self.singular_values.y,
            m13: self.u.m13 * self.singular_values.z,
            m23: self.u.m23 * self.singular_values.z,
            m33: self.u.m33 * self.singular_values.z,
            m43: self.u.m43 * self.singular_values.z,
            m14: self.u.m14 * self.singular_values.w,
            m24: self.u.m24 * self.singular_values.w,
            m34: self.u.m34 * self.singular_values.w,
            m44: self.u.m44 * self.singular_values.w,
        }
            .mul_mat(self.v_t)
    }

    /// The Moore-Penrose pseudo-inverse `V · diag(σ⁺) · Uᵀ` (4x4), `σ⁺_i = 1 / σ_i` when
    /// `σ_i > eps` and `0` otherwise, or `None` when `eps` is negative. Three roundings per entry
    /// (the reciprocal, the scaling, the fused sum). `eps = 0` keeps a singular value of one raw
    /// unit, whose reciprocal overflows: pass an `eps` matched to the problem. Panics on overflow.
    /// Upstream: `SVD::pseudo_inverse` (`Err` on a negative `eps`).
    fn pseudo_inverse(self: Svd4<T>, eps: T) -> Option<Matrix4<T>> {
        revoke_ap_tracking();
        if eps.is_sign_negative() {
            return None;
        }
        let p0 = SvdRightImpl::<T>::inverted(self.singular_values.x, eps);
        let p1 = SvdRightImpl::<T>::inverted(self.singular_values.y, eps);
        let p2 = SvdRightImpl::<T>::inverted(self.singular_values.z, eps);
        let p3 = SvdRightImpl::<T>::inverted(self.singular_values.w, eps);
        Some(
            Matrix4 {
                m11: self.v_t.m11 * p0,
                m21: self.v_t.m12 * p0,
                m31: self.v_t.m13 * p0,
                m41: self.v_t.m14 * p0,
                m12: self.v_t.m21 * p1,
                m22: self.v_t.m22 * p1,
                m32: self.v_t.m23 * p1,
                m42: self.v_t.m24 * p1,
                m13: self.v_t.m31 * p2,
                m23: self.v_t.m32 * p2,
                m33: self.v_t.m33 * p2,
                m43: self.v_t.m34 * p2,
                m14: self.v_t.m41 * p3,
                m24: self.v_t.m42 * p3,
                m34: self.v_t.m43 * p3,
                m44: self.v_t.m44 * p3,
            }
                .mul_mat(
                    Matrix4 {
                        m11: self.u.m11,
                        m21: self.u.m12,
                        m31: self.u.m13,
                        m41: self.u.m14,
                        m12: self.u.m21,
                        m22: self.u.m22,
                        m32: self.u.m23,
                        m42: self.u.m24,
                        m13: self.u.m31,
                        m23: self.u.m32,
                        m33: self.u.m33,
                        m43: self.u.m34,
                        m14: self.u.m41,
                        m24: self.u.m42,
                        m34: self.u.m43,
                        m44: self.u.m44,
                    },
                ),
        )
    }

    /// The least-squares solution of `M x = b`, `V · (Uᵀ b / σ)` with the components whose
    /// singular value is `<= eps` zeroed, or `None` when `eps` is negative. One fused sum, one
    /// correctly rounded division and one fused sum per component. Upstream: `SVD::solve` (any
    /// right-hand side there; a vector here).
    fn solve(self: Svd4<T>, b: Vector4<T>, eps: T) -> Option<Vector4<T>> {
        revoke_ap_tracking();
        if eps.is_sign_negative() {
            return None;
        }
        let y = self.u.tr_mul(b);
        Some(
            self
                .v_t
                .tr_mul(
                    Vector4 {
                        x: SvdRightImpl::<T>::divided(y.x, self.singular_values.x, eps),
                        y: SvdRightImpl::<T>::divided(y.y, self.singular_values.y, eps),
                        z: SvdRightImpl::<T>::divided(y.z, self.singular_values.z, eps),
                        w: SvdRightImpl::<T>::divided(y.w, self.singular_values.w, eps),
                    },
                ),
        )
    }

    /// The LEFT polar decomposition `M = P · U`, as `Some((P, U))`: `P = u · diag(σ) · uᵀ`
    /// (4x4, symmetric positive semi-definite: its upper triangle is computed and mirrored)
    /// and `U = u · v_t` (4x4). Two roundings per entry of `P`, one per entry of `U`. Always
    /// `Some` (upstream returns `None` only when `u` or `v_t` was not computed). Panics on
    /// overflow. Upstream: `SVD::to_polar`.
    fn to_polar(self: Svd4<T>) -> Option<(Matrix4<T>, Matrix4<T>)> {
        revoke_ap_tracking();
        let a0_0 = self.u.m11 * self.singular_values.x;
        let a0_1 = self.u.m12 * self.singular_values.y;
        let a0_2 = self.u.m13 * self.singular_values.z;
        let a0_3 = self.u.m14 * self.singular_values.w;
        let a1_0 = self.u.m21 * self.singular_values.x;
        let a1_1 = self.u.m22 * self.singular_values.y;
        let a1_2 = self.u.m23 * self.singular_values.z;
        let a1_3 = self.u.m24 * self.singular_values.w;
        let a2_0 = self.u.m31 * self.singular_values.x;
        let a2_1 = self.u.m32 * self.singular_values.y;
        let a2_2 = self.u.m33 * self.singular_values.z;
        let a2_3 = self.u.m34 * self.singular_values.w;
        let a3_0 = self.u.m41 * self.singular_values.x;
        let a3_1 = self.u.m42 * self.singular_values.y;
        let a3_2 = self.u.m43 * self.singular_values.z;
        let a3_3 = self.u.m44 * self.singular_values.w;
        let p0_0 = R::sum_prod4(
            a0_0, self.u.m11, a0_1, self.u.m12, a0_2, self.u.m13, a0_3, self.u.m14,
        );
        let p0_1 = R::sum_prod4(
            a0_0, self.u.m21, a0_1, self.u.m22, a0_2, self.u.m23, a0_3, self.u.m24,
        );
        let p0_2 = R::sum_prod4(
            a0_0, self.u.m31, a0_1, self.u.m32, a0_2, self.u.m33, a0_3, self.u.m34,
        );
        let p0_3 = R::sum_prod4(
            a0_0, self.u.m41, a0_1, self.u.m42, a0_2, self.u.m43, a0_3, self.u.m44,
        );
        let p1_1 = R::sum_prod4(
            a1_0, self.u.m21, a1_1, self.u.m22, a1_2, self.u.m23, a1_3, self.u.m24,
        );
        let p1_2 = R::sum_prod4(
            a1_0, self.u.m31, a1_1, self.u.m32, a1_2, self.u.m33, a1_3, self.u.m34,
        );
        let p1_3 = R::sum_prod4(
            a1_0, self.u.m41, a1_1, self.u.m42, a1_2, self.u.m43, a1_3, self.u.m44,
        );
        let p2_2 = R::sum_prod4(
            a2_0, self.u.m31, a2_1, self.u.m32, a2_2, self.u.m33, a2_3, self.u.m34,
        );
        let p2_3 = R::sum_prod4(
            a2_0, self.u.m41, a2_1, self.u.m42, a2_2, self.u.m43, a2_3, self.u.m44,
        );
        let p3_3 = R::sum_prod4(
            a3_0, self.u.m41, a3_1, self.u.m42, a3_2, self.u.m43, a3_3, self.u.m44,
        );
        Some(
            (
                Matrix4 {
                    m11: p0_0,
                    m21: p0_1,
                    m31: p0_2,
                    m41: p0_3,
                    m12: p0_1,
                    m22: p1_1,
                    m32: p1_2,
                    m42: p1_3,
                    m13: p0_2,
                    m23: p1_2,
                    m33: p2_2,
                    m43: p2_3,
                    m14: p0_3,
                    m24: p1_3,
                    m34: p2_3,
                    m44: p3_3,
                },
                self.u.mul_mat(self.v_t),
            ),
        )
    }

    /// Sorts the singular values DESCENDING, permuting the columns of `u` and the rows of `v_t`
    /// with them (stable odd-even transposition network, strict comparison: equal values keep
    /// their order). `new` already returns them sorted, so this only matters after the fields
    /// were edited. Upstream: `SVD::sort_by_singular_values`.
    fn sort_by_singular_values(ref self: Svd4<T>) {
        revoke_ap_tracking();
        let mut s0 = self.singular_values.x;
        let mut s1 = self.singular_values.y;
        let mut s2 = self.singular_values.z;
        let mut s3 = self.singular_values.w;
        let mut uc0 = Vector4 { x: self.u.m11, y: self.u.m21, z: self.u.m31, w: self.u.m41 };
        let mut uc1 = Vector4 { x: self.u.m12, y: self.u.m22, z: self.u.m32, w: self.u.m42 };
        let mut uc2 = Vector4 { x: self.u.m13, y: self.u.m23, z: self.u.m33, w: self.u.m43 };
        let mut uc3 = Vector4 { x: self.u.m14, y: self.u.m24, z: self.u.m34, w: self.u.m44 };
        let mut vr0 = Vector4 {
            x: self.v_t.m11, y: self.v_t.m12, z: self.v_t.m13, w: self.v_t.m14,
        };
        let mut vr1 = Vector4 {
            x: self.v_t.m21, y: self.v_t.m22, z: self.v_t.m23, w: self.v_t.m24,
        };
        let mut vr2 = Vector4 {
            x: self.v_t.m31, y: self.v_t.m32, z: self.v_t.m33, w: self.v_t.m34,
        };
        let mut vr3 = Vector4 {
            x: self.v_t.m41, y: self.v_t.m42, z: self.v_t.m43, w: self.v_t.m44,
        };
        if s1 > s0 {
            let tmp0 = s0;
            let tmp1 = uc0;
            let tmp2 = vr0;
            s0 = s1;
            uc0 = uc1;
            vr0 = vr1;
            s1 = tmp0;
            uc1 = tmp1;
            vr1 = tmp2;
        }
        if s3 > s2 {
            let tmp0 = s2;
            let tmp1 = uc2;
            let tmp2 = vr2;
            s2 = s3;
            uc2 = uc3;
            vr2 = vr3;
            s3 = tmp0;
            uc3 = tmp1;
            vr3 = tmp2;
        }
        if s2 > s1 {
            let tmp0 = s1;
            let tmp1 = uc1;
            let tmp2 = vr1;
            s1 = s2;
            uc1 = uc2;
            vr1 = vr2;
            s2 = tmp0;
            uc2 = tmp1;
            vr2 = tmp2;
        }
        if s1 > s0 {
            let tmp0 = s0;
            let tmp1 = uc0;
            let tmp2 = vr0;
            s0 = s1;
            uc0 = uc1;
            vr0 = vr1;
            s1 = tmp0;
            uc1 = tmp1;
            vr1 = tmp2;
        }
        if s3 > s2 {
            let tmp0 = s2;
            let tmp1 = uc2;
            let tmp2 = vr2;
            s2 = s3;
            uc2 = uc3;
            vr2 = vr3;
            s3 = tmp0;
            uc3 = tmp1;
            vr3 = tmp2;
        }
        if s2 > s1 {
            let tmp0 = s1;
            let tmp1 = uc1;
            let tmp2 = vr1;
            s1 = s2;
            uc1 = uc2;
            vr1 = vr2;
            s2 = tmp0;
            uc2 = tmp1;
            vr2 = tmp2;
        }
        self =
            Svd4 {
                u: Matrix4 {
                    m11: uc0.x,
                    m21: uc0.y,
                    m31: uc0.z,
                    m41: uc0.w,
                    m12: uc1.x,
                    m22: uc1.y,
                    m32: uc1.z,
                    m42: uc1.w,
                    m13: uc2.x,
                    m23: uc2.y,
                    m33: uc2.z,
                    m43: uc2.w,
                    m14: uc3.x,
                    m24: uc3.y,
                    m34: uc3.z,
                    m44: uc3.w,
                },
                singular_values: Vector4 { x: s0, y: s1, z: s2, w: s3 },
                v_t: Matrix4 {
                    m11: vr0.x,
                    m21: vr1.x,
                    m31: vr2.x,
                    m41: vr3.x,
                    m12: vr0.y,
                    m22: vr1.y,
                    m32: vr2.y,
                    m42: vr3.y,
                    m13: vr0.z,
                    m23: vr1.z,
                    m33: vr2.z,
                    m43: vr3.z,
                    m14: vr0.w,
                    m24: vr1.w,
                    m34: vr2.w,
                    m44: vr3.w,
                },
            };
    }
}

/// The columns `w_i = M v_i`, their norms and the right vectors, sorted (private state of
/// `Svd4InternalTrait`).
#[derive(Copy, Drop)]
pub(crate) struct SortedSvd4<T> {
    s0: T,
    s1: T,
    s2: T,
    s3: T,
    w0: Vector4<T>,
    w1: Vector4<T>,
    w2: Vector4<T>,
    w3: Vector4<T>,
    v0: Vector4<T>,
    v1: Vector4<T>,
    v2: Vector4<T>,
    v3: Vector4<T>,
}

/// Crate-internal kernels of `Svd4<T>`: the Gram matrix, the right singular vectors, the sorted
/// columns `M v_i`, the decomposition.
#[generate_trait]
pub(crate) impl Svd4InternalImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Svd4InternalTrait<T> {
    /// `m / max |m_ij|` (one prepared divisor per 6 entries), or `m` when it is zero: the input
    /// of the Gram matrix. The right singular vectors do not depend on the scale, but their
    /// fixed-point PRECISION does: the Gram matrix of a small matrix has tiny entries, whose
    /// absolute rounding is a large relative error on the eigenvectors (measured on `Svd4`:
    /// `pseudo_inverse` 1 749 ulp over the oracle tolerance on `small` inputs without it), and
    /// the Gram matrix of a large one overflows. Normalised, `MᵀM` has entries in `[0, 4]`.
    fn normalised(m: Matrix4<T>) -> Matrix4<T> {
        let mut a = m.m11.abs();
        if m.m21.abs() > a {
            a = m.m21.abs();
        }
        if m.m31.abs() > a {
            a = m.m31.abs();
        }
        if m.m41.abs() > a {
            a = m.m41.abs();
        }
        if m.m12.abs() > a {
            a = m.m12.abs();
        }
        if m.m22.abs() > a {
            a = m.m22.abs();
        }
        if m.m32.abs() > a {
            a = m.m32.abs();
        }
        if m.m42.abs() > a {
            a = m.m42.abs();
        }
        if m.m13.abs() > a {
            a = m.m13.abs();
        }
        if m.m23.abs() > a {
            a = m.m23.abs();
        }
        if m.m33.abs() > a {
            a = m.m33.abs();
        }
        if m.m43.abs() > a {
            a = m.m43.abs();
        }
        if m.m14.abs() > a {
            a = m.m14.abs();
        }
        if m.m24.abs() > a {
            a = m.m24.abs();
        }
        if m.m34.abs() > a {
            a = m.m34.abs();
        }
        if m.m44.abs() > a {
            a = m.m44.abs();
        }
        if a == R::zero() {
            return m;
        }
        let (q0, q1, q2, q3, q4, q5) = R::div6(m.m11, m.m21, m.m31, m.m41, m.m12, m.m22, a);
        let (q6, q7, q8, q9, q10, q11) = R::div6(m.m32, m.m42, m.m13, m.m23, m.m33, m.m43, a);
        let (q12, q13, q14, q15) = R::div4(m.m14, m.m24, m.m34, m.m44, a);
        Matrix4 {
            m11: q0,
            m21: q1,
            m31: q2,
            m41: q3,
            m12: q4,
            m22: q5,
            m32: q6,
            m42: q7,
            m13: q8,
            m23: q9,
            m33: q10,
            m43: q11,
            m14: q12,
            m24: q13,
            m34: q14,
            m44: q15,
        }
    }

    /// `MᵀM` (10 fused sums of 4 products, one rounding each).
    #[inline(always)]
    fn gram(m: Matrix4<T>) -> Sym4<T> {
        Sym4 {
            m11: R::sum_prod4(m.m11, m.m11, m.m21, m.m21, m.m31, m.m31, m.m41, m.m41),
            m12: R::sum_prod4(m.m11, m.m12, m.m21, m.m22, m.m31, m.m32, m.m41, m.m42),
            m13: R::sum_prod4(m.m11, m.m13, m.m21, m.m23, m.m31, m.m33, m.m41, m.m43),
            m14: R::sum_prod4(m.m11, m.m14, m.m21, m.m24, m.m31, m.m34, m.m41, m.m44),
            m22: R::sum_prod4(m.m12, m.m12, m.m22, m.m22, m.m32, m.m32, m.m42, m.m42),
            m23: R::sum_prod4(m.m12, m.m13, m.m22, m.m23, m.m32, m.m33, m.m42, m.m43),
            m24: R::sum_prod4(m.m12, m.m14, m.m22, m.m24, m.m32, m.m34, m.m42, m.m44),
            m33: R::sum_prod4(m.m13, m.m13, m.m23, m.m23, m.m33, m.m33, m.m43, m.m43),
            m34: R::sum_prod4(m.m13, m.m14, m.m23, m.m24, m.m33, m.m34, m.m43, m.m44),
            m44: R::sum_prod4(m.m14, m.m14, m.m24, m.m24, m.m34, m.m34, m.m44, m.m44),
        }
    }

    /// The right singular vectors (the columns of `v`, ascending eigenvalue order of `MᵀM`).
    #[inline(always)]
    fn right(m: Matrix4<T>) -> Matrix4<T> {
        SvdRightImpl::<T>::right4(Self::gram(Self::normalised(m)))
    }

    /// `right`, or `None` when the eigen decomposition of `MᵀM` did not converge within `eps`.
    #[inline(always)]
    fn try_right(m: Matrix4<T>, eps: T) -> Option<Matrix4<T>> {
        SvdRightImpl::<T>::try_right4(Self::gram(Self::normalised(m)), eps)
    }

    /// `w_i = M v_i` (`MatrixMul::mul_mat`: one fused sum per component) and `σ_i = |w_i|`
    /// (floored norm of the exact
    /// sum of squares) for the right vectors `v` (columns), the triples sorted DESCENDING by `σ`
    /// (stable odd-even transposition network, strict comparison: ties keep their order). The
    /// singular values alone stop here.
    fn sorted(m: Matrix4<T>, v: Matrix4<T>) -> SortedSvd4<T> {
        revoke_ap_tracking();
        let v0 = Vector4 { x: v.m11, y: v.m21, z: v.m31, w: v.m41 };
        let v1 = Vector4 { x: v.m12, y: v.m22, z: v.m32, w: v.m42 };
        let v2 = Vector4 { x: v.m13, y: v.m23, z: v.m33, w: v.m43 };
        let v3 = Vector4 { x: v.m14, y: v.m24, z: v.m34, w: v.m44 };
        let mv = m.mul_mat(v);
        let w0 = Vector4 { x: mv.m11, y: mv.m21, z: mv.m31, w: mv.m41 };
        let w1 = Vector4 { x: mv.m12, y: mv.m22, z: mv.m32, w: mv.m42 };
        let w2 = Vector4 { x: mv.m13, y: mv.m23, z: mv.m33, w: mv.m43 };
        let w3 = Vector4 { x: mv.m14, y: mv.m24, z: mv.m34, w: mv.m44 };
        let s0 = R::norm4(w0.x, w0.y, w0.z, w0.w);
        let s1 = R::norm4(w1.x, w1.y, w1.z, w1.w);
        let s2 = R::norm4(w2.x, w2.y, w2.z, w2.w);
        let s3 = R::norm4(w3.x, w3.y, w3.z, w3.w);
        let (mut s0, mut w0, mut v0) = (s0, w0, v0);
        let (mut s1, mut w1, mut v1) = (s1, w1, v1);
        let (mut s2, mut w2, mut v2) = (s2, w2, v2);
        let (mut s3, mut w3, mut v3) = (s3, w3, v3);
        if s1 > s0 {
            let tmp0 = s0;
            let tmp1 = w0;
            let tmp2 = v0;
            s0 = s1;
            w0 = w1;
            v0 = v1;
            s1 = tmp0;
            w1 = tmp1;
            v1 = tmp2;
        }
        if s3 > s2 {
            let tmp0 = s2;
            let tmp1 = w2;
            let tmp2 = v2;
            s2 = s3;
            w2 = w3;
            v2 = v3;
            s3 = tmp0;
            w3 = tmp1;
            v3 = tmp2;
        }
        if s2 > s1 {
            let tmp0 = s1;
            let tmp1 = w1;
            let tmp2 = v1;
            s1 = s2;
            w1 = w2;
            v1 = v2;
            s2 = tmp0;
            w2 = tmp1;
            v2 = tmp2;
        }
        if s1 > s0 {
            let tmp0 = s0;
            let tmp1 = w0;
            let tmp2 = v0;
            s0 = s1;
            w0 = w1;
            v0 = v1;
            s1 = tmp0;
            w1 = tmp1;
            v1 = tmp2;
        }
        if s3 > s2 {
            let tmp0 = s2;
            let tmp1 = w2;
            let tmp2 = v2;
            s2 = s3;
            w2 = w3;
            v2 = v3;
            s3 = tmp0;
            w3 = tmp1;
            v3 = tmp2;
        }
        if s2 > s1 {
            let tmp0 = s1;
            let tmp1 = w1;
            let tmp2 = v1;
            s1 = s2;
            w1 = w2;
            v1 = v2;
            s2 = tmp0;
            w2 = tmp1;
            v2 = tmp2;
        }
        SortedSvd4 { s0, s1, s2, s3, w0, w1, w2, w3, v0, v1, v2, v3 }
    }

    /// The decomposition from the right singular vectors `v` (columns): `sorted`, then the left
    /// vectors by classical Gram-Schmidt run TWICE ("twice is enough", `SvdComplete4::gs*`):
    /// the second pass costs about as much as the first and keeps `U` orthonormal to the
    /// rounding of the residual even when `σ_k` is tiny (rank deficiency), where one pass leaves
    /// `u_k` as far from the others as `rounding / σ_k`. A column that vanishes EXACTLY (or `σ_1
    /// = 0`) is completed by the axis least represented in the span of the previous ones.
    fn from_right(m: Matrix4<T>, v: Matrix4<T>) -> Svd4<T> {
        let t = Self::sorted(m, v);
        let (s0, w0, v0) = (t.s0, t.w0, t.v0);
        let (s1, w1, v1) = (t.s1, t.w1, t.v1);
        let (s2, w2, v2) = (t.s2, t.w2, t.v2);
        let (s3, w3, v3) = (t.s3, t.w3, t.v3);
        let u0 = SvdComplete4Impl::<T>::first(w0, s0);
        let u1 = SvdComplete4Impl::<T>::gs1(u0, w1);
        let u2 = SvdComplete4Impl::<T>::gs2(u0, u1, w2);
        let u3 = SvdComplete4Impl::<T>::gs3(u0, u1, u2, w3);
        Svd4 {
            u: Matrix4 {
                m11: u0.x,
                m21: u0.y,
                m31: u0.z,
                m41: u0.w,
                m12: u1.x,
                m22: u1.y,
                m32: u1.z,
                m42: u1.w,
                m13: u2.x,
                m23: u2.y,
                m33: u2.z,
                m43: u2.w,
                m14: u3.x,
                m24: u3.y,
                m34: u3.z,
                m44: u3.w,
            },
            singular_values: Vector4 { x: s0, y: s1, z: s2, w: s3 },
            v_t: Matrix4 {
                m11: v0.x,
                m21: v1.x,
                m31: v2.x,
                m41: v3.x,
                m12: v0.y,
                m22: v1.y,
                m32: v2.y,
                m42: v3.y,
                m13: v0.z,
                m23: v1.z,
                m33: v2.z,
                m43: v3.z,
                m14: v0.w,
                m24: v1.w,
                m34: v2.w,
                m44: v3.w,
            },
        }
    }

    /// The singular values alone: `sorted`, without the left vectors.
    fn values_from_right(m: Matrix4<T>, v: Matrix4<T>) -> Vector4<T> {
        let t = Self::sorted(m, v);
        Vector4 { x: t.s0, y: t.s1, z: t.s2, w: t.s3 }
    }
}

/// Upstream's `Matrix` methods that go through the SVD, on `Matrix4`. Import `Matrix4SvdTrait` to
/// use them.
#[generate_trait]
pub impl Matrix4SvdImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix4SvdTrait<T> {
    /// The singular value decomposition, see `Svd4Trait::new`. Upstream: `Matrix::svd(true, true)`.
    #[inline(always)]
    fn svd(self: Matrix4<T>) -> Svd4<T> {
        Svd4Trait::new(self)
    }

    /// `svd` (already sorted, see `Svd4Trait::new_unordered`). Upstream: `Matrix::svd_unordered`.
    #[inline(always)]
    fn svd_unordered(self: Matrix4<T>) -> Svd4<T> {
        Svd4Trait::new(self)
    }

    /// See `Svd4Trait::try_new`. Upstream: `Matrix::try_svd(true, true, eps, max_niter)`.
    #[inline(always)]
    fn try_svd(self: Matrix4<T>, eps: T, max_niter: usize) -> Option<Svd4<T>> {
        Svd4Trait::try_new(self, eps, max_niter)
    }

    /// See `Svd4Trait::try_new_unordered`. Upstream: `Matrix::try_svd_unordered`.
    #[inline(always)]
    fn try_svd_unordered(self: Matrix4<T>, eps: T, max_niter: usize) -> Option<Svd4<T>> {
        Svd4Trait::try_new(self, eps, max_niter)
    }

    /// The singular values alone, DESCENDING: the decomposition without the left vectors
    /// (cheaper than `svd().singular_values`, bit-identical to it). Upstream:
    /// `Matrix::singular_values`.
    fn singular_values(self: Matrix4<T>) -> Vector4<T> {
        Svd4InternalTrait::values_from_right(self, Svd4InternalTrait::right(self))
    }

    /// `singular_values` (sorted; see `Svd4Trait::new_unordered`). Upstream:
    /// `Matrix::singular_values_unordered`.
    #[inline(always)]
    fn singular_values_unordered(self: Matrix4<T>) -> Vector4<T> {
        Self::singular_values(self)
    }

    /// The number of singular values strictly greater than `eps`, from `singular_values`.
    /// Upstream: `Matrix::rank` (which asserts `eps >= 0`; a negative `eps` counts every value
    /// here).
    fn rank(self: Matrix4<T>, eps: T) -> usize {
        let s = Self::singular_values(self);
        let mut n: usize = 0;
        if s.x > eps {
            n += 1;
        }
        if s.y > eps {
            n += 1;
        }
        if s.z > eps {
            n += 1;
        }
        if s.w > eps {
            n += 1;
        }
        n
    }

    /// The Moore-Penrose pseudo-inverse, see `Svd4Trait::pseudo_inverse`. Upstream:
    /// `Matrix::pseudo_inverse` (`Err` on a negative `eps`; `None` here).
    #[inline(always)]
    fn pseudo_inverse(self: Matrix4<T>, eps: T) -> Option<Matrix4<T>> {
        Svd4Trait::new(self).pseudo_inverse(eps)
    }

    /// The left polar decomposition `M = P · U`, see `Svd4Trait::to_polar`. Upstream:
    /// `Matrix::polar`.
    fn polar(self: Matrix4<T>) -> (Matrix4<T>, Matrix4<T>) {
        Svd4Trait::new(self).to_polar().unwrap()
    }

    /// `polar`, or `None` when the decomposition did not converge within `eps`, see
    /// `Svd4Trait::try_new`. Upstream: `Matrix::try_polar`.
    fn try_polar(self: Matrix4<T>, eps: T, max_niter: usize) -> Option<(Matrix4<T>, Matrix4<T>)> {
        match Svd4Trait::try_new(self, eps, max_niter) {
            Some(d) => d.to_polar(),
            None => None,
        }
    }
}
