// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/linalg_p14.py.
//! Householder building blocks (upstream `nalgebra::linalg::householder`,
//! `src/linalg/householder.rs`):
//! `reflection_axis_mut` on the static column vectors. `clear_column_unchecked`,
//! `clear_row_unchecked` and `assemble_q` belong to the Householder reductions of P16
//! (`Bidiagonal`, `Hessenberg`, `SymmetricTridiagonal`) and come with them.

use simba::scalar::Real;
use crate::base::matrix1::Matrix1;
use crate::base::vector2::Vector2;
use crate::base::vector3::Vector3;
use crate::base::vector4::Vector4;
use crate::base::vector5::Vector5;
use crate::base::vector6::Vector6;

/// The Householder axis of one column vector shape (crate-private: the free function below is
/// upstream's interface).
pub(crate) trait HouseholderAxis<V, T> {
    fn reflection_axis_mut(ref column: V) -> (T, bool);
}

/// Turns `column` into the unit axis of the Householder reflection that maps it onto `-r e_0`,
/// and returns `(r, true)` with `r = -sign(column[0]) * |column|` (`sign(0) = 1`), or leaves it
/// unchanged and returns `(0, false)` when it is exactly zero. Upstream:
/// `nalgebra::linalg::reflection_axis_mut` (`#[doc(hidden)]`), `D = 1..6`.
///
/// `|column|` is ONE rounding (the exact sum of squares, its square root floored). The axis is
/// `column + sign * |column| e_0` normalised ONCE: its norm recomputed exactly and floored, then
/// one correctly rounded quotient per component (one prepared divisor). Upstream divides by
/// `sqrt(2 (|column|² + |column[0]| |column|))` — that very norm in exact arithmetic — and
/// then normalises again; the second pass only repeats the first, so the single normalisation is
/// kept (cheaper, and one rounding per component instead of two). Panics on overflow.
pub fn reflection_axis_mut<V, T, impl H: HouseholderAxis<V, T>>(ref column: V) -> (T, bool) {
    H::reflection_axis_mut(ref column)
}

impl Matrix1HouseholderAxis<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of HouseholderAxis<Matrix1<T>, T> {
    fn reflection_axis_mut(ref column: Matrix1<T>) -> (T, bool) {
        let norm = R::wide_sqrt(R::wide_add_prod(R::wide_zero(), column.x, column.x));
        if norm == R::zero() {
            return (R::zero(), false);
        }
        let x0 = column.x;
        let signed = if x0 < R::zero() {
            -norm
        } else {
            norm
        };
        let y0 = x0 + signed;
        let d = R::wide_sqrt(R::wide_add_prod(R::wide_zero(), y0, y0));
        let u0 = R::div(y0, d);
        column = Matrix1 { x: u0 };
        (-signed, true)
    }
}

impl Vector2HouseholderAxis<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of HouseholderAxis<Vector2<T>, T> {
    fn reflection_axis_mut(ref column: Vector2<T>) -> (T, bool) {
        let norm = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(R::wide_zero(), column.x, column.x), column.y, column.y,
            ),
        );
        if norm == R::zero() {
            return (R::zero(), false);
        }
        let x0 = column.x;
        let signed = if x0 < R::zero() {
            -norm
        } else {
            norm
        };
        let y0 = x0 + signed;
        let d = R::wide_sqrt(
            R::wide_add_prod(R::wide_add_prod(R::wide_zero(), y0, y0), column.y, column.y),
        );
        let u0 = R::div(y0, d);
        let u1 = R::div(column.y, d);
        column = Vector2 { x: u0, y: u1 };
        (-signed, true)
    }
}

impl Vector3HouseholderAxis<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of HouseholderAxis<Vector3<T>, T> {
    fn reflection_axis_mut(ref column: Vector3<T>) -> (T, bool) {
        let norm = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(R::wide_zero(), column.x, column.x), column.y, column.y,
                ),
                column.z,
                column.z,
            ),
        );
        if norm == R::zero() {
            return (R::zero(), false);
        }
        let x0 = column.x;
        let signed = if x0 < R::zero() {
            -norm
        } else {
            norm
        };
        let y0 = x0 + signed;
        let d = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(R::wide_add_prod(R::wide_zero(), y0, y0), column.y, column.y),
                column.z,
                column.z,
            ),
        );
        let (u0, u1, u2) = R::div3(y0, column.y, column.z, d);
        column = Vector3 { x: u0, y: u1, z: u2 };
        (-signed, true)
    }
}

impl Vector4HouseholderAxis<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of HouseholderAxis<Vector4<T>, T> {
    fn reflection_axis_mut(ref column: Vector4<T>) -> (T, bool) {
        let norm = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(R::wide_zero(), column.x, column.x), column.y, column.y,
                    ),
                    column.z,
                    column.z,
                ),
                column.w,
                column.w,
            ),
        );
        if norm == R::zero() {
            return (R::zero(), false);
        }
        let x0 = column.x;
        let signed = if x0 < R::zero() {
            -norm
        } else {
            norm
        };
        let y0 = x0 + signed;
        let d = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(R::wide_add_prod(R::wide_zero(), y0, y0), column.y, column.y),
                    column.z,
                    column.z,
                ),
                column.w,
                column.w,
            ),
        );
        let (u0, u1, u2, u3) = R::div4(y0, column.y, column.z, column.w, d);
        column = Vector4 { x: u0, y: u1, z: u2, w: u3 };
        (-signed, true)
    }
}

impl Vector5HouseholderAxis<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of HouseholderAxis<Vector5<T>, T> {
    fn reflection_axis_mut(ref column: Vector5<T>) -> (T, bool) {
        let norm = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(R::wide_zero(), column.x, column.x),
                            column.y,
                            column.y,
                        ),
                        column.z,
                        column.z,
                    ),
                    column.w,
                    column.w,
                ),
                column.a,
                column.a,
            ),
        );
        if norm == R::zero() {
            return (R::zero(), false);
        }
        let x0 = column.x;
        let signed = if x0 < R::zero() {
            -norm
        } else {
            norm
        };
        let y0 = x0 + signed;
        let d = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(R::wide_zero(), y0, y0), column.y, column.y,
                        ),
                        column.z,
                        column.z,
                    ),
                    column.w,
                    column.w,
                ),
                column.a,
                column.a,
            ),
        );
        let (u0, u1, u2, u3, u4) = R::div5(y0, column.y, column.z, column.w, column.a, d);
        column = Vector5 { x: u0, y: u1, z: u2, w: u3, a: u4 };
        (-signed, true)
    }
}

impl Vector6HouseholderAxis<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of HouseholderAxis<Vector6<T>, T> {
    fn reflection_axis_mut(ref column: Vector6<T>) -> (T, bool) {
        let norm = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), column.x, column.x),
                                column.y,
                                column.y,
                            ),
                            column.z,
                            column.z,
                        ),
                        column.w,
                        column.w,
                    ),
                    column.a,
                    column.a,
                ),
                column.b,
                column.b,
            ),
        );
        if norm == R::zero() {
            return (R::zero(), false);
        }
        let x0 = column.x;
        let signed = if x0 < R::zero() {
            -norm
        } else {
            norm
        };
        let y0 = x0 + signed;
        let d = R::wide_sqrt(
            R::wide_add_prod(
                R::wide_add_prod(
                    R::wide_add_prod(
                        R::wide_add_prod(
                            R::wide_add_prod(
                                R::wide_add_prod(R::wide_zero(), y0, y0), column.y, column.y,
                            ),
                            column.z,
                            column.z,
                        ),
                        column.w,
                        column.w,
                    ),
                    column.a,
                    column.a,
                ),
                column.b,
                column.b,
            ),
        );
        let (u0, u1, u2, u3, u4, u5) = R::div6(
            y0, column.y, column.z, column.w, column.a, column.b, d,
        );
        column = Vector6 { x: u0, y: u1, z: u2, w: u3, a: u4, b: u5 };
        (-signed, true)
    }
}
