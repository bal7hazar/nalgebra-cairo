// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Eigen1`: the eigen decomposition of a `Matrix1` with real eigenvalues (upstream
//! `nalgebra::linalg::Eigen<T, U1>`), WP 8.5-P16.

use simba::scalar::Real;
use crate::base::matrix1::Matrix1;
use crate::linalg::schur::schur1::Schur1Trait;

/// The eigen decomposition `A V = V diag(eigenvalues)` of a `Matrix1<T>` whose eigenvalues are real
/// and simple: `eigenvectors` holds unit eigenvectors in its columns, in the order of
/// `eigenvalues`. Built by `Eigen1Trait::new`. Upstream: `nalgebra::linalg::Eigen` (whose module
/// upstream keeps out of its build, "not complete enough for publishing": it handles only
/// eigenvalues of multiplicity one; its `new` is ported as written).
#[derive(Copy, Drop, Serde, Debug)]
pub struct Eigen1<T> {
    /// The unit eigenvectors, one per column.
    pub eigenvectors: Matrix1<T>,
    /// The eigenvalues, in the order of the columns of `eigenvectors`.
    pub eigenvalues: Matrix1<T>,
}

/// Methods of `Eigen1<T>` for any `Real` scalar.
#[generate_trait]
pub impl Eigen1Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Eigen1Trait<T> {
    /// The eigen decomposition of `m`, or `None` when its real Schur form `Q T Qᵀ`
    /// (`Schur1Trait::new`) has a 2x2 block (complex eigenvalues) or two exactly equal
    /// eigenvalues `t_ii = t_jj` with a nonzero coupling `t_ij`. Upstream: `Eigen::new`.
    ///
    /// Upstream's algorithm: for `j` = 1..0 and `i` < `j`, `z = -t_ij / (t_ii - t_jj)`
    /// (correctly rounded), the rows `t_i,k -= z t_j,k` (`k > j`) and the eigenvector columns `v_j
    /// += z v_i` (one `Real::mul_add` each), then every column normalised (its norm one floored
    /// square root of the exact sum of squares, one prepared divisor). Deviation: when `t_ii =
    /// t_jj`
    /// and `t_ij = 0`, upstream divides `0 / 0` (NaN); here the step is skipped (`z = 0`). Panics
    /// on overflow (a tiny `t_ii - t_jj` makes `z` large).
    fn new(m: Matrix1<T>) -> Option<Eigen1<T>> {
        let (q, t) = Schur1Trait::unpack(Schur1Trait::new(m));
        let mut v00 = q.x;
        let nrm = R::abs(v00);
        if nrm != R::zero() {
            v00 = R::div(v00, nrm);
        }
        Option::Some(Eigen1 { eigenvectors: Matrix1 { x: v00 }, eigenvalues: Matrix1 { x: t.x } })
    }
}
