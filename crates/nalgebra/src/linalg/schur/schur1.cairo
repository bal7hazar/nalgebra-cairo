// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Schur1`: the real Schur decomposition of a `Matrix1` (upstream
//! `nalgebra::linalg::Schur<T, U1>`), WP 8.5-P16.

use simba::scalar::Real;
use crate::base::matrix1::Matrix1;

/// The real Schur decomposition `A = Q T Qᵀ` of a `Matrix1<T>`: `Q` orthogonal, `T` upper
/// quasi-triangular (1x1 blocks for the real eigenvalues, 2x2 blocks with complex-conjugate
/// eigenvalues). Built by `Schur1Trait::new` / `try_new` or `Matrix1SchurTrait::schur` /
/// `try_schur`.
/// Upstream: `nalgebra::linalg::Schur`.
#[derive(Copy, Drop, Serde, Debug)]
pub struct Schur1<T> {
    /// The orthogonal factor.
    pub q: Matrix1<T>,
    /// The quasi-triangular factor.
    pub t: Matrix1<T>,
}

/// Methods of `Schur1<T>` for any `Real` scalar.
#[generate_trait]
pub impl Schur1Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Schur1Trait<T> {
    /// `try_new(m, default_epsilon(), 0)`, unwrapped (`max_niter = 0`: no limit, see `try_new`).
    /// Upstream: `Schur::new` / `m.schur()`.
    #[inline(always)]
    fn new(m: Matrix1<T>) -> Schur1<T> {
        Self::try_new(m, R::default_epsilon(), 0).unwrap()
    }

    /// The real Schur decomposition of `m`: `(Q, T) = (1, m)`, always `Some` (`eps` and
    /// `max_niter` are not used, as upstream). Upstream: `Schur::try_new`.
    fn try_new(m: Matrix1<T>, eps: T, max_niter: usize) -> Option<Schur1<T>> {
        let _ = (eps, max_niter);
        Option::Some(Schur1 { q: Matrix1 { x: R::one() }, t: m })
    }

    /// `(Q, T)`. Upstream: `Schur::unpack`.
    #[inline(always)]
    fn unpack(self: Schur1<T>) -> (Matrix1<T>, Matrix1<T>) {
        (self.q, self.t)
    }

    /// The eigenvalues (the diagonal of `T`) when they are all real, i.e. every subdiagonal entry
    /// of `T` is exactly zero, `None` otherwise. Exact. Upstream: `Schur::eigenvalues`.
    fn eigenvalues(self: Schur1<T>) -> Option<Matrix1<T>> {
        Option::Some(Matrix1 { x: self.t.x })
    }

    /// The complex eigenvalues as `(re, im)`: a 1x1 block `t_kk` gives `(t_kk, 0)`, a 2x2 block
    /// (nonzero subdiagonal entry) the pair `(tra / 2, ±√(-discr))` (`4 discr = 4 hnm hmn + (hmm
    /// -
    /// hnn)²` exact then floored, one floored square root, halvings floored; `0` if the block's
    /// discriminant is not negative, which a decomposition built here never gives). The Cairo form
    /// of upstream's `OVector<Complex<T>, D>` (`num-complex`'s `Complex` is not a scalar type
    /// here):
    /// two vectors, real parts and imaginary parts, in upstream's order. Upstream:
    /// `Schur::complex_eigenvalues`.
    fn complex_eigenvalues(self: Schur1<T>) -> (Matrix1<T>, Matrix1<T>) {
        let mut re0 = self.t.x;
        let mut im0 = R::zero();
        (Matrix1 { x: re0 }, Matrix1 { x: im0 })
    }
}

/// `Matrix1` methods that go through the Schur decomposition. Import `Matrix1SchurTrait`.
#[generate_trait]
pub impl Matrix1SchurImpl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix1SchurTrait<T> {
    /// The real Schur decomposition. Upstream: `SquareMatrix::schur`.
    #[inline(always)]
    fn schur(self: Matrix1<T>) -> Schur1<T> {
        Schur1Trait::new(self)
    }

    /// The real Schur decomposition, or `None` when `max_niter` iterations (`0`: no limit) do not
    /// converge (see `Schur1Trait::try_new`). Upstream: `SquareMatrix::try_schur`.
    #[inline(always)]
    fn try_schur(self: Matrix1<T>, eps: T, max_niter: usize) -> Option<Schur1<T>> {
        Schur1Trait::try_new(self, eps, max_niter)
    }

    /// The eigenvalues when they are all real, `None` otherwise: `Some(m)`. Upstream:
    /// `SquareMatrix::eigenvalues`.
    fn eigenvalues(self: Matrix1<T>) -> Option<Matrix1<T>> {
        Option::Some(Matrix1 { x: self.x })
    }

    /// The complex eigenvalues `(re, im)` (see `Schur1Trait::complex_eigenvalues`) of `m`.
    /// Upstream: `SquareMatrix::complex_eigenvalues`.
    fn complex_eigenvalues(self: Matrix1<T>) -> (Matrix1<T>, Matrix1<T>) {
        (Matrix1 { x: self.x }, Matrix1 { x: R::zero() })
    }
}
