// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/shapes.py.
//! `RowVector6`: a statically sized 6-dimensional row vector (upstream `nalgebra::RowVector6`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one overflow
//! check per output scalar. Operators (`+`, `-`, unary `-` and their assigning forms, conversions
//! from / to `[T; 6]`) are implemented in this module, so they need no import; the other operations
//! are methods of `RowVector6Trait`, the products with every conformable shape `MatrixMul::mul_mat`
//! (`self * rhs`) and `MatrixTrMul::tr_mul` (`selfᵀ * rhs`).

use core::num::traits::Bounded;
use core::ops::{AddAssign, DivAssign, IndexView, MulAssign, SubAssign};
use simba::scalar::{Real, Transcendental};
use crate::geometry::quaternion::ApproxEqTrait;
use super::errors;
use super::kernels::{Fused, Powi};
use super::matrix1::Matrix1;
use super::matrix2x6::Matrix2x6;
use super::matrix3x6::Matrix3x6;
use super::matrix4x6::Matrix4x6;
use super::matrix5x6::Matrix5x6;
use super::matrix6::{Matrix6, Matrix6Trait};
use super::matrix6x2::Matrix6x2;
use super::matrix6x3::Matrix6x3;
use super::matrix6x4::Matrix6x4;
use super::matrix6x5::Matrix6x5;
use super::matrix_index::MatrixIndex;
use super::matrix_kronecker::MatrixKronecker;
use super::matrix_mul::MatrixMul;
use super::matrix_tr_mul::MatrixTrMul;
use super::matrix_view::{CropFrom6, FixedColumns, FixedRows, FixedView, PadTo6, ShapeDims};
use super::norm::{EuclideanNorm, LpNorm, Norm, OneNorm, UniformNorm};
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;
use super::row_vector4::RowVector4;
use super::row_vector5::RowVector5;
use super::vector2::Vector2;
use super::vector3::Vector3;
use super::vector4::Vector4;
use super::vector5::Vector5;
use super::vector6::Vector6;

/// A 6-dimensional row vector. Components are named like upstream's `Deref` targets (`x, y, z, w,
/// a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct RowVector6<T> {
    pub x: T,
    pub y: T,
    pub z: T,
    pub w: T,
    pub a: T,
    pub b: T,
}

/// Upstream alias `Matrix1x6`: the same type as `RowVector6` (one Cairo struct per shape).
pub type Matrix1x6<T> = RowVector6<T>;

/// Methods of `RowVector6<T>` for any `Real` scalar.
#[generate_trait]
pub impl RowVector6Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector6Trait<T> {
    /// The 6-dimensional row vector with the given components. Upstream: `RowVector6::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T, w: T, a: T, b: T) -> RowVector6<T> {
        RowVector6 { x, y, z, w, a, b }
    }

    /// The zero 6-dimensional row vector. Upstream: `RowVector6::zeros`.
    #[inline(always)]
    fn zeros() -> RowVector6<T> {
        RowVector6 {
            x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
        }
    }

    /// The transpose, a `Vector6`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: RowVector6<T>) -> Vector6<T> {
        Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }
    }

    /// Component-wise absolute value. Exact; panics on overflow (`|MIN|`). Upstream: `abs`.
    #[inline(always)]
    fn abs(self: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: R::abs(self.x),
            y: R::abs(self.y),
            z: R::abs(self.z),
            w: R::abs(self.w),
            a: R::abs(self.a),
            b: R::abs(self.b),
        }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: RowVector6<T>, k: T) -> RowVector6<T> {
        RowVector6 {
            x: self.x * k,
            y: self.y * k,
            z: self.z * k,
            w: self.w * k,
            a: self.a * k,
            b: self.b * k,
        }
    }

    /// Whether every component of `self` is within `ulps` smallest units (raw units for fixed
    /// point) of `other`'s; cannot overflow. Upstream: `approx::AbsDiffEq::abs_diff_eq`, the
    /// tolerance being counted in ulp instead of a float epsilon (DESIGN D3).
    #[inline(always)]
    fn abs_diff_eq(self: RowVector6<T>, other: RowVector6<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.x, other.x, ulps)
            && R::abs_diff_eq(self.y, other.y, ulps)
            && R::abs_diff_eq(self.z, other.z, ulps)
            && R::abs_diff_eq(self.w, other.w, ulps)
            && R::abs_diff_eq(self.a, other.a, ulps)
            && R::abs_diff_eq(self.b, other.b, ulps)
    }

    /// The 6-dimensional row vector whose components all equal `elem`. Upstream:
    /// `RowVector6::repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> RowVector6<T> {
        RowVector6 { x: elem, y: elem, z: elem, w: elem, a: elem, b: elem }
    }

    /// Alias of `repeat`. Upstream: `RowVector6::from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> RowVector6<T> {
        RowVector6 { x: elem, y: elem, z: elem, w: elem, a: elem, b: elem }
    }

    /// The 6-dimensional row vector whose component `(i, j)` (row, column, 0-based) is `f(i, j)`,
    /// `f` being called in column-major order like upstream. `f` is any closure or `Fn` value of
    /// `(usize, usize)` whose output converts `Into<T>` (the identity included): Cairo cannot state
    /// `Output = T` on the closure without the `associated_item_constraints` experimental feature.
    /// Upstream: `RowVector6::from_fn`.
    fn from_fn<
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (usize, usize)>,
        +Into<Func::Output, T>,
        +Drop<Func::Output>,
    >(
        f: F,
    ) -> RowVector6<T> {
        RowVector6 {
            x: f(0, 0).into(),
            y: f(0, 1).into(),
            z: f(0, 2).into(),
            w: f(0, 3).into(),
            a: f(0, 4).into(),
            b: f(0, 5).into(),
        }
    }

    /// The 6-dimensional row vector of the 6 values of `data`, in row-major order. Panics with
    /// `nalgebra: wrong slice length` unless `data.len() == 6`. Upstream:
    /// `RowVector6::from_row_slice` (`&[T]`).
    ///
    /// `data` is read as ONE fixed-size array (`Span -> @Box<[T; 6]>`, one length check): measured
    /// about 5 times cheaper than a bounds-checked `*data[k]` per component
    /// (`bench_matrix3_from_row_slice__alt_span_index`).
    #[inline(always)]
    fn from_row_slice(data: Span<T>) -> RowVector6<T> {
        let boxed: @Box<[T; 6]> = data.try_into().expect(errors::SLICE_LENGTH);
        let [v0, v1, v2, v3, v4, v5] = boxed.unbox();
        RowVector6 { x: v0, y: v1, z: v2, w: v3, a: v4, b: v5 }
    }

    /// The 6-dimensional row vector of the 6 values of `data`, in column-major order. Panics with
    /// `nalgebra: wrong slice length` unless `data.len() == 6`. Upstream:
    /// `RowVector6::from_column_slice` (`&[T]`).
    ///
    /// `data` is read as ONE fixed-size array (`Span -> @Box<[T; 6]>`, one length check): measured
    /// about 5 times cheaper than a bounds-checked `*data[k]` per component
    /// (`bench_matrix3_from_row_slice__alt_span_index`).
    #[inline(always)]
    fn from_column_slice(data: Span<T>) -> RowVector6<T> {
        let boxed: @Box<[T; 6]> = data.try_into().expect(errors::SLICE_LENGTH);
        let [v0, v1, v2, v3, v4, v5] = boxed.unbox();
        RowVector6 { x: v0, y: v1, z: v2, w: v3, a: v4, b: v5 }
    }

    /// The 6-dimensional row vector whose first `data.len()` diagonal components are `data`, every
    /// other component zero. Panics with `nalgebra: diagonal too long` when `data.len() > 1`.
    /// Upstream: `RowVector6::from_partial_diagonal` (`&[T]`).
    #[inline(always)]
    fn from_partial_diagonal(data: Span<T>) -> RowVector6<T> {
        let len = data.len();
        if len > 1 {
            core::panic_with_felt252(errors::TOO_MANY_DIAGONAL);
        }
        RowVector6 {
            x: if len > 0 {
                *data[0]
            } else {
                R::zero()
            },
            y: R::zero(),
            z: R::zero(),
            w: R::zero(),
            a: R::zero(),
            b: R::zero(),
        }
    }

    /// `true` when every component is zero. Upstream: `Zero::is_zero`.
    #[inline(always)]
    fn is_zero(self: RowVector6<T>) -> bool {
        self.x == R::zero()
            && self.y == R::zero()
            && self.z == R::zero()
            && self.w == R::zero()
            && self.a == R::zero()
            && self.b == R::zero()
    }

    /// Component-wise (Hadamard) product, each component floored once. Panics on overflow.
    /// Upstream: `component_mul`.
    #[inline(always)]
    fn component_mul(self: RowVector6<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: self.x * rhs.x,
            y: self.y * rhs.y,
            z: self.z * rhs.z,
            w: self.w * rhs.w,
            a: self.a * rhs.a,
            b: self.b * rhs.b,
        }
    }

    /// `self = self.component_mul(rhs)`. Upstream: `component_mul_assign`.
    #[inline(always)]
    fn component_mul_assign(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        self =
            RowVector6 {
                x: self.x * rhs.x,
                y: self.y * rhs.y,
                z: self.z * rhs.z,
                w: self.w * rhs.w,
                a: self.a * rhs.a,
                b: self.b * rhs.b,
            };
    }

    /// Component-wise quotient, each component rounded to nearest (ties to even). Panics on a zero
    /// component of `rhs` and on overflow. Upstream: `component_div`.
    #[inline(always)]
    fn component_div(self: RowVector6<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: R::div(self.x, rhs.x),
            y: R::div(self.y, rhs.y),
            z: R::div(self.z, rhs.z),
            w: R::div(self.w, rhs.w),
            a: R::div(self.a, rhs.a),
            b: R::div(self.b, rhs.b),
        }
    }

    /// `self = self.component_div(rhs)`. Upstream: `component_div_assign`.
    #[inline(always)]
    fn component_div_assign(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        self =
            RowVector6 {
                x: R::div(self.x, rhs.x),
                y: R::div(self.y, rhs.y),
                z: R::div(self.z, rhs.z),
                w: R::div(self.w, rhs.w),
                a: R::div(self.a, rhs.a),
                b: R::div(self.b, rhs.b),
            };
    }

    /// Component-wise minimum (infimum). Exact. Upstream: `inf`.
    #[inline(always)]
    fn inf(self: RowVector6<T>, other: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: R::min(self.x, other.x),
            y: R::min(self.y, other.y),
            z: R::min(self.z, other.z),
            w: R::min(self.w, other.w),
            a: R::min(self.a, other.a),
            b: R::min(self.b, other.b),
        }
    }

    /// Component-wise maximum (supremum). Exact. Upstream: `sup`.
    #[inline(always)]
    fn sup(self: RowVector6<T>, other: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: R::max(self.x, other.x),
            y: R::max(self.y, other.y),
            z: R::max(self.z, other.z),
            w: R::max(self.w, other.w),
            a: R::max(self.a, other.a),
            b: R::max(self.b, other.b),
        }
    }

    /// `(self.inf(other), self.sup(other))`. Exact. Upstream: `inf_sup`.
    #[inline(always)]
    fn inf_sup(self: RowVector6<T>, other: RowVector6<T>) -> (RowVector6<T>, RowVector6<T>) {
        (Self::inf(self, other), Self::sup(self, other))
    }

    /// `self + k` added to every component. Exact; panics on overflow. Upstream: `add_scalar`.
    #[inline(always)]
    fn add_scalar(self: RowVector6<T>, k: T) -> RowVector6<T> {
        RowVector6 {
            x: self.x + k,
            y: self.y + k,
            z: self.z + k,
            w: self.w + k,
            a: self.a + k,
            b: self.b + k,
        }
    }

    /// `self = alpha * a ∘ b + beta * self` (component-wise product): per component `alpha * a`
    /// is floored, then the two products are ONE fused `sum_prod2` (floored once). Panics on
    /// overflow.
    /// Upstream: `cmpy` (which skips reading `self` when `beta` is zero: a difference only for NaN,
    /// which fixed point has not).
    #[inline(always)]
    fn cmpy(ref self: RowVector6<T>, alpha: T, a: RowVector6<T>, b: RowVector6<T>, beta: T) {
        self =
            RowVector6 {
                x: R::sum_prod2(alpha * a.x, b.x, beta, self.x),
                y: R::sum_prod2(alpha * a.y, b.y, beta, self.y),
                z: R::sum_prod2(alpha * a.z, b.z, beta, self.z),
                w: R::sum_prod2(alpha * a.w, b.w, beta, self.w),
                a: R::sum_prod2(alpha * a.a, b.a, beta, self.a),
                b: R::sum_prod2(alpha * a.b, b.b, beta, self.b),
            };
    }

    /// `self = alpha * a / b + beta * self` (component-wise quotient): per component `alpha * a` is
    /// floored, divided by `b` (rounded to nearest), then `beta * self + quotient` is ONE `mul_add`
    /// (floored once). Panics on a zero component of `b` and on overflow. Upstream: `cdpy`.
    #[inline(always)]
    fn cdpy(ref self: RowVector6<T>, alpha: T, a: RowVector6<T>, b: RowVector6<T>, beta: T) {
        self =
            RowVector6 {
                x: R::mul_add(beta, self.x, R::div(alpha * a.x, b.x)),
                y: R::mul_add(beta, self.y, R::div(alpha * a.y, b.y)),
                z: R::mul_add(beta, self.z, R::div(alpha * a.z, b.z)),
                w: R::mul_add(beta, self.w, R::div(alpha * a.w, b.w)),
                a: R::mul_add(beta, self.a, R::div(alpha * a.a, b.a)),
                b: R::mul_add(beta, self.b, R::div(alpha * a.b, b.b)),
            };
    }

    /// The smallest component. Exact. Upstream: `min`.
    #[inline(always)]
    fn min(self: RowVector6<T>) -> T {
        R::min(R::min(R::min(R::min(R::min(self.x, self.y), self.z), self.w), self.a), self.b)
    }

    /// The largest component. Exact. Upstream: `max`.
    #[inline(always)]
    fn max(self: RowVector6<T>) -> T {
        R::max(R::max(R::max(R::max(R::max(self.x, self.y), self.z), self.w), self.a), self.b)
    }

    /// The smallest absolute value of a component. Panics on the scalar's `MIN`. Upstream: `amin`.
    #[inline(always)]
    fn amin(self: RowVector6<T>) -> T {
        R::min(
            R::min(
                R::min(
                    R::min(R::min(R::abs(self.x), R::abs(self.y)), R::abs(self.z)), R::abs(self.w),
                ),
                R::abs(self.a),
            ),
            R::abs(self.b),
        )
    }

    /// The largest absolute value of a component (the uniform norm). Panics on the scalar's `MIN`.
    /// Upstream: `amax`.
    #[inline(always)]
    fn amax(self: RowVector6<T>) -> T {
        R::max(
            R::max(
                R::max(
                    R::max(R::max(R::abs(self.x), R::abs(self.y)), R::abs(self.z)), R::abs(self.w),
                ),
                R::abs(self.a),
            ),
            R::abs(self.b),
        )
    }

    /// `amin`: the modulus of a real scalar is its absolute value. Upstream: `camin`.
    #[inline(always)]
    fn camin(self: RowVector6<T>) -> T {
        R::min(
            R::min(
                R::min(
                    R::min(R::min(R::abs(self.x), R::abs(self.y)), R::abs(self.z)), R::abs(self.w),
                ),
                R::abs(self.a),
            ),
            R::abs(self.b),
        )
    }

    /// `amax`: the modulus of a real scalar is its absolute value. Upstream: `camax`.
    #[inline(always)]
    fn camax(self: RowVector6<T>) -> T {
        R::max(
            R::max(
                R::max(
                    R::max(R::max(R::abs(self.x), R::abs(self.y)), R::abs(self.z)), R::abs(self.w),
                ),
                R::abs(self.a),
            ),
            R::abs(self.b),
        )
    }

    /// `(row, column)` of the component with the largest absolute value, the first one in
    /// column-major order on ties. Panics on the scalar's `MIN`. Upstream: `iamax_full`.
    #[inline(always)]
    fn iamax_full(self: RowVector6<T>) -> (usize, usize) {
        let mut best: (usize, usize) = (0, 0);
        let mut m = R::abs(self.x);
        let v = R::abs(self.y);
        if v > m {
            m = v;
            best = (0, 1);
        }
        let v = R::abs(self.z);
        if v > m {
            m = v;
            best = (0, 2);
        }
        let v = R::abs(self.w);
        if v > m {
            m = v;
            best = (0, 3);
        }
        let v = R::abs(self.a);
        if v > m {
            m = v;
            best = (0, 4);
        }
        let v = R::abs(self.b);
        if v > m {
            best = (0, 5);
        }
        best
    }

    /// `iamax_full`: the modulus of a real scalar is its absolute value. Upstream: `icamax_full`.
    #[inline(always)]
    fn icamax_full(self: RowVector6<T>) -> (usize, usize) {
        Self::iamax_full(self)
    }

    /// Dot product (the sum of the component-wise products, upstream's Frobenius inner product for
    /// matrices): the exact sum is floored ONCE, only the result must fit. Upstream: `dot`.
    #[inline(always)]
    fn dot(self: RowVector6<T>, rhs: RowVector6<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.x, rhs.x);
        let w = R::wide_add_prod(w, self.y, rhs.y);
        let w = R::wide_add_prod(w, self.z, rhs.z);
        let w = R::wide_add_prod(w, self.w, rhs.w);
        let w = R::wide_add_prod(w, self.a, rhs.a);
        R::wide_rescale(R::wide_add_prod(w, self.b, rhs.b))
    }

    /// Squared Euclidean (Frobenius) norm: the exact sum of squares floored once. Panics on
    /// overflow (above a norm of about 46 340 in Q32.32 only `norm` works). Upstream:
    /// `norm_squared`.
    #[inline(always)]
    fn norm_squared(self: RowVector6<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.x, self.x);
        let w = R::wide_add_prod(w, self.y, self.y);
        let w = R::wide_add_prod(w, self.z, self.z);
        let w = R::wide_add_prod(w, self.w, self.w);
        let w = R::wide_add_prod(w, self.a, self.a);
        R::wide_rescale(R::wide_add_prod(w, self.b, self.b))
    }

    /// Euclidean (Frobenius) norm: square root of the UNSCALED exact sum of squares, floored once.
    /// No intermediate overflow: only the result must fit. Upstream: `norm`.
    #[inline(always)]
    fn norm(self: RowVector6<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.x, self.x);
        let w = R::wide_add_prod(w, self.y, self.y);
        let w = R::wide_add_prod(w, self.z, self.z);
        let w = R::wide_add_prod(w, self.w, self.w);
        let w = R::wide_add_prod(w, self.a, self.a);
        R::wide_sqrt(R::wide_add_prod(w, self.b, self.b))
    }

    /// Alias of `norm_squared`. Upstream: `magnitude_squared`.
    #[inline(always)]
    fn magnitude_squared(self: RowVector6<T>) -> T {
        Self::norm_squared(self)
    }

    /// Alias of `norm`. Upstream: `magnitude`.
    #[inline(always)]
    fn magnitude(self: RowVector6<T>) -> T {
        Self::norm(self)
    }

    /// `(self - rhs).norm()`: the differences are exact, then one fused norm. Panics when a
    /// difference or the result overflows. Upstream: `metric_distance`.
    #[inline(always)]
    fn metric_distance(self: RowVector6<T>, rhs: RowVector6<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.x - rhs.x, self.x - rhs.x);
        let w = R::wide_add_prod(w, self.y - rhs.y, self.y - rhs.y);
        let w = R::wide_add_prod(w, self.z - rhs.z, self.z - rhs.z);
        let w = R::wide_add_prod(w, self.w - rhs.w, self.w - rhs.w);
        let w = R::wide_add_prod(w, self.a - rhs.a, self.a - rhs.a);
        R::wide_sqrt(R::wide_add_prod(w, self.b - rhs.b, self.b - rhs.b))
    }

    /// `self / k`, each component the correctly rounded quotient (nearest, ties to even), through 1
    /// prepared-divisor `Real::divN` call(s), bit-identical to one `Real::div` per component.
    /// Panics on a zero `k` and on overflow. Upstream: `unscale` (`self / k`).
    #[inline(always)]
    fn unscale(self: RowVector6<T>, k: T) -> RowVector6<T> {
        let (x, y, z, w, a, b) = R::div6(self.x, self.y, self.z, self.w, self.a, self.b, k);
        RowVector6 { x, y, z, w, a, b }
    }

    /// `self / self.norm()`: the floored norm, then `unscale`. Panics with a division by zero when
    /// the norm is zero, and on overflow when the norm does not fit. Upstream: `normalize`.
    #[inline(always)]
    fn normalize(self: RowVector6<T>) -> RowVector6<T> {
        Self::unscale(self, Self::norm(self))
    }

    /// `Some(self.normalize())`, or `None` when the norm is `<= min_norm` (never divides by zero
    /// for `min_norm >= 0`). Upstream: `try_normalize`.
    #[inline(always)]
    fn try_normalize(self: RowVector6<T>, min_norm: T) -> Option<RowVector6<T>> {
        let n = Self::norm(self);
        if n <= min_norm {
            None
        } else {
            Some(Self::unscale(self, n))
        }
    }

    /// `self` when its norm is `<= max`, otherwise `self.scale(max / norm)` (the ratio rounded to
    /// nearest, like upstream's `max / n`). Panics only when the norm does not fit. Upstream:
    /// `cap_magnitude`.
    #[inline(always)]
    fn cap_magnitude(self: RowVector6<T>, max: T) -> RowVector6<T> {
        let n = Self::norm(self);
        if n <= max {
            self
        } else {
            Self::scale(self, R::div(max, n))
        }
    }

    /// Scales `self` to the norm `magnitude` (`self.scale(magnitude / norm)`, the ratio rounded to
    /// nearest) when its norm is `> min_magnitude`, leaves it unchanged otherwise. Upstream:
    /// `try_set_magnitude` (`&mut self`).
    #[inline(always)]
    fn try_set_magnitude(ref self: RowVector6<T>, magnitude: T, min_magnitude: T) {
        let n = Self::norm(self);
        if n > min_magnitude {
            self = Self::scale(self, R::div(magnitude, n));
        }
    }

    /// The induced 1-norm: the largest absolute column sum (the L1 norm of a column vector, the
    /// largest absolute value of a row vector). Exact; panics on overflow. Upstream: `one_norm`.
    #[inline(always)]
    fn one_norm(self: RowVector6<T>) -> T {
        R::max(
            R::max(
                R::max(
                    R::max(R::max(R::abs(self.x), R::abs(self.y)), R::abs(self.z)), R::abs(self.w),
                ),
                R::abs(self.a),
            ),
            R::abs(self.b),
        )
    }

    /// The conjugate transpose, a `Vector6`: the transpose for a real scalar. Exact. Upstream:
    /// `adjoint`.
    #[inline(always)]
    fn adjoint(self: RowVector6<T>) -> Vector6<T> {
        Self::transpose(self)
    }

    /// Alias of `adjoint` (deprecated upstream). Upstream: `conjugate_transpose`.
    #[inline(always)]
    fn conjugate_transpose(self: RowVector6<T>) -> Vector6<T> {
        Self::transpose(self)
    }

    /// The component-wise conjugate: `self` for a real scalar. Upstream: `conjugate`.
    #[inline(always)]
    fn conjugate(self: RowVector6<T>) -> RowVector6<T> {
        self
    }

    /// The same shape with every component converted by `Into<T, U>`. With the single scalar of
    /// this library (`Fixed`) it is the identity; it exists for scalar-generic code. Upstream:
    /// `cast` (and `SubsetOf<Matrix<U>>`, the `nalgebra::convert` it goes through).
    fn cast<U, +Into<T, U>, +Drop<U>>(self: RowVector6<T>) -> RowVector6<U> {
        RowVector6 {
            x: self.x.into(),
            y: self.y.into(),
            z: self.z.into(),
            w: self.w.into(),
            a: self.a.into(),
            b: self.b.into(),
        }
    }

    /// `Some` of the shape with every component converted by `TryInto<T, U>`, `None` as soon as one
    /// conversion fails. Upstream: `try_cast`.
    fn try_cast<U, +TryInto<T, U>, +Drop<U>>(self: RowVector6<T>) -> Option<RowVector6<U>> {
        let x: U = self.x.try_into()?;
        let y: U = self.y.try_into()?;
        let z: U = self.z.try_into()?;
        let w: U = self.w.try_into()?;
        let a: U = self.a.try_into()?;
        let b: U = self.b.try_into()?;
        Option::Some(RowVector6 { x, y, z, w, a, b })
    }

    /// `true` when every component is within `epsilon` ulp of `other`'s, or has the same sign and
    /// lies within `max_relative` times the larger magnitude of the two (`|a - b| <= max(|a|, |b|)
    /// · max_relative`). Panics on a component equal to the scalar's `MIN`, and on overflow of
    /// that product (only possible with `max_relative > 1`). Upstream:
    /// `approx::RelativeEq::relative_eq`, `epsilon` counted in ulp instead of a float epsilon
    /// (DESIGN D3).
    #[inline(always)]
    fn relative_eq(
        self: RowVector6<T>, other: RowVector6<T>, epsilon: u64, max_relative: T,
    ) -> bool {
        ApproxEqTrait::relative_eq(self.x, other.x, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.y, other.y, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.z, other.z, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.w, other.w, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.a, other.a, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.b, other.b, epsilon, max_relative)
    }

    /// `true` when every component is within `epsilon` ulp of `other`'s, or has the same sign and
    /// lies within `max_ulps` ulp (in fixed point the distance in ulp IS the raw difference; the
    /// `max_ulps` budget does not cross zero, like upstream's float `ulps_eq`). Cannot overflow.
    /// Upstream: `approx::UlpsEq::ulps_eq`.
    #[inline(always)]
    fn ulps_eq(self: RowVector6<T>, other: RowVector6<T>, epsilon: u64, max_ulps: u32) -> bool {
        ApproxEqTrait::ulps_eq(self.x, other.x, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.y, other.y, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.z, other.z, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.w, other.w, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.a, other.a, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.b, other.b, epsilon, max_ulps)
    }

    /// The 6-dimensional row vector of `f(x)` for every component `x` (called in column-major
    /// order). `f` is any closure or `Fn` value; Cairo closures take their arguments by value and
    /// cannot mutate their captures. Upstream: `map`.
    #[inline]
    fn map<F, +Drop<F>, impl Func: core::ops::Fn<F, (T,)>, +Drop<Func::Output>>(
        self: RowVector6<T>, f: F,
    ) -> RowVector6<Func::Output> {
        RowVector6 {
            x: f(self.x), y: f(self.y), z: f(self.z), w: f(self.w), a: f(self.a), b: f(self.b),
        }
    }

    /// The 6-dimensional row vector of `f(i, j, x)` for every component `x` at row `i`, column `j`
    /// (0-based, column-major order). `f` is any closure or `Fn` value; Cairo closures take their
    /// arguments by value and cannot mutate their captures. Upstream: `map_with_location`.
    #[inline]
    fn map_with_location<
        F, +Drop<F>, impl Func: core::ops::Fn<F, (usize, usize, T)>, +Drop<Func::Output>,
    >(
        self: RowVector6<T>, f: F,
    ) -> RowVector6<Func::Output> {
        RowVector6 {
            x: f(0, 0, self.x),
            y: f(0, 1, self.y),
            z: f(0, 2, self.z),
            w: f(0, 3, self.w),
            a: f(0, 4, self.a),
            b: f(0, 5, self.b),
        }
    }

    /// The 6-dimensional row vector of `f(a, b)` for the components `a` of `self` and `b` of `rhs`
    /// at the same position. `f` is any closure or `Fn` value; Cairo closures take their arguments
    /// by value and cannot mutate their captures. Upstream: `zip_map`.
    #[inline]
    fn zip_map<
        T2,
        +Copy<T2>,
        +Drop<T2>,
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (T, T2)>,
        +Drop<Func::Output>,
    >(
        self: RowVector6<T>, rhs: RowVector6<T2>, f: F,
    ) -> RowVector6<Func::Output> {
        RowVector6 {
            x: f(self.x, rhs.x),
            y: f(self.y, rhs.y),
            z: f(self.z, rhs.z),
            w: f(self.w, rhs.w),
            a: f(self.a, rhs.a),
            b: f(self.b, rhs.b),
        }
    }

    /// The 6-dimensional row vector of `f(a, b, c)` for the components of `self`, `b` and `c` at
    /// the same position. `f` is any closure or `Fn` value; Cairo closures take their arguments by
    /// value and cannot mutate their captures. Upstream: `zip_zip_map`.
    #[inline]
    fn zip_zip_map<
        T2,
        T3,
        +Copy<T2>,
        +Drop<T2>,
        +Copy<T3>,
        +Drop<T3>,
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (T, T2, T3)>,
        +Drop<Func::Output>,
    >(
        self: RowVector6<T>, b: RowVector6<T2>, c: RowVector6<T3>, f: F,
    ) -> RowVector6<Func::Output> {
        RowVector6 {
            x: f(self.x, b.x, c.x),
            y: f(self.y, b.y, c.y),
            z: f(self.z, b.z, c.z),
            w: f(self.w, b.w, c.w),
            a: f(self.a, b.a, c.a),
            b: f(self.b, b.b, c.b),
        }
    }

    /// `f(.. f(f(init, x0), x1) .., x5)` over the components in column-major order. The closure's
    /// output converts `Into<Acc>` (the identity included): Cairo cannot state `Output = Acc`
    /// without the `associated_item_constraints` experimental feature. `f` is any closure or `Fn`
    /// value; Cairo closures take their arguments by value and cannot mutate their captures.
    /// Upstream: `fold`.
    #[inline]
    fn fold<Acc, F, +Drop<F>, impl Func: core::ops::Fn<F, (Acc, T)>, +Into<Func::Output, Acc>>(
        self: RowVector6<T>, init: Acc, f: F,
    ) -> Acc {
        let acc: Acc = f(init, self.x).into();
        let acc: Acc = f(acc, self.y).into();
        let acc: Acc = f(acc, self.z).into();
        let acc: Acc = f(acc, self.w).into();
        let acc: Acc = f(acc, self.a).into();
        f(acc, self.b).into()
    }

    /// `init_f(Some(x0))`, then `f(acc, x)` over the other components in column-major order:
    /// upstream's `fold_with` (`init_f` receives the first component, `None` only for an empty
    /// matrix, which a static shape never is). The accumulator has the type of `init_f`'s output;
    /// `f`'s output converts `Into` it. The closures receive values instead of upstream's `&T`.
    /// Upstream: `fold_with`.
    #[inline]
    fn fold_with<
        G,
        +Drop<G>,
        impl Init: core::ops::Fn<G, (Option<T>,)>,
        +Drop<Init::Output>,
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (Init::Output, T)>,
        +Into<Func::Output, Init::Output>,
    >(
        self: RowVector6<T>, init_f: G, f: F,
    ) -> Init::Output {
        let acc: Init::Output = init_f(Option::Some(self.x));
        let acc: Init::Output = f(acc, self.y).into();
        let acc: Init::Output = f(acc, self.z).into();
        let acc: Init::Output = f(acc, self.w).into();
        let acc: Init::Output = f(acc, self.a).into();
        f(acc, self.b).into()
    }

    /// `fold` over the pairs of components of `self` and `rhs` at the same position: `f(acc, a,
    /// b)`, column-major. `f` is any closure or `Fn` value; Cairo closures take their arguments by
    /// value and cannot mutate their captures. Upstream: `zip_fold`.
    #[inline]
    fn zip_fold<
        T2,
        +Copy<T2>,
        +Drop<T2>,
        Acc,
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (Acc, T, T2)>,
        +Into<Func::Output, Acc>,
    >(
        self: RowVector6<T>, rhs: RowVector6<T2>, init: Acc, f: F,
    ) -> Acc {
        let acc: Acc = f(init, self.x, rhs.x).into();
        let acc: Acc = f(acc, self.y, rhs.y).into();
        let acc: Acc = f(acc, self.z, rhs.z).into();
        let acc: Acc = f(acc, self.w, rhs.w).into();
        let acc: Acc = f(acc, self.a, rhs.a).into();
        f(acc, self.b, rhs.b).into()
    }

    /// Replaces every component `x` by `f(x)` (column-major order). Upstream's closure is
    /// `FnMut(&mut T)`, writing through the reference; a Cairo closure cannot, so it RETURNS the
    /// new component (its output converts `Into<T>`). Upstream: `apply`.
    #[inline]
    fn apply<F, +Drop<F>, impl Func: core::ops::Fn<F, (T,)>, +Into<Func::Output, T>>(
        ref self: RowVector6<T>, f: F,
    ) {
        self =
            RowVector6 {
                x: f(self.x).into(),
                y: f(self.y).into(),
                z: f(self.z).into(),
                w: f(self.w).into(),
                a: f(self.a).into(),
                b: f(self.b).into(),
            };
    }

    /// `self` with every component `x` replaced by `f(x)`: `apply` by value. Upstream's closure is
    /// `FnMut(&mut T)`, writing through the reference; a Cairo closure cannot, so it RETURNS the
    /// new component (its output converts `Into<T>`). Upstream: `apply_into`.
    #[inline]
    fn apply_into<F, +Drop<F>, impl Func: core::ops::Fn<F, (T,)>, +Into<Func::Output, T>>(
        self: RowVector6<T>, f: F,
    ) -> RowVector6<T> {
        RowVector6 {
            x: f(self.x).into(),
            y: f(self.y).into(),
            z: f(self.z).into(),
            w: f(self.w).into(),
            a: f(self.a).into(),
            b: f(self.b).into(),
        }
    }

    /// Replaces every component `a` by `f(a, b)`, `b` the component of `rhs` at the same position.
    /// Upstream's closure is `FnMut(&mut T)`, writing through the reference; a Cairo closure
    /// cannot, so it RETURNS the new component (its output converts `Into<T>`). Upstream:
    /// `zip_apply`.
    #[inline]
    fn zip_apply<
        T2,
        +Copy<T2>,
        +Drop<T2>,
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (T, T2)>,
        +Into<Func::Output, T>,
    >(
        ref self: RowVector6<T>, rhs: RowVector6<T2>, f: F,
    ) {
        self =
            RowVector6 {
                x: f(self.x, rhs.x).into(),
                y: f(self.y, rhs.y).into(),
                z: f(self.z, rhs.z).into(),
                w: f(self.w, rhs.w).into(),
                a: f(self.a, rhs.a).into(),
                b: f(self.b, rhs.b).into(),
            };
    }

    /// Replaces every component `a` by `f(a, b, c)`, `b` and `c` the components of `b` and `c` at
    /// the same position. Upstream's closure is `FnMut(&mut T)`, writing through the reference; a
    /// Cairo closure cannot, so it RETURNS the new component (its output converts `Into<T>`).
    /// Upstream: `zip_zip_apply`.
    #[inline]
    fn zip_zip_apply<
        T2,
        T3,
        +Copy<T2>,
        +Drop<T2>,
        +Copy<T3>,
        +Drop<T3>,
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (T, T2, T3)>,
        +Into<Func::Output, T>,
    >(
        ref self: RowVector6<T>, b: RowVector6<T2>, c: RowVector6<T3>, f: F,
    ) {
        self =
            RowVector6 {
                x: f(self.x, b.x, c.x).into(),
                y: f(self.y, b.y, c.y).into(),
                z: f(self.z, b.z, c.z).into(),
                w: f(self.w, b.w, c.w).into(),
                a: f(self.a, b.a, c.a).into(),
                b: f(self.b, b.b, c.b).into(),
            };
    }

    /// Sets every component to `f()` (one call per component, column-major). The closure's output
    /// converts `Into<T>`. Upstream: `fill_with` (`impl Fn() -> T`).
    #[inline]
    fn fill_with<F, +Drop<F>, impl Func: core::ops::Fn<F, ()>, +Into<Func::Output, T>>(
        ref self: RowVector6<T>, f: F,
    ) {
        self =
            RowVector6 {
                x: f().into(),
                y: f().into(),
                z: f().into(),
                w: f().into(),
                a: f().into(),
                b: f().into(),
            };
    }

    /// Sets every component to `val`. Upstream: `fill`.
    #[inline(always)]
    fn fill(ref self: RowVector6<T>, val: T) {
        self = RowVector6 { x: val, y: val, z: val, w: val, a: val, b: val };
    }

    /// Sets the 1 diagonal components to `val`, the others unchanged. Upstream: `fill_diagonal`.
    #[inline(always)]
    fn fill_diagonal(ref self: RowVector6<T>, val: T) {
        self = RowVector6 { x: val, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b };
    }

    /// Sets `self` to the identity: ones on the diagonal, zeros elsewhere (`fill(0)` then
    /// `fill_diagonal(1)`). Upstream: `fill_with_identity`.
    #[inline(always)]
    fn fill_with_identity(ref self: RowVector6<T>) {
        self =
            RowVector6 {
                x: R::one(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
            };
    }

    /// Sets the 6 components of row `i` to `val`. Panics with `nalgebra: index out of bounds` for
    /// `i >= 1`. Upstream: `fill_row`.
    ///
    /// ONE `match` on `i` selects the literal: measured 2.1 times cheaper than one comparison per
    /// component (`bench_matrix4_fill_row__alt_per_component`).
    #[inline(always)]
    fn fill_row(ref self: RowVector6<T>, i: usize, val: T) {
        self = match i {
            0 => RowVector6 { x: val, y: val, z: val, w: val, a: val, b: val },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        };
    }

    /// Sets the 1 components of column `j` to `val`. Panics with `nalgebra: index out of bounds`
    /// for `j >= 6`. Upstream: `fill_column`.
    #[inline(always)]
    fn fill_column(ref self: RowVector6<T>, j: usize, val: T) {
        self = match j {
            0 => RowVector6 { x: val, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
            1 => RowVector6 { x: self.x, y: val, z: self.z, w: self.w, a: self.a, b: self.b },
            2 => RowVector6 { x: self.x, y: self.y, z: val, w: self.w, a: self.a, b: self.b },
            3 => RowVector6 { x: self.x, y: self.y, z: self.z, w: val, a: self.a, b: self.b },
            4 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: val, b: self.b },
            5 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: val },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        };
    }

    /// Sets every component `(i, j)` with `i >= j + shift` to `val`: the lower triangle with the
    /// diagonal for `shift = 0`, without it for `shift = 1`, leaving `shift - 1` subdiagonals as
    /// well above; nothing changes for `shift >= 1`. ONE `match` on `shift` selects the literal:
    /// measured 2.7 times cheaper than one threshold test per component
    /// (`bench_matrix4_fill_lower_triangle__alt_per_component`). Upstream: `fill_lower_triangle`.
    #[inline(always)]
    fn fill_lower_triangle(ref self: RowVector6<T>, val: T, shift: usize) {
        self = match shift {
            0 => RowVector6 { x: val, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
            _ => self,
        };
    }

    /// Sets every component `(i, j)` with `j >= i + shift` to `val`: the upper triangle with the
    /// diagonal for `shift = 0`, without it for `shift = 1`, leaving `shift - 1` superdiagonals as
    /// well below; nothing changes for `shift >= 6`. ONE `match` on `shift` selects the literal.
    /// Upstream: `fill_upper_triangle`.
    #[inline(always)]
    fn fill_upper_triangle(ref self: RowVector6<T>, val: T, shift: usize) {
        self = match shift {
            0 => RowVector6 { x: val, y: val, z: val, w: val, a: val, b: val },
            1 => RowVector6 { x: self.x, y: val, z: val, w: val, a: val, b: val },
            2 => RowVector6 { x: self.x, y: self.y, z: val, w: val, a: val, b: val },
            3 => RowVector6 { x: self.x, y: self.y, z: self.z, w: val, a: val, b: val },
            4 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: val, b: val },
            5 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: val },
            _ => self,
        };
    }

    /// Replaces row `i` by `row`, a `RowVector6`. Panics with `nalgebra: index out of bounds` for
    /// `i >= 1`. Upstream: `set_row`.
    #[inline(always)]
    fn set_row(ref self: RowVector6<T>, i: usize, row: RowVector6<T>) {
        self = match i {
            0 => RowVector6 { x: row.x, y: row.y, z: row.z, w: row.w, a: row.a, b: row.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        };
    }

    /// Replaces column `j` by `column`, a `Matrix1`. Panics with `nalgebra: index out of bounds`
    /// for `j >= 6`. Upstream: `set_column`.
    #[inline(always)]
    fn set_column(ref self: RowVector6<T>, j: usize, column: Matrix1<T>) {
        self = match j {
            0 => RowVector6 { x: column.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
            1 => RowVector6 { x: self.x, y: column.x, z: self.z, w: self.w, a: self.a, b: self.b },
            2 => RowVector6 { x: self.x, y: self.y, z: column.x, w: self.w, a: self.a, b: self.b },
            3 => RowVector6 { x: self.x, y: self.y, z: self.z, w: column.x, a: self.a, b: self.b },
            4 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: column.x, b: self.b },
            5 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: column.x },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        };
    }

    /// Replaces the diagonal by `diag`, a `Matrix1` (upstream requires the length `min(R, C)` = 1).
    /// Upstream: `set_diagonal`.
    #[inline(always)]
    fn set_diagonal(ref self: RowVector6<T>, diag: Matrix1<T>) {
        self = RowVector6 { x: diag.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b };
    }

    /// Replaces the first `min(diag.len(), 1)` diagonal components by the values of `diag` (the
    /// extra values are ignored, like upstream's `take`), the others unchanged. Upstream:
    /// `set_partial_diagonal` (an iterator; a `Span` here).
    #[inline(always)]
    fn set_partial_diagonal(ref self: RowVector6<T>, diag: Span<T>) {
        let len = diag.len();
        self =
            RowVector6 {
                x: if len > 0 {
                    *diag[0]
                } else {
                    self.x
                },
                y: self.y,
                z: self.z,
                w: self.w,
                a: self.a,
                b: self.b,
            };
    }

    /// Sets `self` to `other` (a `RowVector6`: upstream requires the same shape). Upstream:
    /// `copy_from`.
    #[inline(always)]
    fn copy_from(ref self: RowVector6<T>, other: RowVector6<T>) {
        self = other;
    }

    /// Sets `self` to the 6 values of `slice` in column-major order (`from_column_slice`). Panics
    /// with `nalgebra: wrong slice length` unless `slice.len() == 6`. Upstream: `copy_from_slice`
    /// (`&[T]`).
    #[inline(always)]
    fn copy_from_slice(ref self: RowVector6<T>, slice: Span<T>) {
        self = Self::from_column_slice(slice);
    }

    /// Sets `self` to the transpose of `other`, a `Vector6`. Exact. Upstream: `tr_copy_from`.
    #[inline(always)]
    fn tr_copy_from(ref self: RowVector6<T>, other: Vector6<T>) {
        self =
            RowVector6 { x: other.x, y: other.y, z: other.z, w: other.w, a: other.a, b: other.b };
    }

    /// Exchanges the components at `row_cols1` and `row_cols2` (`(row, column)`). Panics with
    /// `nalgebra: index out of bounds` when either is out of the shape. Upstream: `swap`.
    #[inline]
    fn swap(ref self: RowVector6<T>, row_cols1: (usize, usize), row_cols2: (usize, usize)) {
        let a = MatrixIndex::index(self, row_cols1);
        let b = MatrixIndex::index(self, row_cols2);
        self =
            RowVector6EditTrait::replace(
                RowVector6EditTrait::replace(self, row_cols1, b), row_cols2, a,
            );
    }

    /// Exchanges rows `irow1` and `irow2`. Panics with `nalgebra: index out of bounds` when either
    /// is `>= 1`. Upstream: `swap_rows`.
    ///
    /// Two reads and two writes of a row (one `match` each). ONE nested `match` on both rows is 940
    /// gas (15%) cheaper on `Matrix3` (`bench_matrix3_swap_rows__alt_pair_match`) but generates R²
    /// whole-shape literals (about 40 000 lines over the 36 shapes, per method): kept as a
    /// benchmark.
    #[inline]
    fn swap_rows(ref self: RowVector6<T>, irow1: usize, irow2: usize) {
        let a = RowVector6EditTrait::row_at(self, irow1);
        let b = RowVector6EditTrait::row_at(self, irow2);
        Self::set_row(ref self, irow1, b);
        Self::set_row(ref self, irow2, a);
    }

    /// Exchanges columns `icol1` and `icol2`. Panics with `nalgebra: index out of bounds` when
    /// either is `>= 6`. Upstream: `swap_columns`.
    #[inline]
    fn swap_columns(ref self: RowVector6<T>, icol1: usize, icol2: usize) {
        let a = RowVector6EditTrait::column_at(self, icol1);
        let b = RowVector6EditTrait::column_at(self, icol2);
        Self::set_column(ref self, icol1, b);
        Self::set_column(ref self, icol2, a);
    }

    /// `self = -self`. Exact; panics on overflow (`-MIN`). Upstream: `neg_mut`.
    #[inline(always)]
    fn neg_mut(ref self: RowVector6<T>) {
        self = -self;
    }

    /// `self = self.scale(k)`, each component floored once. Panics on overflow. Upstream:
    /// `scale_mut`.
    #[inline(always)]
    fn scale_mut(ref self: RowVector6<T>, k: T) {
        self = Self::scale(self, k);
    }

    /// `self = self.unscale(k)`, each component correctly rounded. Panics on a zero `k` and on
    /// overflow. Upstream: `unscale_mut`.
    #[inline(always)]
    fn unscale_mut(ref self: RowVector6<T>, k: T) {
        self = Self::unscale(self, k);
    }

    /// Normalizes `self` in place (`self = self.normalize()`, bit-identical) and returns the norm
    /// it had. Panics with a division by zero when the norm is zero. Upstream: `normalize_mut`.
    #[inline(always)]
    fn normalize_mut(ref self: RowVector6<T>) -> T {
        let n = Self::norm(self);
        self = Self::unscale(self, n);
        n
    }

    /// Normalizes `self` in place and returns `Some` of the norm it had, or leaves it unchanged and
    /// returns `None` when that norm is `<= min_norm`. Upstream: `try_normalize_mut`.
    #[inline(always)]
    fn try_normalize_mut(ref self: RowVector6<T>, min_norm: T) -> Option<T> {
        let n = Self::norm(self);
        if n <= min_norm {
            return None;
        }
        self = Self::unscale(self, n);
        Some(n)
    }

    /// Scales `self` to the norm `magnitude`: `self.scale(magnitude / norm)`, the ratio rounded to
    /// nearest (like `try_set_magnitude`). Panics with a division by zero when the norm is zero
    /// (upstream's floats give NaN). Upstream: `set_magnitude`.
    #[inline(always)]
    fn set_magnitude(ref self: RowVector6<T>, magnitude: T) {
        let n = Self::norm(self);
        self = Self::scale(self, R::div(magnitude, n));
    }

    /// `self = self.add_scalar(k)`. Exact; panics on overflow. Upstream: `add_scalar_mut`.
    #[inline(always)]
    fn add_scalar_mut(ref self: RowVector6<T>, k: T) {
        self = Self::add_scalar(self, k);
    }

    /// Alias of `component_mul_assign` (deprecated upstream). Upstream: `component_mul_mut`.
    #[inline(always)]
    fn component_mul_mut(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        Self::component_mul_assign(ref self, rhs);
    }

    /// Alias of `component_div_assign` (deprecated upstream). Upstream: `component_div_mut`.
    #[inline(always)]
    fn component_div_mut(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        Self::component_div_assign(ref self, rhs);
    }

    /// Conjugates every component in place: nothing changes for a real scalar. Upstream:
    /// `conjugate_mut`.
    #[inline(always)]
    fn conjugate_mut(ref self: RowVector6<T>) {
        self = Self::conjugate(self);
    }

    /// Writes the transpose of `self` into `out`, a `Vector6`. Exact. Upstream: `transpose_to`.
    #[inline(always)]
    fn transpose_to(self: RowVector6<T>, ref out: Vector6<T>) {
        out = Self::transpose(self);
    }

    /// Writes the adjoint of `self` (its transpose for a real scalar) into `out`, a `Vector6`.
    /// Exact. Upstream: `adjoint_to`.
    #[inline(always)]
    fn adjoint_to(self: RowVector6<T>, ref out: Vector6<T>) {
        out = Self::transpose(self);
    }

    /// Writes the adjoint of `self` (deprecated upstream alias of `adjoint_to`) into `out`, a
    /// `Vector6`. Exact. Upstream: `conjugate_transpose_to`.
    #[inline(always)]
    fn conjugate_transpose_to(self: RowVector6<T>, ref out: Vector6<T>) {
        out = Self::transpose(self);
    }

    /// Writes the sum `self + rhs` into `out`. Exact; panics on overflow. Upstream: `add_to`.
    #[inline(always)]
    fn add_to(self: RowVector6<T>, rhs: RowVector6<T>, ref out: RowVector6<T>) {
        out = self + rhs;
    }

    /// Writes the difference `self - rhs` into `out`. Exact; panics on overflow. Upstream:
    /// `sub_to`.
    #[inline(always)]
    fn sub_to(self: RowVector6<T>, rhs: RowVector6<T>, ref out: RowVector6<T>) {
        out = self - rhs;
    }

    /// The norm `norm` of `self`: `EuclideanNorm {}` (`norm`), `LpNorm { p }` (`lp_norm(p)`),
    /// `OneNorm {}` (`one_norm`) or `UniformNorm {}` (`amax`), through their `Norm` impls (static
    /// dispatch). Upstream: `apply_norm` (`&impl Norm<T>`; the markers are `Copy` values here).
    #[inline]
    fn apply_norm<N, +Drop<N>, impl Nm: Norm<N, RowVector6<T>, T>>(
        self: RowVector6<T>, norm: N,
    ) -> T {
        Nm::norm(@norm, self)
    }

    /// The distance between `self` and `rhs` in the norm `norm` (see `apply_norm`; the Euclidean
    /// one is the fused `metric_distance`, the others the norm of the exact difference). Upstream:
    /// `apply_metric_distance`.
    #[inline]
    fn apply_metric_distance<N, +Drop<N>, impl Nm: Norm<N, RowVector6<T>, T>>(
        self: RowVector6<T>, rhs: RowVector6<T>, norm: N,
    ) -> T {
        Nm::metric_distance(@norm, self, rhs)
    }

    /// The number of rows, 1. Upstream: `nrows`.
    #[inline(always)]
    fn nrows(self: RowVector6<T>) -> usize {
        1
    }

    /// The number of columns, 6. Upstream: `ncols`.
    #[inline(always)]
    fn ncols(self: RowVector6<T>) -> usize {
        6
    }

    /// `(nrows, ncols)`, `(1, 6)`. Upstream: `shape`.
    #[inline(always)]
    fn shape(self: RowVector6<T>) -> (usize, usize) {
        (1, 6)
    }

    /// Whether the shape is square: `false`. Upstream: `is_square`.
    #[inline(always)]
    fn is_square(self: RowVector6<T>) -> bool {
        false
    }

    /// The `(row, column)` of the `i`-th component in column-major order: `(i % 1, i / 1)`, one
    /// `DivRem` (no bounds check, like upstream). Upstream: `vector_to_matrix_index`.
    #[inline(always)]
    fn vector_to_matrix_index(self: RowVector6<T>, i: usize) -> (usize, usize) {
        (0, i)
    }

    /// Row `i`, a `RowVector6` (an owned copy: Cairo has no borrowed views). Panics with `nalgebra:
    /// index out of bounds` for `i >= 1`. ONE `match` on `i` selects the literal (the private
    /// `row_at` of `swap_rows`). Upstream: `row` (a view).
    #[inline(always)]
    fn row(self: RowVector6<T>, i: usize) -> RowVector6<T> {
        RowVector6EditTrait::row_at(self, i)
    }

    /// Column `j`, a `Matrix1` (an owned copy: Cairo has no borrowed views). Panics with `nalgebra:
    /// index out of bounds` for `j >= 6`. ONE `match` on `j` selects the literal (the private
    /// `column_at` of `swap_columns`). Upstream: `column` (a view).
    #[inline(always)]
    fn column(self: RowVector6<T>, j: usize) -> Matrix1<T> {
        RowVector6EditTrait::column_at(self, j)
    }

    /// The upper triangle of `self` (the diagonal included), the components below the diagonal set
    /// to zero. Exact. Upstream: `upper_triangle`.
    #[inline(always)]
    fn upper_triangle(self: RowVector6<T>) -> RowVector6<T> {
        RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }
    }

    /// The lower triangle of `self` (the diagonal included), the components above the diagonal set
    /// to zero. Exact. Upstream: `lower_triangle`.
    #[inline(always)]
    fn lower_triangle(self: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: self.x, y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero(), b: R::zero(),
        }
    }

    /// The 6-dimensional row vector whose 1 rows are the given vectors, each a `Vector6` of the
    /// row's 6 components (the argument form of the former `Matrix2/3/4::from_rows`, used by
    /// `linalg`). Exact. Upstream: `from_rows` (a slice of row vectors).
    #[inline(always)]
    fn from_rows(r1: Vector6<T>) -> RowVector6<T> {
        RowVector6 { x: r1.x, y: r1.y, z: r1.z, w: r1.w, a: r1.a, b: r1.b }
    }

    /// The 6-dimensional row vector whose 6 columns are the given `Matrix1`s. Exact. Upstream:
    /// `from_columns` (a slice of column vectors).
    #[inline(always)]
    fn from_columns(
        c1: Matrix1<T>,
        c2: Matrix1<T>,
        c3: Matrix1<T>,
        c4: Matrix1<T>,
        c5: Matrix1<T>,
        c6: Matrix1<T>,
    ) -> RowVector6<T> {
        RowVector6 { x: c1.x, y: c2.x, z: c3.x, w: c4.x, a: c5.x, b: c6.x }
    }

    /// Whether the columns of `self` are orthonormal: `selfᵀ * self` (fused `tr_mul`, one
    /// rounding per component) is the 6x6 identity within `ulps`. Upstream: `is_orthogonal` (`eps`:
    /// here a tolerance in ulp, DESIGN D3).
    #[inline(always)]
    fn is_orthogonal(self: RowVector6<T>, ulps: u64) -> bool {
        Matrix6Trait::is_identity(MatrixTrMul::tr_mul(self, self), ulps)
    }

    /// `self` with a row of `val` inserted at index `i` (`i <= 1`), a `Matrix2x6`. Panics with
    /// `nalgebra: index out of bounds` for `i > 1`. Upstream: `insert_row`.
    #[inline(always)]
    fn insert_row(self: RowVector6<T>, i: usize, val: T) -> Matrix2x6<T> {
        match i {
            0 => Matrix2x6 {
                m11: val,
                m21: self.x,
                m12: val,
                m22: self.y,
                m13: val,
                m23: self.z,
                m14: val,
                m24: self.w,
                m15: val,
                m25: self.a,
                m16: val,
                m26: self.b,
            },
            1 => Matrix2x6 {
                m11: self.x,
                m21: val,
                m12: self.y,
                m22: val,
                m13: self.z,
                m23: val,
                m14: self.w,
                m24: val,
                m15: self.a,
                m25: val,
                m16: self.b,
                m26: val,
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    /// `self` without its column `i`, a `RowVector5`. Panics with `nalgebra: index out of bounds`
    /// for `i >= 6`. Upstream: `remove_column`.
    #[inline(always)]
    fn remove_column(self: RowVector6<T>, i: usize) -> RowVector5<T> {
        match i {
            0 => RowVector5 { x: self.y, y: self.z, z: self.w, w: self.a, a: self.b },
            1 => RowVector5 { x: self.x, y: self.z, z: self.w, w: self.a, a: self.b },
            2 => RowVector5 { x: self.x, y: self.y, z: self.w, w: self.a, a: self.b },
            3 => RowVector5 { x: self.x, y: self.y, z: self.z, w: self.a, a: self.b },
            4 => RowVector5 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.b },
            5 => RowVector5 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The operations of `RowVector6<T>` that need `Transcendental` (inverse trigonometry, `exp`,
/// `ln`):
/// a scalar may implement `Real` only.
#[generate_trait]
pub impl RowVector6AngleImpl<
    T,
    impl R: Real<T>,
    impl Tr: Transcendental<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector6AngleTrait<T> {
    /// The angle between `self` and `other` seen as vectors of the Frobenius inner product, in `[0,
    /// π]` (up to the rounding of `atan2`); `0` when one of them is zero. Computed as `2 *
    /// atan2(|u - v|, |u + v|)` on the normalized `u`, `v` (Kahan): unlike upstream's `acos(dot /
    /// (|a| *
    /// |b|))` it cannot overflow on long inputs and stays accurate for nearly parallel ones. Panics
    /// when a norm does not fit. Upstream: `angle`.
    fn angle(self: RowVector6<T>, other: RowVector6<T>) -> T {
        let n1 = RowVector6Trait::norm(self);
        let n2 = RowVector6Trait::norm(other);
        if n1 == R::zero() || n2 == R::zero() {
            return R::zero();
        }
        let u = RowVector6Trait::unscale(self, n1);
        let v = RowVector6Trait::unscale(other, n2);
        let half = Tr::atan2(RowVector6Trait::metric_distance(u, v), RowVector6Trait::norm(u + v));
        half + half
    }

    /// The entrywise Lp norm `(Σ |a|^p)^(1/p)`. `p = 1` is the exact sum of the absolute values
    /// and `p = 2` the fused `norm` (both exact up to their one rounding); above, the components
    /// are first divided by the largest absolute value `m` (so no power can overflow), `Σ (|a| /
    /// m)^p`
    /// is summed (`p` floored products each), and the root is `m * exp(ln(Σ) / p)`: a few ulp
    /// relative to the result, the rounding of `exp` / `ln`. Panics with `nalgebra: lp_norm needs p
    /// >= 1` for `p < 1` (upstream returns meaningless values: an infinite root for `p = 0`).
    /// Upstream: `lp_norm`.
    fn lp_norm(self: RowVector6<T>, p: i32) -> T {
        if p < 1 {
            core::panic_with_felt252(errors::LP_NORM_P);
        }
        if p == 1 {
            return R::abs(self.x)
                + R::abs(self.y)
                + R::abs(self.z)
                + R::abs(self.w)
                + R::abs(self.a)
                + R::abs(self.b);
        }
        if p == 2 {
            return RowVector6Trait::norm(self);
        }
        let m = RowVector6Trait::amax(self);
        if m == R::zero() {
            return R::zero();
        }
        let r = RowVector6Trait::unscale(RowVector6Trait::abs(self), m);
        let q: u32 = p.try_into().unwrap();
        let s = Powi::powi(r.x, q)
            + Powi::powi(r.y, q)
            + Powi::powi(r.z, q)
            + Powi::powi(r.w, q)
            + Powi::powi(r.a, q)
            + Powi::powi(r.b, q);
        m * Tr::exp(R::div(Tr::ln(s), R::from_int(p)))
    }
}

// --- operators -----------------------------------------------------------------------------------

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl RowVector6Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<RowVector6<T>> {
    #[inline(always)]
    fn add(lhs: RowVector6<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: lhs.x + rhs.x,
            y: lhs.y + rhs.y,
            z: lhs.z + rhs.z,
            w: lhs.w + rhs.w,
            a: lhs.a + rhs.a,
            b: lhs.b + rhs.b,
        }
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl RowVector6Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<RowVector6<T>> {
    #[inline(always)]
    fn sub(lhs: RowVector6<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: lhs.x - rhs.x,
            y: lhs.y - rhs.y,
            z: lhs.z - rhs.z,
            w: lhs.w - rhs.w,
            a: lhs.a - rhs.a,
            b: lhs.b - rhs.b,
        }
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl RowVector6Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<RowVector6<T>> {
    #[inline(always)]
    fn neg(a: RowVector6<T>) -> RowVector6<T> {
        RowVector6 { x: -a.x, y: -a.y, z: -a.z, w: -a.w, a: -a.a, b: -a.b }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl RowVector6AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn add_assign(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        self = self + rhs;
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl RowVector6SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn sub_assign(ref self: RowVector6<T>, rhs: RowVector6<T>) {
        self = self - rhs;
    }
}

/// `[x, y, z, w, a, b].into()`. Upstream: `From<[T; 6]>`.
pub impl RowVector6FromArray<T> of Into<[T; 6], RowVector6<T>> {
    #[inline(always)]
    fn into(self: [T; 6]) -> RowVector6<T> {
        let [x, y, z, w, a, b] = self;
        RowVector6 { x, y, z, w, a, b }
    }
}

/// The components as a fixed-size array `[x, y, z, w, a, b]`. Upstream: `Into<[T; 6]>`.
pub impl RowVector6IntoArray<T> of Into<RowVector6<T>, [T; 6]> {
    #[inline(always)]
    fn into(self: RowVector6<T>) -> [T; 6] {
        let RowVector6 { x, y, z, w, a, b } = self;
        [x, y, z, w, a, b]
    }
}

// --- products ------------------------------------------------------------------------------------

/// `self * rhs`, a `Matrix1`: the 6 products of each component accumulated exactly in `Real::Wide`
/// and rescaled once. Panics on overflow. Upstream: `RowVector6 * Vector6` (`Mul`).
pub impl RowVector6MulVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Vector6<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector6<T>, rhs: Vector6<T>) -> Matrix1<T> {
        Matrix1 {
            x: Fused::sum_prod6(
                self.x,
                rhs.x,
                self.y,
                rhs.y,
                self.z,
                rhs.z,
                self.w,
                rhs.w,
                self.a,
                rhs.a,
                self.b,
                rhs.b,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector2`: the 6 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector6 * Matrix6x2` (`Mul`).
pub impl RowVector6MulMatrix6x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Matrix6x2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector6<T>, rhs: Matrix6x2<T>) -> RowVector2<T> {
        RowVector2 {
            x: Fused::sum_prod6(
                self.x,
                rhs.m11,
                self.y,
                rhs.m21,
                self.z,
                rhs.m31,
                self.w,
                rhs.m41,
                self.a,
                rhs.m51,
                self.b,
                rhs.m61,
            ),
            y: Fused::sum_prod6(
                self.x,
                rhs.m12,
                self.y,
                rhs.m22,
                self.z,
                rhs.m32,
                self.w,
                rhs.m42,
                self.a,
                rhs.m52,
                self.b,
                rhs.m62,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector3`: the 6 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector6 * Matrix6x3` (`Mul`).
pub impl RowVector6MulMatrix6x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Matrix6x3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector6<T>, rhs: Matrix6x3<T>) -> RowVector3<T> {
        RowVector3 {
            x: Fused::sum_prod6(
                self.x,
                rhs.m11,
                self.y,
                rhs.m21,
                self.z,
                rhs.m31,
                self.w,
                rhs.m41,
                self.a,
                rhs.m51,
                self.b,
                rhs.m61,
            ),
            y: Fused::sum_prod6(
                self.x,
                rhs.m12,
                self.y,
                rhs.m22,
                self.z,
                rhs.m32,
                self.w,
                rhs.m42,
                self.a,
                rhs.m52,
                self.b,
                rhs.m62,
            ),
            z: Fused::sum_prod6(
                self.x,
                rhs.m13,
                self.y,
                rhs.m23,
                self.z,
                rhs.m33,
                self.w,
                rhs.m43,
                self.a,
                rhs.m53,
                self.b,
                rhs.m63,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector4`: the 6 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector6 * Matrix6x4` (`Mul`).
pub impl RowVector6MulMatrix6x4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Matrix6x4<T>> {
    type Output = RowVector4<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector6<T>, rhs: Matrix6x4<T>) -> RowVector4<T> {
        RowVector4 {
            x: Fused::sum_prod6(
                self.x,
                rhs.m11,
                self.y,
                rhs.m21,
                self.z,
                rhs.m31,
                self.w,
                rhs.m41,
                self.a,
                rhs.m51,
                self.b,
                rhs.m61,
            ),
            y: Fused::sum_prod6(
                self.x,
                rhs.m12,
                self.y,
                rhs.m22,
                self.z,
                rhs.m32,
                self.w,
                rhs.m42,
                self.a,
                rhs.m52,
                self.b,
                rhs.m62,
            ),
            z: Fused::sum_prod6(
                self.x,
                rhs.m13,
                self.y,
                rhs.m23,
                self.z,
                rhs.m33,
                self.w,
                rhs.m43,
                self.a,
                rhs.m53,
                self.b,
                rhs.m63,
            ),
            w: Fused::sum_prod6(
                self.x,
                rhs.m14,
                self.y,
                rhs.m24,
                self.z,
                rhs.m34,
                self.w,
                rhs.m44,
                self.a,
                rhs.m54,
                self.b,
                rhs.m64,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector5`: the 6 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector6 * Matrix6x5` (`Mul`).
pub impl RowVector6MulMatrix6x5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Matrix6x5<T>> {
    type Output = RowVector5<T>;
    fn mul_mat(self: RowVector6<T>, rhs: Matrix6x5<T>) -> RowVector5<T> {
        RowVector5 {
            x: Fused::sum_prod6(
                self.x,
                rhs.m11,
                self.y,
                rhs.m21,
                self.z,
                rhs.m31,
                self.w,
                rhs.m41,
                self.a,
                rhs.m51,
                self.b,
                rhs.m61,
            ),
            y: Fused::sum_prod6(
                self.x,
                rhs.m12,
                self.y,
                rhs.m22,
                self.z,
                rhs.m32,
                self.w,
                rhs.m42,
                self.a,
                rhs.m52,
                self.b,
                rhs.m62,
            ),
            z: Fused::sum_prod6(
                self.x,
                rhs.m13,
                self.y,
                rhs.m23,
                self.z,
                rhs.m33,
                self.w,
                rhs.m43,
                self.a,
                rhs.m53,
                self.b,
                rhs.m63,
            ),
            w: Fused::sum_prod6(
                self.x,
                rhs.m14,
                self.y,
                rhs.m24,
                self.z,
                rhs.m34,
                self.w,
                rhs.m44,
                self.a,
                rhs.m54,
                self.b,
                rhs.m64,
            ),
            a: Fused::sum_prod6(
                self.x,
                rhs.m15,
                self.y,
                rhs.m25,
                self.z,
                rhs.m35,
                self.w,
                rhs.m45,
                self.a,
                rhs.m55,
                self.b,
                rhs.m65,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector6`: the 6 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector6 * Matrix6` (`Mul`).
pub impl RowVector6MulMatrix6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector6<T>, Matrix6<T>> {
    type Output = RowVector6<T>;
    fn mul_mat(self: RowVector6<T>, rhs: Matrix6<T>) -> RowVector6<T> {
        RowVector6 {
            x: Fused::sum_prod6(
                self.x,
                rhs.m11,
                self.y,
                rhs.m21,
                self.z,
                rhs.m31,
                self.w,
                rhs.m41,
                self.a,
                rhs.m51,
                self.b,
                rhs.m61,
            ),
            y: Fused::sum_prod6(
                self.x,
                rhs.m12,
                self.y,
                rhs.m22,
                self.z,
                rhs.m32,
                self.w,
                rhs.m42,
                self.a,
                rhs.m52,
                self.b,
                rhs.m62,
            ),
            z: Fused::sum_prod6(
                self.x,
                rhs.m13,
                self.y,
                rhs.m23,
                self.z,
                rhs.m33,
                self.w,
                rhs.m43,
                self.a,
                rhs.m53,
                self.b,
                rhs.m63,
            ),
            w: Fused::sum_prod6(
                self.x,
                rhs.m14,
                self.y,
                rhs.m24,
                self.z,
                rhs.m34,
                self.w,
                rhs.m44,
                self.a,
                rhs.m54,
                self.b,
                rhs.m64,
            ),
            a: Fused::sum_prod6(
                self.x,
                rhs.m15,
                self.y,
                rhs.m25,
                self.z,
                rhs.m35,
                self.w,
                rhs.m45,
                self.a,
                rhs.m55,
                self.b,
                rhs.m65,
            ),
            b: Fused::sum_prod6(
                self.x,
                rhs.m16,
                self.y,
                rhs.m26,
                self.z,
                rhs.m36,
                self.w,
                rhs.m46,
                self.a,
                rhs.m56,
                self.b,
                rhs.m66,
            ),
        }
    }
}

/// `selfᵀ * rhs`, a `Vector6`: `mul_mat` of the transposed components, so one floored product per
/// component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only
/// relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector6TrMulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector6<T>, Matrix1<T>> {
    type Output = Vector6<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector6<T>, rhs: Matrix1<T>) -> Vector6<T> {
        MatrixMul::mul_mat(
            Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }, rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix6x2`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector6TrMulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector6<T>, RowVector2<T>> {
    type Output = Matrix6x2<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector6<T>, rhs: RowVector2<T>) -> Matrix6x2<T> {
        MatrixMul::mul_mat(
            Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }, rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix6x3`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector6TrMulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector6<T>, RowVector3<T>> {
    type Output = Matrix6x3<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector6<T>, rhs: RowVector3<T>) -> Matrix6x3<T> {
        MatrixMul::mul_mat(
            Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }, rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix6x4`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector6TrMulRowVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector6<T>, RowVector4<T>> {
    type Output = Matrix6x4<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector6<T>, rhs: RowVector4<T>) -> Matrix6x4<T> {
        MatrixMul::mul_mat(
            Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }, rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix6x5`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector6TrMulRowVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector6<T>, RowVector5<T>> {
    type Output = Matrix6x5<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector6<T>, rhs: RowVector5<T>) -> Matrix6x5<T> {
        MatrixMul::mul_mat(
            Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }, rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix6`: `mul_mat` of the transposed components, so one floored product per
/// component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only
/// relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector6TrMulRowVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector6<T>, RowVector6<T>> {
    type Output = Matrix6<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector6<T>, rhs: RowVector6<T>) -> Matrix6<T> {
        MatrixMul::mul_mat(
            Vector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b }, rhs,
        )
    }
}

// --- indexing, comparisons, conversions ----------------------------------------------------------

/// `m.get(i)` / `m.index(..)`: the component `index` in column-major (storage) order. `get` is
/// `None` out of bounds, `index` panics with `nalgebra: index out of bounds`. Upstream:
/// `Matrix::get` / `Matrix::index` (their `MatrixIndex` argument).
pub impl RowVector6MatrixIndexLinear<T, +Copy<T>, +Drop<T>> of MatrixIndex<RowVector6<T>, usize> {
    type Output = T;
    #[inline(always)]
    fn get(self: RowVector6<T>, index: usize) -> Option<T> {
        match index {
            0 => Option::Some(self.x),
            1 => Option::Some(self.y),
            2 => Option::Some(self.z),
            3 => Option::Some(self.w),
            4 => Option::Some(self.a),
            5 => Option::Some(self.b),
            _ => Option::None,
        }
    }
    #[inline(always)]
    fn index(self: RowVector6<T>, index: usize) -> T {
        match index {
            0 => self.x,
            1 => self.y,
            2 => self.z,
            3 => self.w,
            4 => self.a,
            5 => self.b,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[i]`: the component `index` in column-major (storage) order. Panics with `nalgebra: index out
/// of bounds`. Upstream: `Index<usize>`.
pub impl RowVector6IndexLinear<T, +Copy<T>, +Drop<T>> of IndexView<RowVector6<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @RowVector6<T>, index: usize) -> T {
        match index {
            0 => *self.x,
            1 => *self.y,
            2 => *self.z,
            3 => *self.w,
            4 => *self.a,
            5 => *self.b,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m.get((i, j))` / `m.index(..)`: the component at `(row, column)`. `get` is `None` out of
/// bounds, `index` panics with `nalgebra: index out of bounds`. Upstream: `Matrix::get` /
/// `Matrix::index` (their `MatrixIndex` argument).
pub impl RowVector6MatrixIndexPair<
    T, +Copy<T>, +Drop<T>,
> of MatrixIndex<RowVector6<T>, (usize, usize)> {
    type Output = T;
    #[inline(always)]
    fn get(self: RowVector6<T>, index: (usize, usize)) -> Option<T> {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => Option::Some(self.x),
                _ => Option::None,
            },
            1 => match i {
                0 => Option::Some(self.y),
                _ => Option::None,
            },
            2 => match i {
                0 => Option::Some(self.z),
                _ => Option::None,
            },
            3 => match i {
                0 => Option::Some(self.w),
                _ => Option::None,
            },
            4 => match i {
                0 => Option::Some(self.a),
                _ => Option::None,
            },
            5 => match i {
                0 => Option::Some(self.b),
                _ => Option::None,
            },
            _ => Option::None,
        }
    }
    #[inline(always)]
    fn index(self: RowVector6<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => self.x,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => self.y,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match i {
                0 => self.z,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match i {
                0 => self.w,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            4 => match i {
                0 => self.a,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            5 => match i {
                0 => self.b,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at `(row, column)`. Panics with `nalgebra: index out of bounds`.
/// Upstream: `Index<(usize, usize)>`.
pub impl RowVector6IndexPair<T, +Copy<T>, +Drop<T>> of IndexView<RowVector6<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @RowVector6<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.x,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.y,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match i {
                0 => *self.z,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match i {
                0 => *self.w,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            4 => match i {
                0 => *self.a,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            5 => match i {
                0 => *self.b,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The component-wise partial order: `a < b` when EVERY component of `a` is smaller than `b`'s
/// (likewise `<=`, `>`, `>=`), so two matrices may be unordered (`!(a < b) && !(a >= b)`).
/// Upstream: `PartialOrd for Matrix`.
pub impl RowVector6PartialOrd<T, +PartialOrd<T>, +Copy<T>, +Drop<T>> of PartialOrd<RowVector6<T>> {
    #[inline(always)]
    fn lt(lhs: RowVector6<T>, rhs: RowVector6<T>) -> bool {
        lhs.x < rhs.x
            && lhs.y < rhs.y
            && lhs.z < rhs.z
            && lhs.w < rhs.w
            && lhs.a < rhs.a
            && lhs.b < rhs.b
    }
    #[inline(always)]
    fn le(lhs: RowVector6<T>, rhs: RowVector6<T>) -> bool {
        lhs.x <= rhs.x
            && lhs.y <= rhs.y
            && lhs.z <= rhs.z
            && lhs.w <= rhs.w
            && lhs.a <= rhs.a
            && lhs.b <= rhs.b
    }
    #[inline(always)]
    fn gt(lhs: RowVector6<T>, rhs: RowVector6<T>) -> bool {
        lhs.x > rhs.x
            && lhs.y > rhs.y
            && lhs.z > rhs.z
            && lhs.w > rhs.w
            && lhs.a > rhs.a
            && lhs.b > rhs.b
    }
    #[inline(always)]
    fn ge(lhs: RowVector6<T>, rhs: RowVector6<T>) -> bool {
        lhs.x >= rhs.x
            && lhs.y >= rhs.y
            && lhs.z >= rhs.z
            && lhs.w >= rhs.w
            && lhs.a >= rhs.a
            && lhs.b >= rhs.b
    }
}

/// The component-wise bounds: every component `Bounded::<T>::MIN` / `MAX`. Upstream: `num::Bounded
/// for Matrix`.
pub impl RowVector6Bounded<T, +Bounded<T>, +Drop<T>> of Bounded<RowVector6<T>> {
    const MIN: RowVector6<T> = RowVector6 {
        x: Bounded::<T>::MIN,
        y: Bounded::<T>::MIN,
        z: Bounded::<T>::MIN,
        w: Bounded::<T>::MIN,
        a: Bounded::<T>::MIN,
        b: Bounded::<T>::MIN,
    };
    const MAX: RowVector6<T> = RowVector6 {
        x: Bounded::<T>::MAX,
        y: Bounded::<T>::MAX,
        z: Bounded::<T>::MAX,
        w: Bounded::<T>::MAX,
        a: Bounded::<T>::MAX,
        b: Bounded::<T>::MAX,
    };
}

/// The 6-dimensional row vector of the given COLUMNS (`[[m11, m21, ..], [m12, ..], ..]`). Upstream:
/// `From<[[T; R]; C]>`.
pub impl RowVector6FromColumnArrays<T, +Drop<T>> of Into<[[T; 1]; 6], RowVector6<T>> {
    #[inline(always)]
    fn into(self: [[T; 1]; 6]) -> RowVector6<T> {
        let [c0, c1, c2, c3, c4, c5] = self;
        let [x] = c0;
        let [y] = c1;
        let [z] = c2;
        let [w] = c3;
        let [a] = c4;
        let [b] = c5;
        RowVector6 { x, y, z, w, a, b }
    }
}

/// The columns of the 6-dimensional row vector as nested arrays (`[[m11, m21, ..], [m12, ..],
/// ..]`). Upstream: `Into<[[T; R]; C]>`.
pub impl RowVector6IntoColumnArrays<T, +Drop<T>> of Into<RowVector6<T>, [[T; 1]; 6]> {
    #[inline(always)]
    fn into(self: RowVector6<T>) -> [[T; 1]; 6] {
        let RowVector6 { x, y, z, w, a, b } = self;
        [[x], [y], [z], [w], [a], [b]]
    }
}

/// `self *= k` for a scalar `k`: `scale` in place, each component floored once. Panics on overflow.
/// Upstream: `MulAssign<T>`.
pub impl RowVector6MulAssignScalar<T, +Mul<T>, +Copy<T>, +Drop<T>> of MulAssign<RowVector6<T>, T> {
    #[inline(always)]
    fn mul_assign(ref self: RowVector6<T>, rhs: T) {
        self =
            RowVector6 {
                x: self.x * rhs,
                y: self.y * rhs,
                z: self.z * rhs,
                w: self.w * rhs,
                a: self.a * rhs,
                b: self.b * rhs,
            };
    }
}

/// `self /= k` for a scalar `k`: `unscale` in place, each component correctly rounded. Panics on a
/// zero `k` and on overflow. Upstream: `DivAssign<T>`.
pub impl RowVector6DivAssignScalar<
    T, impl R: Real<T>, +Copy<T>, +Drop<T>,
> of DivAssign<RowVector6<T>, T> {
    #[inline(always)]
    fn div_assign(ref self: RowVector6<T>, rhs: T) {
        let (x, y, z, w, a, b) = R::div6(self.x, self.y, self.z, self.w, self.a, self.b, rhs);
        self = RowVector6 { x, y, z, w, a, b };
    }
}

// --- functional and in-place variants ------------------------------------------------------------

/// Private helpers of the `swap*` methods (runtime positions: one `match` each).
#[generate_trait]
impl RowVector6EditImpl<T, +Copy<T>, +Drop<T>> of RowVector6EditTrait<T> {
    /// `self` with the component at `index` (`(row, column)`) replaced by `v`; panics out of
    /// bounds.
    #[inline(always)]
    fn replace(self: RowVector6<T>, index: (usize, usize), v: T) -> RowVector6<T> {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => RowVector6 { x: v, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => RowVector6 { x: self.x, y: v, z: self.z, w: self.w, a: self.a, b: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match i {
                0 => RowVector6 { x: self.x, y: self.y, z: v, w: self.w, a: self.a, b: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match i {
                0 => RowVector6 { x: self.x, y: self.y, z: self.z, w: v, a: self.a, b: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            4 => match i {
                0 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: v, b: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            5 => match i {
                0 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: v },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    /// Row `i`, a `RowVector6`; panics out of bounds.
    #[inline(always)]
    fn row_at(self: RowVector6<T>, i: usize) -> RowVector6<T> {
        match i {
            0 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    /// Column `j`, a `Matrix1`; panics out of bounds.
    #[inline(always)]
    fn column_at(self: RowVector6<T>, j: usize) -> Matrix1<T> {
        match j {
            0 => Matrix1 { x: self.x },
            1 => Matrix1 { x: self.y },
            2 => Matrix1 { x: self.z },
            3 => Matrix1 { x: self.w },
            4 => Matrix1 { x: self.a },
            5 => Matrix1 { x: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `EuclideanNorm` on `RowVector6`: `m.norm()` and the fused `metric_distance`. Upstream: `Norm<T>
/// for EuclideanNorm`.
pub impl RowVector6EuclideanNorm<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Norm<EuclideanNorm, RowVector6<T>, T> {
    #[inline(always)]
    fn norm(self: @EuclideanNorm, m: RowVector6<T>) -> T {
        RowVector6Trait::norm(m)
    }
    #[inline(always)]
    fn metric_distance(self: @EuclideanNorm, m1: RowVector6<T>, m2: RowVector6<T>) -> T {
        RowVector6Trait::metric_distance(m1, m2)
    }
}

/// `LpNorm` on `RowVector6`: `m.lp_norm(p)`, of `m1 - m2` for the distance. Upstream: `Norm<T> for
/// LpNorm`.
pub impl RowVector6LpNorm<
    T,
    impl R: Real<T>,
    impl Tr: Transcendental<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Norm<LpNorm, RowVector6<T>, T> {
    #[inline(always)]
    fn norm(self: @LpNorm, m: RowVector6<T>) -> T {
        RowVector6AngleTrait::lp_norm(m, *self.p)
    }
    #[inline(always)]
    fn metric_distance(self: @LpNorm, m1: RowVector6<T>, m2: RowVector6<T>) -> T {
        RowVector6AngleTrait::lp_norm(m1 - m2, *self.p)
    }
}

/// `OneNorm` on `RowVector6`: `m.one_norm()`, of `m1 - m2` for the distance. Upstream: `Norm<T> for
/// OneNorm`.
pub impl RowVector6OneNorm<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Norm<OneNorm, RowVector6<T>, T> {
    #[inline(always)]
    fn norm(self: @OneNorm, m: RowVector6<T>) -> T {
        RowVector6Trait::one_norm(m)
    }
    #[inline(always)]
    fn metric_distance(self: @OneNorm, m1: RowVector6<T>, m2: RowVector6<T>) -> T {
        RowVector6Trait::one_norm(m1 - m2)
    }
}

/// `UniformNorm` on `RowVector6`: `m.amax()`, of `m1 - m2` for the distance. Upstream: `Norm<T> for
/// UniformNorm`.
pub impl RowVector6UniformNorm<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Norm<UniformNorm, RowVector6<T>, T> {
    #[inline(always)]
    fn norm(self: @UniformNorm, m: RowVector6<T>) -> T {
        RowVector6Trait::amax(m)
    }
    #[inline(always)]
    fn metric_distance(self: @UniformNorm, m1: RowVector6<T>, m2: RowVector6<T>) -> T {
        RowVector6Trait::amax(m1 - m2)
    }
}

// --- rows, columns, blocks and edition (WP 8.2c) -------------------------------------------------

/// The 1 consecutive rows of a `RowVector6` as a `RowVector6` (`rows` / `rows_range`: default
/// methods). Upstream: `fixed_rows::<1>`, `select_rows`.
pub impl RowVector6FixedRowsRowVector6<
    T, +Copy<T>, +Drop<T>,
> of FixedRows<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn fixed_rows(self: RowVector6<T>, i: usize) -> RowVector6<T> {
        match i {
            0 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_rows(self: RowVector6<T>, irows: Span<usize>) -> RowVector6<T> {
        if irows.len() != 1 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let r0 = RowVector6EditTrait::row_at(self, *irows[0]);
        RowVector6 { x: r0.x, y: r0.y, z: r0.z, w: r0.w, a: r0.a, b: r0.b }
    }
}

/// The 1 consecutive columns of a `RowVector6` as a `Matrix1` (`columns` / `columns_range`: default
/// methods). Upstream: `fixed_columns::<1>`, `select_columns`.
pub impl RowVector6FixedColumnsMatrix1<
    T, +Copy<T>, +Drop<T>,
> of FixedColumns<RowVector6<T>, Matrix1<T>> {
    #[inline(always)]
    fn fixed_columns(self: RowVector6<T>, i: usize) -> Matrix1<T> {
        match i {
            0 => Matrix1 { x: self.x },
            1 => Matrix1 { x: self.y },
            2 => Matrix1 { x: self.z },
            3 => Matrix1 { x: self.w },
            4 => Matrix1 { x: self.a },
            5 => Matrix1 { x: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_columns(self: RowVector6<T>, icols: Span<usize>) -> Matrix1<T> {
        if icols.len() != 1 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let c0 = RowVector6EditTrait::column_at(self, *icols[0]);
        Matrix1 { x: c0.x }
    }
}

/// The 2 consecutive columns of a `RowVector6` as a `RowVector2` (`columns` / `columns_range`:
/// default methods). Upstream: `fixed_columns::<2>`, `select_columns`.
pub impl RowVector6FixedColumnsRowVector2<
    T, +Copy<T>, +Drop<T>,
> of FixedColumns<RowVector6<T>, RowVector2<T>> {
    #[inline(always)]
    fn fixed_columns(self: RowVector6<T>, i: usize) -> RowVector2<T> {
        match i {
            0 => RowVector2 { x: self.x, y: self.y },
            1 => RowVector2 { x: self.y, y: self.z },
            2 => RowVector2 { x: self.z, y: self.w },
            3 => RowVector2 { x: self.w, y: self.a },
            4 => RowVector2 { x: self.a, y: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_columns(self: RowVector6<T>, icols: Span<usize>) -> RowVector2<T> {
        if icols.len() != 2 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let c0 = RowVector6EditTrait::column_at(self, *icols[0]);
        let c1 = RowVector6EditTrait::column_at(self, *icols[1]);
        RowVector2 { x: c0.x, y: c1.x }
    }
}

/// The 3 consecutive columns of a `RowVector6` as a `RowVector3` (`columns` / `columns_range`:
/// default methods). Upstream: `fixed_columns::<3>`, `select_columns`.
pub impl RowVector6FixedColumnsRowVector3<
    T, +Copy<T>, +Drop<T>,
> of FixedColumns<RowVector6<T>, RowVector3<T>> {
    #[inline(always)]
    fn fixed_columns(self: RowVector6<T>, i: usize) -> RowVector3<T> {
        match i {
            0 => RowVector3 { x: self.x, y: self.y, z: self.z },
            1 => RowVector3 { x: self.y, y: self.z, z: self.w },
            2 => RowVector3 { x: self.z, y: self.w, z: self.a },
            3 => RowVector3 { x: self.w, y: self.a, z: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_columns(self: RowVector6<T>, icols: Span<usize>) -> RowVector3<T> {
        if icols.len() != 3 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let c0 = RowVector6EditTrait::column_at(self, *icols[0]);
        let c1 = RowVector6EditTrait::column_at(self, *icols[1]);
        let c2 = RowVector6EditTrait::column_at(self, *icols[2]);
        RowVector3 { x: c0.x, y: c1.x, z: c2.x }
    }
}

/// The 4 consecutive columns of a `RowVector6` as a `RowVector4` (`columns` / `columns_range`:
/// default methods). Upstream: `fixed_columns::<4>`, `select_columns`.
pub impl RowVector6FixedColumnsRowVector4<
    T, +Copy<T>, +Drop<T>,
> of FixedColumns<RowVector6<T>, RowVector4<T>> {
    #[inline(always)]
    fn fixed_columns(self: RowVector6<T>, i: usize) -> RowVector4<T> {
        match i {
            0 => RowVector4 { x: self.x, y: self.y, z: self.z, w: self.w },
            1 => RowVector4 { x: self.y, y: self.z, z: self.w, w: self.a },
            2 => RowVector4 { x: self.z, y: self.w, z: self.a, w: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_columns(self: RowVector6<T>, icols: Span<usize>) -> RowVector4<T> {
        if icols.len() != 4 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let c0 = RowVector6EditTrait::column_at(self, *icols[0]);
        let c1 = RowVector6EditTrait::column_at(self, *icols[1]);
        let c2 = RowVector6EditTrait::column_at(self, *icols[2]);
        let c3 = RowVector6EditTrait::column_at(self, *icols[3]);
        RowVector4 { x: c0.x, y: c1.x, z: c2.x, w: c3.x }
    }
}

/// The 5 consecutive columns of a `RowVector6` as a `RowVector5` (`columns` / `columns_range`:
/// default methods). Upstream: `fixed_columns::<5>`, `select_columns`.
pub impl RowVector6FixedColumnsRowVector5<
    T, +Copy<T>, +Drop<T>,
> of FixedColumns<RowVector6<T>, RowVector5<T>> {
    #[inline(always)]
    fn fixed_columns(self: RowVector6<T>, i: usize) -> RowVector5<T> {
        match i {
            0 => RowVector5 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a },
            1 => RowVector5 { x: self.y, y: self.z, z: self.w, w: self.a, a: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_columns(self: RowVector6<T>, icols: Span<usize>) -> RowVector5<T> {
        if icols.len() != 5 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let c0 = RowVector6EditTrait::column_at(self, *icols[0]);
        let c1 = RowVector6EditTrait::column_at(self, *icols[1]);
        let c2 = RowVector6EditTrait::column_at(self, *icols[2]);
        let c3 = RowVector6EditTrait::column_at(self, *icols[3]);
        let c4 = RowVector6EditTrait::column_at(self, *icols[4]);
        RowVector5 { x: c0.x, y: c1.x, z: c2.x, w: c3.x, a: c4.x }
    }
}

/// The 6 consecutive columns of a `RowVector6` as a `RowVector6` (`columns` / `columns_range`:
/// default methods). Upstream: `fixed_columns::<6>`, `select_columns`.
pub impl RowVector6FixedColumnsRowVector6<
    T, +Copy<T>, +Drop<T>,
> of FixedColumns<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn fixed_columns(self: RowVector6<T>, i: usize) -> RowVector6<T> {
        match i {
            0 => RowVector6 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }

    #[inline(always)]
    fn select_columns(self: RowVector6<T>, icols: Span<usize>) -> RowVector6<T> {
        if icols.len() != 6 {
            core::panic_with_felt252(errors::DIMENSION_MISMATCH)
        }
        let c0 = RowVector6EditTrait::column_at(self, *icols[0]);
        let c1 = RowVector6EditTrait::column_at(self, *icols[1]);
        let c2 = RowVector6EditTrait::column_at(self, *icols[2]);
        let c3 = RowVector6EditTrait::column_at(self, *icols[3]);
        let c4 = RowVector6EditTrait::column_at(self, *icols[4]);
        let c5 = RowVector6EditTrait::column_at(self, *icols[5]);
        RowVector6 { x: c0.x, y: c1.x, z: c2.x, w: c3.x, a: c4.x, b: c5.x }
    }
}

/// The 1x1 blocks of a `RowVector6` as a `Matrix1` (`view`, `fixed_slice`, `slice`: default
/// methods). Upstream: `fixed_view::<1, 1>`.
pub impl RowVector6FixedViewMatrix1<T, +Copy<T>, +Drop<T>> of FixedView<RowVector6<T>, Matrix1<T>> {
    #[inline(always)]
    fn fixed_view(self: RowVector6<T>, irow: usize, icol: usize) -> Matrix1<T> {
        match icol {
            0 => match irow {
                0 => Matrix1 { x: self.x },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match irow {
                0 => Matrix1 { x: self.y },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match irow {
                0 => Matrix1 { x: self.z },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match irow {
                0 => Matrix1 { x: self.w },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            4 => match irow {
                0 => Matrix1 { x: self.a },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            5 => match irow {
                0 => Matrix1 { x: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The 1x2 blocks of a `RowVector6` as a `RowVector2` (`view`, `fixed_slice`, `slice`: default
/// methods). Upstream: `fixed_view::<1, 2>`.
pub impl RowVector6FixedViewRowVector2<
    T, +Copy<T>, +Drop<T>,
> of FixedView<RowVector6<T>, RowVector2<T>> {
    #[inline(always)]
    fn fixed_view(self: RowVector6<T>, irow: usize, icol: usize) -> RowVector2<T> {
        match icol {
            0 => match irow {
                0 => RowVector2 { x: self.x, y: self.y },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match irow {
                0 => RowVector2 { x: self.y, y: self.z },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match irow {
                0 => RowVector2 { x: self.z, y: self.w },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match irow {
                0 => RowVector2 { x: self.w, y: self.a },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            4 => match irow {
                0 => RowVector2 { x: self.a, y: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The 1x3 blocks of a `RowVector6` as a `RowVector3` (`view`, `fixed_slice`, `slice`: default
/// methods). Upstream: `fixed_view::<1, 3>`.
pub impl RowVector6FixedViewRowVector3<
    T, +Copy<T>, +Drop<T>,
> of FixedView<RowVector6<T>, RowVector3<T>> {
    #[inline(always)]
    fn fixed_view(self: RowVector6<T>, irow: usize, icol: usize) -> RowVector3<T> {
        match icol {
            0 => match irow {
                0 => RowVector3 { x: self.x, y: self.y, z: self.z },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match irow {
                0 => RowVector3 { x: self.y, y: self.z, z: self.w },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match irow {
                0 => RowVector3 { x: self.z, y: self.w, z: self.a },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            3 => match irow {
                0 => RowVector3 { x: self.w, y: self.a, z: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The 1x4 blocks of a `RowVector6` as a `RowVector4` (`view`, `fixed_slice`, `slice`: default
/// methods). Upstream: `fixed_view::<1, 4>`.
pub impl RowVector6FixedViewRowVector4<
    T, +Copy<T>, +Drop<T>,
> of FixedView<RowVector6<T>, RowVector4<T>> {
    #[inline(always)]
    fn fixed_view(self: RowVector6<T>, irow: usize, icol: usize) -> RowVector4<T> {
        match icol {
            0 => match irow {
                0 => RowVector4 { x: self.x, y: self.y, z: self.z, w: self.w },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match irow {
                0 => RowVector4 { x: self.y, y: self.z, z: self.w, w: self.a },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            2 => match irow {
                0 => RowVector4 { x: self.z, y: self.w, z: self.a, w: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The 1x5 blocks of a `RowVector6` as a `RowVector5` (`view`, `fixed_slice`, `slice`: default
/// methods). Upstream: `fixed_view::<1, 5>`.
pub impl RowVector6FixedViewRowVector5<
    T, +Copy<T>, +Drop<T>,
> of FixedView<RowVector6<T>, RowVector5<T>> {
    #[inline(always)]
    fn fixed_view(self: RowVector6<T>, irow: usize, icol: usize) -> RowVector5<T> {
        match icol {
            0 => match irow {
                0 => RowVector5 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match irow {
                0 => RowVector5 { x: self.y, y: self.z, z: self.w, w: self.a, a: self.b },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The 1x6 blocks of a `RowVector6` as a `RowVector6` (`view`, `fixed_slice`, `slice`: default
/// methods). Upstream: `fixed_view::<1, 6>`.
pub impl RowVector6FixedViewRowVector6<
    T, +Copy<T>, +Drop<T>,
> of FixedView<RowVector6<T>, RowVector6<T>> {
    #[inline(always)]
    fn fixed_view(self: RowVector6<T>, irow: usize, icol: usize) -> RowVector6<T> {
        match icol {
            0 => match irow {
                0 => RowVector6 {
                    x: self.x, y: self.y, z: self.z, w: self.w, a: self.a, b: self.b,
                },
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The Kronecker product of a `RowVector6` and a `Matrix1`, a `RowVector6`: one floored product per
/// component. Panics on overflow. Upstream: `kronecker`.
pub impl RowVector6KroneckerMatrix1<
    T, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixKronecker<RowVector6<T>, Matrix1<T>> {
    type Output = RowVector6<T>;
    #[inline(always)]
    fn kronecker(self: RowVector6<T>, rhs: Matrix1<T>) -> RowVector6<T> {
        RowVector6 {
            x: self.x * rhs.x,
            y: self.y * rhs.x,
            z: self.z * rhs.x,
            w: self.w * rhs.x,
            a: self.a * rhs.x,
            b: self.b * rhs.x,
        }
    }
}

/// The Kronecker product of a `RowVector6` and a `Vector2`, a `Matrix2x6`: one floored product per
/// component. Panics on overflow. Upstream: `kronecker`.
pub impl RowVector6KroneckerVector2<
    T, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixKronecker<RowVector6<T>, Vector2<T>> {
    type Output = Matrix2x6<T>;
    #[inline(always)]
    fn kronecker(self: RowVector6<T>, rhs: Vector2<T>) -> Matrix2x6<T> {
        Matrix2x6 {
            m11: self.x * rhs.x,
            m21: self.x * rhs.y,
            m12: self.y * rhs.x,
            m22: self.y * rhs.y,
            m13: self.z * rhs.x,
            m23: self.z * rhs.y,
            m14: self.w * rhs.x,
            m24: self.w * rhs.y,
            m15: self.a * rhs.x,
            m25: self.a * rhs.y,
            m16: self.b * rhs.x,
            m26: self.b * rhs.y,
        }
    }
}

/// The Kronecker product of a `RowVector6` and a `Vector3`, a `Matrix3x6`: one floored product per
/// component. Panics on overflow. Upstream: `kronecker`.
pub impl RowVector6KroneckerVector3<
    T, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixKronecker<RowVector6<T>, Vector3<T>> {
    type Output = Matrix3x6<T>;
    fn kronecker(self: RowVector6<T>, rhs: Vector3<T>) -> Matrix3x6<T> {
        Matrix3x6 {
            m11: self.x * rhs.x,
            m21: self.x * rhs.y,
            m31: self.x * rhs.z,
            m12: self.y * rhs.x,
            m22: self.y * rhs.y,
            m32: self.y * rhs.z,
            m13: self.z * rhs.x,
            m23: self.z * rhs.y,
            m33: self.z * rhs.z,
            m14: self.w * rhs.x,
            m24: self.w * rhs.y,
            m34: self.w * rhs.z,
            m15: self.a * rhs.x,
            m25: self.a * rhs.y,
            m35: self.a * rhs.z,
            m16: self.b * rhs.x,
            m26: self.b * rhs.y,
            m36: self.b * rhs.z,
        }
    }
}

/// The Kronecker product of a `RowVector6` and a `Vector4`, a `Matrix4x6`: one floored product per
/// component. Panics on overflow. Upstream: `kronecker`.
pub impl RowVector6KroneckerVector4<
    T, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixKronecker<RowVector6<T>, Vector4<T>> {
    type Output = Matrix4x6<T>;
    fn kronecker(self: RowVector6<T>, rhs: Vector4<T>) -> Matrix4x6<T> {
        Matrix4x6 {
            m11: self.x * rhs.x,
            m21: self.x * rhs.y,
            m31: self.x * rhs.z,
            m41: self.x * rhs.w,
            m12: self.y * rhs.x,
            m22: self.y * rhs.y,
            m32: self.y * rhs.z,
            m42: self.y * rhs.w,
            m13: self.z * rhs.x,
            m23: self.z * rhs.y,
            m33: self.z * rhs.z,
            m43: self.z * rhs.w,
            m14: self.w * rhs.x,
            m24: self.w * rhs.y,
            m34: self.w * rhs.z,
            m44: self.w * rhs.w,
            m15: self.a * rhs.x,
            m25: self.a * rhs.y,
            m35: self.a * rhs.z,
            m45: self.a * rhs.w,
            m16: self.b * rhs.x,
            m26: self.b * rhs.y,
            m36: self.b * rhs.z,
            m46: self.b * rhs.w,
        }
    }
}

/// The Kronecker product of a `RowVector6` and a `Vector5`, a `Matrix5x6`: one floored product per
/// component. Panics on overflow. Upstream: `kronecker`.
pub impl RowVector6KroneckerVector5<
    T, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixKronecker<RowVector6<T>, Vector5<T>> {
    type Output = Matrix5x6<T>;
    fn kronecker(self: RowVector6<T>, rhs: Vector5<T>) -> Matrix5x6<T> {
        Matrix5x6 {
            m11: self.x * rhs.x,
            m21: self.x * rhs.y,
            m31: self.x * rhs.z,
            m41: self.x * rhs.w,
            m51: self.x * rhs.a,
            m12: self.y * rhs.x,
            m22: self.y * rhs.y,
            m32: self.y * rhs.z,
            m42: self.y * rhs.w,
            m52: self.y * rhs.a,
            m13: self.z * rhs.x,
            m23: self.z * rhs.y,
            m33: self.z * rhs.z,
            m43: self.z * rhs.w,
            m53: self.z * rhs.a,
            m14: self.w * rhs.x,
            m24: self.w * rhs.y,
            m34: self.w * rhs.z,
            m44: self.w * rhs.w,
            m54: self.w * rhs.a,
            m15: self.a * rhs.x,
            m25: self.a * rhs.y,
            m35: self.a * rhs.z,
            m45: self.a * rhs.w,
            m55: self.a * rhs.a,
            m16: self.b * rhs.x,
            m26: self.b * rhs.y,
            m36: self.b * rhs.z,
            m46: self.b * rhs.w,
            m56: self.b * rhs.a,
        }
    }
}

/// The Kronecker product of a `RowVector6` and a `Vector6`, a `Matrix6`: one floored product per
/// component. Panics on overflow. Upstream: `kronecker`.
pub impl RowVector6KroneckerVector6<
    T, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixKronecker<RowVector6<T>, Vector6<T>> {
    type Output = Matrix6<T>;
    fn kronecker(self: RowVector6<T>, rhs: Vector6<T>) -> Matrix6<T> {
        Matrix6 {
            m11: self.x * rhs.x,
            m21: self.x * rhs.y,
            m31: self.x * rhs.z,
            m41: self.x * rhs.w,
            m51: self.x * rhs.a,
            m61: self.x * rhs.b,
            m12: self.y * rhs.x,
            m22: self.y * rhs.y,
            m32: self.y * rhs.z,
            m42: self.y * rhs.w,
            m52: self.y * rhs.a,
            m62: self.y * rhs.b,
            m13: self.z * rhs.x,
            m23: self.z * rhs.y,
            m33: self.z * rhs.z,
            m43: self.z * rhs.w,
            m53: self.z * rhs.a,
            m63: self.z * rhs.b,
            m14: self.w * rhs.x,
            m24: self.w * rhs.y,
            m34: self.w * rhs.z,
            m44: self.w * rhs.w,
            m54: self.w * rhs.a,
            m64: self.w * rhs.b,
            m15: self.a * rhs.x,
            m25: self.a * rhs.y,
            m35: self.a * rhs.z,
            m45: self.a * rhs.w,
            m55: self.a * rhs.a,
            m65: self.a * rhs.b,
            m16: self.b * rhs.x,
            m26: self.b * rhs.y,
            m36: self.b * rhs.z,
            m46: self.b * rhs.w,
            m56: self.b * rhs.a,
            m66: self.b * rhs.b,
        }
    }
}

/// `self` in the top-left corner of a `Matrix6` filled with `val` (`FixedResize`).
pub(crate) impl RowVector6PadTo6<T, +Copy<T>, +Drop<T>> of PadTo6<RowVector6<T>, T> {
    #[inline(always)]
    fn pad(self: RowVector6<T>, val: T) -> Matrix6<T> {
        Matrix6 {
            m11: self.x,
            m21: val,
            m31: val,
            m41: val,
            m51: val,
            m61: val,
            m12: self.y,
            m22: val,
            m32: val,
            m42: val,
            m52: val,
            m62: val,
            m13: self.z,
            m23: val,
            m33: val,
            m43: val,
            m53: val,
            m63: val,
            m14: self.w,
            m24: val,
            m34: val,
            m44: val,
            m54: val,
            m64: val,
            m15: self.a,
            m25: val,
            m35: val,
            m45: val,
            m55: val,
            m65: val,
            m16: self.b,
            m26: val,
            m36: val,
            m46: val,
            m56: val,
            m66: val,
        }
    }
}

/// The top-left `RowVector6` of a `Matrix6` (`FixedResize`).
pub(crate) impl RowVector6CropFrom6<T, +Copy<T>, +Drop<T>> of CropFrom6<RowVector6<T>, T> {
    #[inline(always)]
    fn crop(m: Matrix6<T>) -> RowVector6<T> {
        RowVector6 { x: m.m11, y: m.m12, z: m.m13, w: m.m14, a: m.m15, b: m.m16 }
    }
}

/// `(1, 6)`: the size checks of the runtime-sized views.
pub(crate) impl RowVector6ShapeDims<T> of ShapeDims<RowVector6<T>> {
    #[inline(always)]
    fn dims() -> (usize, usize) {
        (1, 6)
    }
}
