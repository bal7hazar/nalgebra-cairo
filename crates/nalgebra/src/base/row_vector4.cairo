// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/shapes.py.
//! `RowVector4`: a statically sized 4-dimensional row vector (upstream `nalgebra::RowVector4`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one overflow
//! check per output scalar. Operators (`+`, `-`, unary `-` and their assigning forms, conversions
//! from / to `[T; 4]`) are implemented in this module, so they need no import; the other operations
//! are methods of `RowVector4Trait`, the products with every conformable shape `MatrixMul::mul_mat`
//! (`self * rhs`) and `MatrixTrMul::tr_mul` (`selfᵀ * rhs`).

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::matrix1::Matrix1;
use super::matrix4::Matrix4;
use super::matrix4x2::Matrix4x2;
use super::matrix4x3::Matrix4x3;
use super::matrix4x5::Matrix4x5;
use super::matrix4x6::Matrix4x6;
use super::matrix_mul::MatrixMul;
use super::matrix_tr_mul::MatrixTrMul;
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;
use super::row_vector5::RowVector5;
use super::row_vector6::RowVector6;
use super::vector4::Vector4;

/// A 4-dimensional row vector. Components are named like upstream's `Deref` targets (`x, y, z, w,
/// a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct RowVector4<T> {
    pub x: T,
    pub y: T,
    pub z: T,
    pub w: T,
}

/// Upstream alias `Matrix1x4`: the same type as `RowVector4` (one Cairo struct per shape).
pub type Matrix1x4<T> = RowVector4<T>;

/// Methods of `RowVector4<T>` for any `Real` scalar.
#[generate_trait]
pub impl RowVector4Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector4Trait<T> {
    /// The 4-dimensional row vector with the given components. Upstream: `RowVector4::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T, w: T) -> RowVector4<T> {
        RowVector4 { x, y, z, w }
    }

    /// The zero 4-dimensional row vector. Upstream: `RowVector4::zeros`.
    #[inline(always)]
    fn zeros() -> RowVector4<T> {
        RowVector4 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero() }
    }

    /// The transpose, a `Vector4`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: RowVector4<T>) -> Vector4<T> {
        Vector4 { x: self.x, y: self.y, z: self.z, w: self.w }
    }

    /// Component-wise absolute value. Exact; panics on overflow (`|MIN|`). Upstream: `abs`.
    #[inline(always)]
    fn abs(self: RowVector4<T>) -> RowVector4<T> {
        RowVector4 { x: R::abs(self.x), y: R::abs(self.y), z: R::abs(self.z), w: R::abs(self.w) }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: RowVector4<T>, k: T) -> RowVector4<T> {
        RowVector4 { x: self.x * k, y: self.y * k, z: self.z * k, w: self.w * k }
    }

    /// Whether every component of `self` is within `ulps` smallest units (raw units for fixed
    /// point) of `other`'s; cannot overflow. Upstream: `approx::AbsDiffEq::abs_diff_eq`, the
    /// tolerance being counted in ulp instead of a float epsilon (DESIGN D3).
    #[inline(always)]
    fn abs_diff_eq(self: RowVector4<T>, other: RowVector4<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.x, other.x, ulps)
            && R::abs_diff_eq(self.y, other.y, ulps)
            && R::abs_diff_eq(self.z, other.z, ulps)
            && R::abs_diff_eq(self.w, other.w, ulps)
    }
}

// --- operators -----------------------------------------------------------------------------------

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl RowVector4Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<RowVector4<T>> {
    #[inline(always)]
    fn add(lhs: RowVector4<T>, rhs: RowVector4<T>) -> RowVector4<T> {
        RowVector4 { x: lhs.x + rhs.x, y: lhs.y + rhs.y, z: lhs.z + rhs.z, w: lhs.w + rhs.w }
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl RowVector4Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<RowVector4<T>> {
    #[inline(always)]
    fn sub(lhs: RowVector4<T>, rhs: RowVector4<T>) -> RowVector4<T> {
        RowVector4 { x: lhs.x - rhs.x, y: lhs.y - rhs.y, z: lhs.z - rhs.z, w: lhs.w - rhs.w }
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl RowVector4Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<RowVector4<T>> {
    #[inline(always)]
    fn neg(a: RowVector4<T>) -> RowVector4<T> {
        RowVector4 { x: -a.x, y: -a.y, z: -a.z, w: -a.w }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl RowVector4AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<RowVector4<T>, RowVector4<T>> {
    #[inline(always)]
    fn add_assign(ref self: RowVector4<T>, rhs: RowVector4<T>) {
        self = self + rhs;
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl RowVector4SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<RowVector4<T>, RowVector4<T>> {
    #[inline(always)]
    fn sub_assign(ref self: RowVector4<T>, rhs: RowVector4<T>) {
        self = self - rhs;
    }
}

/// `[x, y, z, w].into()`. Upstream: `From<[T; 4]>`.
pub impl RowVector4FromArray<T> of Into<[T; 4], RowVector4<T>> {
    #[inline(always)]
    fn into(self: [T; 4]) -> RowVector4<T> {
        let [x, y, z, w] = self;
        RowVector4 { x, y, z, w }
    }
}

/// The components as a fixed-size array `[x, y, z, w]`. Upstream: `Into<[T; 4]>`.
pub impl RowVector4IntoArray<T> of Into<RowVector4<T>, [T; 4]> {
    #[inline(always)]
    fn into(self: RowVector4<T>) -> [T; 4] {
        let RowVector4 { x, y, z, w } = self;
        [x, y, z, w]
    }
}

// --- products ------------------------------------------------------------------------------------

/// `self * rhs`, a `Matrix1`: one `sum_prod4` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector4 * Vector4` (`Mul`).
pub impl RowVector4MulVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector4<T>, Vector4<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector4<T>, rhs: Vector4<T>) -> Matrix1<T> {
        Matrix1 { x: R::sum_prod4(self.x, rhs.x, self.y, rhs.y, self.z, rhs.z, self.w, rhs.w) }
    }
}

/// `self * rhs`, a `RowVector2`: one `sum_prod4` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector4 * Matrix4x2` (`Mul`).
pub impl RowVector4MulMatrix4x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector4<T>, Matrix4x2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector4<T>, rhs: Matrix4x2<T>) -> RowVector2<T> {
        RowVector2 {
            x: R::sum_prod4(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41),
            y: R::sum_prod4(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42),
        }
    }
}

/// `self * rhs`, a `RowVector3`: one `sum_prod4` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector4 * Matrix4x3` (`Mul`).
pub impl RowVector4MulMatrix4x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector4<T>, Matrix4x3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector4<T>, rhs: Matrix4x3<T>) -> RowVector3<T> {
        RowVector3 {
            x: R::sum_prod4(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41),
            y: R::sum_prod4(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42),
            z: R::sum_prod4(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43),
        }
    }
}

/// `self * rhs`, a `RowVector4`: one `sum_prod4` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector4 * Matrix4` (`Mul`).
pub impl RowVector4MulMatrix4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector4<T>, Matrix4<T>> {
    type Output = RowVector4<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector4<T>, rhs: Matrix4<T>) -> RowVector4<T> {
        RowVector4 {
            x: R::sum_prod4(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41),
            y: R::sum_prod4(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42),
            z: R::sum_prod4(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43),
            w: R::sum_prod4(self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34, self.w, rhs.m44),
        }
    }
}

/// `self * rhs`, a `RowVector5`: one `sum_prod4` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector4 * Matrix4x5` (`Mul`).
pub impl RowVector4MulMatrix4x5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector4<T>, Matrix4x5<T>> {
    type Output = RowVector5<T>;
    fn mul_mat(self: RowVector4<T>, rhs: Matrix4x5<T>) -> RowVector5<T> {
        RowVector5 {
            x: R::sum_prod4(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41),
            y: R::sum_prod4(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42),
            z: R::sum_prod4(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43),
            w: R::sum_prod4(self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34, self.w, rhs.m44),
            a: R::sum_prod4(self.x, rhs.m15, self.y, rhs.m25, self.z, rhs.m35, self.w, rhs.m45),
        }
    }
}

/// `self * rhs`, a `RowVector6`: one `sum_prod4` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector4 * Matrix4x6` (`Mul`).
pub impl RowVector4MulMatrix4x6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector4<T>, Matrix4x6<T>> {
    type Output = RowVector6<T>;
    fn mul_mat(self: RowVector4<T>, rhs: Matrix4x6<T>) -> RowVector6<T> {
        RowVector6 {
            x: R::sum_prod4(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41),
            y: R::sum_prod4(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42),
            z: R::sum_prod4(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43),
            w: R::sum_prod4(self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34, self.w, rhs.m44),
            a: R::sum_prod4(self.x, rhs.m15, self.y, rhs.m25, self.z, rhs.m35, self.w, rhs.m45),
            b: R::sum_prod4(self.x, rhs.m16, self.y, rhs.m26, self.z, rhs.m36, self.w, rhs.m46),
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Vector4`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector4TrMulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector4<T>, Matrix1<T>> {
    type Output = Vector4<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector4<T>, rhs: Matrix1<T>) -> Vector4<T> {
        Vector4 { x: self.x * rhs.x, y: self.y * rhs.x, z: self.z * rhs.x, w: self.w * rhs.x }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix4x2`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector4TrMulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector4<T>, RowVector2<T>> {
    type Output = Matrix4x2<T>;
    fn tr_mul(self: RowVector4<T>, rhs: RowVector2<T>) -> Matrix4x2<T> {
        Matrix4x2 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix4x3`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector4TrMulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector4<T>, RowVector3<T>> {
    type Output = Matrix4x3<T>;
    fn tr_mul(self: RowVector4<T>, rhs: RowVector3<T>) -> Matrix4x3<T> {
        Matrix4x3 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix4`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector4TrMulRowVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector4<T>, RowVector4<T>> {
    type Output = Matrix4<T>;
    fn tr_mul(self: RowVector4<T>, rhs: RowVector4<T>) -> Matrix4<T> {
        Matrix4 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m14: self.x * rhs.w,
            m24: self.y * rhs.w,
            m34: self.z * rhs.w,
            m44: self.w * rhs.w,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix4x5`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector4TrMulRowVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector4<T>, RowVector5<T>> {
    type Output = Matrix4x5<T>;
    fn tr_mul(self: RowVector4<T>, rhs: RowVector5<T>) -> Matrix4x5<T> {
        Matrix4x5 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m14: self.x * rhs.w,
            m24: self.y * rhs.w,
            m34: self.z * rhs.w,
            m44: self.w * rhs.w,
            m15: self.x * rhs.a,
            m25: self.y * rhs.a,
            m35: self.z * rhs.a,
            m45: self.w * rhs.a,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix4x6`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector4TrMulRowVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector4<T>, RowVector6<T>> {
    type Output = Matrix4x6<T>;
    fn tr_mul(self: RowVector4<T>, rhs: RowVector6<T>) -> Matrix4x6<T> {
        Matrix4x6 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m14: self.x * rhs.w,
            m24: self.y * rhs.w,
            m34: self.z * rhs.w,
            m44: self.w * rhs.w,
            m15: self.x * rhs.a,
            m25: self.y * rhs.a,
            m35: self.z * rhs.a,
            m45: self.w * rhs.a,
            m16: self.x * rhs.b,
            m26: self.y * rhs.b,
            m36: self.z * rhs.b,
            m46: self.w * rhs.b,
        }
    }
}
