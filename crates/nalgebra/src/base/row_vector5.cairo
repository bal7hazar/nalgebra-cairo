// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/shapes.py.
//! `RowVector5`: a statically sized 5-dimensional row vector (upstream `nalgebra::RowVector5`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one overflow
//! check per output scalar. Operators (`+`, `-`, unary `-` and their assigning forms, conversions
//! from / to `[T; 5]`) are implemented in this module, so they need no import; the other operations
//! are methods of `RowVector5Trait`, the products with every conformable shape `MatrixMul::mul_mat`
//! (`self * rhs`) and `MatrixTrMul::tr_mul` (`selfᵀ * rhs`).

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::kernels::Fused;
use super::matrix1::Matrix1;
use super::matrix5::Matrix5;
use super::matrix5x2::Matrix5x2;
use super::matrix5x3::Matrix5x3;
use super::matrix5x4::Matrix5x4;
use super::matrix5x6::Matrix5x6;
use super::matrix_mul::MatrixMul;
use super::matrix_tr_mul::MatrixTrMul;
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;
use super::row_vector4::RowVector4;
use super::row_vector6::RowVector6;
use super::vector5::Vector5;

/// A 5-dimensional row vector. Components are named like upstream's `Deref` targets (`x, y, z, w,
/// a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct RowVector5<T> {
    pub x: T,
    pub y: T,
    pub z: T,
    pub w: T,
    pub a: T,
}

/// Upstream alias `Matrix1x5`: the same type as `RowVector5` (one Cairo struct per shape).
pub type Matrix1x5<T> = RowVector5<T>;

/// Methods of `RowVector5<T>` for any `Real` scalar.
#[generate_trait]
pub impl RowVector5Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector5Trait<T> {
    /// The 5-dimensional row vector with the given components. Upstream: `RowVector5::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T, w: T, a: T) -> RowVector5<T> {
        RowVector5 { x, y, z, w, a }
    }

    /// The zero 5-dimensional row vector. Upstream: `RowVector5::zeros`.
    #[inline(always)]
    fn zeros() -> RowVector5<T> {
        RowVector5 { x: R::zero(), y: R::zero(), z: R::zero(), w: R::zero(), a: R::zero() }
    }

    /// The transpose, a `Vector5`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: RowVector5<T>) -> Vector5<T> {
        Vector5 { x: self.x, y: self.y, z: self.z, w: self.w, a: self.a }
    }

    /// Component-wise absolute value. Exact; panics on overflow (`|MIN|`). Upstream: `abs`.
    #[inline(always)]
    fn abs(self: RowVector5<T>) -> RowVector5<T> {
        RowVector5 {
            x: R::abs(self.x),
            y: R::abs(self.y),
            z: R::abs(self.z),
            w: R::abs(self.w),
            a: R::abs(self.a),
        }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: RowVector5<T>, k: T) -> RowVector5<T> {
        RowVector5 { x: self.x * k, y: self.y * k, z: self.z * k, w: self.w * k, a: self.a * k }
    }

    /// Whether every component of `self` is within `ulps` smallest units (raw units for fixed
    /// point) of `other`'s; cannot overflow. Upstream: `approx::AbsDiffEq::abs_diff_eq`, the
    /// tolerance being counted in ulp instead of a float epsilon (DESIGN D3).
    #[inline(always)]
    fn abs_diff_eq(self: RowVector5<T>, other: RowVector5<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.x, other.x, ulps)
            && R::abs_diff_eq(self.y, other.y, ulps)
            && R::abs_diff_eq(self.z, other.z, ulps)
            && R::abs_diff_eq(self.w, other.w, ulps)
            && R::abs_diff_eq(self.a, other.a, ulps)
    }
}

// --- operators -----------------------------------------------------------------------------------

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl RowVector5Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<RowVector5<T>> {
    #[inline(always)]
    fn add(lhs: RowVector5<T>, rhs: RowVector5<T>) -> RowVector5<T> {
        RowVector5 {
            x: lhs.x + rhs.x,
            y: lhs.y + rhs.y,
            z: lhs.z + rhs.z,
            w: lhs.w + rhs.w,
            a: lhs.a + rhs.a,
        }
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl RowVector5Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<RowVector5<T>> {
    #[inline(always)]
    fn sub(lhs: RowVector5<T>, rhs: RowVector5<T>) -> RowVector5<T> {
        RowVector5 {
            x: lhs.x - rhs.x,
            y: lhs.y - rhs.y,
            z: lhs.z - rhs.z,
            w: lhs.w - rhs.w,
            a: lhs.a - rhs.a,
        }
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl RowVector5Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<RowVector5<T>> {
    #[inline(always)]
    fn neg(a: RowVector5<T>) -> RowVector5<T> {
        RowVector5 { x: -a.x, y: -a.y, z: -a.z, w: -a.w, a: -a.a }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl RowVector5AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<RowVector5<T>, RowVector5<T>> {
    #[inline(always)]
    fn add_assign(ref self: RowVector5<T>, rhs: RowVector5<T>) {
        self = self + rhs;
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl RowVector5SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<RowVector5<T>, RowVector5<T>> {
    #[inline(always)]
    fn sub_assign(ref self: RowVector5<T>, rhs: RowVector5<T>) {
        self = self - rhs;
    }
}

/// `[x, y, z, w, a].into()`. Upstream: `From<[T; 5]>`.
pub impl RowVector5FromArray<T> of Into<[T; 5], RowVector5<T>> {
    #[inline(always)]
    fn into(self: [T; 5]) -> RowVector5<T> {
        let [x, y, z, w, a] = self;
        RowVector5 { x, y, z, w, a }
    }
}

/// The components as a fixed-size array `[x, y, z, w, a]`. Upstream: `Into<[T; 5]>`.
pub impl RowVector5IntoArray<T> of Into<RowVector5<T>, [T; 5]> {
    #[inline(always)]
    fn into(self: RowVector5<T>) -> [T; 5] {
        let RowVector5 { x, y, z, w, a } = self;
        [x, y, z, w, a]
    }
}

// --- products ------------------------------------------------------------------------------------

/// `self * rhs`, a `Matrix1`: the 5 products of each component accumulated exactly in `Real::Wide`
/// and rescaled once. Panics on overflow. Upstream: `RowVector5 * Vector5` (`Mul`).
pub impl RowVector5MulVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector5<T>, Vector5<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector5<T>, rhs: Vector5<T>) -> Matrix1<T> {
        Matrix1 {
            x: Fused::sum_prod5(
                self.x, rhs.x, self.y, rhs.y, self.z, rhs.z, self.w, rhs.w, self.a, rhs.a,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector2`: the 5 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector5 * Matrix5x2` (`Mul`).
pub impl RowVector5MulMatrix5x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector5<T>, Matrix5x2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector5<T>, rhs: Matrix5x2<T>) -> RowVector2<T> {
        RowVector2 {
            x: Fused::sum_prod5(
                self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41, self.a, rhs.m51,
            ),
            y: Fused::sum_prod5(
                self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42, self.a, rhs.m52,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector3`: the 5 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector5 * Matrix5x3` (`Mul`).
pub impl RowVector5MulMatrix5x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector5<T>, Matrix5x3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector5<T>, rhs: Matrix5x3<T>) -> RowVector3<T> {
        RowVector3 {
            x: Fused::sum_prod5(
                self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41, self.a, rhs.m51,
            ),
            y: Fused::sum_prod5(
                self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42, self.a, rhs.m52,
            ),
            z: Fused::sum_prod5(
                self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43, self.a, rhs.m53,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector4`: the 5 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector5 * Matrix5x4` (`Mul`).
pub impl RowVector5MulMatrix5x4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector5<T>, Matrix5x4<T>> {
    type Output = RowVector4<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector5<T>, rhs: Matrix5x4<T>) -> RowVector4<T> {
        RowVector4 {
            x: Fused::sum_prod5(
                self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41, self.a, rhs.m51,
            ),
            y: Fused::sum_prod5(
                self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42, self.a, rhs.m52,
            ),
            z: Fused::sum_prod5(
                self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43, self.a, rhs.m53,
            ),
            w: Fused::sum_prod5(
                self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34, self.w, rhs.m44, self.a, rhs.m54,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector5`: the 5 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector5 * Matrix5` (`Mul`).
pub impl RowVector5MulMatrix5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector5<T>, Matrix5<T>> {
    type Output = RowVector5<T>;
    fn mul_mat(self: RowVector5<T>, rhs: Matrix5<T>) -> RowVector5<T> {
        RowVector5 {
            x: Fused::sum_prod5(
                self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41, self.a, rhs.m51,
            ),
            y: Fused::sum_prod5(
                self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42, self.a, rhs.m52,
            ),
            z: Fused::sum_prod5(
                self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43, self.a, rhs.m53,
            ),
            w: Fused::sum_prod5(
                self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34, self.w, rhs.m44, self.a, rhs.m54,
            ),
            a: Fused::sum_prod5(
                self.x, rhs.m15, self.y, rhs.m25, self.z, rhs.m35, self.w, rhs.m45, self.a, rhs.m55,
            ),
        }
    }
}

/// `self * rhs`, a `RowVector6`: the 5 products of each component accumulated exactly in
/// `Real::Wide` and rescaled once. Panics on overflow. Upstream: `RowVector5 * Matrix5x6` (`Mul`).
pub impl RowVector5MulMatrix5x6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector5<T>, Matrix5x6<T>> {
    type Output = RowVector6<T>;
    fn mul_mat(self: RowVector5<T>, rhs: Matrix5x6<T>) -> RowVector6<T> {
        RowVector6 {
            x: Fused::sum_prod5(
                self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31, self.w, rhs.m41, self.a, rhs.m51,
            ),
            y: Fused::sum_prod5(
                self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32, self.w, rhs.m42, self.a, rhs.m52,
            ),
            z: Fused::sum_prod5(
                self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33, self.w, rhs.m43, self.a, rhs.m53,
            ),
            w: Fused::sum_prod5(
                self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34, self.w, rhs.m44, self.a, rhs.m54,
            ),
            a: Fused::sum_prod5(
                self.x, rhs.m15, self.y, rhs.m25, self.z, rhs.m35, self.w, rhs.m45, self.a, rhs.m55,
            ),
            b: Fused::sum_prod5(
                self.x, rhs.m16, self.y, rhs.m26, self.z, rhs.m36, self.w, rhs.m46, self.a, rhs.m56,
            ),
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Vector5`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector5TrMulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector5<T>, Matrix1<T>> {
    type Output = Vector5<T>;
    fn tr_mul(self: RowVector5<T>, rhs: Matrix1<T>) -> Vector5<T> {
        Vector5 {
            x: self.x * rhs.x,
            y: self.y * rhs.x,
            z: self.z * rhs.x,
            w: self.w * rhs.x,
            a: self.a * rhs.x,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix5x2`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector5TrMulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector5<T>, RowVector2<T>> {
    type Output = Matrix5x2<T>;
    fn tr_mul(self: RowVector5<T>, rhs: RowVector2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m51: self.a * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m52: self.a * rhs.y,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix5x3`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector5TrMulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector5<T>, RowVector3<T>> {
    type Output = Matrix5x3<T>;
    fn tr_mul(self: RowVector5<T>, rhs: RowVector3<T>) -> Matrix5x3<T> {
        Matrix5x3 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m51: self.a * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m52: self.a * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m53: self.a * rhs.z,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix5x4`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector5TrMulRowVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector5<T>, RowVector4<T>> {
    type Output = Matrix5x4<T>;
    fn tr_mul(self: RowVector5<T>, rhs: RowVector4<T>) -> Matrix5x4<T> {
        Matrix5x4 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m51: self.a * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m52: self.a * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m53: self.a * rhs.z,
            m14: self.x * rhs.w,
            m24: self.y * rhs.w,
            m34: self.z * rhs.w,
            m44: self.w * rhs.w,
            m54: self.a * rhs.w,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix5`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector5TrMulRowVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector5<T>, RowVector5<T>> {
    type Output = Matrix5<T>;
    fn tr_mul(self: RowVector5<T>, rhs: RowVector5<T>) -> Matrix5<T> {
        Matrix5 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m51: self.a * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m52: self.a * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m53: self.a * rhs.z,
            m14: self.x * rhs.w,
            m24: self.y * rhs.w,
            m34: self.z * rhs.w,
            m44: self.w * rhs.w,
            m54: self.a * rhs.w,
            m15: self.x * rhs.a,
            m25: self.y * rhs.a,
            m35: self.z * rhs.a,
            m45: self.w * rhs.a,
            m55: self.a * rhs.a,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix5x6`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector5TrMulRowVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector5<T>, RowVector6<T>> {
    type Output = Matrix5x6<T>;
    fn tr_mul(self: RowVector5<T>, rhs: RowVector6<T>) -> Matrix5x6<T> {
        Matrix5x6 {
            m11: self.x * rhs.x,
            m21: self.y * rhs.x,
            m31: self.z * rhs.x,
            m41: self.w * rhs.x,
            m51: self.a * rhs.x,
            m12: self.x * rhs.y,
            m22: self.y * rhs.y,
            m32: self.z * rhs.y,
            m42: self.w * rhs.y,
            m52: self.a * rhs.y,
            m13: self.x * rhs.z,
            m23: self.y * rhs.z,
            m33: self.z * rhs.z,
            m43: self.w * rhs.z,
            m53: self.a * rhs.z,
            m14: self.x * rhs.w,
            m24: self.y * rhs.w,
            m34: self.z * rhs.w,
            m44: self.w * rhs.w,
            m54: self.a * rhs.w,
            m15: self.x * rhs.a,
            m25: self.y * rhs.a,
            m35: self.z * rhs.a,
            m45: self.w * rhs.a,
            m55: self.a * rhs.a,
            m16: self.x * rhs.b,
            m26: self.y * rhs.b,
            m36: self.z * rhs.b,
            m46: self.w * rhs.b,
            m56: self.a * rhs.b,
        }
    }
}
