// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/shapes.py.
//! `RowVector3`: a statically sized 3-dimensional row vector (upstream `nalgebra::RowVector3`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one overflow
//! check per output scalar. Operators (`+`, `-`, unary `-` and their assigning forms, conversions
//! from / to `[T; 3]`) are implemented in this module, so they need no import; the other operations
//! are methods of `RowVector3Trait`, the products with every conformable shape `MatrixMul::mul_mat`
//! (`self * rhs`) and `MatrixTrMul::tr_mul` (`selfᵀ * rhs`).

use core::ops::{AddAssign, SubAssign};
use simba::scalar::Real;
use super::matrix1::Matrix1;
use super::matrix3::Matrix3;
use super::matrix3x2::Matrix3x2;
use super::matrix3x4::Matrix3x4;
use super::matrix3x5::Matrix3x5;
use super::matrix3x6::Matrix3x6;
use super::matrix_mul::MatrixMul;
use super::matrix_tr_mul::MatrixTrMul;
use super::row_vector2::RowVector2;
use super::row_vector4::RowVector4;
use super::row_vector5::RowVector5;
use super::row_vector6::RowVector6;
use super::vector3::Vector3;

/// A 3-dimensional row vector. Components are named like upstream's `Deref` targets (`x, y, z, w,
/// a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct RowVector3<T> {
    pub x: T,
    pub y: T,
    pub z: T,
}

/// Upstream alias `Matrix1x3`: the same type as `RowVector3` (one Cairo struct per shape).
pub type Matrix1x3<T> = RowVector3<T>;

/// Methods of `RowVector3<T>` for any `Real` scalar.
#[generate_trait]
pub impl RowVector3Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of RowVector3Trait<T> {
    /// The 3-dimensional row vector with the given components. Upstream: `RowVector3::new`.
    #[inline(always)]
    fn new(x: T, y: T, z: T) -> RowVector3<T> {
        RowVector3 { x, y, z }
    }

    /// The zero 3-dimensional row vector. Upstream: `RowVector3::zeros`.
    #[inline(always)]
    fn zeros() -> RowVector3<T> {
        RowVector3 { x: R::zero(), y: R::zero(), z: R::zero() }
    }

    /// The transpose, a `Vector3`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: RowVector3<T>) -> Vector3<T> {
        Vector3 { x: self.x, y: self.y, z: self.z }
    }

    /// Component-wise absolute value. Exact; panics on overflow (`|MIN|`). Upstream: `abs`.
    #[inline(always)]
    fn abs(self: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: R::abs(self.x), y: R::abs(self.y), z: R::abs(self.z) }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: RowVector3<T>, k: T) -> RowVector3<T> {
        RowVector3 { x: self.x * k, y: self.y * k, z: self.z * k }
    }

    /// Whether every component of `self` is within `ulps` smallest units (raw units for fixed
    /// point) of `other`'s; cannot overflow. Upstream: `approx::AbsDiffEq::abs_diff_eq`, the
    /// tolerance being counted in ulp instead of a float epsilon (DESIGN D3).
    #[inline(always)]
    fn abs_diff_eq(self: RowVector3<T>, other: RowVector3<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.x, other.x, ulps)
            && R::abs_diff_eq(self.y, other.y, ulps)
            && R::abs_diff_eq(self.z, other.z, ulps)
    }
}

// --- operators -----------------------------------------------------------------------------------

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl RowVector3Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<RowVector3<T>> {
    #[inline(always)]
    fn add(lhs: RowVector3<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: lhs.x + rhs.x, y: lhs.y + rhs.y, z: lhs.z + rhs.z }
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl RowVector3Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<RowVector3<T>> {
    #[inline(always)]
    fn sub(lhs: RowVector3<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: lhs.x - rhs.x, y: lhs.y - rhs.y, z: lhs.z - rhs.z }
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl RowVector3Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<RowVector3<T>> {
    #[inline(always)]
    fn neg(a: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: -a.x, y: -a.y, z: -a.z }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl RowVector3AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<RowVector3<T>, RowVector3<T>> {
    #[inline(always)]
    fn add_assign(ref self: RowVector3<T>, rhs: RowVector3<T>) {
        self = self + rhs;
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl RowVector3SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<RowVector3<T>, RowVector3<T>> {
    #[inline(always)]
    fn sub_assign(ref self: RowVector3<T>, rhs: RowVector3<T>) {
        self = self - rhs;
    }
}

/// `[x, y, z].into()`. Upstream: `From<[T; 3]>`.
pub impl RowVector3FromArray<T> of Into<[T; 3], RowVector3<T>> {
    #[inline(always)]
    fn into(self: [T; 3]) -> RowVector3<T> {
        let [x, y, z] = self;
        RowVector3 { x, y, z }
    }
}

/// The components as a fixed-size array `[x, y, z]`. Upstream: `Into<[T; 3]>`.
pub impl RowVector3IntoArray<T> of Into<RowVector3<T>, [T; 3]> {
    #[inline(always)]
    fn into(self: RowVector3<T>) -> [T; 3] {
        let RowVector3 { x, y, z } = self;
        [x, y, z]
    }
}

// --- products ------------------------------------------------------------------------------------

/// `self * rhs`, a `Matrix1`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector3 * Vector3` (`Mul`).
pub impl RowVector3MulVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Vector3<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Vector3<T>) -> Matrix1<T> {
        Matrix1 { x: R::sum_prod3(self.x, rhs.x, self.y, rhs.y, self.z, rhs.z) }
    }
}

/// `self * rhs`, a `RowVector2`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector3 * Matrix3x2` (`Mul`).
pub impl RowVector3MulMatrix3x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3x2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3x2<T>) -> RowVector2<T> {
        RowVector2 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
        }
    }
}

/// `self * rhs`, a `RowVector3`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector3 * Matrix3` (`Mul`).
pub impl RowVector3MulMatrix3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3<T>) -> RowVector3<T> {
        RowVector3 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
            z: R::sum_prod3(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33),
        }
    }
}

/// `self * rhs`, a `RowVector4`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector3 * Matrix3x4` (`Mul`).
pub impl RowVector3MulMatrix3x4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3x4<T>> {
    type Output = RowVector4<T>;
    #[inline(always)]
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3x4<T>) -> RowVector4<T> {
        RowVector4 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
            z: R::sum_prod3(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33),
            w: R::sum_prod3(self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34),
        }
    }
}

/// `self * rhs`, a `RowVector5`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector3 * Matrix3x5` (`Mul`).
pub impl RowVector3MulMatrix3x5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3x5<T>> {
    type Output = RowVector5<T>;
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3x5<T>) -> RowVector5<T> {
        RowVector5 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
            z: R::sum_prod3(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33),
            w: R::sum_prod3(self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34),
            a: R::sum_prod3(self.x, rhs.m15, self.y, rhs.m25, self.z, rhs.m35),
        }
    }
}

/// `self * rhs`, a `RowVector6`: one `sum_prod3` per component (one rounding each). Panics on
/// overflow. Upstream: `RowVector3 * Matrix3x6` (`Mul`).
pub impl RowVector3MulMatrix3x6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<RowVector3<T>, Matrix3x6<T>> {
    type Output = RowVector6<T>;
    fn mul_mat(self: RowVector3<T>, rhs: Matrix3x6<T>) -> RowVector6<T> {
        RowVector6 {
            x: R::sum_prod3(self.x, rhs.m11, self.y, rhs.m21, self.z, rhs.m31),
            y: R::sum_prod3(self.x, rhs.m12, self.y, rhs.m22, self.z, rhs.m32),
            z: R::sum_prod3(self.x, rhs.m13, self.y, rhs.m23, self.z, rhs.m33),
            w: R::sum_prod3(self.x, rhs.m14, self.y, rhs.m24, self.z, rhs.m34),
            a: R::sum_prod3(self.x, rhs.m15, self.y, rhs.m25, self.z, rhs.m35),
            b: R::sum_prod3(self.x, rhs.m16, self.y, rhs.m26, self.z, rhs.m36),
        }
    }
}

/// `selfᵀ * rhs`, a `Vector3`: `mul_mat` of the transposed components, so one floored product per
/// component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only
/// relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector3TrMulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector3<T>, Matrix1<T>> {
    type Output = Vector3<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector3<T>, rhs: Matrix1<T>) -> Vector3<T> {
        MatrixMul::mul_mat(Vector3 { x: self.x, y: self.y, z: self.z }, rhs)
    }
}

/// `selfᵀ * rhs`, a `Matrix3x2`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector3TrMulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector3<T>, RowVector2<T>> {
    type Output = Matrix3x2<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector3<T>, rhs: RowVector2<T>) -> Matrix3x2<T> {
        MatrixMul::mul_mat(Vector3 { x: self.x, y: self.y, z: self.z }, rhs)
    }
}

/// `selfᵀ * rhs`, a `Matrix3`: `mul_mat` of the transposed components, so one floored product per
/// component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only
/// relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector3TrMulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector3<T>, RowVector3<T>> {
    type Output = Matrix3<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector3<T>, rhs: RowVector3<T>) -> Matrix3<T> {
        MatrixMul::mul_mat(Vector3 { x: self.x, y: self.y, z: self.z }, rhs)
    }
}

/// `selfᵀ * rhs`, a `Matrix3x4`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector3TrMulRowVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector3<T>, RowVector4<T>> {
    type Output = Matrix3x4<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector3<T>, rhs: RowVector4<T>) -> Matrix3x4<T> {
        MatrixMul::mul_mat(Vector3 { x: self.x, y: self.y, z: self.z }, rhs)
    }
}

/// `selfᵀ * rhs`, a `Matrix3x5`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector3TrMulRowVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector3<T>, RowVector5<T>> {
    type Output = Matrix3x5<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector3<T>, rhs: RowVector5<T>) -> Matrix3x5<T> {
        MatrixMul::mul_mat(Vector3 { x: self.x, y: self.y, z: self.z }, rhs)
    }
}

/// `selfᵀ * rhs`, a `Matrix3x6`: `mul_mat` of the transposed components, so one floored product
/// per component; bit-identical to `self.transpose().mul_mat(rhs)`, at the same gas (the transpose
/// only relabels values). Panics on overflow. Upstream: `tr_mul`.
pub impl RowVector3TrMulRowVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<RowVector3<T>, RowVector6<T>> {
    type Output = Matrix3x6<T>;
    #[inline(always)]
    fn tr_mul(self: RowVector3<T>, rhs: RowVector6<T>) -> Matrix3x6<T> {
        MatrixMul::mul_mat(Vector3 { x: self.x, y: self.y, z: self.z }, rhs)
    }
}
