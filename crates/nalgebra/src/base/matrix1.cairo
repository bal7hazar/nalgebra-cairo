// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/shapes.py.
//! `Matrix1`: a statically sized 1x1 matrix (upstream `nalgebra::Matrix1`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one overflow
//! check per output scalar. Operators (`+`, `-`, unary `-`, `*` between matrices and their
//! assigning forms, conversions from / to `[T; 1]`) are implemented in this module, so they need no
//! import; the other operations are methods of `Matrix1Trait`, the products with every conformable
//! shape `MatrixMul::mul_mat` (`self * rhs`) and `MatrixTrMul::tr_mul` (`selfᵀ * rhs`).

use core::ops::{AddAssign, MulAssign, SubAssign};
use simba::scalar::Real;
use super::matrix_mul::MatrixMul;
use super::matrix_tr_mul::MatrixTrMul;
use super::row_vector2::RowVector2;
use super::row_vector3::RowVector3;
use super::row_vector4::RowVector4;
use super::row_vector5::RowVector5;
use super::row_vector6::RowVector6;
use super::unit::Unit;

/// A 1x1 matrix. Components are named like upstream's `Deref` targets (`x, y, z, w, a, b`).
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Matrix1<T> {
    pub x: T,
}

/// Upstream alias `Vector1`: the same type as `Matrix1` (one Cairo struct per shape).
pub type Vector1<T> = Matrix1<T>;

/// Upstream alias `RowVector1`: the same type as `Matrix1` (one Cairo struct per shape).
pub type RowVector1<T> = Matrix1<T>;

/// Upstream alias `UnitVector1`: a `Matrix1` of unit norm (`Unit<Matrix1<T>>`).
pub type UnitVector1<T> = Unit<Matrix1<T>>;

/// Methods of `Matrix1<T>` for any `Real` scalar.
#[generate_trait]
pub impl Matrix1Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix1Trait<T> {
    /// The 1x1 matrix with the given components. Upstream: `Matrix1::new`.
    #[inline(always)]
    fn new(x: T) -> Matrix1<T> {
        Matrix1 { x }
    }

    /// The zero 1x1 matrix. Upstream: `Matrix1::zeros`.
    #[inline(always)]
    fn zeros() -> Matrix1<T> {
        Matrix1 { x: R::zero() }
    }

    /// The identity matrix. Upstream: `Matrix1::identity`.
    #[inline(always)]
    fn identity() -> Matrix1<T> {
        Matrix1 { x: R::one() }
    }

    /// The diagonal matrix `diag(d)`. Upstream: `Matrix1::from_diagonal`.
    #[inline(always)]
    fn from_diagonal(d: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: d.x }
    }

    /// The matrix `e * I`. Upstream: `Matrix1::from_diagonal_element`.
    #[inline(always)]
    fn from_diagonal_element(e: T) -> Matrix1<T> {
        Matrix1 { x: e }
    }

    /// The diagonal, a `Matrix1`. Upstream: `diagonal`.
    #[inline(always)]
    fn diagonal(self: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: self.x }
    }

    /// The transpose, a `Matrix1`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: self.x }
    }

    /// Sum of the diagonal, left to right. Exact; panics on overflow. Upstream: `trace`.
    #[inline(always)]
    fn trace(self: Matrix1<T>) -> T {
        self.x
    }

    /// Component-wise absolute value. Exact; panics on overflow (`|MIN|`). Upstream: `abs`.
    #[inline(always)]
    fn abs(self: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: R::abs(self.x) }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Matrix1<T>, k: T) -> Matrix1<T> {
        Matrix1 { x: self.x * k }
    }

    /// `self + (rhs - self) * t` per component (`Real::lerp`: exact difference and product, one
    /// floor rounding). `t` is not clamped; `t = 0` gives `self` and `t = 1` gives `rhs` exactly.
    /// Panics on overflow of the result. Upstream: `lerp` (`self * (1 - t) + rhs * t`).
    #[inline(always)]
    fn lerp(self: Matrix1<T>, rhs: Matrix1<T>, t: T) -> Matrix1<T> {
        Matrix1 { x: R::lerp(self.x, rhs.x, t) }
    }

    /// The single component `x`. Upstream: `into_scalar`.
    #[inline(always)]
    fn into_scalar(self: Matrix1<T>) -> T {
        self.x
    }

    /// The single component `x`. Upstream: `to_scalar`.
    #[inline(always)]
    fn to_scalar(self: Matrix1<T>) -> T {
        self.x
    }

    /// The single component `x`. Upstream: `as_scalar`, which returns `&T`: Cairo values are `Copy`
    /// and passed by value.
    #[inline(always)]
    fn as_scalar(self: Matrix1<T>) -> T {
        self.x
    }

    /// Whether every component is within `ulps` smallest units of the identity's. Upstream:
    /// `is_identity(eps)`, with the tolerance in raw units instead of a float epsilon.
    #[inline(always)]
    fn is_identity(self: Matrix1<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.x, R::one(), ulps)
    }

    /// Whether every component of `self` is within `ulps` smallest units (raw units for fixed
    /// point) of `other`'s; cannot overflow. Upstream: `approx::AbsDiffEq::abs_diff_eq`, the
    /// tolerance being counted in ulp instead of a float epsilon (DESIGN D3).
    #[inline(always)]
    fn abs_diff_eq(self: Matrix1<T>, other: Matrix1<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.x, other.x, ulps)
    }
}

// --- operators -----------------------------------------------------------------------------------

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Matrix1Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Matrix1<T>> {
    #[inline(always)]
    fn add(lhs: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: lhs.x + rhs.x }
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Matrix1Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Matrix1<T>> {
    #[inline(always)]
    fn sub(lhs: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: lhs.x - rhs.x }
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Matrix1Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Matrix1<T>> {
    #[inline(always)]
    fn neg(a: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: -a.x }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Matrix1AddAssign<T, +Add<T>, +Copy<T>, +Drop<T>> of AddAssign<Matrix1<T>, Matrix1<T>> {
    #[inline(always)]
    fn add_assign(ref self: Matrix1<T>, rhs: Matrix1<T>) {
        self = self + rhs;
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Matrix1SubAssign<T, +Sub<T>, +Copy<T>, +Drop<T>> of SubAssign<Matrix1<T>, Matrix1<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Matrix1<T>, rhs: Matrix1<T>) {
        self = self - rhs;
    }
}

/// `a * b` (matrix product): one floored product per component. Panics on overflow. Upstream:
/// `Mul`.
pub impl Matrix1Mul<T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>> of Mul<Matrix1<T>> {
    #[inline(always)]
    fn mul(lhs: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: lhs.x * rhs.x }
    }
}

/// `a *= b` (matrix product, `a = a * b`). Upstream: `MulAssign`.
pub impl Matrix1MulAssign<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>,
> of MulAssign<Matrix1<T>, Matrix1<T>> {
    #[inline(always)]
    fn mul_assign(ref self: Matrix1<T>, rhs: Matrix1<T>) {
        self = self * rhs;
    }
}

/// `[x].into()`. Upstream: `From<[T; 1]>`.
pub impl Matrix1FromArray<T> of Into<[T; 1], Matrix1<T>> {
    #[inline(always)]
    fn into(self: [T; 1]) -> Matrix1<T> {
        let [x] = self;
        Matrix1 { x }
    }
}

/// The components as a fixed-size array `[x]`. Upstream: `Into<[T; 1]>`.
pub impl Matrix1IntoArray<T> of Into<Matrix1<T>, [T; 1]> {
    #[inline(always)]
    fn into(self: Matrix1<T>) -> [T; 1] {
        let Matrix1 { x } = self;
        [x]
    }
}

// --- products ------------------------------------------------------------------------------------

/// `self * rhs` through `MatrixMul`: the `*` operator (same kernel, same gas), for code written
/// over every conformable shape. Upstream: `Mul`.
pub impl Matrix1MulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, Matrix1<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        self * rhs
    }
}

/// `self * rhs`, a `RowVector2`: one floored product per component. Panics on overflow. Upstream:
/// `Matrix1 * RowVector2` (`Mul`).
pub impl Matrix1MulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: RowVector2<T>) -> RowVector2<T> {
        RowVector2 { x: self.x * rhs.x, y: self.x * rhs.y }
    }
}

/// `self * rhs`, a `RowVector3`: one floored product per component. Panics on overflow. Upstream:
/// `Matrix1 * RowVector3` (`Mul`).
pub impl Matrix1MulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: self.x * rhs.x, y: self.x * rhs.y, z: self.x * rhs.z }
    }
}

/// `self * rhs`, a `RowVector4`: one floored product per component. Panics on overflow. Upstream:
/// `Matrix1 * RowVector4` (`Mul`).
pub impl Matrix1MulRowVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector4<T>> {
    type Output = RowVector4<T>;
    #[inline(always)]
    fn mul_mat(self: Matrix1<T>, rhs: RowVector4<T>) -> RowVector4<T> {
        RowVector4 { x: self.x * rhs.x, y: self.x * rhs.y, z: self.x * rhs.z, w: self.x * rhs.w }
    }
}

/// `self * rhs`, a `RowVector5`: one floored product per component. Panics on overflow. Upstream:
/// `Matrix1 * RowVector5` (`Mul`).
pub impl Matrix1MulRowVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector5<T>> {
    type Output = RowVector5<T>;
    fn mul_mat(self: Matrix1<T>, rhs: RowVector5<T>) -> RowVector5<T> {
        RowVector5 {
            x: self.x * rhs.x,
            y: self.x * rhs.y,
            z: self.x * rhs.z,
            w: self.x * rhs.w,
            a: self.x * rhs.a,
        }
    }
}

/// `self * rhs`, a `RowVector6`: one floored product per component. Panics on overflow. Upstream:
/// `Matrix1 * RowVector6` (`Mul`).
pub impl Matrix1MulRowVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix1<T>, RowVector6<T>> {
    type Output = RowVector6<T>;
    fn mul_mat(self: Matrix1<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: self.x * rhs.x,
            y: self.x * rhs.y,
            z: self.x * rhs.z,
            w: self.x * rhs.w,
            a: self.x * rhs.a,
            b: self.x * rhs.b,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `Matrix1`: one floored product per component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl Matrix1TrMulMatrix1<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix1<T>, Matrix1<T>> {
    type Output = Matrix1<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix1<T>, rhs: Matrix1<T>) -> Matrix1<T> {
        Matrix1 { x: self.x * rhs.x }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `RowVector2`: one floored product per
/// component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl Matrix1TrMulRowVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix1<T>, RowVector2<T>> {
    type Output = RowVector2<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix1<T>, rhs: RowVector2<T>) -> RowVector2<T> {
        RowVector2 { x: self.x * rhs.x, y: self.x * rhs.y }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `RowVector3`: one floored product per
/// component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl Matrix1TrMulRowVector3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix1<T>, RowVector3<T>> {
    type Output = RowVector3<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix1<T>, rhs: RowVector3<T>) -> RowVector3<T> {
        RowVector3 { x: self.x * rhs.x, y: self.x * rhs.y, z: self.x * rhs.z }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `RowVector4`: one floored product per
/// component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl Matrix1TrMulRowVector4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix1<T>, RowVector4<T>> {
    type Output = RowVector4<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix1<T>, rhs: RowVector4<T>) -> RowVector4<T> {
        RowVector4 { x: self.x * rhs.x, y: self.x * rhs.y, z: self.x * rhs.z, w: self.x * rhs.w }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `RowVector5`: one floored product per
/// component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl Matrix1TrMulRowVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix1<T>, RowVector5<T>> {
    type Output = RowVector5<T>;
    fn tr_mul(self: Matrix1<T>, rhs: RowVector5<T>) -> RowVector5<T> {
        RowVector5 {
            x: self.x * rhs.x,
            y: self.x * rhs.y,
            z: self.x * rhs.z,
            w: self.x * rhs.w,
            a: self.x * rhs.a,
        }
    }
}

/// `selfᵀ * rhs` without forming the transpose, a `RowVector6`: one floored product per
/// component.
/// Bit-identical to `self.transpose().mul_mat(rhs)`. Panics on overflow. Upstream: `tr_mul`.
pub impl Matrix1TrMulRowVector6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix1<T>, RowVector6<T>> {
    type Output = RowVector6<T>;
    fn tr_mul(self: Matrix1<T>, rhs: RowVector6<T>) -> RowVector6<T> {
        RowVector6 {
            x: self.x * rhs.x,
            y: self.x * rhs.y,
            z: self.x * rhs.z,
            w: self.x * rhs.w,
            a: self.x * rhs.a,
            b: self.x * rhs.b,
        }
    }
}
