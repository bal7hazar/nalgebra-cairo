// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/views.py.
//! The swizzles of the points (upstream `geometry/swizzle.rs`): `p.xy()`, `p.zyx()`... build a
//! `Point2` / `Point3` from components of `p`, on every point that has them (upstream: every
//! index below the dimension). One `<Point>SwizzleTrait` per point (import it to call them);
//! `Point3::xy` predates them and stays in `Point3Trait`.

use crate::base::point2::Point2;
use crate::base::point3::Point3;
use crate::geometry::point1::Point1;
use crate::geometry::point4::Point4;
use crate::geometry::point5::Point5;
use crate::geometry::point6::Point6;

/// The swizzles of `Point1`.
#[generate_trait]
pub impl Point1SwizzleImpl<T, +Copy<T>, +Drop<T>> of Point1SwizzleTrait<T> {
    /// The `Point2` `(x, x)` of coordinates of `self`. Upstream: the `xx` swizzle.
    #[inline(always)]
    fn xx(self: Point1<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.x }
    }

    /// The `Point3` `(x, x, x)` of coordinates of `self`. Upstream: the `xxx` swizzle.
    #[inline(always)]
    fn xxx(self: Point1<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.x }
    }
}

/// The swizzles of `Point2`.
#[generate_trait]
pub impl Point2SwizzleImpl<T, +Copy<T>, +Drop<T>> of Point2SwizzleTrait<T> {
    /// The `Point2` `(x, x)` of coordinates of `self`. Upstream: the `xx` swizzle.
    #[inline(always)]
    fn xx(self: Point2<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.x }
    }

    /// The `Point3` `(x, x, x)` of coordinates of `self`. Upstream: the `xxx` swizzle.
    #[inline(always)]
    fn xxx(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.x }
    }

    /// The `Point2` `(x, y)` of coordinates of `self`. Upstream: the `xy` swizzle.
    #[inline(always)]
    fn xy(self: Point2<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.y }
    }

    /// The `Point2` `(y, x)` of coordinates of `self`. Upstream: the `yx` swizzle.
    #[inline(always)]
    fn yx(self: Point2<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.x }
    }

    /// The `Point2` `(y, y)` of coordinates of `self`. Upstream: the `yy` swizzle.
    #[inline(always)]
    fn yy(self: Point2<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.y }
    }

    /// The `Point3` `(x, x, y)` of coordinates of `self`. Upstream: the `xxy` swizzle.
    #[inline(always)]
    fn xxy(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.y }
    }

    /// The `Point3` `(x, y, x)` of coordinates of `self`. Upstream: the `xyx` swizzle.
    #[inline(always)]
    fn xyx(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.x }
    }

    /// The `Point3` `(x, y, y)` of coordinates of `self`. Upstream: the `xyy` swizzle.
    #[inline(always)]
    fn xyy(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.y }
    }

    /// The `Point3` `(y, x, x)` of coordinates of `self`. Upstream: the `yxx` swizzle.
    #[inline(always)]
    fn yxx(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.x }
    }

    /// The `Point3` `(y, x, y)` of coordinates of `self`. Upstream: the `yxy` swizzle.
    #[inline(always)]
    fn yxy(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.y }
    }

    /// The `Point3` `(y, y, x)` of coordinates of `self`. Upstream: the `yyx` swizzle.
    #[inline(always)]
    fn yyx(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.x }
    }

    /// The `Point3` `(y, y, y)` of coordinates of `self`. Upstream: the `yyy` swizzle.
    #[inline(always)]
    fn yyy(self: Point2<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.y }
    }
}

/// The swizzles of `Point3`.
#[generate_trait]
pub impl Point3SwizzleImpl<T, +Copy<T>, +Drop<T>> of Point3SwizzleTrait<T> {
    /// The `Point2` `(x, x)` of coordinates of `self`. Upstream: the `xx` swizzle.
    #[inline(always)]
    fn xx(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.x }
    }

    /// The `Point3` `(x, x, x)` of coordinates of `self`. Upstream: the `xxx` swizzle.
    #[inline(always)]
    fn xxx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.x }
    }

    /// The `Point2` `(y, x)` of coordinates of `self`. Upstream: the `yx` swizzle.
    #[inline(always)]
    fn yx(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.x }
    }

    /// The `Point2` `(y, y)` of coordinates of `self`. Upstream: the `yy` swizzle.
    #[inline(always)]
    fn yy(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.y }
    }

    /// The `Point3` `(x, x, y)` of coordinates of `self`. Upstream: the `xxy` swizzle.
    #[inline(always)]
    fn xxy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.y }
    }

    /// The `Point3` `(x, y, x)` of coordinates of `self`. Upstream: the `xyx` swizzle.
    #[inline(always)]
    fn xyx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.x }
    }

    /// The `Point3` `(x, y, y)` of coordinates of `self`. Upstream: the `xyy` swizzle.
    #[inline(always)]
    fn xyy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.y }
    }

    /// The `Point3` `(y, x, x)` of coordinates of `self`. Upstream: the `yxx` swizzle.
    #[inline(always)]
    fn yxx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.x }
    }

    /// The `Point3` `(y, x, y)` of coordinates of `self`. Upstream: the `yxy` swizzle.
    #[inline(always)]
    fn yxy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.y }
    }

    /// The `Point3` `(y, y, x)` of coordinates of `self`. Upstream: the `yyx` swizzle.
    #[inline(always)]
    fn yyx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.x }
    }

    /// The `Point3` `(y, y, y)` of coordinates of `self`. Upstream: the `yyy` swizzle.
    #[inline(always)]
    fn yyy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.y }
    }

    /// The `Point2` `(x, z)` of coordinates of `self`. Upstream: the `xz` swizzle.
    #[inline(always)]
    fn xz(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.z }
    }

    /// The `Point2` `(y, z)` of coordinates of `self`. Upstream: the `yz` swizzle.
    #[inline(always)]
    fn yz(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.z }
    }

    /// The `Point2` `(z, x)` of coordinates of `self`. Upstream: the `zx` swizzle.
    #[inline(always)]
    fn zx(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.x }
    }

    /// The `Point2` `(z, y)` of coordinates of `self`. Upstream: the `zy` swizzle.
    #[inline(always)]
    fn zy(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.y }
    }

    /// The `Point2` `(z, z)` of coordinates of `self`. Upstream: the `zz` swizzle.
    #[inline(always)]
    fn zz(self: Point3<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.z }
    }

    /// The `Point3` `(x, x, z)` of coordinates of `self`. Upstream: the `xxz` swizzle.
    #[inline(always)]
    fn xxz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.z }
    }

    /// The `Point3` `(x, y, z)` of coordinates of `self`. Upstream: the `xyz` swizzle.
    #[inline(always)]
    fn xyz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.z }
    }

    /// The `Point3` `(x, z, x)` of coordinates of `self`. Upstream: the `xzx` swizzle.
    #[inline(always)]
    fn xzx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.x }
    }

    /// The `Point3` `(x, z, y)` of coordinates of `self`. Upstream: the `xzy` swizzle.
    #[inline(always)]
    fn xzy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.y }
    }

    /// The `Point3` `(x, z, z)` of coordinates of `self`. Upstream: the `xzz` swizzle.
    #[inline(always)]
    fn xzz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.z }
    }

    /// The `Point3` `(y, x, z)` of coordinates of `self`. Upstream: the `yxz` swizzle.
    #[inline(always)]
    fn yxz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.z }
    }

    /// The `Point3` `(y, y, z)` of coordinates of `self`. Upstream: the `yyz` swizzle.
    #[inline(always)]
    fn yyz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.z }
    }

    /// The `Point3` `(y, z, x)` of coordinates of `self`. Upstream: the `yzx` swizzle.
    #[inline(always)]
    fn yzx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.x }
    }

    /// The `Point3` `(y, z, y)` of coordinates of `self`. Upstream: the `yzy` swizzle.
    #[inline(always)]
    fn yzy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.y }
    }

    /// The `Point3` `(y, z, z)` of coordinates of `self`. Upstream: the `yzz` swizzle.
    #[inline(always)]
    fn yzz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.z }
    }

    /// The `Point3` `(z, x, x)` of coordinates of `self`. Upstream: the `zxx` swizzle.
    #[inline(always)]
    fn zxx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.x }
    }

    /// The `Point3` `(z, x, y)` of coordinates of `self`. Upstream: the `zxy` swizzle.
    #[inline(always)]
    fn zxy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.y }
    }

    /// The `Point3` `(z, x, z)` of coordinates of `self`. Upstream: the `zxz` swizzle.
    #[inline(always)]
    fn zxz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.z }
    }

    /// The `Point3` `(z, y, x)` of coordinates of `self`. Upstream: the `zyx` swizzle.
    #[inline(always)]
    fn zyx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.x }
    }

    /// The `Point3` `(z, y, y)` of coordinates of `self`. Upstream: the `zyy` swizzle.
    #[inline(always)]
    fn zyy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.y }
    }

    /// The `Point3` `(z, y, z)` of coordinates of `self`. Upstream: the `zyz` swizzle.
    #[inline(always)]
    fn zyz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.z }
    }

    /// The `Point3` `(z, z, x)` of coordinates of `self`. Upstream: the `zzx` swizzle.
    #[inline(always)]
    fn zzx(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.x }
    }

    /// The `Point3` `(z, z, y)` of coordinates of `self`. Upstream: the `zzy` swizzle.
    #[inline(always)]
    fn zzy(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.y }
    }

    /// The `Point3` `(z, z, z)` of coordinates of `self`. Upstream: the `zzz` swizzle.
    #[inline(always)]
    fn zzz(self: Point3<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.z }
    }
}

/// The swizzles of `Point4`.
#[generate_trait]
pub impl Point4SwizzleImpl<T, +Copy<T>, +Drop<T>> of Point4SwizzleTrait<T> {
    /// The `Point2` `(x, x)` of coordinates of `self`. Upstream: the `xx` swizzle.
    #[inline(always)]
    fn xx(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.x }
    }

    /// The `Point3` `(x, x, x)` of coordinates of `self`. Upstream: the `xxx` swizzle.
    #[inline(always)]
    fn xxx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.x }
    }

    /// The `Point2` `(x, y)` of coordinates of `self`. Upstream: the `xy` swizzle.
    #[inline(always)]
    fn xy(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.y }
    }

    /// The `Point2` `(y, x)` of coordinates of `self`. Upstream: the `yx` swizzle.
    #[inline(always)]
    fn yx(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.x }
    }

    /// The `Point2` `(y, y)` of coordinates of `self`. Upstream: the `yy` swizzle.
    #[inline(always)]
    fn yy(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.y }
    }

    /// The `Point3` `(x, x, y)` of coordinates of `self`. Upstream: the `xxy` swizzle.
    #[inline(always)]
    fn xxy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.y }
    }

    /// The `Point3` `(x, y, x)` of coordinates of `self`. Upstream: the `xyx` swizzle.
    #[inline(always)]
    fn xyx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.x }
    }

    /// The `Point3` `(x, y, y)` of coordinates of `self`. Upstream: the `xyy` swizzle.
    #[inline(always)]
    fn xyy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.y }
    }

    /// The `Point3` `(y, x, x)` of coordinates of `self`. Upstream: the `yxx` swizzle.
    #[inline(always)]
    fn yxx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.x }
    }

    /// The `Point3` `(y, x, y)` of coordinates of `self`. Upstream: the `yxy` swizzle.
    #[inline(always)]
    fn yxy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.y }
    }

    /// The `Point3` `(y, y, x)` of coordinates of `self`. Upstream: the `yyx` swizzle.
    #[inline(always)]
    fn yyx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.x }
    }

    /// The `Point3` `(y, y, y)` of coordinates of `self`. Upstream: the `yyy` swizzle.
    #[inline(always)]
    fn yyy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.y }
    }

    /// The `Point2` `(x, z)` of coordinates of `self`. Upstream: the `xz` swizzle.
    #[inline(always)]
    fn xz(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.z }
    }

    /// The `Point2` `(y, z)` of coordinates of `self`. Upstream: the `yz` swizzle.
    #[inline(always)]
    fn yz(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.z }
    }

    /// The `Point2` `(z, x)` of coordinates of `self`. Upstream: the `zx` swizzle.
    #[inline(always)]
    fn zx(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.x }
    }

    /// The `Point2` `(z, y)` of coordinates of `self`. Upstream: the `zy` swizzle.
    #[inline(always)]
    fn zy(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.y }
    }

    /// The `Point2` `(z, z)` of coordinates of `self`. Upstream: the `zz` swizzle.
    #[inline(always)]
    fn zz(self: Point4<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.z }
    }

    /// The `Point3` `(x, x, z)` of coordinates of `self`. Upstream: the `xxz` swizzle.
    #[inline(always)]
    fn xxz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.z }
    }

    /// The `Point3` `(x, y, z)` of coordinates of `self`. Upstream: the `xyz` swizzle.
    #[inline(always)]
    fn xyz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.z }
    }

    /// The `Point3` `(x, z, x)` of coordinates of `self`. Upstream: the `xzx` swizzle.
    #[inline(always)]
    fn xzx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.x }
    }

    /// The `Point3` `(x, z, y)` of coordinates of `self`. Upstream: the `xzy` swizzle.
    #[inline(always)]
    fn xzy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.y }
    }

    /// The `Point3` `(x, z, z)` of coordinates of `self`. Upstream: the `xzz` swizzle.
    #[inline(always)]
    fn xzz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.z }
    }

    /// The `Point3` `(y, x, z)` of coordinates of `self`. Upstream: the `yxz` swizzle.
    #[inline(always)]
    fn yxz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.z }
    }

    /// The `Point3` `(y, y, z)` of coordinates of `self`. Upstream: the `yyz` swizzle.
    #[inline(always)]
    fn yyz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.z }
    }

    /// The `Point3` `(y, z, x)` of coordinates of `self`. Upstream: the `yzx` swizzle.
    #[inline(always)]
    fn yzx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.x }
    }

    /// The `Point3` `(y, z, y)` of coordinates of `self`. Upstream: the `yzy` swizzle.
    #[inline(always)]
    fn yzy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.y }
    }

    /// The `Point3` `(y, z, z)` of coordinates of `self`. Upstream: the `yzz` swizzle.
    #[inline(always)]
    fn yzz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.z }
    }

    /// The `Point3` `(z, x, x)` of coordinates of `self`. Upstream: the `zxx` swizzle.
    #[inline(always)]
    fn zxx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.x }
    }

    /// The `Point3` `(z, x, y)` of coordinates of `self`. Upstream: the `zxy` swizzle.
    #[inline(always)]
    fn zxy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.y }
    }

    /// The `Point3` `(z, x, z)` of coordinates of `self`. Upstream: the `zxz` swizzle.
    #[inline(always)]
    fn zxz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.z }
    }

    /// The `Point3` `(z, y, x)` of coordinates of `self`. Upstream: the `zyx` swizzle.
    #[inline(always)]
    fn zyx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.x }
    }

    /// The `Point3` `(z, y, y)` of coordinates of `self`. Upstream: the `zyy` swizzle.
    #[inline(always)]
    fn zyy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.y }
    }

    /// The `Point3` `(z, y, z)` of coordinates of `self`. Upstream: the `zyz` swizzle.
    #[inline(always)]
    fn zyz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.z }
    }

    /// The `Point3` `(z, z, x)` of coordinates of `self`. Upstream: the `zzx` swizzle.
    #[inline(always)]
    fn zzx(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.x }
    }

    /// The `Point3` `(z, z, y)` of coordinates of `self`. Upstream: the `zzy` swizzle.
    #[inline(always)]
    fn zzy(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.y }
    }

    /// The `Point3` `(z, z, z)` of coordinates of `self`. Upstream: the `zzz` swizzle.
    #[inline(always)]
    fn zzz(self: Point4<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.z }
    }
}

/// The swizzles of `Point5`.
#[generate_trait]
pub impl Point5SwizzleImpl<T, +Copy<T>, +Drop<T>> of Point5SwizzleTrait<T> {
    /// The `Point2` `(x, x)` of coordinates of `self`. Upstream: the `xx` swizzle.
    #[inline(always)]
    fn xx(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.x }
    }

    /// The `Point3` `(x, x, x)` of coordinates of `self`. Upstream: the `xxx` swizzle.
    #[inline(always)]
    fn xxx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.x }
    }

    /// The `Point2` `(x, y)` of coordinates of `self`. Upstream: the `xy` swizzle.
    #[inline(always)]
    fn xy(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.y }
    }

    /// The `Point2` `(y, x)` of coordinates of `self`. Upstream: the `yx` swizzle.
    #[inline(always)]
    fn yx(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.x }
    }

    /// The `Point2` `(y, y)` of coordinates of `self`. Upstream: the `yy` swizzle.
    #[inline(always)]
    fn yy(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.y }
    }

    /// The `Point3` `(x, x, y)` of coordinates of `self`. Upstream: the `xxy` swizzle.
    #[inline(always)]
    fn xxy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.y }
    }

    /// The `Point3` `(x, y, x)` of coordinates of `self`. Upstream: the `xyx` swizzle.
    #[inline(always)]
    fn xyx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.x }
    }

    /// The `Point3` `(x, y, y)` of coordinates of `self`. Upstream: the `xyy` swizzle.
    #[inline(always)]
    fn xyy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.y }
    }

    /// The `Point3` `(y, x, x)` of coordinates of `self`. Upstream: the `yxx` swizzle.
    #[inline(always)]
    fn yxx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.x }
    }

    /// The `Point3` `(y, x, y)` of coordinates of `self`. Upstream: the `yxy` swizzle.
    #[inline(always)]
    fn yxy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.y }
    }

    /// The `Point3` `(y, y, x)` of coordinates of `self`. Upstream: the `yyx` swizzle.
    #[inline(always)]
    fn yyx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.x }
    }

    /// The `Point3` `(y, y, y)` of coordinates of `self`. Upstream: the `yyy` swizzle.
    #[inline(always)]
    fn yyy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.y }
    }

    /// The `Point2` `(x, z)` of coordinates of `self`. Upstream: the `xz` swizzle.
    #[inline(always)]
    fn xz(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.z }
    }

    /// The `Point2` `(y, z)` of coordinates of `self`. Upstream: the `yz` swizzle.
    #[inline(always)]
    fn yz(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.z }
    }

    /// The `Point2` `(z, x)` of coordinates of `self`. Upstream: the `zx` swizzle.
    #[inline(always)]
    fn zx(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.x }
    }

    /// The `Point2` `(z, y)` of coordinates of `self`. Upstream: the `zy` swizzle.
    #[inline(always)]
    fn zy(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.y }
    }

    /// The `Point2` `(z, z)` of coordinates of `self`. Upstream: the `zz` swizzle.
    #[inline(always)]
    fn zz(self: Point5<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.z }
    }

    /// The `Point3` `(x, x, z)` of coordinates of `self`. Upstream: the `xxz` swizzle.
    #[inline(always)]
    fn xxz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.z }
    }

    /// The `Point3` `(x, y, z)` of coordinates of `self`. Upstream: the `xyz` swizzle.
    #[inline(always)]
    fn xyz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.z }
    }

    /// The `Point3` `(x, z, x)` of coordinates of `self`. Upstream: the `xzx` swizzle.
    #[inline(always)]
    fn xzx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.x }
    }

    /// The `Point3` `(x, z, y)` of coordinates of `self`. Upstream: the `xzy` swizzle.
    #[inline(always)]
    fn xzy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.y }
    }

    /// The `Point3` `(x, z, z)` of coordinates of `self`. Upstream: the `xzz` swizzle.
    #[inline(always)]
    fn xzz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.z }
    }

    /// The `Point3` `(y, x, z)` of coordinates of `self`. Upstream: the `yxz` swizzle.
    #[inline(always)]
    fn yxz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.z }
    }

    /// The `Point3` `(y, y, z)` of coordinates of `self`. Upstream: the `yyz` swizzle.
    #[inline(always)]
    fn yyz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.z }
    }

    /// The `Point3` `(y, z, x)` of coordinates of `self`. Upstream: the `yzx` swizzle.
    #[inline(always)]
    fn yzx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.x }
    }

    /// The `Point3` `(y, z, y)` of coordinates of `self`. Upstream: the `yzy` swizzle.
    #[inline(always)]
    fn yzy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.y }
    }

    /// The `Point3` `(y, z, z)` of coordinates of `self`. Upstream: the `yzz` swizzle.
    #[inline(always)]
    fn yzz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.z }
    }

    /// The `Point3` `(z, x, x)` of coordinates of `self`. Upstream: the `zxx` swizzle.
    #[inline(always)]
    fn zxx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.x }
    }

    /// The `Point3` `(z, x, y)` of coordinates of `self`. Upstream: the `zxy` swizzle.
    #[inline(always)]
    fn zxy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.y }
    }

    /// The `Point3` `(z, x, z)` of coordinates of `self`. Upstream: the `zxz` swizzle.
    #[inline(always)]
    fn zxz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.z }
    }

    /// The `Point3` `(z, y, x)` of coordinates of `self`. Upstream: the `zyx` swizzle.
    #[inline(always)]
    fn zyx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.x }
    }

    /// The `Point3` `(z, y, y)` of coordinates of `self`. Upstream: the `zyy` swizzle.
    #[inline(always)]
    fn zyy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.y }
    }

    /// The `Point3` `(z, y, z)` of coordinates of `self`. Upstream: the `zyz` swizzle.
    #[inline(always)]
    fn zyz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.z }
    }

    /// The `Point3` `(z, z, x)` of coordinates of `self`. Upstream: the `zzx` swizzle.
    #[inline(always)]
    fn zzx(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.x }
    }

    /// The `Point3` `(z, z, y)` of coordinates of `self`. Upstream: the `zzy` swizzle.
    #[inline(always)]
    fn zzy(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.y }
    }

    /// The `Point3` `(z, z, z)` of coordinates of `self`. Upstream: the `zzz` swizzle.
    #[inline(always)]
    fn zzz(self: Point5<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.z }
    }
}

/// The swizzles of `Point6`.
#[generate_trait]
pub impl Point6SwizzleImpl<T, +Copy<T>, +Drop<T>> of Point6SwizzleTrait<T> {
    /// The `Point2` `(x, x)` of coordinates of `self`. Upstream: the `xx` swizzle.
    #[inline(always)]
    fn xx(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.x }
    }

    /// The `Point3` `(x, x, x)` of coordinates of `self`. Upstream: the `xxx` swizzle.
    #[inline(always)]
    fn xxx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.x }
    }

    /// The `Point2` `(x, y)` of coordinates of `self`. Upstream: the `xy` swizzle.
    #[inline(always)]
    fn xy(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.y }
    }

    /// The `Point2` `(y, x)` of coordinates of `self`. Upstream: the `yx` swizzle.
    #[inline(always)]
    fn yx(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.x }
    }

    /// The `Point2` `(y, y)` of coordinates of `self`. Upstream: the `yy` swizzle.
    #[inline(always)]
    fn yy(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.y }
    }

    /// The `Point3` `(x, x, y)` of coordinates of `self`. Upstream: the `xxy` swizzle.
    #[inline(always)]
    fn xxy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.y }
    }

    /// The `Point3` `(x, y, x)` of coordinates of `self`. Upstream: the `xyx` swizzle.
    #[inline(always)]
    fn xyx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.x }
    }

    /// The `Point3` `(x, y, y)` of coordinates of `self`. Upstream: the `xyy` swizzle.
    #[inline(always)]
    fn xyy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.y }
    }

    /// The `Point3` `(y, x, x)` of coordinates of `self`. Upstream: the `yxx` swizzle.
    #[inline(always)]
    fn yxx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.x }
    }

    /// The `Point3` `(y, x, y)` of coordinates of `self`. Upstream: the `yxy` swizzle.
    #[inline(always)]
    fn yxy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.y }
    }

    /// The `Point3` `(y, y, x)` of coordinates of `self`. Upstream: the `yyx` swizzle.
    #[inline(always)]
    fn yyx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.x }
    }

    /// The `Point3` `(y, y, y)` of coordinates of `self`. Upstream: the `yyy` swizzle.
    #[inline(always)]
    fn yyy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.y }
    }

    /// The `Point2` `(x, z)` of coordinates of `self`. Upstream: the `xz` swizzle.
    #[inline(always)]
    fn xz(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.x, y: self.z }
    }

    /// The `Point2` `(y, z)` of coordinates of `self`. Upstream: the `yz` swizzle.
    #[inline(always)]
    fn yz(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.y, y: self.z }
    }

    /// The `Point2` `(z, x)` of coordinates of `self`. Upstream: the `zx` swizzle.
    #[inline(always)]
    fn zx(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.x }
    }

    /// The `Point2` `(z, y)` of coordinates of `self`. Upstream: the `zy` swizzle.
    #[inline(always)]
    fn zy(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.y }
    }

    /// The `Point2` `(z, z)` of coordinates of `self`. Upstream: the `zz` swizzle.
    #[inline(always)]
    fn zz(self: Point6<T>) -> Point2<T> {
        Point2 { x: self.z, y: self.z }
    }

    /// The `Point3` `(x, x, z)` of coordinates of `self`. Upstream: the `xxz` swizzle.
    #[inline(always)]
    fn xxz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.x, z: self.z }
    }

    /// The `Point3` `(x, y, z)` of coordinates of `self`. Upstream: the `xyz` swizzle.
    #[inline(always)]
    fn xyz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.y, z: self.z }
    }

    /// The `Point3` `(x, z, x)` of coordinates of `self`. Upstream: the `xzx` swizzle.
    #[inline(always)]
    fn xzx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.x }
    }

    /// The `Point3` `(x, z, y)` of coordinates of `self`. Upstream: the `xzy` swizzle.
    #[inline(always)]
    fn xzy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.y }
    }

    /// The `Point3` `(x, z, z)` of coordinates of `self`. Upstream: the `xzz` swizzle.
    #[inline(always)]
    fn xzz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.x, y: self.z, z: self.z }
    }

    /// The `Point3` `(y, x, z)` of coordinates of `self`. Upstream: the `yxz` swizzle.
    #[inline(always)]
    fn yxz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.x, z: self.z }
    }

    /// The `Point3` `(y, y, z)` of coordinates of `self`. Upstream: the `yyz` swizzle.
    #[inline(always)]
    fn yyz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.y, z: self.z }
    }

    /// The `Point3` `(y, z, x)` of coordinates of `self`. Upstream: the `yzx` swizzle.
    #[inline(always)]
    fn yzx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.x }
    }

    /// The `Point3` `(y, z, y)` of coordinates of `self`. Upstream: the `yzy` swizzle.
    #[inline(always)]
    fn yzy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.y }
    }

    /// The `Point3` `(y, z, z)` of coordinates of `self`. Upstream: the `yzz` swizzle.
    #[inline(always)]
    fn yzz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.y, y: self.z, z: self.z }
    }

    /// The `Point3` `(z, x, x)` of coordinates of `self`. Upstream: the `zxx` swizzle.
    #[inline(always)]
    fn zxx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.x }
    }

    /// The `Point3` `(z, x, y)` of coordinates of `self`. Upstream: the `zxy` swizzle.
    #[inline(always)]
    fn zxy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.y }
    }

    /// The `Point3` `(z, x, z)` of coordinates of `self`. Upstream: the `zxz` swizzle.
    #[inline(always)]
    fn zxz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.x, z: self.z }
    }

    /// The `Point3` `(z, y, x)` of coordinates of `self`. Upstream: the `zyx` swizzle.
    #[inline(always)]
    fn zyx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.x }
    }

    /// The `Point3` `(z, y, y)` of coordinates of `self`. Upstream: the `zyy` swizzle.
    #[inline(always)]
    fn zyy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.y }
    }

    /// The `Point3` `(z, y, z)` of coordinates of `self`. Upstream: the `zyz` swizzle.
    #[inline(always)]
    fn zyz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.y, z: self.z }
    }

    /// The `Point3` `(z, z, x)` of coordinates of `self`. Upstream: the `zzx` swizzle.
    #[inline(always)]
    fn zzx(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.x }
    }

    /// The `Point3` `(z, z, y)` of coordinates of `self`. Upstream: the `zzy` swizzle.
    #[inline(always)]
    fn zzy(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.y }
    }

    /// The `Point3` `(z, z, z)` of coordinates of `self`. Upstream: the `zzz` swizzle.
    #[inline(always)]
    fn zzz(self: Point6<T>) -> Point3<T> {
        Point3 { x: self.z, y: self.z, z: self.z }
    }
}
