// Generated by tools/shapegen/shapegen.py: do not edit by hand. Template:
// tools/shapegen/shapes.py.
//! `Matrix5x2`: a statically sized 5x2 matrix (upstream `nalgebra::Matrix5x2`).
//!
//! Every sum of products goes through a fused `Real` kernel: one rounding (floor) and one overflow
//! check per output scalar. Operators (`+`, `-`, unary `-` and their assigning forms) are
//! implemented in this module, so they need no import; the other operations are methods of
//! `Matrix5x2Trait`, the products with every conformable shape `MatrixMul::mul_mat` (`self * rhs`)
//! and `MatrixTrMul::tr_mul` (`selfᵀ * rhs`).

use core::num::traits::Bounded;
use core::ops::{AddAssign, DivAssign, IndexView, MulAssign, SubAssign};
use simba::scalar::{Real, Transcendental};
use crate::geometry::Rotation2;
use crate::geometry::quaternion::ApproxEqTrait;
use super::errors;
use super::kernels::Powi;
use super::matrix2::Matrix2;
use super::matrix2x3::Matrix2x3;
use super::matrix2x4::Matrix2x4;
use super::matrix2x5::Matrix2x5;
use super::matrix2x6::Matrix2x6;
use super::matrix5::Matrix5;
use super::matrix5x3::Matrix5x3;
use super::matrix5x4::Matrix5x4;
use super::matrix5x6::Matrix5x6;
use super::matrix_index::MatrixIndex;
use super::matrix_mul::MatrixMul;
use super::matrix_tr_mul::MatrixTrMul;
use super::vector2::Vector2;
use super::vector5::Vector5;

/// A 5x2 matrix. `mRC` is the component at row `R`, column `C`.
///
/// Fields are declared in column-major order, so `Serde` matches upstream's storage order, while
/// `new` takes its arguments in row-major order like upstream.
#[derive(Copy, Drop, PartialEq, Serde, Default, Debug, Hash)]
pub struct Matrix5x2<T> {
    pub m11: T,
    pub m21: T,
    pub m31: T,
    pub m41: T,
    pub m51: T,
    pub m12: T,
    pub m22: T,
    pub m32: T,
    pub m42: T,
    pub m52: T,
}

/// Methods of `Matrix5x2<T>` for any `Real` scalar.
#[generate_trait]
pub impl Matrix5x2Impl<
    T,
    impl R: Real<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix5x2Trait<T> {
    /// The 5x2 matrix with the given components, in ROW-major order. Upstream: `Matrix5x2::new`.
    #[inline(always)]
    fn new(
        m11: T, m12: T, m21: T, m22: T, m31: T, m32: T, m41: T, m42: T, m51: T, m52: T,
    ) -> Matrix5x2<T> {
        Matrix5x2 { m11, m21, m31, m41, m51, m12, m22, m32, m42, m52 }
    }

    /// The zero 5x2 matrix. Upstream: `Matrix5x2::zeros`.
    #[inline(always)]
    fn zeros() -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::zero(),
            m21: R::zero(),
            m31: R::zero(),
            m41: R::zero(),
            m51: R::zero(),
            m12: R::zero(),
            m22: R::zero(),
            m32: R::zero(),
            m42: R::zero(),
            m52: R::zero(),
        }
    }

    /// The transpose, a `Matrix2x5`. Exact. Upstream: `transpose`.
    #[inline(always)]
    fn transpose(self: Matrix5x2<T>) -> Matrix2x5<T> {
        Matrix2x5 {
            m11: self.m11,
            m21: self.m12,
            m12: self.m21,
            m22: self.m22,
            m13: self.m31,
            m23: self.m32,
            m14: self.m41,
            m24: self.m42,
            m15: self.m51,
            m25: self.m52,
        }
    }

    /// Component-wise absolute value. Exact; panics on overflow (`|MIN|`). Upstream: `abs`.
    #[inline(always)]
    fn abs(self: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::abs(self.m11),
            m21: R::abs(self.m21),
            m31: R::abs(self.m31),
            m41: R::abs(self.m41),
            m51: R::abs(self.m51),
            m12: R::abs(self.m12),
            m22: R::abs(self.m22),
            m32: R::abs(self.m32),
            m42: R::abs(self.m42),
            m52: R::abs(self.m52),
        }
    }

    /// `self * k`, each component floored once. Panics on overflow. Upstream: `scale` (`self * k`).
    #[inline(always)]
    fn scale(self: Matrix5x2<T>, k: T) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: self.m11 * k,
            m21: self.m21 * k,
            m31: self.m31 * k,
            m41: self.m41 * k,
            m51: self.m51 * k,
            m12: self.m12 * k,
            m22: self.m22 * k,
            m32: self.m32 * k,
            m42: self.m42 * k,
            m52: self.m52 * k,
        }
    }

    /// Whether every component of `self` is within `ulps` smallest units (raw units for fixed
    /// point) of `other`'s; cannot overflow. Upstream: `approx::AbsDiffEq::abs_diff_eq`, the
    /// tolerance being counted in ulp instead of a float epsilon (DESIGN D3).
    #[inline(always)]
    fn abs_diff_eq(self: Matrix5x2<T>, other: Matrix5x2<T>, ulps: u64) -> bool {
        R::abs_diff_eq(self.m11, other.m11, ulps)
            && R::abs_diff_eq(self.m21, other.m21, ulps)
            && R::abs_diff_eq(self.m31, other.m31, ulps)
            && R::abs_diff_eq(self.m41, other.m41, ulps)
            && R::abs_diff_eq(self.m51, other.m51, ulps)
            && R::abs_diff_eq(self.m12, other.m12, ulps)
            && R::abs_diff_eq(self.m22, other.m22, ulps)
            && R::abs_diff_eq(self.m32, other.m32, ulps)
            && R::abs_diff_eq(self.m42, other.m42, ulps)
            && R::abs_diff_eq(self.m52, other.m52, ulps)
    }

    /// The 5x2 matrix whose components all equal `elem`. Upstream: `Matrix5x2::repeat`.
    #[inline(always)]
    fn repeat(elem: T) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: elem,
            m21: elem,
            m31: elem,
            m41: elem,
            m51: elem,
            m12: elem,
            m22: elem,
            m32: elem,
            m42: elem,
            m52: elem,
        }
    }

    /// Alias of `repeat`. Upstream: `Matrix5x2::from_element`.
    #[inline(always)]
    fn from_element(elem: T) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: elem,
            m21: elem,
            m31: elem,
            m41: elem,
            m51: elem,
            m12: elem,
            m22: elem,
            m32: elem,
            m42: elem,
            m52: elem,
        }
    }

    /// The 5x2 matrix whose component `(i, j)` (row, column, 0-based) is `f(i, j)`, `f` being
    /// called in column-major order like upstream. `f` is any closure or `Fn` value of `(usize,
    /// usize)` whose output converts `Into<T>` (the identity included): Cairo cannot state `Output
    /// = T` on the closure without the `associated_item_constraints` experimental feature.
    /// Upstream: `Matrix5x2::from_fn`.
    fn from_fn<
        F,
        +Drop<F>,
        impl Func: core::ops::Fn<F, (usize, usize)>,
        +Into<Func::Output, T>,
        +Drop<Func::Output>,
    >(
        f: F,
    ) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: f(0, 0).into(),
            m21: f(1, 0).into(),
            m31: f(2, 0).into(),
            m41: f(3, 0).into(),
            m51: f(4, 0).into(),
            m12: f(0, 1).into(),
            m22: f(1, 1).into(),
            m32: f(2, 1).into(),
            m42: f(3, 1).into(),
            m52: f(4, 1).into(),
        }
    }

    /// The 5x2 matrix of the 10 values of `data`, in row-major order. Panics with `nalgebra: wrong
    /// slice length` unless `data.len() == 10`. Upstream: `Matrix5x2::from_row_slice` (`&[T]`).
    #[inline(always)]
    fn from_row_slice(data: Span<T>) -> Matrix5x2<T> {
        if data.len() != 10 {
            core::panic_with_felt252(errors::SLICE_LENGTH);
        }
        Matrix5x2 {
            m11: *data[0],
            m21: *data[2],
            m31: *data[4],
            m41: *data[6],
            m51: *data[8],
            m12: *data[1],
            m22: *data[3],
            m32: *data[5],
            m42: *data[7],
            m52: *data[9],
        }
    }

    /// The 5x2 matrix of the 10 values of `data`, in column-major order. Panics with `nalgebra:
    /// wrong slice length` unless `data.len() == 10`. Upstream: `Matrix5x2::from_column_slice`
    /// (`&[T]`).
    #[inline(always)]
    fn from_column_slice(data: Span<T>) -> Matrix5x2<T> {
        if data.len() != 10 {
            core::panic_with_felt252(errors::SLICE_LENGTH);
        }
        Matrix5x2 {
            m11: *data[0],
            m21: *data[1],
            m31: *data[2],
            m41: *data[3],
            m51: *data[4],
            m12: *data[5],
            m22: *data[6],
            m32: *data[7],
            m42: *data[8],
            m52: *data[9],
        }
    }

    /// The 5x2 matrix whose first `data.len()` diagonal components are `data`, every other
    /// component zero. Panics with `nalgebra: diagonal too long` when `data.len() > 2`. Upstream:
    /// `Matrix5x2::from_partial_diagonal` (`&[T]`).
    #[inline(always)]
    fn from_partial_diagonal(data: Span<T>) -> Matrix5x2<T> {
        let len = data.len();
        if len > 2 {
            core::panic_with_felt252(errors::TOO_MANY_DIAGONAL);
        }
        Matrix5x2 {
            m11: if len > 0 {
                *data[0]
            } else {
                R::zero()
            },
            m21: R::zero(),
            m31: R::zero(),
            m41: R::zero(),
            m51: R::zero(),
            m12: R::zero(),
            m22: if len > 1 {
                *data[1]
            } else {
                R::zero()
            },
            m32: R::zero(),
            m42: R::zero(),
            m52: R::zero(),
        }
    }

    /// `true` when every component is zero. Upstream: `Zero::is_zero`.
    #[inline(always)]
    fn is_zero(self: Matrix5x2<T>) -> bool {
        self.m11 == R::zero()
            && self.m21 == R::zero()
            && self.m31 == R::zero()
            && self.m41 == R::zero()
            && self.m51 == R::zero()
            && self.m12 == R::zero()
            && self.m22 == R::zero()
            && self.m32 == R::zero()
            && self.m42 == R::zero()
            && self.m52 == R::zero()
    }

    /// Component-wise (Hadamard) product, each component floored once. Panics on overflow.
    /// Upstream: `component_mul`.
    #[inline(always)]
    fn component_mul(self: Matrix5x2<T>, rhs: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: self.m11 * rhs.m11,
            m21: self.m21 * rhs.m21,
            m31: self.m31 * rhs.m31,
            m41: self.m41 * rhs.m41,
            m51: self.m51 * rhs.m51,
            m12: self.m12 * rhs.m12,
            m22: self.m22 * rhs.m22,
            m32: self.m32 * rhs.m32,
            m42: self.m42 * rhs.m42,
            m52: self.m52 * rhs.m52,
        }
    }

    /// `self = self.component_mul(rhs)`. Upstream: `component_mul_assign`.
    #[inline(always)]
    fn component_mul_assign(ref self: Matrix5x2<T>, rhs: Matrix5x2<T>) {
        self =
            Matrix5x2 {
                m11: self.m11 * rhs.m11,
                m21: self.m21 * rhs.m21,
                m31: self.m31 * rhs.m31,
                m41: self.m41 * rhs.m41,
                m51: self.m51 * rhs.m51,
                m12: self.m12 * rhs.m12,
                m22: self.m22 * rhs.m22,
                m32: self.m32 * rhs.m32,
                m42: self.m42 * rhs.m42,
                m52: self.m52 * rhs.m52,
            };
    }

    /// Component-wise quotient, each component rounded to nearest (ties to even). Panics on a zero
    /// component of `rhs` and on overflow. Upstream: `component_div`.
    #[inline(always)]
    fn component_div(self: Matrix5x2<T>, rhs: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::div(self.m11, rhs.m11),
            m21: R::div(self.m21, rhs.m21),
            m31: R::div(self.m31, rhs.m31),
            m41: R::div(self.m41, rhs.m41),
            m51: R::div(self.m51, rhs.m51),
            m12: R::div(self.m12, rhs.m12),
            m22: R::div(self.m22, rhs.m22),
            m32: R::div(self.m32, rhs.m32),
            m42: R::div(self.m42, rhs.m42),
            m52: R::div(self.m52, rhs.m52),
        }
    }

    /// `self = self.component_div(rhs)`. Upstream: `component_div_assign`.
    #[inline(always)]
    fn component_div_assign(ref self: Matrix5x2<T>, rhs: Matrix5x2<T>) {
        self =
            Matrix5x2 {
                m11: R::div(self.m11, rhs.m11),
                m21: R::div(self.m21, rhs.m21),
                m31: R::div(self.m31, rhs.m31),
                m41: R::div(self.m41, rhs.m41),
                m51: R::div(self.m51, rhs.m51),
                m12: R::div(self.m12, rhs.m12),
                m22: R::div(self.m22, rhs.m22),
                m32: R::div(self.m32, rhs.m32),
                m42: R::div(self.m42, rhs.m42),
                m52: R::div(self.m52, rhs.m52),
            };
    }

    /// Component-wise minimum (infimum). Exact. Upstream: `inf`.
    #[inline(always)]
    fn inf(self: Matrix5x2<T>, other: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::min(self.m11, other.m11),
            m21: R::min(self.m21, other.m21),
            m31: R::min(self.m31, other.m31),
            m41: R::min(self.m41, other.m41),
            m51: R::min(self.m51, other.m51),
            m12: R::min(self.m12, other.m12),
            m22: R::min(self.m22, other.m22),
            m32: R::min(self.m32, other.m32),
            m42: R::min(self.m42, other.m42),
            m52: R::min(self.m52, other.m52),
        }
    }

    /// Component-wise maximum (supremum). Exact. Upstream: `sup`.
    #[inline(always)]
    fn sup(self: Matrix5x2<T>, other: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::max(self.m11, other.m11),
            m21: R::max(self.m21, other.m21),
            m31: R::max(self.m31, other.m31),
            m41: R::max(self.m41, other.m41),
            m51: R::max(self.m51, other.m51),
            m12: R::max(self.m12, other.m12),
            m22: R::max(self.m22, other.m22),
            m32: R::max(self.m32, other.m32),
            m42: R::max(self.m42, other.m42),
            m52: R::max(self.m52, other.m52),
        }
    }

    /// `(self.inf(other), self.sup(other))`. Exact. Upstream: `inf_sup`.
    #[inline(always)]
    fn inf_sup(self: Matrix5x2<T>, other: Matrix5x2<T>) -> (Matrix5x2<T>, Matrix5x2<T>) {
        (Self::inf(self, other), Self::sup(self, other))
    }

    /// `self + k` added to every component. Exact; panics on overflow. Upstream: `add_scalar`.
    #[inline(always)]
    fn add_scalar(self: Matrix5x2<T>, k: T) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: self.m11 + k,
            m21: self.m21 + k,
            m31: self.m31 + k,
            m41: self.m41 + k,
            m51: self.m51 + k,
            m12: self.m12 + k,
            m22: self.m22 + k,
            m32: self.m32 + k,
            m42: self.m42 + k,
            m52: self.m52 + k,
        }
    }

    /// `self = alpha * a ∘ b + beta * self` (component-wise product): per component `alpha * a`
    /// is floored, then the two products are ONE fused `sum_prod2` (floored once). Panics on
    /// overflow.
    /// Upstream: `cmpy` (which skips reading `self` when `beta` is zero: a difference only for NaN,
    /// which fixed point has not).
    #[inline(always)]
    fn cmpy(ref self: Matrix5x2<T>, alpha: T, a: Matrix5x2<T>, b: Matrix5x2<T>, beta: T) {
        self =
            Matrix5x2 {
                m11: R::sum_prod2(alpha * a.m11, b.m11, beta, self.m11),
                m21: R::sum_prod2(alpha * a.m21, b.m21, beta, self.m21),
                m31: R::sum_prod2(alpha * a.m31, b.m31, beta, self.m31),
                m41: R::sum_prod2(alpha * a.m41, b.m41, beta, self.m41),
                m51: R::sum_prod2(alpha * a.m51, b.m51, beta, self.m51),
                m12: R::sum_prod2(alpha * a.m12, b.m12, beta, self.m12),
                m22: R::sum_prod2(alpha * a.m22, b.m22, beta, self.m22),
                m32: R::sum_prod2(alpha * a.m32, b.m32, beta, self.m32),
                m42: R::sum_prod2(alpha * a.m42, b.m42, beta, self.m42),
                m52: R::sum_prod2(alpha * a.m52, b.m52, beta, self.m52),
            };
    }

    /// `self = alpha * a / b + beta * self` (component-wise quotient): per component `alpha * a` is
    /// floored, divided by `b` (rounded to nearest), then `beta * self + quotient` is ONE `mul_add`
    /// (floored once). Panics on a zero component of `b` and on overflow. Upstream: `cdpy`.
    #[inline(always)]
    fn cdpy(ref self: Matrix5x2<T>, alpha: T, a: Matrix5x2<T>, b: Matrix5x2<T>, beta: T) {
        self =
            Matrix5x2 {
                m11: R::mul_add(beta, self.m11, R::div(alpha * a.m11, b.m11)),
                m21: R::mul_add(beta, self.m21, R::div(alpha * a.m21, b.m21)),
                m31: R::mul_add(beta, self.m31, R::div(alpha * a.m31, b.m31)),
                m41: R::mul_add(beta, self.m41, R::div(alpha * a.m41, b.m41)),
                m51: R::mul_add(beta, self.m51, R::div(alpha * a.m51, b.m51)),
                m12: R::mul_add(beta, self.m12, R::div(alpha * a.m12, b.m12)),
                m22: R::mul_add(beta, self.m22, R::div(alpha * a.m22, b.m22)),
                m32: R::mul_add(beta, self.m32, R::div(alpha * a.m32, b.m32)),
                m42: R::mul_add(beta, self.m42, R::div(alpha * a.m42, b.m42)),
                m52: R::mul_add(beta, self.m52, R::div(alpha * a.m52, b.m52)),
            };
    }

    /// The smallest component. Exact. Upstream: `min`.
    #[inline(always)]
    fn min(self: Matrix5x2<T>) -> T {
        R::min(
            R::min(
                R::min(
                    R::min(
                        R::min(
                            R::min(
                                R::min(R::min(R::min(self.m11, self.m21), self.m31), self.m41),
                                self.m51,
                            ),
                            self.m12,
                        ),
                        self.m22,
                    ),
                    self.m32,
                ),
                self.m42,
            ),
            self.m52,
        )
    }

    /// The largest component. Exact. Upstream: `max`.
    #[inline(always)]
    fn max(self: Matrix5x2<T>) -> T {
        R::max(
            R::max(
                R::max(
                    R::max(
                        R::max(
                            R::max(
                                R::max(R::max(R::max(self.m11, self.m21), self.m31), self.m41),
                                self.m51,
                            ),
                            self.m12,
                        ),
                        self.m22,
                    ),
                    self.m32,
                ),
                self.m42,
            ),
            self.m52,
        )
    }

    /// The smallest absolute value of a component. Panics on the scalar's `MIN`. Upstream: `amin`.
    #[inline(always)]
    fn amin(self: Matrix5x2<T>) -> T {
        R::min(
            R::min(
                R::min(
                    R::min(
                        R::min(
                            R::min(
                                R::min(
                                    R::min(
                                        R::min(R::abs(self.m11), R::abs(self.m21)),
                                        R::abs(self.m31),
                                    ),
                                    R::abs(self.m41),
                                ),
                                R::abs(self.m51),
                            ),
                            R::abs(self.m12),
                        ),
                        R::abs(self.m22),
                    ),
                    R::abs(self.m32),
                ),
                R::abs(self.m42),
            ),
            R::abs(self.m52),
        )
    }

    /// The largest absolute value of a component (the uniform norm). Panics on the scalar's `MIN`.
    /// Upstream: `amax`.
    #[inline(always)]
    fn amax(self: Matrix5x2<T>) -> T {
        R::max(
            R::max(
                R::max(
                    R::max(
                        R::max(
                            R::max(
                                R::max(
                                    R::max(
                                        R::max(R::abs(self.m11), R::abs(self.m21)),
                                        R::abs(self.m31),
                                    ),
                                    R::abs(self.m41),
                                ),
                                R::abs(self.m51),
                            ),
                            R::abs(self.m12),
                        ),
                        R::abs(self.m22),
                    ),
                    R::abs(self.m32),
                ),
                R::abs(self.m42),
            ),
            R::abs(self.m52),
        )
    }

    /// `amin`: the modulus of a real scalar is its absolute value. Upstream: `camin`.
    #[inline(always)]
    fn camin(self: Matrix5x2<T>) -> T {
        R::min(
            R::min(
                R::min(
                    R::min(
                        R::min(
                            R::min(
                                R::min(
                                    R::min(
                                        R::min(R::abs(self.m11), R::abs(self.m21)),
                                        R::abs(self.m31),
                                    ),
                                    R::abs(self.m41),
                                ),
                                R::abs(self.m51),
                            ),
                            R::abs(self.m12),
                        ),
                        R::abs(self.m22),
                    ),
                    R::abs(self.m32),
                ),
                R::abs(self.m42),
            ),
            R::abs(self.m52),
        )
    }

    /// `amax`: the modulus of a real scalar is its absolute value. Upstream: `camax`.
    #[inline(always)]
    fn camax(self: Matrix5x2<T>) -> T {
        R::max(
            R::max(
                R::max(
                    R::max(
                        R::max(
                            R::max(
                                R::max(
                                    R::max(
                                        R::max(R::abs(self.m11), R::abs(self.m21)),
                                        R::abs(self.m31),
                                    ),
                                    R::abs(self.m41),
                                ),
                                R::abs(self.m51),
                            ),
                            R::abs(self.m12),
                        ),
                        R::abs(self.m22),
                    ),
                    R::abs(self.m32),
                ),
                R::abs(self.m42),
            ),
            R::abs(self.m52),
        )
    }

    /// `(row, column)` of the component with the largest absolute value, the first one in
    /// column-major order on ties. Panics on the scalar's `MIN`. Upstream: `iamax_full`.
    fn iamax_full(self: Matrix5x2<T>) -> (usize, usize) {
        let mut best: (usize, usize) = (0, 0);
        let mut m = R::abs(self.m11);
        let v = R::abs(self.m21);
        if v > m {
            m = v;
            best = (1, 0);
        }
        let v = R::abs(self.m31);
        if v > m {
            m = v;
            best = (2, 0);
        }
        let v = R::abs(self.m41);
        if v > m {
            m = v;
            best = (3, 0);
        }
        let v = R::abs(self.m51);
        if v > m {
            m = v;
            best = (4, 0);
        }
        let v = R::abs(self.m12);
        if v > m {
            m = v;
            best = (0, 1);
        }
        let v = R::abs(self.m22);
        if v > m {
            m = v;
            best = (1, 1);
        }
        let v = R::abs(self.m32);
        if v > m {
            m = v;
            best = (2, 1);
        }
        let v = R::abs(self.m42);
        if v > m {
            m = v;
            best = (3, 1);
        }
        let v = R::abs(self.m52);
        if v > m {
            best = (4, 1);
        }
        best
    }

    /// `iamax_full`: the modulus of a real scalar is its absolute value. Upstream: `icamax_full`.
    #[inline(always)]
    fn icamax_full(self: Matrix5x2<T>) -> (usize, usize) {
        Self::iamax_full(self)
    }

    /// Dot product (the sum of the component-wise products, upstream's Frobenius inner product for
    /// matrices): the exact sum is floored ONCE, only the result must fit. Upstream: `dot`.
    fn dot(self: Matrix5x2<T>, rhs: Matrix5x2<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, rhs.m11);
        let w = R::wide_add_prod(w, self.m21, rhs.m21);
        let w = R::wide_add_prod(w, self.m31, rhs.m31);
        let w = R::wide_add_prod(w, self.m41, rhs.m41);
        let w = R::wide_add_prod(w, self.m51, rhs.m51);
        let w = R::wide_add_prod(w, self.m12, rhs.m12);
        let w = R::wide_add_prod(w, self.m22, rhs.m22);
        let w = R::wide_add_prod(w, self.m32, rhs.m32);
        let w = R::wide_add_prod(w, self.m42, rhs.m42);
        R::wide_rescale(R::wide_add_prod(w, self.m52, rhs.m52))
    }

    /// Squared Euclidean (Frobenius) norm: the exact sum of squares floored once. Panics on
    /// overflow (above a norm of about 46 340 in Q32.32 only `norm` works). Upstream:
    /// `norm_squared`.
    fn norm_squared(self: Matrix5x2<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, self.m11);
        let w = R::wide_add_prod(w, self.m21, self.m21);
        let w = R::wide_add_prod(w, self.m31, self.m31);
        let w = R::wide_add_prod(w, self.m41, self.m41);
        let w = R::wide_add_prod(w, self.m51, self.m51);
        let w = R::wide_add_prod(w, self.m12, self.m12);
        let w = R::wide_add_prod(w, self.m22, self.m22);
        let w = R::wide_add_prod(w, self.m32, self.m32);
        let w = R::wide_add_prod(w, self.m42, self.m42);
        R::wide_rescale(R::wide_add_prod(w, self.m52, self.m52))
    }

    /// Euclidean (Frobenius) norm: square root of the UNSCALED exact sum of squares, floored once.
    /// No intermediate overflow: only the result must fit. Upstream: `norm`.
    fn norm(self: Matrix5x2<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11, self.m11);
        let w = R::wide_add_prod(w, self.m21, self.m21);
        let w = R::wide_add_prod(w, self.m31, self.m31);
        let w = R::wide_add_prod(w, self.m41, self.m41);
        let w = R::wide_add_prod(w, self.m51, self.m51);
        let w = R::wide_add_prod(w, self.m12, self.m12);
        let w = R::wide_add_prod(w, self.m22, self.m22);
        let w = R::wide_add_prod(w, self.m32, self.m32);
        let w = R::wide_add_prod(w, self.m42, self.m42);
        R::wide_sqrt(R::wide_add_prod(w, self.m52, self.m52))
    }

    /// Alias of `norm_squared`. Upstream: `magnitude_squared`.
    #[inline(always)]
    fn magnitude_squared(self: Matrix5x2<T>) -> T {
        Self::norm_squared(self)
    }

    /// Alias of `norm`. Upstream: `magnitude`.
    #[inline(always)]
    fn magnitude(self: Matrix5x2<T>) -> T {
        Self::norm(self)
    }

    /// `(self - rhs).norm()`: the differences are exact, then one fused norm. Panics when a
    /// difference or the result overflows. Upstream: `metric_distance`.
    fn metric_distance(self: Matrix5x2<T>, rhs: Matrix5x2<T>) -> T {
        let w = R::wide_add_prod(R::wide_zero(), self.m11 - rhs.m11, self.m11 - rhs.m11);
        let w = R::wide_add_prod(w, self.m21 - rhs.m21, self.m21 - rhs.m21);
        let w = R::wide_add_prod(w, self.m31 - rhs.m31, self.m31 - rhs.m31);
        let w = R::wide_add_prod(w, self.m41 - rhs.m41, self.m41 - rhs.m41);
        let w = R::wide_add_prod(w, self.m51 - rhs.m51, self.m51 - rhs.m51);
        let w = R::wide_add_prod(w, self.m12 - rhs.m12, self.m12 - rhs.m12);
        let w = R::wide_add_prod(w, self.m22 - rhs.m22, self.m22 - rhs.m22);
        let w = R::wide_add_prod(w, self.m32 - rhs.m32, self.m32 - rhs.m32);
        let w = R::wide_add_prod(w, self.m42 - rhs.m42, self.m42 - rhs.m42);
        R::wide_sqrt(R::wide_add_prod(w, self.m52 - rhs.m52, self.m52 - rhs.m52))
    }

    /// `self / k`, each component the correctly rounded quotient (nearest, ties to even), through 2
    /// prepared-divisor `Real::divN` call(s), bit-identical to one `Real::div` per component.
    /// Panics on a zero `k` and on overflow. Upstream: `unscale` (`self / k`).
    fn unscale(self: Matrix5x2<T>, k: T) -> Matrix5x2<T> {
        let (m11, m21, m31, m41, m51, m12, m22, m32, m42) = R::div9(
            self.m11,
            self.m21,
            self.m31,
            self.m41,
            self.m51,
            self.m12,
            self.m22,
            self.m32,
            self.m42,
            k,
        );
        let m52 = R::div(self.m52, k);
        Matrix5x2 { m11, m21, m31, m41, m51, m12, m22, m32, m42, m52 }
    }

    /// `self / self.norm()`: the floored norm, then `unscale`. Panics with a division by zero when
    /// the norm is zero, and on overflow when the norm does not fit. Upstream: `normalize`.
    fn normalize(self: Matrix5x2<T>) -> Matrix5x2<T> {
        Self::unscale(self, Self::norm(self))
    }

    /// `Some(self.normalize())`, or `None` when the norm is `<= min_norm` (never divides by zero
    /// for `min_norm >= 0`). Upstream: `try_normalize`.
    fn try_normalize(self: Matrix5x2<T>, min_norm: T) -> Option<Matrix5x2<T>> {
        let n = Self::norm(self);
        if n <= min_norm {
            None
        } else {
            Some(Self::unscale(self, n))
        }
    }

    /// `self` when its norm is `<= max`, otherwise `self.scale(max / norm)` (the ratio rounded to
    /// nearest, like upstream's `max / n`). Panics only when the norm does not fit. Upstream:
    /// `cap_magnitude`.
    fn cap_magnitude(self: Matrix5x2<T>, max: T) -> Matrix5x2<T> {
        let n = Self::norm(self);
        if n <= max {
            self
        } else {
            Self::scale(self, R::div(max, n))
        }
    }

    /// Scales `self` to the norm `magnitude` (`self.scale(magnitude / norm)`, the ratio rounded to
    /// nearest) when its norm is `> min_magnitude`, leaves it unchanged otherwise. Upstream:
    /// `try_set_magnitude` (`&mut self`).
    fn try_set_magnitude(ref self: Matrix5x2<T>, magnitude: T, min_magnitude: T) {
        let n = Self::norm(self);
        if n > min_magnitude {
            self = Self::scale(self, R::div(magnitude, n));
        }
    }

    /// The induced 1-norm: the largest absolute column sum (the L1 norm of a column vector, the
    /// largest absolute value of a row vector). Exact; panics on overflow. Upstream: `one_norm`.
    #[inline(always)]
    fn one_norm(self: Matrix5x2<T>) -> T {
        R::max(
            R::abs(self.m11)
                + R::abs(self.m21)
                + R::abs(self.m31)
                + R::abs(self.m41)
                + R::abs(self.m51),
            R::abs(self.m12)
                + R::abs(self.m22)
                + R::abs(self.m32)
                + R::abs(self.m42)
                + R::abs(self.m52),
        )
    }

    /// The conjugate transpose, a `Matrix2x5`: the transpose for a real scalar. Exact. Upstream:
    /// `adjoint`.
    #[inline(always)]
    fn adjoint(self: Matrix5x2<T>) -> Matrix2x5<T> {
        Self::transpose(self)
    }

    /// Alias of `adjoint` (deprecated upstream). Upstream: `conjugate_transpose`.
    #[inline(always)]
    fn conjugate_transpose(self: Matrix5x2<T>) -> Matrix2x5<T> {
        Self::transpose(self)
    }

    /// The component-wise conjugate: `self` for a real scalar. Upstream: `conjugate`.
    #[inline(always)]
    fn conjugate(self: Matrix5x2<T>) -> Matrix5x2<T> {
        self
    }

    /// The same shape with every component converted by `Into<T, U>`. With the single scalar of
    /// this library (`Fixed`) it is the identity; it exists for scalar-generic code. Upstream:
    /// `cast` (and `SubsetOf<Matrix<U>>`, the `nalgebra::convert` it goes through).
    fn cast<U, +Into<T, U>, +Drop<U>>(self: Matrix5x2<T>) -> Matrix5x2<U> {
        Matrix5x2 {
            m11: self.m11.into(),
            m21: self.m21.into(),
            m31: self.m31.into(),
            m41: self.m41.into(),
            m51: self.m51.into(),
            m12: self.m12.into(),
            m22: self.m22.into(),
            m32: self.m32.into(),
            m42: self.m42.into(),
            m52: self.m52.into(),
        }
    }

    /// `Some` of the shape with every component converted by `TryInto<T, U>`, `None` as soon as one
    /// conversion fails. Upstream: `try_cast`.
    fn try_cast<U, +TryInto<T, U>, +Drop<U>>(self: Matrix5x2<T>) -> Option<Matrix5x2<U>> {
        let m11: U = match self.m11.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m21: U = match self.m21.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m31: U = match self.m31.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m41: U = match self.m41.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m51: U = match self.m51.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m12: U = match self.m12.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m22: U = match self.m22.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m32: U = match self.m32.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m42: U = match self.m42.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        let m52: U = match self.m52.try_into() {
            Option::Some(v) => v,
            Option::None => { return Option::None; },
        };
        Option::Some(Matrix5x2 { m11, m21, m31, m41, m51, m12, m22, m32, m42, m52 })
    }

    /// `true` when every component is within `epsilon` ulp of `other`'s, or has the same sign and
    /// lies within `max_relative` times the larger magnitude of the two (`|a - b| <= max(|a|, |b|)
    /// · max_relative`). Panics on a component equal to the scalar's `MIN`, and on overflow of
    /// that product (only possible with `max_relative > 1`). Upstream:
    /// `approx::RelativeEq::relative_eq`, `epsilon` counted in ulp instead of a float epsilon
    /// (DESIGN D3).
    #[inline(always)]
    fn relative_eq(self: Matrix5x2<T>, other: Matrix5x2<T>, epsilon: u64, max_relative: T) -> bool {
        ApproxEqTrait::relative_eq(self.m11, other.m11, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m21, other.m21, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m31, other.m31, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m41, other.m41, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m51, other.m51, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m12, other.m12, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m22, other.m22, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m32, other.m32, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m42, other.m42, epsilon, max_relative)
            && ApproxEqTrait::relative_eq(self.m52, other.m52, epsilon, max_relative)
    }

    /// `true` when every component is within `epsilon` ulp of `other`'s, or has the same sign and
    /// lies within `max_ulps` ulp (in fixed point the distance in ulp IS the raw difference; the
    /// `max_ulps` budget does not cross zero, like upstream's float `ulps_eq`). Cannot overflow.
    /// Upstream: `approx::UlpsEq::ulps_eq`.
    #[inline(always)]
    fn ulps_eq(self: Matrix5x2<T>, other: Matrix5x2<T>, epsilon: u64, max_ulps: u32) -> bool {
        ApproxEqTrait::ulps_eq(self.m11, other.m11, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m21, other.m21, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m31, other.m31, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m41, other.m41, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m51, other.m51, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m12, other.m12, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m22, other.m22, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m32, other.m32, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m42, other.m42, epsilon, max_ulps)
            && ApproxEqTrait::ulps_eq(self.m52, other.m52, epsilon, max_ulps)
    }
}

/// The operations of `Matrix5x2<T>` that need `Transcendental` (inverse trigonometry, `exp`, `ln`):
/// a scalar may implement `Real` only.
#[generate_trait]
pub impl Matrix5x2AngleImpl<
    T,
    impl R: Real<T>,
    impl Tr: Transcendental<T>,
    +Copy<T>,
    +Drop<T>,
    +Drop<R::Wide>,
    +Add<T>,
    +Sub<T>,
    +Mul<T>,
    +Neg<T>,
    +PartialEq<T>,
    +PartialOrd<T>,
> of Matrix5x2AngleTrait<T> {
    /// The angle between `self` and `other` seen as vectors of the Frobenius inner product, in `[0,
    /// π]` (up to the rounding of `atan2`); `0` when one of them is zero. Computed as `2 *
    /// atan2(|u - v|, |u + v|)` on the normalized `u`, `v` (Kahan): unlike upstream's `acos(dot /
    /// (|a| *
    /// |b|))` it cannot overflow on long inputs and stays accurate for nearly parallel ones. Panics
    /// when a norm does not fit. Upstream: `angle`.
    fn angle(self: Matrix5x2<T>, other: Matrix5x2<T>) -> T {
        let n1 = Matrix5x2Trait::norm(self);
        let n2 = Matrix5x2Trait::norm(other);
        if n1 == R::zero() || n2 == R::zero() {
            return R::zero();
        }
        let u = Matrix5x2Trait::unscale(self, n1);
        let v = Matrix5x2Trait::unscale(other, n2);
        let half = Tr::atan2(Matrix5x2Trait::metric_distance(u, v), Matrix5x2Trait::norm(u + v));
        half + half
    }

    /// The entrywise Lp norm `(Σ |a|^p)^(1/p)`. `p = 1` is the exact sum of the absolute values
    /// and `p = 2` the fused `norm` (both exact up to their one rounding); above, the components
    /// are first divided by the largest absolute value `m` (so no power can overflow), `Σ (|a| /
    /// m)^p`
    /// is summed (`p` floored products each), and the root is `m * exp(ln(Σ) / p)`: a few ulp
    /// relative to the result, the rounding of `exp` / `ln`. Panics with `nalgebra: lp_norm needs p
    /// >= 1` for `p < 1` (upstream returns meaningless values: an infinite root for `p = 0`).
    /// Upstream: `lp_norm`.
    fn lp_norm(self: Matrix5x2<T>, p: i32) -> T {
        if p < 1 {
            core::panic_with_felt252(errors::LP_NORM_P);
        }
        if p == 1 {
            return R::abs(self.m11)
                + R::abs(self.m21)
                + R::abs(self.m31)
                + R::abs(self.m41)
                + R::abs(self.m51)
                + R::abs(self.m12)
                + R::abs(self.m22)
                + R::abs(self.m32)
                + R::abs(self.m42)
                + R::abs(self.m52);
        }
        if p == 2 {
            return Matrix5x2Trait::norm(self);
        }
        let m = Matrix5x2Trait::amax(self);
        if m == R::zero() {
            return R::zero();
        }
        let r = Matrix5x2Trait::unscale(Matrix5x2Trait::abs(self), m);
        let q: u32 = p.try_into().unwrap();
        let s = Powi::powi(r.m11, q)
            + Powi::powi(r.m21, q)
            + Powi::powi(r.m31, q)
            + Powi::powi(r.m41, q)
            + Powi::powi(r.m51, q)
            + Powi::powi(r.m12, q)
            + Powi::powi(r.m22, q)
            + Powi::powi(r.m32, q)
            + Powi::powi(r.m42, q)
            + Powi::powi(r.m52, q);
        m * Tr::exp(R::div(Tr::ln(s), R::from_int(p)))
    }
}

// --- operators -----------------------------------------------------------------------------------

/// `lhs + rhs`, component-wise. Exact; panics on overflow. Upstream: `Add`.
pub impl Matrix5x2Add<T, +Add<T>, +Copy<T>, +Drop<T>> of Add<Matrix5x2<T>> {
    #[inline(always)]
    fn add(lhs: Matrix5x2<T>, rhs: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: lhs.m11 + rhs.m11,
            m21: lhs.m21 + rhs.m21,
            m31: lhs.m31 + rhs.m31,
            m41: lhs.m41 + rhs.m41,
            m51: lhs.m51 + rhs.m51,
            m12: lhs.m12 + rhs.m12,
            m22: lhs.m22 + rhs.m22,
            m32: lhs.m32 + rhs.m32,
            m42: lhs.m42 + rhs.m42,
            m52: lhs.m52 + rhs.m52,
        }
    }
}

/// `lhs - rhs`, component-wise. Exact; panics on overflow. Upstream: `Sub`.
pub impl Matrix5x2Sub<T, +Sub<T>, +Copy<T>, +Drop<T>> of Sub<Matrix5x2<T>> {
    #[inline(always)]
    fn sub(lhs: Matrix5x2<T>, rhs: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: lhs.m11 - rhs.m11,
            m21: lhs.m21 - rhs.m21,
            m31: lhs.m31 - rhs.m31,
            m41: lhs.m41 - rhs.m41,
            m51: lhs.m51 - rhs.m51,
            m12: lhs.m12 - rhs.m12,
            m22: lhs.m22 - rhs.m22,
            m32: lhs.m32 - rhs.m32,
            m42: lhs.m42 - rhs.m42,
            m52: lhs.m52 - rhs.m52,
        }
    }
}

/// `-a`, component-wise. Exact; panics on overflow (`-MIN`). Upstream: `Neg`.
pub impl Matrix5x2Neg<T, +Neg<T>, +Copy<T>, +Drop<T>> of Neg<Matrix5x2<T>> {
    #[inline(always)]
    fn neg(a: Matrix5x2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: -a.m11,
            m21: -a.m21,
            m31: -a.m31,
            m41: -a.m41,
            m51: -a.m51,
            m12: -a.m12,
            m22: -a.m22,
            m32: -a.m32,
            m42: -a.m42,
            m52: -a.m52,
        }
    }
}

/// `self += rhs`. Exact; panics on overflow. Upstream: `AddAssign`.
pub impl Matrix5x2AddAssign<
    T, +Add<T>, +Copy<T>, +Drop<T>,
> of AddAssign<Matrix5x2<T>, Matrix5x2<T>> {
    #[inline(always)]
    fn add_assign(ref self: Matrix5x2<T>, rhs: Matrix5x2<T>) {
        self = self + rhs;
    }
}

/// `self -= rhs`. Exact; panics on overflow. Upstream: `SubAssign`.
pub impl Matrix5x2SubAssign<
    T, +Sub<T>, +Copy<T>, +Drop<T>,
> of SubAssign<Matrix5x2<T>, Matrix5x2<T>> {
    #[inline(always)]
    fn sub_assign(ref self: Matrix5x2<T>, rhs: Matrix5x2<T>) {
        self = self - rhs;
    }
}

// --- products ------------------------------------------------------------------------------------

/// `self * rhs`, a `Vector5`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Matrix5x2 * Vector2` (`Mul`).
pub impl Matrix5x2MulVector2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix5x2<T>, Vector2<T>> {
    type Output = Vector5<T>;
    fn mul_mat(self: Matrix5x2<T>, rhs: Vector2<T>) -> Vector5<T> {
        Vector5 {
            x: R::sum_prod2(self.m11, rhs.x, self.m12, rhs.y),
            y: R::sum_prod2(self.m21, rhs.x, self.m22, rhs.y),
            z: R::sum_prod2(self.m31, rhs.x, self.m32, rhs.y),
            w: R::sum_prod2(self.m41, rhs.x, self.m42, rhs.y),
            a: R::sum_prod2(self.m51, rhs.x, self.m52, rhs.y),
        }
    }
}

/// `self * rhs`, a `Matrix5x2`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Matrix5x2 * Matrix2` (`Mul`).
pub impl Matrix5x2MulMatrix2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix5x2<T>, Matrix2<T>> {
    type Output = Matrix5x2<T>;
    fn mul_mat(self: Matrix5x2<T>, rhs: Matrix2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m41: R::sum_prod2(self.m41, rhs.m11, self.m42, rhs.m21),
            m51: R::sum_prod2(self.m51, rhs.m11, self.m52, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
            m42: R::sum_prod2(self.m41, rhs.m12, self.m42, rhs.m22),
            m52: R::sum_prod2(self.m51, rhs.m12, self.m52, rhs.m22),
        }
    }
}

/// `self * rhs`, a `Matrix5x3`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Matrix5x2 * Matrix2x3` (`Mul`).
pub impl Matrix5x2MulMatrix2x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix5x2<T>, Matrix2x3<T>> {
    type Output = Matrix5x3<T>;
    fn mul_mat(self: Matrix5x2<T>, rhs: Matrix2x3<T>) -> Matrix5x3<T> {
        Matrix5x3 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m41: R::sum_prod2(self.m41, rhs.m11, self.m42, rhs.m21),
            m51: R::sum_prod2(self.m51, rhs.m11, self.m52, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
            m42: R::sum_prod2(self.m41, rhs.m12, self.m42, rhs.m22),
            m52: R::sum_prod2(self.m51, rhs.m12, self.m52, rhs.m22),
            m13: R::sum_prod2(self.m11, rhs.m13, self.m12, rhs.m23),
            m23: R::sum_prod2(self.m21, rhs.m13, self.m22, rhs.m23),
            m33: R::sum_prod2(self.m31, rhs.m13, self.m32, rhs.m23),
            m43: R::sum_prod2(self.m41, rhs.m13, self.m42, rhs.m23),
            m53: R::sum_prod2(self.m51, rhs.m13, self.m52, rhs.m23),
        }
    }
}

/// `self * rhs`, a `Matrix5x4`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Matrix5x2 * Matrix2x4` (`Mul`).
pub impl Matrix5x2MulMatrix2x4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix5x2<T>, Matrix2x4<T>> {
    type Output = Matrix5x4<T>;
    fn mul_mat(self: Matrix5x2<T>, rhs: Matrix2x4<T>) -> Matrix5x4<T> {
        Matrix5x4 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m41: R::sum_prod2(self.m41, rhs.m11, self.m42, rhs.m21),
            m51: R::sum_prod2(self.m51, rhs.m11, self.m52, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
            m42: R::sum_prod2(self.m41, rhs.m12, self.m42, rhs.m22),
            m52: R::sum_prod2(self.m51, rhs.m12, self.m52, rhs.m22),
            m13: R::sum_prod2(self.m11, rhs.m13, self.m12, rhs.m23),
            m23: R::sum_prod2(self.m21, rhs.m13, self.m22, rhs.m23),
            m33: R::sum_prod2(self.m31, rhs.m13, self.m32, rhs.m23),
            m43: R::sum_prod2(self.m41, rhs.m13, self.m42, rhs.m23),
            m53: R::sum_prod2(self.m51, rhs.m13, self.m52, rhs.m23),
            m14: R::sum_prod2(self.m11, rhs.m14, self.m12, rhs.m24),
            m24: R::sum_prod2(self.m21, rhs.m14, self.m22, rhs.m24),
            m34: R::sum_prod2(self.m31, rhs.m14, self.m32, rhs.m24),
            m44: R::sum_prod2(self.m41, rhs.m14, self.m42, rhs.m24),
            m54: R::sum_prod2(self.m51, rhs.m14, self.m52, rhs.m24),
        }
    }
}

/// `self * rhs`, a `Matrix5`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Matrix5x2 * Matrix2x5` (`Mul`).
pub impl Matrix5x2MulMatrix2x5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix5x2<T>, Matrix2x5<T>> {
    type Output = Matrix5<T>;
    fn mul_mat(self: Matrix5x2<T>, rhs: Matrix2x5<T>) -> Matrix5<T> {
        Matrix5 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m41: R::sum_prod2(self.m41, rhs.m11, self.m42, rhs.m21),
            m51: R::sum_prod2(self.m51, rhs.m11, self.m52, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
            m42: R::sum_prod2(self.m41, rhs.m12, self.m42, rhs.m22),
            m52: R::sum_prod2(self.m51, rhs.m12, self.m52, rhs.m22),
            m13: R::sum_prod2(self.m11, rhs.m13, self.m12, rhs.m23),
            m23: R::sum_prod2(self.m21, rhs.m13, self.m22, rhs.m23),
            m33: R::sum_prod2(self.m31, rhs.m13, self.m32, rhs.m23),
            m43: R::sum_prod2(self.m41, rhs.m13, self.m42, rhs.m23),
            m53: R::sum_prod2(self.m51, rhs.m13, self.m52, rhs.m23),
            m14: R::sum_prod2(self.m11, rhs.m14, self.m12, rhs.m24),
            m24: R::sum_prod2(self.m21, rhs.m14, self.m22, rhs.m24),
            m34: R::sum_prod2(self.m31, rhs.m14, self.m32, rhs.m24),
            m44: R::sum_prod2(self.m41, rhs.m14, self.m42, rhs.m24),
            m54: R::sum_prod2(self.m51, rhs.m14, self.m52, rhs.m24),
            m15: R::sum_prod2(self.m11, rhs.m15, self.m12, rhs.m25),
            m25: R::sum_prod2(self.m21, rhs.m15, self.m22, rhs.m25),
            m35: R::sum_prod2(self.m31, rhs.m15, self.m32, rhs.m25),
            m45: R::sum_prod2(self.m41, rhs.m15, self.m42, rhs.m25),
            m55: R::sum_prod2(self.m51, rhs.m15, self.m52, rhs.m25),
        }
    }
}

/// `self * rhs`, a `Matrix5x6`: one `sum_prod2` per component (one rounding each). Panics on
/// overflow. Upstream: `Matrix5x2 * Matrix2x6` (`Mul`).
pub impl Matrix5x2MulMatrix2x6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixMul<Matrix5x2<T>, Matrix2x6<T>> {
    type Output = Matrix5x6<T>;
    fn mul_mat(self: Matrix5x2<T>, rhs: Matrix2x6<T>) -> Matrix5x6<T> {
        Matrix5x6 {
            m11: R::sum_prod2(self.m11, rhs.m11, self.m12, rhs.m21),
            m21: R::sum_prod2(self.m21, rhs.m11, self.m22, rhs.m21),
            m31: R::sum_prod2(self.m31, rhs.m11, self.m32, rhs.m21),
            m41: R::sum_prod2(self.m41, rhs.m11, self.m42, rhs.m21),
            m51: R::sum_prod2(self.m51, rhs.m11, self.m52, rhs.m21),
            m12: R::sum_prod2(self.m11, rhs.m12, self.m12, rhs.m22),
            m22: R::sum_prod2(self.m21, rhs.m12, self.m22, rhs.m22),
            m32: R::sum_prod2(self.m31, rhs.m12, self.m32, rhs.m22),
            m42: R::sum_prod2(self.m41, rhs.m12, self.m42, rhs.m22),
            m52: R::sum_prod2(self.m51, rhs.m12, self.m52, rhs.m22),
            m13: R::sum_prod2(self.m11, rhs.m13, self.m12, rhs.m23),
            m23: R::sum_prod2(self.m21, rhs.m13, self.m22, rhs.m23),
            m33: R::sum_prod2(self.m31, rhs.m13, self.m32, rhs.m23),
            m43: R::sum_prod2(self.m41, rhs.m13, self.m42, rhs.m23),
            m53: R::sum_prod2(self.m51, rhs.m13, self.m52, rhs.m23),
            m14: R::sum_prod2(self.m11, rhs.m14, self.m12, rhs.m24),
            m24: R::sum_prod2(self.m21, rhs.m14, self.m22, rhs.m24),
            m34: R::sum_prod2(self.m31, rhs.m14, self.m32, rhs.m24),
            m44: R::sum_prod2(self.m41, rhs.m14, self.m42, rhs.m24),
            m54: R::sum_prod2(self.m51, rhs.m14, self.m52, rhs.m24),
            m15: R::sum_prod2(self.m11, rhs.m15, self.m12, rhs.m25),
            m25: R::sum_prod2(self.m21, rhs.m15, self.m22, rhs.m25),
            m35: R::sum_prod2(self.m31, rhs.m15, self.m32, rhs.m25),
            m45: R::sum_prod2(self.m41, rhs.m15, self.m42, rhs.m25),
            m55: R::sum_prod2(self.m51, rhs.m15, self.m52, rhs.m25),
            m16: R::sum_prod2(self.m11, rhs.m16, self.m12, rhs.m26),
            m26: R::sum_prod2(self.m21, rhs.m16, self.m22, rhs.m26),
            m36: R::sum_prod2(self.m31, rhs.m16, self.m32, rhs.m26),
            m46: R::sum_prod2(self.m41, rhs.m16, self.m42, rhs.m26),
            m56: R::sum_prod2(self.m51, rhs.m16, self.m52, rhs.m26),
        }
    }
}

/// `selfᵀ * rhs`, a `Vector2`: `mul_mat` of the transposed components, so the 5 products of each
/// component accumulated exactly in `Real::Wide` and rescaled once; bit-identical to
/// `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only relabels values). Panics on
/// overflow. Upstream: `tr_mul`.
pub impl Matrix5x2TrMulVector5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix5x2<T>, Vector5<T>> {
    type Output = Vector2<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix5x2<T>, rhs: Vector5<T>) -> Vector2<T> {
        MatrixMul::mul_mat(
            Matrix2x5 {
                m11: self.m11,
                m21: self.m12,
                m12: self.m21,
                m22: self.m22,
                m13: self.m31,
                m23: self.m32,
                m14: self.m41,
                m24: self.m42,
                m15: self.m51,
                m25: self.m52,
            },
            rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix2`: `mul_mat` of the transposed components, so the 5 products of each
/// component accumulated exactly in `Real::Wide` and rescaled once; bit-identical to
/// `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only relabels values). Panics on
/// overflow. Upstream: `tr_mul`.
pub impl Matrix5x2TrMulMatrix5x2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix5x2<T>, Matrix5x2<T>> {
    type Output = Matrix2<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix5x2<T>, rhs: Matrix5x2<T>) -> Matrix2<T> {
        MatrixMul::mul_mat(
            Matrix2x5 {
                m11: self.m11,
                m21: self.m12,
                m12: self.m21,
                m22: self.m22,
                m13: self.m31,
                m23: self.m32,
                m14: self.m41,
                m24: self.m42,
                m15: self.m51,
                m25: self.m52,
            },
            rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix2x3`: `mul_mat` of the transposed components, so the 5 products of
/// each component accumulated exactly in `Real::Wide` and rescaled once; bit-identical to
/// `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only relabels values). Panics on
/// overflow. Upstream: `tr_mul`.
pub impl Matrix5x2TrMulMatrix5x3<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix5x2<T>, Matrix5x3<T>> {
    type Output = Matrix2x3<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix5x2<T>, rhs: Matrix5x3<T>) -> Matrix2x3<T> {
        MatrixMul::mul_mat(
            Matrix2x5 {
                m11: self.m11,
                m21: self.m12,
                m12: self.m21,
                m22: self.m22,
                m13: self.m31,
                m23: self.m32,
                m14: self.m41,
                m24: self.m42,
                m15: self.m51,
                m25: self.m52,
            },
            rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix2x4`: `mul_mat` of the transposed components, so the 5 products of
/// each component accumulated exactly in `Real::Wide` and rescaled once; bit-identical to
/// `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only relabels values). Panics on
/// overflow. Upstream: `tr_mul`.
pub impl Matrix5x2TrMulMatrix5x4<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix5x2<T>, Matrix5x4<T>> {
    type Output = Matrix2x4<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix5x2<T>, rhs: Matrix5x4<T>) -> Matrix2x4<T> {
        MatrixMul::mul_mat(
            Matrix2x5 {
                m11: self.m11,
                m21: self.m12,
                m12: self.m21,
                m22: self.m22,
                m13: self.m31,
                m23: self.m32,
                m14: self.m41,
                m24: self.m42,
                m15: self.m51,
                m25: self.m52,
            },
            rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix2x5`: `mul_mat` of the transposed components, so the 5 products of
/// each component accumulated exactly in `Real::Wide` and rescaled once; bit-identical to
/// `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only relabels values). Panics on
/// overflow. Upstream: `tr_mul`.
pub impl Matrix5x2TrMulMatrix5<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix5x2<T>, Matrix5<T>> {
    type Output = Matrix2x5<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix5x2<T>, rhs: Matrix5<T>) -> Matrix2x5<T> {
        MatrixMul::mul_mat(
            Matrix2x5 {
                m11: self.m11,
                m21: self.m12,
                m12: self.m21,
                m22: self.m22,
                m13: self.m31,
                m23: self.m32,
                m14: self.m41,
                m24: self.m42,
                m15: self.m51,
                m25: self.m52,
            },
            rhs,
        )
    }
}

/// `selfᵀ * rhs`, a `Matrix2x6`: `mul_mat` of the transposed components, so the 5 products of
/// each component accumulated exactly in `Real::Wide` and rescaled once; bit-identical to
/// `self.transpose().mul_mat(rhs)`, at the same gas (the transpose only relabels values). Panics on
/// overflow. Upstream: `tr_mul`.
pub impl Matrix5x2TrMulMatrix5x6<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>, +Drop<R::Wide>,
> of MatrixTrMul<Matrix5x2<T>, Matrix5x6<T>> {
    type Output = Matrix2x6<T>;
    #[inline(always)]
    fn tr_mul(self: Matrix5x2<T>, rhs: Matrix5x6<T>) -> Matrix2x6<T> {
        MatrixMul::mul_mat(
            Matrix2x5 {
                m11: self.m11,
                m21: self.m12,
                m12: self.m21,
                m22: self.m22,
                m13: self.m31,
                m23: self.m32,
                m14: self.m41,
                m24: self.m42,
                m15: self.m51,
                m25: self.m52,
            },
            rhs,
        )
    }
}

// --- indexing, comparisons, conversions ----------------------------------------------------------

/// `m.get(i)` / `m.index(..)`: the component `index` in column-major (storage) order. `get` is
/// `None` out of bounds, `index` panics with `nalgebra: index out of bounds`. Upstream:
/// `Matrix::get` / `Matrix::index` (their `MatrixIndex` argument).
pub impl Matrix5x2MatrixIndexLinear<T, +Copy<T>, +Drop<T>> of MatrixIndex<Matrix5x2<T>, usize> {
    type Output = T;
    #[inline(always)]
    fn get(self: Matrix5x2<T>, index: usize) -> Option<T> {
        match index {
            0 => Option::Some(self.m11),
            1 => Option::Some(self.m21),
            2 => Option::Some(self.m31),
            3 => Option::Some(self.m41),
            4 => Option::Some(self.m51),
            5 => Option::Some(self.m12),
            6 => Option::Some(self.m22),
            7 => Option::Some(self.m32),
            8 => Option::Some(self.m42),
            9 => Option::Some(self.m52),
            _ => Option::None,
        }
    }
    #[inline(always)]
    fn index(self: Matrix5x2<T>, index: usize) -> T {
        match index {
            0 => self.m11,
            1 => self.m21,
            2 => self.m31,
            3 => self.m41,
            4 => self.m51,
            5 => self.m12,
            6 => self.m22,
            7 => self.m32,
            8 => self.m42,
            9 => self.m52,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[i]`: the component `index` in column-major (storage) order. Panics with `nalgebra: index out
/// of bounds`. Upstream: `Index<usize>`.
pub impl Matrix5x2IndexLinear<T, +Copy<T>, +Drop<T>> of IndexView<Matrix5x2<T>, usize> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix5x2<T>, index: usize) -> T {
        match index {
            0 => *self.m11,
            1 => *self.m21,
            2 => *self.m31,
            3 => *self.m41,
            4 => *self.m51,
            5 => *self.m12,
            6 => *self.m22,
            7 => *self.m32,
            8 => *self.m42,
            9 => *self.m52,
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m.get((i, j))` / `m.index(..)`: the component at `(row, column)`. `get` is `None` out of
/// bounds, `index` panics with `nalgebra: index out of bounds`. Upstream: `Matrix::get` /
/// `Matrix::index` (their `MatrixIndex` argument).
pub impl Matrix5x2MatrixIndexPair<
    T, +Copy<T>, +Drop<T>,
> of MatrixIndex<Matrix5x2<T>, (usize, usize)> {
    type Output = T;
    #[inline(always)]
    fn get(self: Matrix5x2<T>, index: (usize, usize)) -> Option<T> {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => Option::Some(self.m11),
                1 => Option::Some(self.m21),
                2 => Option::Some(self.m31),
                3 => Option::Some(self.m41),
                4 => Option::Some(self.m51),
                _ => Option::None,
            },
            1 => match i {
                0 => Option::Some(self.m12),
                1 => Option::Some(self.m22),
                2 => Option::Some(self.m32),
                3 => Option::Some(self.m42),
                4 => Option::Some(self.m52),
                _ => Option::None,
            },
            _ => Option::None,
        }
    }
    #[inline(always)]
    fn index(self: Matrix5x2<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => self.m11,
                1 => self.m21,
                2 => self.m31,
                3 => self.m41,
                4 => self.m51,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => self.m12,
                1 => self.m22,
                2 => self.m32,
                3 => self.m42,
                4 => self.m52,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// `m[(i, j)]`: the component at `(row, column)`. Panics with `nalgebra: index out of bounds`.
/// Upstream: `Index<(usize, usize)>`.
pub impl Matrix5x2IndexPair<T, +Copy<T>, +Drop<T>> of IndexView<Matrix5x2<T>, (usize, usize)> {
    type Target = T;
    #[inline(always)]
    fn index(self: @Matrix5x2<T>, index: (usize, usize)) -> T {
        let (i, j) = index;
        match j {
            0 => match i {
                0 => *self.m11,
                1 => *self.m21,
                2 => *self.m31,
                3 => *self.m41,
                4 => *self.m51,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            1 => match i {
                0 => *self.m12,
                1 => *self.m22,
                2 => *self.m32,
                3 => *self.m42,
                4 => *self.m52,
                _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
            },
            _ => core::panic_with_felt252(errors::INDEX_OUT_OF_BOUNDS),
        }
    }
}

/// The component-wise partial order: `a < b` when EVERY component of `a` is smaller than `b`'s
/// (likewise `<=`, `>`, `>=`), so two matrices may be unordered (`!(a < b) && !(a >= b)`).
/// Upstream: `PartialOrd for Matrix`.
pub impl Matrix5x2PartialOrd<T, +PartialOrd<T>, +Copy<T>, +Drop<T>> of PartialOrd<Matrix5x2<T>> {
    #[inline(always)]
    fn lt(lhs: Matrix5x2<T>, rhs: Matrix5x2<T>) -> bool {
        lhs.m11 < rhs.m11
            && lhs.m21 < rhs.m21
            && lhs.m31 < rhs.m31
            && lhs.m41 < rhs.m41
            && lhs.m51 < rhs.m51
            && lhs.m12 < rhs.m12
            && lhs.m22 < rhs.m22
            && lhs.m32 < rhs.m32
            && lhs.m42 < rhs.m42
            && lhs.m52 < rhs.m52
    }
    #[inline(always)]
    fn le(lhs: Matrix5x2<T>, rhs: Matrix5x2<T>) -> bool {
        lhs.m11 <= rhs.m11
            && lhs.m21 <= rhs.m21
            && lhs.m31 <= rhs.m31
            && lhs.m41 <= rhs.m41
            && lhs.m51 <= rhs.m51
            && lhs.m12 <= rhs.m12
            && lhs.m22 <= rhs.m22
            && lhs.m32 <= rhs.m32
            && lhs.m42 <= rhs.m42
            && lhs.m52 <= rhs.m52
    }
    #[inline(always)]
    fn gt(lhs: Matrix5x2<T>, rhs: Matrix5x2<T>) -> bool {
        lhs.m11 > rhs.m11
            && lhs.m21 > rhs.m21
            && lhs.m31 > rhs.m31
            && lhs.m41 > rhs.m41
            && lhs.m51 > rhs.m51
            && lhs.m12 > rhs.m12
            && lhs.m22 > rhs.m22
            && lhs.m32 > rhs.m32
            && lhs.m42 > rhs.m42
            && lhs.m52 > rhs.m52
    }
    #[inline(always)]
    fn ge(lhs: Matrix5x2<T>, rhs: Matrix5x2<T>) -> bool {
        lhs.m11 >= rhs.m11
            && lhs.m21 >= rhs.m21
            && lhs.m31 >= rhs.m31
            && lhs.m41 >= rhs.m41
            && lhs.m51 >= rhs.m51
            && lhs.m12 >= rhs.m12
            && lhs.m22 >= rhs.m22
            && lhs.m32 >= rhs.m32
            && lhs.m42 >= rhs.m42
            && lhs.m52 >= rhs.m52
    }
}

/// The component-wise bounds: every component `Bounded::<T>::MIN` / `MAX`. Upstream: `num::Bounded
/// for Matrix`.
pub impl Matrix5x2Bounded<T, +Bounded<T>, +Drop<T>> of Bounded<Matrix5x2<T>> {
    const MIN: Matrix5x2<T> = Matrix5x2 {
        m11: Bounded::<T>::MIN,
        m21: Bounded::<T>::MIN,
        m31: Bounded::<T>::MIN,
        m41: Bounded::<T>::MIN,
        m51: Bounded::<T>::MIN,
        m12: Bounded::<T>::MIN,
        m22: Bounded::<T>::MIN,
        m32: Bounded::<T>::MIN,
        m42: Bounded::<T>::MIN,
        m52: Bounded::<T>::MIN,
    };
    const MAX: Matrix5x2<T> = Matrix5x2 {
        m11: Bounded::<T>::MAX,
        m21: Bounded::<T>::MAX,
        m31: Bounded::<T>::MAX,
        m41: Bounded::<T>::MAX,
        m51: Bounded::<T>::MAX,
        m12: Bounded::<T>::MAX,
        m22: Bounded::<T>::MAX,
        m32: Bounded::<T>::MAX,
        m42: Bounded::<T>::MAX,
        m52: Bounded::<T>::MAX,
    };
}

/// The 5x2 matrix of the given COLUMNS (`[[m11, m21, ..], [m12, ..], ..]`). Upstream: `From<[[T;
/// R]; C]>`.
pub impl Matrix5x2FromColumnArrays<T, +Drop<T>> of Into<[[T; 5]; 2], Matrix5x2<T>> {
    #[inline(always)]
    fn into(self: [[T; 5]; 2]) -> Matrix5x2<T> {
        let [c0, c1] = self;
        let [m11, m21, m31, m41, m51] = c0;
        let [m12, m22, m32, m42, m52] = c1;
        Matrix5x2 { m11, m21, m31, m41, m51, m12, m22, m32, m42, m52 }
    }
}

/// The columns of the 5x2 matrix as nested arrays (`[[m11, m21, ..], [m12, ..], ..]`). Upstream:
/// `Into<[[T; R]; C]>`.
pub impl Matrix5x2IntoColumnArrays<T, +Drop<T>> of Into<Matrix5x2<T>, [[T; 5]; 2]> {
    #[inline(always)]
    fn into(self: Matrix5x2<T>) -> [[T; 5]; 2] {
        let Matrix5x2 { m11, m21, m31, m41, m51, m12, m22, m32, m42, m52 } = self;
        [[m11, m21, m31, m41, m51], [m12, m22, m32, m42, m52]]
    }
}

/// `self *= k` for a scalar `k`: `scale` in place, each component floored once. Panics on overflow.
/// Upstream: `MulAssign<T>`.
pub impl Matrix5x2MulAssignScalar<T, +Mul<T>, +Copy<T>, +Drop<T>> of MulAssign<Matrix5x2<T>, T> {
    #[inline(always)]
    fn mul_assign(ref self: Matrix5x2<T>, rhs: T) {
        self =
            Matrix5x2 {
                m11: self.m11 * rhs,
                m21: self.m21 * rhs,
                m31: self.m31 * rhs,
                m41: self.m41 * rhs,
                m51: self.m51 * rhs,
                m12: self.m12 * rhs,
                m22: self.m22 * rhs,
                m32: self.m32 * rhs,
                m42: self.m42 * rhs,
                m52: self.m52 * rhs,
            };
    }
}

/// `self /= k` for a scalar `k`: `unscale` in place, each component correctly rounded. Panics on a
/// zero `k` and on overflow. Upstream: `DivAssign<T>`.
pub impl Matrix5x2DivAssignScalar<
    T, impl R: Real<T>, +Copy<T>, +Drop<T>,
> of DivAssign<Matrix5x2<T>, T> {
    fn div_assign(ref self: Matrix5x2<T>, rhs: T) {
        let (m11, m21, m31, m41, m51, m12, m22, m32, m42) = R::div9(
            self.m11,
            self.m21,
            self.m31,
            self.m41,
            self.m51,
            self.m12,
            self.m22,
            self.m32,
            self.m42,
            rhs,
        );
        let m52 = R::div(self.m52, rhs);
        self = Matrix5x2 { m11, m21, m31, m41, m51, m12, m22, m32, m42, m52 };
    }
}

/// `self * r`, a `Matrix5x2`: the product with the rotation matrix of `r`, each component one fused
/// `sum_prod2` (floored once). Panics on overflow. Upstream: `Mul<Rotation2> for Matrix` (`m * r`).
pub impl Matrix5x2MulRotation2<
    T, impl R: Real<T>, +Mul<T>, +Copy<T>, +Drop<T>,
> of MatrixMul<Matrix5x2<T>, Rotation2<T>> {
    type Output = Matrix5x2<T>;

    fn mul_mat(self: Matrix5x2<T>, rhs: Rotation2<T>) -> Matrix5x2<T> {
        Matrix5x2 {
            m11: R::sum_prod2(self.m11, rhs.matrix.m11, self.m12, rhs.matrix.m21),
            m21: R::sum_prod2(self.m21, rhs.matrix.m11, self.m22, rhs.matrix.m21),
            m31: R::sum_prod2(self.m31, rhs.matrix.m11, self.m32, rhs.matrix.m21),
            m41: R::sum_prod2(self.m41, rhs.matrix.m11, self.m42, rhs.matrix.m21),
            m51: R::sum_prod2(self.m51, rhs.matrix.m11, self.m52, rhs.matrix.m21),
            m12: R::sum_prod2(self.m11, rhs.matrix.m12, self.m12, rhs.matrix.m22),
            m22: R::sum_prod2(self.m21, rhs.matrix.m12, self.m22, rhs.matrix.m22),
            m32: R::sum_prod2(self.m31, rhs.matrix.m12, self.m32, rhs.matrix.m22),
            m42: R::sum_prod2(self.m41, rhs.matrix.m12, self.m42, rhs.matrix.m22),
            m52: R::sum_prod2(self.m51, rhs.matrix.m12, self.m52, rhs.matrix.m22),
        }
    }
}
