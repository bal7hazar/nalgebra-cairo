// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `FullPivLu6` / `Matrix6FullPivLuTrait` through the public API (WP 8.5-P15): oracle vectors
//! (`tools/oracle`
//! suite `pivot`), the factor identities, the rank-revealing property, gas benchmarks.

use core::cmp::{max, min};
use nalgebra::linalg::{FullPivLu6Trait, Matrix6FullPivLuTrait, PermuteColumns, PermuteRows};
use nalgebra::{Matrix6, Matrix6Trait, MatrixMul};
use nalgebra_testing::black_box;
use nalgebra_tests_utils::{Perm6PartialEq, abs_raw, excess, fx, oracle_tol, ulp_diff};
use crate::builders::{amax_6x6, mat6x6, max_ulp_6x6, vec6};
use crate::oracle_pivot as oracle;

const ONE: i64 = 0x100000000;

/// `full_piv_lu6` (oracle): the packed factors within the oracle tolerance, the row and
/// column permutations EXACTLY upstream's (same pivots), `P A Q = L U` within the measured bound.
#[test]
fn test_oracle_full_piv_lu6() {
    let mut cases = oracle::full_piv_lu6_cases();
    let (mut ex, mut rec) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, elu, ep, eq, tol) = *case;
        let a = black_box(mat6x6(a));
        let f = a.full_piv_lu();
        let (p, l, u, q) = f.unpack();
        assert!(p == f.p() && q == f.q() && l == f.l() && u == f.u() && f.lu_internal() == f.lu);
        let e = mat6x6(elu);
        ex = max(ex, excess(ulp_diff(f.lu.m11, e.m11), oracle_tol(abs_raw(e.m11), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m12, e.m12), oracle_tol(abs_raw(e.m12), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m13, e.m13), oracle_tol(abs_raw(e.m13), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m14, e.m14), oracle_tol(abs_raw(e.m14), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m15, e.m15), oracle_tol(abs_raw(e.m15), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m16, e.m16), oracle_tol(abs_raw(e.m16), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m21, e.m21), oracle_tol(abs_raw(e.m21), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m22, e.m22), oracle_tol(abs_raw(e.m22), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m23, e.m23), oracle_tol(abs_raw(e.m23), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m24, e.m24), oracle_tol(abs_raw(e.m24), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m25, e.m25), oracle_tol(abs_raw(e.m25), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m26, e.m26), oracle_tol(abs_raw(e.m26), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m31, e.m31), oracle_tol(abs_raw(e.m31), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m32, e.m32), oracle_tol(abs_raw(e.m32), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m33, e.m33), oracle_tol(abs_raw(e.m33), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m34, e.m34), oracle_tol(abs_raw(e.m34), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m35, e.m35), oracle_tol(abs_raw(e.m35), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m36, e.m36), oracle_tol(abs_raw(e.m36), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m41, e.m41), oracle_tol(abs_raw(e.m41), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m42, e.m42), oracle_tol(abs_raw(e.m42), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m43, e.m43), oracle_tol(abs_raw(e.m43), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m44, e.m44), oracle_tol(abs_raw(e.m44), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m45, e.m45), oracle_tol(abs_raw(e.m45), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m46, e.m46), oracle_tol(abs_raw(e.m46), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m51, e.m51), oracle_tol(abs_raw(e.m51), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m52, e.m52), oracle_tol(abs_raw(e.m52), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m53, e.m53), oracle_tol(abs_raw(e.m53), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m54, e.m54), oracle_tol(abs_raw(e.m54), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m55, e.m55), oracle_tol(abs_raw(e.m55), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m56, e.m56), oracle_tol(abs_raw(e.m56), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m61, e.m61), oracle_tol(abs_raw(e.m61), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m62, e.m62), oracle_tol(abs_raw(e.m62), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m63, e.m63), oracle_tol(abs_raw(e.m63), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m64, e.m64), oracle_tol(abs_raw(e.m64), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m65, e.m65), oracle_tol(abs_raw(e.m65), tol)));
        ex = max(ex, excess(ulp_diff(f.lu.m66, e.m66), oracle_tol(abs_raw(e.m66), tol)));
        let mut pv = vec6((0 * ONE, 1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE));
        p.permute_rows(ref pv);
        assert!(pv == vec6(ep), "row pivots");
        let mut qv = vec6((0 * ONE, 1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE));
        q.permute_rows(ref qv);
        assert!(qv == vec6(eq), "column pivots");
        let mut pa = a;
        p.permute_rows(ref pa);
        q.permute_columns(ref pa);
        rec = max(rec, max_ulp_6x6(l.mul_mat(u), pa) / amax_6x6(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 0, "measured {}", rec);
}

/// `full_piv_lu6_rank` (oracle): on EXACTLY rank-deficient matrices of rank `k`, the first `k`
/// pivots are the large ones and the others are rounding noise (rank-revealing), `P A Q = L U`.
#[test]
fn test_oracle_full_piv_lu6_rank() {
    let mut cases = oracle::full_piv_lu6_rank_cases();
    let (mut lo, mut hi, mut rec) = (0xffffffffffffffff_u128, 0_u128, 0);
    while let Some(case) = cases.pop_front() {
        let (a, k, _) = *case;
        let a = black_box(mat6x6(a));
        let f = a.full_piv_lu();
        let d = array![
            abs_raw(f.lu.m11), abs_raw(f.lu.m22), abs_raw(f.lu.m33), abs_raw(f.lu.m44),
            abs_raw(f.lu.m55), abs_raw(f.lu.m66),
        ];
        let k: u32 = (k / ONE).try_into().unwrap();
        let mut i: u32 = 0;
        let mut ds = d.span();
        while let Some(x) = ds.pop_front() {
            if i < k {
                lo = min(lo, *x);
            } else {
                hi = max(hi, *x);
            }
            i += 1;
        }
        let mut pa = a;
        f.p.permute_rows(ref pa);
        f.q.permute_columns(ref pa);
        rec = max(rec, max_ulp_6x6(f.l().mul_mat(f.u()), pa));
    }
    assert!(hi <= 0 && lo >= 1152921504606846976 && rec <= 0, "measured {} {} {}", hi, lo, rec);
}

/// `full_piv_lu6_solve` (oracle), and `solve_mut` agrees (vector and matrix right-hand sides).
#[test]
fn test_oracle_full_piv_lu6_solve() {
    let mut cases = oracle::full_piv_lu6_solve_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, b, e, tol) = *case;
        let f = black_box(mat6x6(a)).full_piv_lu();
        assert!(f.is_invertible());
        let x = f.solve(vec6(b)).unwrap();
        let e = vec6(e);
        ex = max(ex, excess(ulp_diff(x.x, e.x), oracle_tol(abs_raw(e.x), tol)));
        ex = max(ex, excess(ulp_diff(x.y, e.y), oracle_tol(abs_raw(e.y), tol)));
        ex = max(ex, excess(ulp_diff(x.z, e.z), oracle_tol(abs_raw(e.z), tol)));
        ex = max(ex, excess(ulp_diff(x.w, e.w), oracle_tol(abs_raw(e.w), tol)));
        ex = max(ex, excess(ulp_diff(x.a, e.a), oracle_tol(abs_raw(e.a), tol)));
        ex = max(ex, excess(ulp_diff(x.b, e.b), oracle_tol(abs_raw(e.b), tol)));
        let mut y = vec6(b);
        assert!(f.solve_mut(ref y));
        assert!(y == x);
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `full_piv_lu6_solve_near_singular` (oracle, FLAGGED: loose tolerance).
#[test]
fn test_oracle_full_piv_lu6_solve_near_singular() {
    let mut cases = oracle::full_piv_lu6_solve_near_singular_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, b, e, tol) = *case;
        let x = black_box(mat6x6(a)).full_piv_lu().solve(vec6(b)).unwrap();
        let e = vec6(e);
        ex = max(ex, excess(ulp_diff(x.x, e.x), oracle_tol(abs_raw(e.x), tol)));
        ex = max(ex, excess(ulp_diff(x.y, e.y), oracle_tol(abs_raw(e.y), tol)));
        ex = max(ex, excess(ulp_diff(x.z, e.z), oracle_tol(abs_raw(e.z), tol)));
        ex = max(ex, excess(ulp_diff(x.w, e.w), oracle_tol(abs_raw(e.w), tol)));
        ex = max(ex, excess(ulp_diff(x.a, e.a), oracle_tol(abs_raw(e.a), tol)));
        ex = max(ex, excess(ulp_diff(x.b, e.b), oracle_tol(abs_raw(e.b), tol)));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `full_piv_lu6_inverse` (oracle); `solve_mut` on the identity is bit-identical (the static
/// unit columns only drop exact zeros).
#[test]
fn test_oracle_full_piv_lu6_inverse() {
    let mut cases = oracle::full_piv_lu6_inverse_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, e, tol) = *case;
        let f = black_box(mat6x6(a)).full_piv_lu();
        let x = f.try_inverse().unwrap();
        let e = mat6x6(e);
        ex = max(ex, excess(ulp_diff(x.m11, e.m11), oracle_tol(abs_raw(e.m11), tol)));
        ex = max(ex, excess(ulp_diff(x.m12, e.m12), oracle_tol(abs_raw(e.m12), tol)));
        ex = max(ex, excess(ulp_diff(x.m13, e.m13), oracle_tol(abs_raw(e.m13), tol)));
        ex = max(ex, excess(ulp_diff(x.m14, e.m14), oracle_tol(abs_raw(e.m14), tol)));
        ex = max(ex, excess(ulp_diff(x.m15, e.m15), oracle_tol(abs_raw(e.m15), tol)));
        ex = max(ex, excess(ulp_diff(x.m16, e.m16), oracle_tol(abs_raw(e.m16), tol)));
        ex = max(ex, excess(ulp_diff(x.m21, e.m21), oracle_tol(abs_raw(e.m21), tol)));
        ex = max(ex, excess(ulp_diff(x.m22, e.m22), oracle_tol(abs_raw(e.m22), tol)));
        ex = max(ex, excess(ulp_diff(x.m23, e.m23), oracle_tol(abs_raw(e.m23), tol)));
        ex = max(ex, excess(ulp_diff(x.m24, e.m24), oracle_tol(abs_raw(e.m24), tol)));
        ex = max(ex, excess(ulp_diff(x.m25, e.m25), oracle_tol(abs_raw(e.m25), tol)));
        ex = max(ex, excess(ulp_diff(x.m26, e.m26), oracle_tol(abs_raw(e.m26), tol)));
        ex = max(ex, excess(ulp_diff(x.m31, e.m31), oracle_tol(abs_raw(e.m31), tol)));
        ex = max(ex, excess(ulp_diff(x.m32, e.m32), oracle_tol(abs_raw(e.m32), tol)));
        ex = max(ex, excess(ulp_diff(x.m33, e.m33), oracle_tol(abs_raw(e.m33), tol)));
        ex = max(ex, excess(ulp_diff(x.m34, e.m34), oracle_tol(abs_raw(e.m34), tol)));
        ex = max(ex, excess(ulp_diff(x.m35, e.m35), oracle_tol(abs_raw(e.m35), tol)));
        ex = max(ex, excess(ulp_diff(x.m36, e.m36), oracle_tol(abs_raw(e.m36), tol)));
        ex = max(ex, excess(ulp_diff(x.m41, e.m41), oracle_tol(abs_raw(e.m41), tol)));
        ex = max(ex, excess(ulp_diff(x.m42, e.m42), oracle_tol(abs_raw(e.m42), tol)));
        ex = max(ex, excess(ulp_diff(x.m43, e.m43), oracle_tol(abs_raw(e.m43), tol)));
        ex = max(ex, excess(ulp_diff(x.m44, e.m44), oracle_tol(abs_raw(e.m44), tol)));
        ex = max(ex, excess(ulp_diff(x.m45, e.m45), oracle_tol(abs_raw(e.m45), tol)));
        ex = max(ex, excess(ulp_diff(x.m46, e.m46), oracle_tol(abs_raw(e.m46), tol)));
        ex = max(ex, excess(ulp_diff(x.m51, e.m51), oracle_tol(abs_raw(e.m51), tol)));
        ex = max(ex, excess(ulp_diff(x.m52, e.m52), oracle_tol(abs_raw(e.m52), tol)));
        ex = max(ex, excess(ulp_diff(x.m53, e.m53), oracle_tol(abs_raw(e.m53), tol)));
        ex = max(ex, excess(ulp_diff(x.m54, e.m54), oracle_tol(abs_raw(e.m54), tol)));
        ex = max(ex, excess(ulp_diff(x.m55, e.m55), oracle_tol(abs_raw(e.m55), tol)));
        ex = max(ex, excess(ulp_diff(x.m56, e.m56), oracle_tol(abs_raw(e.m56), tol)));
        ex = max(ex, excess(ulp_diff(x.m61, e.m61), oracle_tol(abs_raw(e.m61), tol)));
        ex = max(ex, excess(ulp_diff(x.m62, e.m62), oracle_tol(abs_raw(e.m62), tol)));
        ex = max(ex, excess(ulp_diff(x.m63, e.m63), oracle_tol(abs_raw(e.m63), tol)));
        ex = max(ex, excess(ulp_diff(x.m64, e.m64), oracle_tol(abs_raw(e.m64), tol)));
        ex = max(ex, excess(ulp_diff(x.m65, e.m65), oracle_tol(abs_raw(e.m65), tol)));
        ex = max(ex, excess(ulp_diff(x.m66, e.m66), oracle_tol(abs_raw(e.m66), tol)));
        let mut m = Matrix6Trait::identity();
        assert!(f.solve_mut(ref m));
        assert!(m == x, "solve_mut(identity)");
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `full_piv_lu6_determinant` (oracle).
#[test]
fn test_oracle_full_piv_lu6_determinant() {
    let mut cases = oracle::full_piv_lu6_determinant_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, e, tol) = *case;
        let d = black_box(mat6x6(a)).full_piv_lu().determinant();
        ex = max(ex, excess(ulp_diff(d, fx(e)), oracle_tol(abs_raw(fx(e)), tol)));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// A singular matrix: not invertible, `solve` / `try_inverse` are `None`, `solve_mut` leaves its
/// argument unchanged, the determinant is exactly zero.
#[test]
fn test_full_piv_lu6_singular() {
    let z = black_box(
        mat6x6(
            [
                [0 * ONE, 1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE],
                [1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE, 6 * ONE],
                [2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE, 6 * ONE, 7 * ONE],
                [3 * ONE, 4 * ONE, 5 * ONE, 6 * ONE, 7 * ONE, 8 * ONE],
                [4 * ONE, 5 * ONE, 6 * ONE, 7 * ONE, 8 * ONE, 9 * ONE],
                [5 * ONE, 6 * ONE, 7 * ONE, 8 * ONE, 9 * ONE, 10 * ONE],
            ],
        ),
    );
    let f = z.full_piv_lu();
    assert!(!f.is_invertible());
    assert!(f.try_inverse().is_none());
    assert!(f.solve(vec6((0 * ONE, 1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE))).is_none());
    let mut b = vec6((0 * ONE, 1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE));
    assert!(!f.solve_mut(ref b));
    assert!(b == vec6((0 * ONE, 1 * ONE, 2 * ONE, 3 * ONE, 4 * ONE, 5 * ONE)));
    assert!(f.determinant() == fx(0));
}

#[test]
#[inline(never)]
fn bench_full_piv_lu6_new__baseline() {
    let (a, _, _, _, _) = *oracle::full_piv_lu6_cases().at(3);
    let a = black_box(mat6x6(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Full pivoting, unrolled.
#[test]
#[inline(never)]
fn bench_full_piv_lu6_new__unrolled() {
    let (a, _, _, _, _) = *oracle::full_piv_lu6_cases().at(3);
    let a = black_box(mat6x6(a));
    let e = black_box(true);
    let f = a.full_piv_lu();
    assert!((f.lu.m11 == f.lu.m11) == e);
}

#[test]
#[inline(never)]
fn bench_full_piv_lu6_solve__baseline() {
    let (a, b, _, _) = *oracle::full_piv_lu6_solve_cases().at(3);
    let f = black_box(mat6x6(a)).full_piv_lu();
    let b = black_box(vec6(b));
    let e = black_box(true);
    let _ = (f, b);
    assert!(e == e);
}

/// Permutations and triangular solves on a factorisation.
#[test]
#[inline(never)]
fn bench_full_piv_lu6_solve__substitution() {
    let (a, b, _, _) = *oracle::full_piv_lu6_solve_cases().at(3);
    let f = black_box(mat6x6(a)).full_piv_lu();
    let b = black_box(vec6(b));
    let e = black_box(true);
    let x = f.solve(b);
    assert!((x.is_some()) == e);
}

#[test]
#[inline(never)]
fn bench_full_piv_lu6_try_inverse__baseline() {
    let (a, _, _) = *oracle::full_piv_lu6_inverse_cases().at(3);
    let f = black_box(mat6x6(a)).full_piv_lu();
    let e = black_box(true);
    let _ = f;
    assert!(e == e);
}

/// Static unit columns, then the permutations (on a factorisation).
#[test]
#[inline(never)]
fn bench_full_piv_lu6_try_inverse__unit_columns() {
    let (a, _, _) = *oracle::full_piv_lu6_inverse_cases().at(3);
    let f = black_box(mat6x6(a)).full_piv_lu();
    let e = black_box(true);
    let x = f.try_inverse();
    assert!((x.is_some()) == e);
}
