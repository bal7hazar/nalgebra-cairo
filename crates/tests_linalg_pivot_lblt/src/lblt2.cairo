// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Lblt2` / `Matrix2LbltTrait` through the public API (WP 8.5-P15): oracle vectors (`tools/oracle`
//! suite `pivot`), upstream's unit tests, gas benchmarks.

use core::cmp::max;
use nalgebra::linalg::{Lblt2Trait, Matrix2LbltTrait};
use nalgebra::{Matrix2Trait, MatrixMul};
use nalgebra_testing::black_box;
use nalgebra_tests_utils::{abs_raw, excess, fx, oracle_tol, ulp_diff};
use crate::builders::{amax_2x2, mat2x2, max_ulp_2x2, vec2};
use crate::oracle_pivot as oracle;

const ONE: i64 = 0x100000000;

/// `lblt2` (oracle): `d()` and `l_permuted()` within the oracle tolerance (same pivots and
/// blocks as upstream), `A = (Pᵀ L) B (Pᵀ L)ᵀ` within the measured bound.
#[test]
fn test_oracle_lblt2() {
    let mut cases = oracle::lblt2_cases();
    let (mut ex, mut rec) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, ed, el, tol) = *case;
        let a = black_box(mat2x2(a));
        let f = a.lblt();
        let (d, l) = (f.d(), f.l_permuted());
        let (ed, el) = (mat2x2(ed), mat2x2(el));
        ex = max(ex, excess(ulp_diff(d.m11, ed.m11), oracle_tol(abs_raw(ed.m11), tol)));
        ex = max(ex, excess(ulp_diff(d.m12, ed.m12), oracle_tol(abs_raw(ed.m12), tol)));
        ex = max(ex, excess(ulp_diff(d.m21, ed.m21), oracle_tol(abs_raw(ed.m21), tol)));
        ex = max(ex, excess(ulp_diff(d.m22, ed.m22), oracle_tol(abs_raw(ed.m22), tol)));
        ex = max(ex, excess(ulp_diff(l.m11, el.m11), oracle_tol(abs_raw(el.m11), tol)));
        ex = max(ex, excess(ulp_diff(l.m12, el.m12), oracle_tol(abs_raw(el.m12), tol)));
        ex = max(ex, excess(ulp_diff(l.m21, el.m21), oracle_tol(abs_raw(el.m21), tol)));
        ex = max(ex, excess(ulp_diff(l.m22, el.m22), oracle_tol(abs_raw(el.m22), tol)));
        assert!(f.zero_pivot.is_none());
        rec = max(rec, max_ulp_2x2(l.mul_mat(d).mul_mat(l.transpose()), a) / amax_2x2(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 0, "measured {}", rec);
}

/// `lblt2_solve` (oracle); `solve_mut` agrees.
#[test]
fn test_oracle_lblt2_solve() {
    let mut cases = oracle::lblt2_solve_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, b, e, tol) = *case;
        let f = black_box(mat2x2(a)).lblt();
        let x = f.solve(vec2(b)).unwrap();
        let e = vec2(e);
        ex = max(ex, excess(ulp_diff(x.x, e.x), oracle_tol(abs_raw(e.x), tol)));
        ex = max(ex, excess(ulp_diff(x.y, e.y), oracle_tol(abs_raw(e.y), tol)));
        let mut y = vec2(b);
        assert!(f.solve_mut(ref y));
        assert!(y == x);
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `lblt2_determinant` (oracle).
#[test]
fn test_oracle_lblt2_determinant() {
    let mut cases = oracle::lblt2_determinant_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, e, tol) = *case;
        let d = black_box(mat2x2(a)).lblt().determinant();
        ex = max(ex, excess(ulp_diff(d, fx(e)), oracle_tol(abs_raw(fx(e)), tol)));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `lblt2_zero_diag` (oracle): `d()` and `l_permuted()` within the oracle tolerance (same pivots
/// and blocks as upstream), `A = (Pᵀ L) B (Pᵀ L)ᵀ` within the measured bound.
#[test]
fn test_oracle_lblt2_zero_diag() {
    let mut cases = oracle::lblt2_zero_diag_cases();
    let (mut ex, mut rec) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, ed, el, tol) = *case;
        let a = black_box(mat2x2(a));
        let f = a.lblt();
        let (d, l) = (f.d(), f.l_permuted());
        let (ed, el) = (mat2x2(ed), mat2x2(el));
        ex = max(ex, excess(ulp_diff(d.m11, ed.m11), oracle_tol(abs_raw(ed.m11), tol)));
        ex = max(ex, excess(ulp_diff(d.m12, ed.m12), oracle_tol(abs_raw(ed.m12), tol)));
        ex = max(ex, excess(ulp_diff(d.m21, ed.m21), oracle_tol(abs_raw(ed.m21), tol)));
        ex = max(ex, excess(ulp_diff(d.m22, ed.m22), oracle_tol(abs_raw(ed.m22), tol)));
        ex = max(ex, excess(ulp_diff(l.m11, el.m11), oracle_tol(abs_raw(el.m11), tol)));
        ex = max(ex, excess(ulp_diff(l.m12, el.m12), oracle_tol(abs_raw(el.m12), tol)));
        ex = max(ex, excess(ulp_diff(l.m21, el.m21), oracle_tol(abs_raw(el.m21), tol)));
        ex = max(ex, excess(ulp_diff(l.m22, el.m22), oracle_tol(abs_raw(el.m22), tol)));
        assert!(f.zero_pivot.is_none());
        rec = max(rec, max_ulp_2x2(l.mul_mat(d).mul_mat(l.transpose()), a) / amax_2x2(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 0, "measured {}", rec);
}

/// `lblt2_zero_diag_solve` (oracle); `solve_mut` agrees.
#[test]
fn test_oracle_lblt2_zero_diag_solve() {
    let mut cases = oracle::lblt2_zero_diag_solve_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, b, e, tol) = *case;
        let f = black_box(mat2x2(a)).lblt();
        let x = f.solve(vec2(b)).unwrap();
        let e = vec2(e);
        ex = max(ex, excess(ulp_diff(x.x, e.x), oracle_tol(abs_raw(e.x), tol)));
        ex = max(ex, excess(ulp_diff(x.y, e.y), oracle_tol(abs_raw(e.y), tol)));
        let mut y = vec2(b);
        assert!(f.solve_mut(ref y));
        assert!(y == x);
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `lblt2_zero_diag_determinant` (oracle).
#[test]
fn test_oracle_lblt2_zero_diag_determinant() {
    let mut cases = oracle::lblt2_zero_diag_determinant_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, e, tol) = *case;
        let d = black_box(mat2x2(a)).lblt().determinant();
        ex = max(ex, excess(ulp_diff(d, fx(e)), oracle_tol(abs_raw(fx(e)), tol)));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// Upstream's `zero_matrix` test: identity `l_permuted`, zero `d`, `zero_pivot == Some(0)`, pivots
/// `(k, 1)`, zero determinant, no solution.
#[test]
fn test_lblt2_zero_matrix() {
    let f = black_box(mat2x2([[0, 0], [0, 0]])).lblt();
    assert!(f.l_permuted() == Matrix2Trait::identity());
    assert!(f.d() == Matrix2Trait::zeros());
    assert!(f.zero_pivot == Some(0));
    assert!(f.p1 == (0, 1) && f.p2 == (1, 1));
    assert!(f.determinant() == fx(0));
    assert!(f.solve(vec2((0 * ONE, 1 * ONE))).is_none());
}

/// Upstream's `identity_matrix` test.
#[test]
fn test_lblt2_identity_matrix() {
    let f = black_box(mat2x2([[ONE, 0], [0, ONE]])).lblt();
    assert!(f.l_permuted() == Matrix2Trait::identity());
    assert!(f.d() == Matrix2Trait::identity());
    assert!(f.zero_pivot.is_none());
    assert!(f.p1 == (0, 1) && f.p2 == (1, 1));
    assert!(f.determinant() == fx(ONE));
}

/// Upstream's `exchange_matrix` test: the expected pivot sequence, and an EXACT reconstruction.
#[test]
fn test_lblt2_exchange_matrix() {
    let a = black_box(mat2x2([[0, ONE], [ONE, 0]]));
    let f = a.lblt();
    let l = f.l_permuted();
    assert!(l.mul_mat(f.d()).mul_mat(l.transpose()) == a);
    assert!(f.p1 == (1, 2) && f.p2 == (1, 2));
}

#[test]
#[inline(never)]
fn bench_lblt2_new__baseline() {
    let (a, _, _, _) = *oracle::lblt2_cases().at(3);
    let a = black_box(mat2x2(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Bunch-Kaufman, unrolled positions.
#[test]
#[inline(never)]
fn bench_lblt2_new__unrolled() {
    let (a, _, _, _) = *oracle::lblt2_cases().at(3);
    let a = black_box(mat2x2(a));
    let e = black_box(true);
    let f = a.lblt();
    assert!((f.zero_pivot.is_none()) == e);
}

#[test]
#[inline(never)]
fn bench_lblt2_solve__baseline() {
    let (a, b, _, _) = *oracle::lblt2_solve_cases().at(3);
    let f = black_box(black_box(mat2x2(a)).lblt());
    let b = black_box(vec2(b));
    let e = black_box(true);
    let _ = (f, b);
    assert!(e == e);
}

/// Permutation, unit triangular solves, `B⁻¹` (on a factorisation).
#[test]
#[inline(never)]
fn bench_lblt2_solve__substitution() {
    let (a, b, _, _) = *oracle::lblt2_solve_cases().at(3);
    let f = black_box(black_box(mat2x2(a)).lblt());
    let b = black_box(vec2(b));
    let e = black_box(true);
    let x = f.solve(b);
    assert!((x.is_some()) == e);
}

#[test]
#[inline(never)]
fn bench_lblt2_l_permuted__baseline() {
    let (a, _, _, _) = *oracle::lblt2_cases().at(3);
    let f = black_box(black_box(mat2x2(a)).lblt());
    let e = black_box(true);
    let _ = f;
    assert!(e == e);
}

/// Multipliers placed by the recorded interchanges.
#[test]
#[inline(never)]
fn bench_lblt2_l_permuted__moves() {
    let (a, _, _, _) = *oracle::lblt2_cases().at(3);
    let f = black_box(black_box(mat2x2(a)).lblt());
    let e = black_box(true);
    let l = f.l_permuted();
    assert!((l == l) == e);
}

#[test]
#[inline(never)]
fn bench_lblt2_determinant__baseline() {
    let (a, _, _) = *oracle::lblt2_determinant_cases().at(1);
    let f = black_box(black_box(mat2x2(a)).lblt());
    let e = black_box(true);
    let _ = f;
    assert!(e == e);
}

/// Block determinants, one floor per product.
#[test]
#[inline(never)]
fn bench_lblt2_determinant__product() {
    let (a, _, _) = *oracle::lblt2_determinant_cases().at(1);
    let f = black_box(black_box(mat2x2(a)).lblt());
    let e = black_box(true);
    let d = f.determinant();
    assert!((d == d) == e);
}
