[package]
name = "simba_fixed"
description = "simba::Real for glam.cairo's shared `fixed::Fixed` scalar: use nalgebra with the physics stack's scalar."
version.workspace = true
edition.workspace = true
cairo-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true

[dependencies]
simba = { path = "../simba", version = "0.1.0" }
fixed = { git = "https://github.com/bal7hazar/glam.cairo", rev = "edf13a2" }

[dev-dependencies]
snforge_std.workspace = true
assert_macros.workspace = true
nalgebra = { path = "../nalgebra" }
nalgebra_testing = { path = "../testing" }

[tool]
fmt.workspace = true
scarb.workspace = true
