//! Package `nalgebra_tests_linalg_solve` (WP 8.5-P14a): tests and gas benchmarks of the triangular
//! solves (`MatrixSolve`), the Givens rotations, the permutation sequences and the LU / QR /
//! Cholesky API completion, through the public API.

#[cfg(test)]
mod solve;
