// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! Builders of the static shapes from the raw oracle layout (ROW-major rows,
//! component tuples), the raw-unit error measures of the tests and the test-only
//! `PartialEq` of the decompositions (upstream has none).

use fixed::Fixed;
use nalgebra::linalg::Svd1x6;
use nalgebra_tests_utils::{abs_raw, fx, ulp_diff};

/// Field-wise equality of `Svd1x6`.
pub impl Svd1x6PartialEq<T, +PartialEq<T>> of PartialEq<Svd1x6<T>> {
    fn eq(lhs: @Svd1x6<T>, rhs: @Svd1x6<T>) -> bool {
        lhs.u == rhs.u && lhs.singular_values == rhs.singular_values && lhs.v_t == rhs.v_t
    }
}
use nalgebra::linalg::Svd2x6;

/// Field-wise equality of `Svd2x6`.
pub impl Svd2x6PartialEq<T, +PartialEq<T>> of PartialEq<Svd2x6<T>> {
    fn eq(lhs: @Svd2x6<T>, rhs: @Svd2x6<T>) -> bool {
        lhs.u == rhs.u && lhs.singular_values == rhs.singular_values && lhs.v_t == rhs.v_t
    }
}
use nalgebra::linalg::Svd3x6;

/// Field-wise equality of `Svd3x6`.
pub impl Svd3x6PartialEq<T, +PartialEq<T>> of PartialEq<Svd3x6<T>> {
    fn eq(lhs: @Svd3x6<T>, rhs: @Svd3x6<T>) -> bool {
        lhs.u == rhs.u && lhs.singular_values == rhs.singular_values && lhs.v_t == rhs.v_t
    }
}
use nalgebra::{
    Matrix1, Matrix2, Matrix2x6, Matrix3, Matrix3x6, Matrix6x2, Matrix6x3, RowVector6, Vector2,
    Vector3, Vector6,
};

/// `Matrix1` from raw ROW-major rows.
pub fn mat1x1(rows: [[i64; 1]; 1]) -> Matrix1<Fixed> {
    let [r0] = rows;
    let [a00] = r0;
    Matrix1 { x: fx(a00) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_1x1(a: Matrix1<Fixed>, b: Matrix1<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_1x1(a: Matrix1<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_1x1(a: Matrix1<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e = core::cmp::max(e, dev(w(a.x) * w(a.x), 0x100000000));
    e
}

/// `RowVector6` from raw ROW-major rows.
pub fn mat1x6(rows: [[i64; 6]; 1]) -> RowVector6<Fixed> {
    let [r0] = rows;
    let [a00, a01, a02, a03, a04, a05] = r0;
    RowVector6 { x: fx(a00), y: fx(a01), z: fx(a02), w: fx(a03), a: fx(a04), b: fx(a05) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_1x6(a: RowVector6<Fixed>, b: RowVector6<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e = core::cmp::max(e, ulp_diff(a.z, b.z));
    e = core::cmp::max(e, ulp_diff(a.w, b.w));
    e = core::cmp::max(e, ulp_diff(a.a, b.a));
    e = core::cmp::max(e, ulp_diff(a.b, b.b));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_1x6(a: RowVector6<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = core::cmp::max(e, abs_raw(a.z));
    e = core::cmp::max(e, abs_raw(a.w));
    e = core::cmp::max(e, abs_raw(a.a));
    e = core::cmp::max(e, abs_raw(a.b));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_1x6(a: RowVector6<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.x) * w(a.x)
                    + w(a.y) * w(a.y)
                    + w(a.z) * w(a.z)
                    + w(a.w) * w(a.w)
                    + w(a.a) * w(a.a)
                    + w(a.b) * w(a.b),
                0x100000000,
            ),
        );
    e
}

/// `Matrix2` from raw ROW-major rows.
pub fn mat2x2(rows: [[i64; 2]; 2]) -> Matrix2<Fixed> {
    let [r0, r1] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    Matrix2 { m11: fx(a00), m21: fx(a10), m12: fx(a01), m22: fx(a11) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_2x2(a: Matrix2<Fixed>, b: Matrix2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_2x2(a: Matrix2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_2x2(a: Matrix2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m11) + w(a.m21) * w(a.m21), 0x100000000));
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m12) + w(a.m21) * w(a.m22), 0));
    e = core::cmp::max(e, dev(w(a.m12) * w(a.m12) + w(a.m22) * w(a.m22), 0x100000000));
    e
}

/// `Matrix2x6` from raw ROW-major rows.
pub fn mat2x6(rows: [[i64; 6]; 2]) -> Matrix2x6<Fixed> {
    let [r0, r1] = rows;
    let [a00, a01, a02, a03, a04, a05] = r0;
    let [a10, a11, a12, a13, a14, a15] = r1;
    Matrix2x6 {
        m11: fx(a00),
        m21: fx(a10),
        m12: fx(a01),
        m22: fx(a11),
        m13: fx(a02),
        m23: fx(a12),
        m14: fx(a03),
        m24: fx(a13),
        m15: fx(a04),
        m25: fx(a14),
        m16: fx(a05),
        m26: fx(a15),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_2x6(a: Matrix2x6<Fixed>, b: Matrix2x6<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m15, b.m15));
    e = core::cmp::max(e, ulp_diff(a.m25, b.m25));
    e = core::cmp::max(e, ulp_diff(a.m16, b.m16));
    e = core::cmp::max(e, ulp_diff(a.m26, b.m26));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_2x6(a: Matrix2x6<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m15));
    e = core::cmp::max(e, abs_raw(a.m25));
    e = core::cmp::max(e, abs_raw(a.m16));
    e = core::cmp::max(e, abs_raw(a.m26));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_2x6(a: Matrix2x6<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m12) * w(a.m12)
                    + w(a.m13) * w(a.m13)
                    + w(a.m14) * w(a.m14)
                    + w(a.m15) * w(a.m15)
                    + w(a.m16) * w(a.m16),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m21)
                    + w(a.m12) * w(a.m22)
                    + w(a.m13) * w(a.m23)
                    + w(a.m14) * w(a.m24)
                    + w(a.m15) * w(a.m25)
                    + w(a.m16) * w(a.m26),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m21) * w(a.m21)
                    + w(a.m22) * w(a.m22)
                    + w(a.m23) * w(a.m23)
                    + w(a.m24) * w(a.m24)
                    + w(a.m25) * w(a.m25)
                    + w(a.m26) * w(a.m26),
                0x100000000,
            ),
        );
    e
}

/// `Matrix3` from raw ROW-major rows.
pub fn mat3x3(rows: [[i64; 3]; 3]) -> Matrix3<Fixed> {
    let [r0, r1, r2] = rows;
    let [a00, a01, a02] = r0;
    let [a10, a11, a12] = r1;
    let [a20, a21, a22] = r2;
    Matrix3 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_3x3(a: Matrix3<Fixed>, b: Matrix3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_3x3(a: Matrix3<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_3x3(a: Matrix3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e, dev(w(a.m11) * w(a.m11) + w(a.m21) * w(a.m21) + w(a.m31) * w(a.m31), 0x100000000),
        );
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m12) + w(a.m21) * w(a.m22) + w(a.m31) * w(a.m32), 0));
    e = core::cmp::max(e, dev(w(a.m11) * w(a.m13) + w(a.m21) * w(a.m23) + w(a.m31) * w(a.m33), 0));
    e =
        core::cmp::max(
            e, dev(w(a.m12) * w(a.m12) + w(a.m22) * w(a.m22) + w(a.m32) * w(a.m32), 0x100000000),
        );
    e = core::cmp::max(e, dev(w(a.m12) * w(a.m13) + w(a.m22) * w(a.m23) + w(a.m32) * w(a.m33), 0));
    e =
        core::cmp::max(
            e, dev(w(a.m13) * w(a.m13) + w(a.m23) * w(a.m23) + w(a.m33) * w(a.m33), 0x100000000),
        );
    e
}

/// `Matrix3x6` from raw ROW-major rows.
pub fn mat3x6(rows: [[i64; 6]; 3]) -> Matrix3x6<Fixed> {
    let [r0, r1, r2] = rows;
    let [a00, a01, a02, a03, a04, a05] = r0;
    let [a10, a11, a12, a13, a14, a15] = r1;
    let [a20, a21, a22, a23, a24, a25] = r2;
    Matrix3x6 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m14: fx(a03),
        m24: fx(a13),
        m34: fx(a23),
        m15: fx(a04),
        m25: fx(a14),
        m35: fx(a24),
        m16: fx(a05),
        m26: fx(a15),
        m36: fx(a25),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_3x6(a: Matrix3x6<Fixed>, b: Matrix3x6<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m14, b.m14));
    e = core::cmp::max(e, ulp_diff(a.m24, b.m24));
    e = core::cmp::max(e, ulp_diff(a.m34, b.m34));
    e = core::cmp::max(e, ulp_diff(a.m15, b.m15));
    e = core::cmp::max(e, ulp_diff(a.m25, b.m25));
    e = core::cmp::max(e, ulp_diff(a.m35, b.m35));
    e = core::cmp::max(e, ulp_diff(a.m16, b.m16));
    e = core::cmp::max(e, ulp_diff(a.m26, b.m26));
    e = core::cmp::max(e, ulp_diff(a.m36, b.m36));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_3x6(a: Matrix3x6<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m14));
    e = core::cmp::max(e, abs_raw(a.m24));
    e = core::cmp::max(e, abs_raw(a.m34));
    e = core::cmp::max(e, abs_raw(a.m15));
    e = core::cmp::max(e, abs_raw(a.m25));
    e = core::cmp::max(e, abs_raw(a.m35));
    e = core::cmp::max(e, abs_raw(a.m16));
    e = core::cmp::max(e, abs_raw(a.m26));
    e = core::cmp::max(e, abs_raw(a.m36));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_3x6(a: Matrix3x6<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m12) * w(a.m12)
                    + w(a.m13) * w(a.m13)
                    + w(a.m14) * w(a.m14)
                    + w(a.m15) * w(a.m15)
                    + w(a.m16) * w(a.m16),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m21)
                    + w(a.m12) * w(a.m22)
                    + w(a.m13) * w(a.m23)
                    + w(a.m14) * w(a.m24)
                    + w(a.m15) * w(a.m25)
                    + w(a.m16) * w(a.m26),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m31)
                    + w(a.m12) * w(a.m32)
                    + w(a.m13) * w(a.m33)
                    + w(a.m14) * w(a.m34)
                    + w(a.m15) * w(a.m35)
                    + w(a.m16) * w(a.m36),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m21) * w(a.m21)
                    + w(a.m22) * w(a.m22)
                    + w(a.m23) * w(a.m23)
                    + w(a.m24) * w(a.m24)
                    + w(a.m25) * w(a.m25)
                    + w(a.m26) * w(a.m26),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m21) * w(a.m31)
                    + w(a.m22) * w(a.m32)
                    + w(a.m23) * w(a.m33)
                    + w(a.m24) * w(a.m34)
                    + w(a.m25) * w(a.m35)
                    + w(a.m26) * w(a.m36),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m31) * w(a.m31)
                    + w(a.m32) * w(a.m32)
                    + w(a.m33) * w(a.m33)
                    + w(a.m34) * w(a.m34)
                    + w(a.m35) * w(a.m35)
                    + w(a.m36) * w(a.m36),
                0x100000000,
            ),
        );
    e
}

/// `Vector6` from raw ROW-major rows.
pub fn mat6x1(rows: [[i64; 1]; 6]) -> Vector6<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00] = r0;
    let [a10] = r1;
    let [a20] = r2;
    let [a30] = r3;
    let [a40] = r4;
    let [a50] = r5;
    Vector6 { x: fx(a00), y: fx(a10), z: fx(a20), w: fx(a30), a: fx(a40), b: fx(a50) }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x1(a: Vector6<Fixed>, b: Vector6<Fixed>) -> u128 {
    let mut e = ulp_diff(a.x, b.x);
    e = core::cmp::max(e, ulp_diff(a.y, b.y));
    e = core::cmp::max(e, ulp_diff(a.z, b.z));
    e = core::cmp::max(e, ulp_diff(a.w, b.w));
    e = core::cmp::max(e, ulp_diff(a.a, b.a));
    e = core::cmp::max(e, ulp_diff(a.b, b.b));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x1(a: Vector6<Fixed>) -> u128 {
    let mut e = abs_raw(a.x);
    e = core::cmp::max(e, abs_raw(a.y));
    e = core::cmp::max(e, abs_raw(a.z));
    e = core::cmp::max(e, abs_raw(a.w));
    e = core::cmp::max(e, abs_raw(a.a));
    e = core::cmp::max(e, abs_raw(a.b));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x1(a: Vector6<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.x) * w(a.x)
                    + w(a.y) * w(a.y)
                    + w(a.z) * w(a.z)
                    + w(a.w) * w(a.w)
                    + w(a.a) * w(a.a)
                    + w(a.b) * w(a.b),
                0x100000000,
            ),
        );
    e
}

/// `Matrix6x2` from raw ROW-major rows.
pub fn mat6x2(rows: [[i64; 2]; 6]) -> Matrix6x2<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00, a01] = r0;
    let [a10, a11] = r1;
    let [a20, a21] = r2;
    let [a30, a31] = r3;
    let [a40, a41] = r4;
    let [a50, a51] = r5;
    Matrix6x2 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m61: fx(a50),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m62: fx(a51),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x2(a: Matrix6x2<Fixed>, b: Matrix6x2<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m61, b.m61));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m62, b.m62));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x2(a: Matrix6x2<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m61));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m62));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x2(a: Matrix6x2<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51)
                    + w(a.m61) * w(a.m61),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52)
                    + w(a.m61) * w(a.m62),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52)
                    + w(a.m62) * w(a.m62),
                0x100000000,
            ),
        );
    e
}

/// `Matrix6x3` from raw ROW-major rows.
pub fn mat6x3(rows: [[i64; 3]; 6]) -> Matrix6x3<Fixed> {
    let [r0, r1, r2, r3, r4, r5] = rows;
    let [a00, a01, a02] = r0;
    let [a10, a11, a12] = r1;
    let [a20, a21, a22] = r2;
    let [a30, a31, a32] = r3;
    let [a40, a41, a42] = r4;
    let [a50, a51, a52] = r5;
    Matrix6x3 {
        m11: fx(a00),
        m21: fx(a10),
        m31: fx(a20),
        m41: fx(a30),
        m51: fx(a40),
        m61: fx(a50),
        m12: fx(a01),
        m22: fx(a11),
        m32: fx(a21),
        m42: fx(a31),
        m52: fx(a41),
        m62: fx(a51),
        m13: fx(a02),
        m23: fx(a12),
        m33: fx(a22),
        m43: fx(a32),
        m53: fx(a42),
        m63: fx(a52),
    }
}

/// Largest component-wise `|a - b|` in raw units.
pub fn max_ulp_6x3(a: Matrix6x3<Fixed>, b: Matrix6x3<Fixed>) -> u128 {
    let mut e = ulp_diff(a.m11, b.m11);
    e = core::cmp::max(e, ulp_diff(a.m21, b.m21));
    e = core::cmp::max(e, ulp_diff(a.m31, b.m31));
    e = core::cmp::max(e, ulp_diff(a.m41, b.m41));
    e = core::cmp::max(e, ulp_diff(a.m51, b.m51));
    e = core::cmp::max(e, ulp_diff(a.m61, b.m61));
    e = core::cmp::max(e, ulp_diff(a.m12, b.m12));
    e = core::cmp::max(e, ulp_diff(a.m22, b.m22));
    e = core::cmp::max(e, ulp_diff(a.m32, b.m32));
    e = core::cmp::max(e, ulp_diff(a.m42, b.m42));
    e = core::cmp::max(e, ulp_diff(a.m52, b.m52));
    e = core::cmp::max(e, ulp_diff(a.m62, b.m62));
    e = core::cmp::max(e, ulp_diff(a.m13, b.m13));
    e = core::cmp::max(e, ulp_diff(a.m23, b.m23));
    e = core::cmp::max(e, ulp_diff(a.m33, b.m33));
    e = core::cmp::max(e, ulp_diff(a.m43, b.m43));
    e = core::cmp::max(e, ulp_diff(a.m53, b.m53));
    e = core::cmp::max(e, ulp_diff(a.m63, b.m63));
    e
}

/// `max(1, max |a_ij|)` in UNITS (raw >> 32): the scale of the relative bounds.
pub fn amax_6x3(a: Matrix6x3<Fixed>) -> u128 {
    let mut e = abs_raw(a.m11);
    e = core::cmp::max(e, abs_raw(a.m21));
    e = core::cmp::max(e, abs_raw(a.m31));
    e = core::cmp::max(e, abs_raw(a.m41));
    e = core::cmp::max(e, abs_raw(a.m51));
    e = core::cmp::max(e, abs_raw(a.m61));
    e = core::cmp::max(e, abs_raw(a.m12));
    e = core::cmp::max(e, abs_raw(a.m22));
    e = core::cmp::max(e, abs_raw(a.m32));
    e = core::cmp::max(e, abs_raw(a.m42));
    e = core::cmp::max(e, abs_raw(a.m52));
    e = core::cmp::max(e, abs_raw(a.m62));
    e = core::cmp::max(e, abs_raw(a.m13));
    e = core::cmp::max(e, abs_raw(a.m23));
    e = core::cmp::max(e, abs_raw(a.m33));
    e = core::cmp::max(e, abs_raw(a.m43));
    e = core::cmp::max(e, abs_raw(a.m53));
    e = core::cmp::max(e, abs_raw(a.m63));
    e = e / 0x100000000;
    if e == 0 {
        1
    } else {
        e
    }
}

/// `max |<x_i, x_j> - delta_ij|` over the columns (rows when wider than tall), exact
/// products floored once, in raw units.
pub fn orth_6x3(a: Matrix6x3<Fixed>) -> u128 {
    let mut e: u128 = 0;
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m11)
                    + w(a.m21) * w(a.m21)
                    + w(a.m31) * w(a.m31)
                    + w(a.m41) * w(a.m41)
                    + w(a.m51) * w(a.m51)
                    + w(a.m61) * w(a.m61),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m12)
                    + w(a.m21) * w(a.m22)
                    + w(a.m31) * w(a.m32)
                    + w(a.m41) * w(a.m42)
                    + w(a.m51) * w(a.m52)
                    + w(a.m61) * w(a.m62),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m11) * w(a.m13)
                    + w(a.m21) * w(a.m23)
                    + w(a.m31) * w(a.m33)
                    + w(a.m41) * w(a.m43)
                    + w(a.m51) * w(a.m53)
                    + w(a.m61) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m12)
                    + w(a.m22) * w(a.m22)
                    + w(a.m32) * w(a.m32)
                    + w(a.m42) * w(a.m42)
                    + w(a.m52) * w(a.m52)
                    + w(a.m62) * w(a.m62),
                0x100000000,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m12) * w(a.m13)
                    + w(a.m22) * w(a.m23)
                    + w(a.m32) * w(a.m33)
                    + w(a.m42) * w(a.m43)
                    + w(a.m52) * w(a.m53)
                    + w(a.m62) * w(a.m63),
                0,
            ),
        );
    e =
        core::cmp::max(
            e,
            dev(
                w(a.m13) * w(a.m13)
                    + w(a.m23) * w(a.m23)
                    + w(a.m33) * w(a.m33)
                    + w(a.m43) * w(a.m43)
                    + w(a.m53) * w(a.m53)
                    + w(a.m63) * w(a.m63),
                0x100000000,
            ),
        );
    e
}

/// `Matrix1` from a raw component tuple.
pub fn vec1(t: (i64,)) -> Matrix1<Fixed> {
    let (a0,) = t;
    Matrix1 { x: fx(a0) }
}

/// `Vector2` from a raw component tuple.
pub fn vec2(t: (i64, i64)) -> Vector2<Fixed> {
    let (a0, a1) = t;
    Vector2 { x: fx(a0), y: fx(a1) }
}

/// `Vector3` from a raw component tuple.
pub fn vec3(t: (i64, i64, i64)) -> Vector3<Fixed> {
    let (a0, a1, a2) = t;
    Vector3 { x: fx(a0), y: fx(a1), z: fx(a2) }
}

/// The raw value, widened.
fn w(a: Fixed) -> i128 {
    a.raw.into()
}

/// `|floor(p / 2^32) - target|` for an exact wide sum of raw products `p`.
fn dev(p: i128, target: i128) -> u128 {
    let q = if p >= 0 {
        p / 0x100000000
    } else {
        -((-p + 0xffffffff) / 0x100000000)
    };
    let d = q - target;
    let d = if d < 0 {
        -d
    } else {
        d
    };
    d.try_into().unwrap()
}
