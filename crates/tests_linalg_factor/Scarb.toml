[package]
name = "nalgebra_tests_linalg_factor"
description = "Tests and gas benchmarks of the Givens rotations, permutation sequences, LU steps, Householder axis and LU / QR / Cholesky / UDU API completion (WP 8.5-P14a; not published)."
version.workspace = true
edition.workspace = true
cairo-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
publish = false

[dependencies]
nalgebra = { path = "../nalgebra", default-features = false, features = ["qr"] }
simba.workspace = true
fixed.workspace = true

[dev-dependencies]
nalgebra_tests_utils = { path = "../tests_utils", features = ["qr"] }
nalgebra_testing = { path = "../testing" }
snforge_std.workspace = true
assert_macros.workspace = true

[tool]
fmt.workspace = true
scarb.workspace = true
