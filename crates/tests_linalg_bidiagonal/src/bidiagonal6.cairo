// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Bidiagonal6` / `Matrix6BidiagonalTrait` through the public API (WP 8.5-P16): oracle vectors
//! (`tools/oracle` suite `schur`), the factor identities, the Householder building blocks, gas
//! benchmarks.

use core::cmp::max;
use fixed::Fixed;
use nalgebra::linalg::{
    Bidiagonal6Trait, Matrix6BidiagonalTrait, clear_column_unchecked, clear_row_unchecked,
};
use nalgebra::{Matrix6, MatrixMul, Vector5, Vector6};
use nalgebra_testing::black_box;
use nalgebra_tests_utils::{abs_raw, excess, fx, oracle_tol, ulp_diff};
use crate::builders::{amax_6x6, mat6x6, max_ulp_6x6, orth_6x6};
use crate::oracle_schur as oracle;

/// `bidiagonal6` (oracle): `u`, `d` and `v_t` entry by entry within the oracle tolerance
/// (upstream's signs), `A = U D Vᵀ`, orthonormal columns of `U` and rows of `Vᵀ` within the
/// measured bounds, the accessors consistent, and the Householder building blocks
/// (`clear_column_unchecked` / `clear_row_unchecked`) reproducing the packed storage bit for bit.
#[test]
fn test_oracle_bidiagonal6() {
    let mut cases = oracle::bidiagonal6_cases();
    let (mut ex, mut rec, mut orth) = (0, 0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eu, ed, evt, tol) = *case;
        let a = black_box(mat6x6(a));
        let bd = a.bidiagonalize();
        let (u, d, vt) = bd.unpack();
        assert!(u == bd.u() && d == bd.d() && vt == bd.v_t() && bd.uv_internal() == bd.uv);
        assert!(bd.is_upper_diagonal() == true);
        assert!(
            bd.diagonal() == Vector6 { x: d.m11, y: d.m22, z: d.m33, w: d.m44, a: d.m55, b: d.m66 },
            "diagonal",
        );
        assert!(
            bd.off_diagonal() == Vector5 { x: d.m12, y: d.m23, z: d.m34, w: d.m45, a: d.m56 },
            "off_diagonal",
        );
        let (eu, ed, evt) = (mat6x6(eu), mat6x6(ed), mat6x6(evt));
        ex = max(ex, excess(ulp_diff(u.m11, eu.m11), oracle_tol(abs_raw(eu.m11), tol)));
        ex = max(ex, excess(ulp_diff(u.m12, eu.m12), oracle_tol(abs_raw(eu.m12), tol)));
        ex = max(ex, excess(ulp_diff(u.m13, eu.m13), oracle_tol(abs_raw(eu.m13), tol)));
        ex = max(ex, excess(ulp_diff(u.m14, eu.m14), oracle_tol(abs_raw(eu.m14), tol)));
        ex = max(ex, excess(ulp_diff(u.m15, eu.m15), oracle_tol(abs_raw(eu.m15), tol)));
        ex = max(ex, excess(ulp_diff(u.m16, eu.m16), oracle_tol(abs_raw(eu.m16), tol)));
        ex = max(ex, excess(ulp_diff(u.m21, eu.m21), oracle_tol(abs_raw(eu.m21), tol)));
        ex = max(ex, excess(ulp_diff(u.m22, eu.m22), oracle_tol(abs_raw(eu.m22), tol)));
        ex = max(ex, excess(ulp_diff(u.m23, eu.m23), oracle_tol(abs_raw(eu.m23), tol)));
        ex = max(ex, excess(ulp_diff(u.m24, eu.m24), oracle_tol(abs_raw(eu.m24), tol)));
        ex = max(ex, excess(ulp_diff(u.m25, eu.m25), oracle_tol(abs_raw(eu.m25), tol)));
        ex = max(ex, excess(ulp_diff(u.m26, eu.m26), oracle_tol(abs_raw(eu.m26), tol)));
        ex = max(ex, excess(ulp_diff(u.m31, eu.m31), oracle_tol(abs_raw(eu.m31), tol)));
        ex = max(ex, excess(ulp_diff(u.m32, eu.m32), oracle_tol(abs_raw(eu.m32), tol)));
        ex = max(ex, excess(ulp_diff(u.m33, eu.m33), oracle_tol(abs_raw(eu.m33), tol)));
        ex = max(ex, excess(ulp_diff(u.m34, eu.m34), oracle_tol(abs_raw(eu.m34), tol)));
        ex = max(ex, excess(ulp_diff(u.m35, eu.m35), oracle_tol(abs_raw(eu.m35), tol)));
        ex = max(ex, excess(ulp_diff(u.m36, eu.m36), oracle_tol(abs_raw(eu.m36), tol)));
        ex = max(ex, excess(ulp_diff(u.m41, eu.m41), oracle_tol(abs_raw(eu.m41), tol)));
        ex = max(ex, excess(ulp_diff(u.m42, eu.m42), oracle_tol(abs_raw(eu.m42), tol)));
        ex = max(ex, excess(ulp_diff(u.m43, eu.m43), oracle_tol(abs_raw(eu.m43), tol)));
        ex = max(ex, excess(ulp_diff(u.m44, eu.m44), oracle_tol(abs_raw(eu.m44), tol)));
        ex = max(ex, excess(ulp_diff(u.m45, eu.m45), oracle_tol(abs_raw(eu.m45), tol)));
        ex = max(ex, excess(ulp_diff(u.m46, eu.m46), oracle_tol(abs_raw(eu.m46), tol)));
        ex = max(ex, excess(ulp_diff(u.m51, eu.m51), oracle_tol(abs_raw(eu.m51), tol)));
        ex = max(ex, excess(ulp_diff(u.m52, eu.m52), oracle_tol(abs_raw(eu.m52), tol)));
        ex = max(ex, excess(ulp_diff(u.m53, eu.m53), oracle_tol(abs_raw(eu.m53), tol)));
        ex = max(ex, excess(ulp_diff(u.m54, eu.m54), oracle_tol(abs_raw(eu.m54), tol)));
        ex = max(ex, excess(ulp_diff(u.m55, eu.m55), oracle_tol(abs_raw(eu.m55), tol)));
        ex = max(ex, excess(ulp_diff(u.m56, eu.m56), oracle_tol(abs_raw(eu.m56), tol)));
        ex = max(ex, excess(ulp_diff(u.m61, eu.m61), oracle_tol(abs_raw(eu.m61), tol)));
        ex = max(ex, excess(ulp_diff(u.m62, eu.m62), oracle_tol(abs_raw(eu.m62), tol)));
        ex = max(ex, excess(ulp_diff(u.m63, eu.m63), oracle_tol(abs_raw(eu.m63), tol)));
        ex = max(ex, excess(ulp_diff(u.m64, eu.m64), oracle_tol(abs_raw(eu.m64), tol)));
        ex = max(ex, excess(ulp_diff(u.m65, eu.m65), oracle_tol(abs_raw(eu.m65), tol)));
        ex = max(ex, excess(ulp_diff(u.m66, eu.m66), oracle_tol(abs_raw(eu.m66), tol)));
        ex = max(ex, excess(ulp_diff(d.m11, ed.m11), oracle_tol(abs_raw(ed.m11), tol)));
        ex = max(ex, excess(ulp_diff(d.m12, ed.m12), oracle_tol(abs_raw(ed.m12), tol)));
        ex = max(ex, excess(ulp_diff(d.m13, ed.m13), oracle_tol(abs_raw(ed.m13), tol)));
        ex = max(ex, excess(ulp_diff(d.m14, ed.m14), oracle_tol(abs_raw(ed.m14), tol)));
        ex = max(ex, excess(ulp_diff(d.m15, ed.m15), oracle_tol(abs_raw(ed.m15), tol)));
        ex = max(ex, excess(ulp_diff(d.m16, ed.m16), oracle_tol(abs_raw(ed.m16), tol)));
        ex = max(ex, excess(ulp_diff(d.m21, ed.m21), oracle_tol(abs_raw(ed.m21), tol)));
        ex = max(ex, excess(ulp_diff(d.m22, ed.m22), oracle_tol(abs_raw(ed.m22), tol)));
        ex = max(ex, excess(ulp_diff(d.m23, ed.m23), oracle_tol(abs_raw(ed.m23), tol)));
        ex = max(ex, excess(ulp_diff(d.m24, ed.m24), oracle_tol(abs_raw(ed.m24), tol)));
        ex = max(ex, excess(ulp_diff(d.m25, ed.m25), oracle_tol(abs_raw(ed.m25), tol)));
        ex = max(ex, excess(ulp_diff(d.m26, ed.m26), oracle_tol(abs_raw(ed.m26), tol)));
        ex = max(ex, excess(ulp_diff(d.m31, ed.m31), oracle_tol(abs_raw(ed.m31), tol)));
        ex = max(ex, excess(ulp_diff(d.m32, ed.m32), oracle_tol(abs_raw(ed.m32), tol)));
        ex = max(ex, excess(ulp_diff(d.m33, ed.m33), oracle_tol(abs_raw(ed.m33), tol)));
        ex = max(ex, excess(ulp_diff(d.m34, ed.m34), oracle_tol(abs_raw(ed.m34), tol)));
        ex = max(ex, excess(ulp_diff(d.m35, ed.m35), oracle_tol(abs_raw(ed.m35), tol)));
        ex = max(ex, excess(ulp_diff(d.m36, ed.m36), oracle_tol(abs_raw(ed.m36), tol)));
        ex = max(ex, excess(ulp_diff(d.m41, ed.m41), oracle_tol(abs_raw(ed.m41), tol)));
        ex = max(ex, excess(ulp_diff(d.m42, ed.m42), oracle_tol(abs_raw(ed.m42), tol)));
        ex = max(ex, excess(ulp_diff(d.m43, ed.m43), oracle_tol(abs_raw(ed.m43), tol)));
        ex = max(ex, excess(ulp_diff(d.m44, ed.m44), oracle_tol(abs_raw(ed.m44), tol)));
        ex = max(ex, excess(ulp_diff(d.m45, ed.m45), oracle_tol(abs_raw(ed.m45), tol)));
        ex = max(ex, excess(ulp_diff(d.m46, ed.m46), oracle_tol(abs_raw(ed.m46), tol)));
        ex = max(ex, excess(ulp_diff(d.m51, ed.m51), oracle_tol(abs_raw(ed.m51), tol)));
        ex = max(ex, excess(ulp_diff(d.m52, ed.m52), oracle_tol(abs_raw(ed.m52), tol)));
        ex = max(ex, excess(ulp_diff(d.m53, ed.m53), oracle_tol(abs_raw(ed.m53), tol)));
        ex = max(ex, excess(ulp_diff(d.m54, ed.m54), oracle_tol(abs_raw(ed.m54), tol)));
        ex = max(ex, excess(ulp_diff(d.m55, ed.m55), oracle_tol(abs_raw(ed.m55), tol)));
        ex = max(ex, excess(ulp_diff(d.m56, ed.m56), oracle_tol(abs_raw(ed.m56), tol)));
        ex = max(ex, excess(ulp_diff(d.m61, ed.m61), oracle_tol(abs_raw(ed.m61), tol)));
        ex = max(ex, excess(ulp_diff(d.m62, ed.m62), oracle_tol(abs_raw(ed.m62), tol)));
        ex = max(ex, excess(ulp_diff(d.m63, ed.m63), oracle_tol(abs_raw(ed.m63), tol)));
        ex = max(ex, excess(ulp_diff(d.m64, ed.m64), oracle_tol(abs_raw(ed.m64), tol)));
        ex = max(ex, excess(ulp_diff(d.m65, ed.m65), oracle_tol(abs_raw(ed.m65), tol)));
        ex = max(ex, excess(ulp_diff(d.m66, ed.m66), oracle_tol(abs_raw(ed.m66), tol)));
        ex = max(ex, excess(ulp_diff(vt.m11, evt.m11), oracle_tol(abs_raw(evt.m11), tol)));
        ex = max(ex, excess(ulp_diff(vt.m12, evt.m12), oracle_tol(abs_raw(evt.m12), tol)));
        ex = max(ex, excess(ulp_diff(vt.m13, evt.m13), oracle_tol(abs_raw(evt.m13), tol)));
        ex = max(ex, excess(ulp_diff(vt.m14, evt.m14), oracle_tol(abs_raw(evt.m14), tol)));
        ex = max(ex, excess(ulp_diff(vt.m15, evt.m15), oracle_tol(abs_raw(evt.m15), tol)));
        ex = max(ex, excess(ulp_diff(vt.m16, evt.m16), oracle_tol(abs_raw(evt.m16), tol)));
        ex = max(ex, excess(ulp_diff(vt.m21, evt.m21), oracle_tol(abs_raw(evt.m21), tol)));
        ex = max(ex, excess(ulp_diff(vt.m22, evt.m22), oracle_tol(abs_raw(evt.m22), tol)));
        ex = max(ex, excess(ulp_diff(vt.m23, evt.m23), oracle_tol(abs_raw(evt.m23), tol)));
        ex = max(ex, excess(ulp_diff(vt.m24, evt.m24), oracle_tol(abs_raw(evt.m24), tol)));
        ex = max(ex, excess(ulp_diff(vt.m25, evt.m25), oracle_tol(abs_raw(evt.m25), tol)));
        ex = max(ex, excess(ulp_diff(vt.m26, evt.m26), oracle_tol(abs_raw(evt.m26), tol)));
        ex = max(ex, excess(ulp_diff(vt.m31, evt.m31), oracle_tol(abs_raw(evt.m31), tol)));
        ex = max(ex, excess(ulp_diff(vt.m32, evt.m32), oracle_tol(abs_raw(evt.m32), tol)));
        ex = max(ex, excess(ulp_diff(vt.m33, evt.m33), oracle_tol(abs_raw(evt.m33), tol)));
        ex = max(ex, excess(ulp_diff(vt.m34, evt.m34), oracle_tol(abs_raw(evt.m34), tol)));
        ex = max(ex, excess(ulp_diff(vt.m35, evt.m35), oracle_tol(abs_raw(evt.m35), tol)));
        ex = max(ex, excess(ulp_diff(vt.m36, evt.m36), oracle_tol(abs_raw(evt.m36), tol)));
        ex = max(ex, excess(ulp_diff(vt.m41, evt.m41), oracle_tol(abs_raw(evt.m41), tol)));
        ex = max(ex, excess(ulp_diff(vt.m42, evt.m42), oracle_tol(abs_raw(evt.m42), tol)));
        ex = max(ex, excess(ulp_diff(vt.m43, evt.m43), oracle_tol(abs_raw(evt.m43), tol)));
        ex = max(ex, excess(ulp_diff(vt.m44, evt.m44), oracle_tol(abs_raw(evt.m44), tol)));
        ex = max(ex, excess(ulp_diff(vt.m45, evt.m45), oracle_tol(abs_raw(evt.m45), tol)));
        ex = max(ex, excess(ulp_diff(vt.m46, evt.m46), oracle_tol(abs_raw(evt.m46), tol)));
        ex = max(ex, excess(ulp_diff(vt.m51, evt.m51), oracle_tol(abs_raw(evt.m51), tol)));
        ex = max(ex, excess(ulp_diff(vt.m52, evt.m52), oracle_tol(abs_raw(evt.m52), tol)));
        ex = max(ex, excess(ulp_diff(vt.m53, evt.m53), oracle_tol(abs_raw(evt.m53), tol)));
        ex = max(ex, excess(ulp_diff(vt.m54, evt.m54), oracle_tol(abs_raw(evt.m54), tol)));
        ex = max(ex, excess(ulp_diff(vt.m55, evt.m55), oracle_tol(abs_raw(evt.m55), tol)));
        ex = max(ex, excess(ulp_diff(vt.m56, evt.m56), oracle_tol(abs_raw(evt.m56), tol)));
        ex = max(ex, excess(ulp_diff(vt.m61, evt.m61), oracle_tol(abs_raw(evt.m61), tol)));
        ex = max(ex, excess(ulp_diff(vt.m62, evt.m62), oracle_tol(abs_raw(evt.m62), tol)));
        ex = max(ex, excess(ulp_diff(vt.m63, evt.m63), oracle_tol(abs_raw(evt.m63), tol)));
        ex = max(ex, excess(ulp_diff(vt.m64, evt.m64), oracle_tol(abs_raw(evt.m64), tol)));
        ex = max(ex, excess(ulp_diff(vt.m65, evt.m65), oracle_tol(abs_raw(evt.m65), tol)));
        ex = max(ex, excess(ulp_diff(vt.m66, evt.m66), oracle_tol(abs_raw(evt.m66), tol)));
        rec = max(rec, max_ulp_6x6(u.mul_mat(d).mul_mat(vt), a) / amax_6x6(a));
        orth = max(orth, max(orth_6x6(u), orth_6x6(vt)));
        let mut m = a;
        let mut packed = Vector6 { x: fx(0), y: fx(0), z: fx(0), w: fx(0), a: fx(0), b: fx(0) };
        let mut work = Vector6 { x: fx(0), y: fx(0), z: fx(0), w: fx(0), a: fx(0), b: fx(0) };
        let mut none: Option<Vector6<Fixed>> = Option::None;
        let d0 = clear_column_unchecked(ref m, 0, 0, ref none);
        let e0 = clear_row_unchecked(ref m, ref packed, ref work, 0, 1);
        let d1 = clear_column_unchecked(ref m, 1, 0, ref none);
        let e1 = clear_row_unchecked(ref m, ref packed, ref work, 1, 1);
        let d2 = clear_column_unchecked(ref m, 2, 0, ref none);
        let e2 = clear_row_unchecked(ref m, ref packed, ref work, 2, 1);
        let d3 = clear_column_unchecked(ref m, 3, 0, ref none);
        let e3 = clear_row_unchecked(ref m, ref packed, ref work, 3, 1);
        let d4 = clear_column_unchecked(ref m, 4, 0, ref none);
        let e4 = clear_row_unchecked(ref m, ref packed, ref work, 4, 1);
        let d5 = clear_column_unchecked(ref m, 5, 0, ref none);
        assert!(
            m == bd.uv
                && Vector6 { x: d0, y: d1, z: d2, w: d3, a: d4, b: d5 } == bd.diagonal
                && Vector5 { x: e0, y: e1, z: e2, w: e3, a: e4 } == bd.off_diagonal,
            "householder steps",
        );
        assert!(none.is_none());
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 19 && orth <= 11, "measured {} {}", rec, orth);
}

#[test]
#[inline(never)]
fn bench_bidiagonal6_new__baseline() {
    let (a, _, _, _, _) = *oracle::bidiagonal6_cases().at(3);
    let a = black_box(mat6x6(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Householder reflections alternately from the left and the right, unrolled.
#[test]
#[inline(never)]
fn bench_bidiagonal6_new__householder() {
    let (a, _, _, _, _) = *oracle::bidiagonal6_cases().at(3);
    let a = black_box(mat6x6(a));
    let e = black_box(true);
    let bd = a.bidiagonalize();
    assert!((bd.uv.m11 == bd.uv.m11) == e);
}

#[test]
#[inline(never)]
fn bench_bidiagonal6_unpack__baseline() {
    let (a, _, _, _, _) = *oracle::bidiagonal6_cases().at(3);
    let a = black_box(black_box(mat6x6(a)).bidiagonalize());
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// `(U, D, Vᵀ)`: the reflections applied to symbolic identity entries.
#[test]
#[inline(never)]
fn bench_bidiagonal6_unpack__symbolic() {
    let (a, _, _, _, _) = *oracle::bidiagonal6_cases().at(3);
    let a = black_box(black_box(mat6x6(a)).bidiagonalize());
    let e = black_box(true);
    let (u, _, vt) = a.unpack();
    assert!((u.m11 == vt.m11 || true) == e);
}
