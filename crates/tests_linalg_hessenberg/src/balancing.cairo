// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `balance_parlett_reinsch` / `unbalance` through the public API (WP 8.5-P16): oracle vectors
//! (`tools/oracle` suite `schur`), the round trip, gas benchmarks.

use core::cmp::max;
use nalgebra::linalg::{balance_parlett_reinsch, unbalance};
use nalgebra_testing::black_box;
use crate::builders::{
    amax_1x1, amax_2x2, amax_3x3, amax_4x4, amax_5x5, amax_6x6, mat1x1, mat2x2, mat3x3, mat4x4,
    mat5x5, mat6x6, max_ulp_1x1, max_ulp_2x2, max_ulp_3x3, max_ulp_4x4, max_ulp_5x5, max_ulp_6x6,
    vec1, vec2, vec3, vec4, vec5, vec6,
};
use crate::oracle_schur as oracle;
use crate::util::excess_all;

/// `balance1` (oracle): the balanced matrix and `d` within the oracle tolerance; `unbalance`
/// restores the input within the measured bound (per unit of max |a|).
#[test]
fn test_oracle_balance1() {
    let mut cases = oracle::balance1_cases();
    let (mut ex, mut back) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eb, ed, tol) = *case;
        let a = black_box(mat1x1(a));
        let mut m = a;
        let d = balance_parlett_reinsch(ref m);
        let (eb, ed) = (mat1x1(eb), vec1(ed));
        ex = max(ex, excess_all(m, eb, tol));
        ex = max(ex, excess_all(d, ed, tol));
        unbalance(ref m, d);
        back = max(back, max_ulp_1x1(m, a) / amax_1x1(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(back <= 0, "measured {}", back);
}

#[test]
#[inline(never)]
fn bench_balance1__baseline() {
    let (a, _, _, _) = *oracle::balance1_cases().at(3);
    let a = black_box(mat1x1(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Upstream's data-dependent loop, the sweeps unrolled.
#[test]
#[inline(never)]
fn bench_balance1__parlett_reinsch() {
    let (a, _, _, _) = *oracle::balance1_cases().at(3);
    let a = black_box(mat1x1(a));
    let e = black_box(true);
    let mut m = a;
    let d = balance_parlett_reinsch(ref m);
    assert!((d.x == d.x) == e);
}

/// `balance2` (oracle): the balanced matrix and `d` within the oracle tolerance; `unbalance`
/// restores the input within the measured bound (per unit of max |a|).
#[test]
fn test_oracle_balance2() {
    let mut cases = oracle::balance2_cases();
    let (mut ex, mut back) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eb, ed, tol) = *case;
        let a = black_box(mat2x2(a));
        let mut m = a;
        let d = balance_parlett_reinsch(ref m);
        let (eb, ed) = (mat2x2(eb), vec2(ed));
        ex = max(ex, excess_all(m, eb, tol));
        ex = max(ex, excess_all(d, ed, tol));
        unbalance(ref m, d);
        back = max(back, max_ulp_2x2(m, a) / amax_2x2(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(back <= 0, "measured {}", back);
}

#[test]
#[inline(never)]
fn bench_balance2__baseline() {
    let (a, _, _, _) = *oracle::balance2_cases().at(3);
    let a = black_box(mat2x2(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Upstream's data-dependent loop, the sweeps unrolled.
#[test]
#[inline(never)]
fn bench_balance2__parlett_reinsch() {
    let (a, _, _, _) = *oracle::balance2_cases().at(3);
    let a = black_box(mat2x2(a));
    let e = black_box(true);
    let mut m = a;
    let d = balance_parlett_reinsch(ref m);
    assert!((d.x == d.x) == e);
}

/// `balance3` (oracle): the balanced matrix and `d` within the oracle tolerance; `unbalance`
/// restores the input within the measured bound (per unit of max |a|).
#[test]
fn test_oracle_balance3() {
    let mut cases = oracle::balance3_cases();
    let (mut ex, mut back) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eb, ed, tol) = *case;
        let a = black_box(mat3x3(a));
        let mut m = a;
        let d = balance_parlett_reinsch(ref m);
        let (eb, ed) = (mat3x3(eb), vec3(ed));
        ex = max(ex, excess_all(m, eb, tol));
        ex = max(ex, excess_all(d, ed, tol));
        unbalance(ref m, d);
        back = max(back, max_ulp_3x3(m, a) / amax_3x3(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(back <= 0, "measured {}", back);
}

#[test]
#[inline(never)]
fn bench_balance3__baseline() {
    let (a, _, _, _) = *oracle::balance3_cases().at(3);
    let a = black_box(mat3x3(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Upstream's data-dependent loop, the sweeps unrolled.
#[test]
#[inline(never)]
fn bench_balance3__parlett_reinsch() {
    let (a, _, _, _) = *oracle::balance3_cases().at(3);
    let a = black_box(mat3x3(a));
    let e = black_box(true);
    let mut m = a;
    let d = balance_parlett_reinsch(ref m);
    assert!((d.x == d.x) == e);
}

/// `balance4` (oracle): the balanced matrix and `d` within the oracle tolerance; `unbalance`
/// restores the input within the measured bound (per unit of max |a|).
#[test]
fn test_oracle_balance4() {
    let mut cases = oracle::balance4_cases();
    let (mut ex, mut back) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eb, ed, tol) = *case;
        let a = black_box(mat4x4(a));
        let mut m = a;
        let d = balance_parlett_reinsch(ref m);
        let (eb, ed) = (mat4x4(eb), vec4(ed));
        ex = max(ex, excess_all(m, eb, tol));
        ex = max(ex, excess_all(d, ed, tol));
        unbalance(ref m, d);
        back = max(back, max_ulp_4x4(m, a) / amax_4x4(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(back <= 1, "measured {}", back);
}

#[test]
#[inline(never)]
fn bench_balance4__baseline() {
    let (a, _, _, _) = *oracle::balance4_cases().at(3);
    let a = black_box(mat4x4(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Upstream's data-dependent loop, the sweeps unrolled.
#[test]
#[inline(never)]
fn bench_balance4__parlett_reinsch() {
    let (a, _, _, _) = *oracle::balance4_cases().at(3);
    let a = black_box(mat4x4(a));
    let e = black_box(true);
    let mut m = a;
    let d = balance_parlett_reinsch(ref m);
    assert!((d.x == d.x) == e);
}

/// `balance5` (oracle): the balanced matrix and `d` within the oracle tolerance; `unbalance`
/// restores the input within the measured bound (per unit of max |a|).
#[test]
fn test_oracle_balance5() {
    let mut cases = oracle::balance5_cases();
    let (mut ex, mut back) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eb, ed, tol) = *case;
        let a = black_box(mat5x5(a));
        let mut m = a;
        let d = balance_parlett_reinsch(ref m);
        let (eb, ed) = (mat5x5(eb), vec5(ed));
        ex = max(ex, excess_all(m, eb, tol));
        ex = max(ex, excess_all(d, ed, tol));
        unbalance(ref m, d);
        back = max(back, max_ulp_5x5(m, a) / amax_5x5(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(back <= 2, "measured {}", back);
}

#[test]
#[inline(never)]
fn bench_balance5__baseline() {
    let (a, _, _, _) = *oracle::balance5_cases().at(3);
    let a = black_box(mat5x5(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Upstream's data-dependent loop, the sweeps unrolled.
#[test]
#[inline(never)]
fn bench_balance5__parlett_reinsch() {
    let (a, _, _, _) = *oracle::balance5_cases().at(3);
    let a = black_box(mat5x5(a));
    let e = black_box(true);
    let mut m = a;
    let d = balance_parlett_reinsch(ref m);
    assert!((d.x == d.x) == e);
}

/// `balance6` (oracle): the balanced matrix and `d` within the oracle tolerance; `unbalance`
/// restores the input within the measured bound (per unit of max |a|).
#[test]
fn test_oracle_balance6() {
    let mut cases = oracle::balance6_cases();
    let (mut ex, mut back) = (0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eb, ed, tol) = *case;
        let a = black_box(mat6x6(a));
        let mut m = a;
        let d = balance_parlett_reinsch(ref m);
        let (eb, ed) = (mat6x6(eb), vec6(ed));
        ex = max(ex, excess_all(m, eb, tol));
        ex = max(ex, excess_all(d, ed, tol));
        unbalance(ref m, d);
        back = max(back, max_ulp_6x6(m, a) / amax_6x6(a));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(back <= 1, "measured {}", back);
}

#[test]
#[inline(never)]
fn bench_balance6__baseline() {
    let (a, _, _, _) = *oracle::balance6_cases().at(3);
    let a = black_box(mat6x6(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Upstream's data-dependent loop, the sweeps unrolled.
#[test]
#[inline(never)]
fn bench_balance6__parlett_reinsch() {
    let (a, _, _, _) = *oracle::balance6_cases().at(3);
    let a = black_box(mat6x6(a));
    let e = black_box(true);
    let mut m = a;
    let d = balance_parlett_reinsch(ref m);
    assert!((d.x == d.x) == e);
}
