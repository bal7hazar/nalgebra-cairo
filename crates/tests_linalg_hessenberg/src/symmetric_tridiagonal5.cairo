// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `SymmetricTridiagonal5` / `Matrix5SymmetricTridiagonalTrait` through the public API (WP
//! 8.5-P16): oracle vectors (`tools/oracle` suite `schur`), the factor identities, gas benchmarks.

use core::cmp::max;
use nalgebra::linalg::{Matrix5SymmetricTridiagonalTrait, SymmetricTridiagonal5Trait};
use nalgebra_testing::black_box;
use crate::builders::{amax_5x5, mat5x5, max_ulp_5x5, orth_5x5, vec4, vec5};
use crate::oracle_schur as oracle;
use crate::util::excess_all;

/// `symmetric_tridiagonal5` (oracle): `q`, the diagonal and the off-diagonal within the oracle
/// tolerance (upstream's signs), `recompose()` = `A` and `QᵀQ = I` within the measured bounds,
/// the accessors consistent.
#[test]
fn test_oracle_symmetric_tridiagonal5() {
    let mut cases = oracle::symmetric_tridiagonal5_cases();
    let (mut ex, mut rec, mut orth) = (0, 0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, eq, ed, ee, tol) = *case;
        let a = black_box(mat5x5(a));
        let t = a.symmetric_tridiagonalize();
        let (q, d, e) = t.unpack();
        assert!(q == t.q() && d == t.diagonal() && t.internal_tri() == t.tri);
        assert!(t.unpack_tridiagonal() == (d, e) && t.off_diagonal() == e);
        let (eq, ed) = (mat5x5(eq), vec5(ed));
        ex = max(ex, excess_all(q, eq, tol));
        ex = max(ex, excess_all(d, ed, tol));
        let ee = vec4(ee);
        ex = max(ex, excess_all(e, ee, tol));
        rec = max(rec, max_ulp_5x5(t.recompose(), a) / amax_5x5(a));
        orth = max(orth, orth_5x5(q));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 30 && orth <= 11, "measured {} {}", rec, orth);
}

#[test]
#[inline(never)]
fn bench_symmetric_tridiagonal5_new__baseline() {
    let (a, _, _, _, _) = *oracle::symmetric_tridiagonal5_cases().at(3);
    let a = black_box(mat5x5(a));
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// Householder reflections on the lower triangle (`dsytd2` form), unrolled.
#[test]
#[inline(never)]
fn bench_symmetric_tridiagonal5_new__householder() {
    let (a, _, _, _, _) = *oracle::symmetric_tridiagonal5_cases().at(3);
    let a = black_box(mat5x5(a));
    let e = black_box(true);
    let t = a.symmetric_tridiagonalize();
    assert!((t.tri.m11 == t.tri.m11) == e);
}

#[test]
#[inline(never)]
fn bench_symmetric_tridiagonal5_q__baseline() {
    let (a, _, _, _, _) = *oracle::symmetric_tridiagonal5_cases().at(3);
    let a = black_box(black_box(mat5x5(a)).symmetric_tridiagonalize());
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// `assemble_q` on symbolic identity entries.
#[test]
#[inline(never)]
fn bench_symmetric_tridiagonal5_q__assemble() {
    let (a, _, _, _, _) = *oracle::symmetric_tridiagonal5_cases().at(3);
    let a = black_box(black_box(mat5x5(a)).symmetric_tridiagonalize());
    let e = black_box(true);
    let q = a.q();
    assert!((q.m11 == q.m11) == e);
}

#[test]
#[inline(never)]
fn bench_symmetric_tridiagonal5_recompose__baseline() {
    let (a, _, _, _, _) = *oracle::symmetric_tridiagonal5_cases().at(3);
    let a = black_box(black_box(mat5x5(a)).symmetric_tridiagonalize());
    let e = black_box(true);
    let _ = a;
    assert!(e == e);
}

/// `Q T Qᵀ`: `Q T` (tridiagonal) then `(Q T) Qᵀ`, fused sums.
#[test]
#[inline(never)]
fn bench_symmetric_tridiagonal5_recompose__two_products() {
    let (a, _, _, _, _) = *oracle::symmetric_tridiagonal5_cases().at(3);
    let a = black_box(black_box(mat5x5(a)).symmetric_tridiagonalize());
    let e = black_box(true);
    let m = a.recompose();
    assert!((m.m11 == m.m11) == e);
}
