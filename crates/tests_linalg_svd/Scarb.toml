[package]
name = "nalgebra_tests_linalg_svd"
description = "Tests and gas benchmarks of the SVD of the squares 1 to 4: singular values, rank, pseudo-inverse, polar decomposition (WP 8.5-P14b; not published)."
version.workspace = true
edition.workspace = true
cairo-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
publish = false

[dependencies]
nalgebra = { path = "../nalgebra", default-features = false, features = ["eigen", "svd"] }
simba.workspace = true
fixed.workspace = true

[dev-dependencies]
nalgebra_tests_utils = { path = "../tests_utils", features = ["svd"] }
nalgebra_testing = { path = "../testing" }
snforge_std.workspace = true
assert_macros.workspace = true

[tool]
fmt.workspace = true
scarb.workspace = true
