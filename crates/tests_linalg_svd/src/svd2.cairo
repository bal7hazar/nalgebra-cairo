// Generated by crates/nalgebra/src/linalg/generate.py: do not edit by hand.
//! `Svd2` / `Matrix2SvdTrait` through the public API (WP 8.5-P14b): oracle vectors (`tools/oracle`
//! suite `spectral`), exact cases, the `try` / unordered forms, gas benchmarks.

use core::cmp::max;
use fixed::Fixed;
use nalgebra::linalg::{Matrix2SvdTrait, Svd2Trait};
use nalgebra::{Matrix2, Vector2};
use nalgebra_testing::black_box;
use nalgebra_tests_utils::{Svd2PartialEq, abs_raw, excess, fx, oracle_tol, ulp_diff};
use crate::builders::{amax_2x2, mat2x2, max_ulp_2x2, orth_2x2, vec2};
use crate::oracle_svd as oracle;

const ONE: i64 = 0x100000000;
/// `round(1e-4 * 2^32)`: the oracle's `pseudo_inverse` / `rank` threshold.
const EPS: i64 = 429497;

/// `(singular-value excess over the oracle tolerance, reconstruction error per unit of
/// `max(1, max |a_ij|)`, orthonormality error of `U` and `V`)` of one case, in raw units; also
/// checks that `singular_values` is bit-identical to the decomposition's.
fn measure(a: Matrix2<Fixed>, expected: Vector2<Fixed>, tol: u64) -> (u128, u128, u128) {
    let d = a.svd();
    assert!(a.singular_values() == d.singular_values, "singular values differ from svd");
    let mut ex = 0;
    ex =
        max(
            ex,
            excess(ulp_diff(d.singular_values.x, expected.x), oracle_tol(abs_raw(expected.x), tol)),
        );
    ex =
        max(
            ex,
            excess(ulp_diff(d.singular_values.y, expected.y), oracle_tol(abs_raw(expected.y), tol)),
        );
    (ex, max_ulp_2x2(d.recompose(), a) / amax_2x2(a), max(orth_2x2(d.u), orth_2x2(d.v_t)))
}

/// The worst `measure` over `cases`.
fn run(cases: Span<([[i64; 2]; 2], (i64, i64), u64)>) -> (u128, u128, u128) {
    let mut cases = cases;
    let (mut ex, mut rec, mut orth) = (0, 0, 0);
    while let Some(case) = cases.pop_front() {
        let (a, expected, tol) = *case;
        let (e, r, o) = measure(black_box(mat2x2(a)), vec2(expected), tol);
        ex = max(ex, e);
        rec = max(rec, r);
        orth = max(orth, o);
    }
    (ex, rec, orth)
}

/// `svd2_singular_values` (oracle, well-conditioned): singular values within the oracle
/// tolerance, `U Σ Vᵀ = M` and the orthonormality of `U` / `V` within the measured bounds.
#[test]
fn test_oracle_svd2_singular_values() {
    let (ex, rec, orth) = run(oracle::svd2_singular_values_cases());
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 3 && orth <= 5, "measured {} {}", rec, orth);
}
/// `svd2_singular_values_deficient` (oracle, EXACTLY rank-deficient integer matrices): the
/// same checks; `U` stays orthonormal although some columns of `M V` vanish.
#[test]
fn test_oracle_svd2_singular_values_deficient() {
    let (ex, rec, orth) = run(oracle::svd2_singular_values_deficient_cases());
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 0 && orth <= 0, "measured {} {}", rec, orth);
}

/// `svd2_singular_values_near_singular` (oracle, FLAGGED: one singular value in [1e-4,
/// 1e-2]).
#[test]
fn test_oracle_svd2_singular_values_near_singular() {
    let (ex, rec, orth) = run(oracle::svd2_singular_values_near_singular_cases());
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
    assert!(rec <= 2 && orth <= 1, "measured {} {}", rec, orth);
}

/// `svd2_rank_deficient` (oracle): `rank(1e-4)` of exactly rank-deficient integer matrices.
#[test]
fn test_oracle_svd2_rank_deficient() {
    let mut cases = oracle::svd2_rank_deficient_cases();
    while let Some(case) = cases.pop_front() {
        let (a, expected, _) = *case;
        let a = black_box(mat2x2(a));
        let rank: i64 = a.rank(fx(EPS)).into();
        assert!(rank * 0x100000000 == expected, "rank {}", rank);
        let rank2: i64 = a.svd().rank(fx(EPS)).into();
        assert!(rank2 == rank);
    }
}
/// `svd2_pseudo_inverse` (oracle): every entry within the oracle tolerance.
#[test]
fn test_oracle_svd2_pseudo_inverse() {
    let mut cases = oracle::svd2_pseudo_inverse_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, e, tol) = *case;
        let p = black_box(mat2x2(a)).pseudo_inverse(fx(EPS)).unwrap();
        let e = mat2x2(e);
        ex = max(ex, excess(ulp_diff(p.m11, e.m11), oracle_tol(abs_raw(e.m11), tol)));
        ex = max(ex, excess(ulp_diff(p.m12, e.m12), oracle_tol(abs_raw(e.m12), tol)));
        ex = max(ex, excess(ulp_diff(p.m21, e.m21), oracle_tol(abs_raw(e.m21), tol)));
        ex = max(ex, excess(ulp_diff(p.m22, e.m22), oracle_tol(abs_raw(e.m22), tol)));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}

/// `svd2_polar` (oracle): `P` and `U` of the left polar decomposition, every entry within
/// the oracle tolerance (both factors are unique for a full-rank `M`).
#[test]
fn test_oracle_svd2_polar() {
    let mut cases = oracle::svd2_polar_cases();
    let mut ex = 0;
    while let Some(case) = cases.pop_front() {
        let (a, ep, eu, tol) = *case;
        let (p, u) = black_box(mat2x2(a)).polar();
        let (ep, eu) = (mat2x2(ep), mat2x2(eu));
        ex = max(ex, excess(ulp_diff(p.m11, ep.m11), oracle_tol(abs_raw(ep.m11), tol)));
        ex = max(ex, excess(ulp_diff(p.m12, ep.m12), oracle_tol(abs_raw(ep.m12), tol)));
        ex = max(ex, excess(ulp_diff(p.m21, ep.m21), oracle_tol(abs_raw(ep.m21), tol)));
        ex = max(ex, excess(ulp_diff(p.m22, ep.m22), oracle_tol(abs_raw(ep.m22), tol)));
        ex = max(ex, excess(ulp_diff(u.m11, eu.m11), oracle_tol(abs_raw(eu.m11), tol)));
        ex = max(ex, excess(ulp_diff(u.m12, eu.m12), oracle_tol(abs_raw(eu.m12), tol)));
        ex = max(ex, excess(ulp_diff(u.m21, eu.m21), oracle_tol(abs_raw(eu.m21), tol)));
        ex = max(ex, excess(ulp_diff(u.m22, eu.m22), oracle_tol(abs_raw(eu.m22), tol)));
    }
    assert!(ex == 0, "oracle tolerance exceeded by {}", ex);
}
#[test]
fn test_svd2_diagonal_and_zero_are_exact() {
    // A rectangular diagonal: the singular values are its entries, sorted, and every factor is
    // exact.
    let a = black_box(mat2x2([[12884901888, 0], [0, 30064771072]]));
    let d = a.svd();
    assert!(d.singular_values == Vector2 { x: fx(7 * ONE), y: fx(3 * ONE) });
    assert!(d.recompose() == a);
    assert!(orth_2x2(d.u) == 0 && orth_2x2(d.v_t) == 0);
    // The zero matrix: nothing divides by zero, `U` is completed to an orthonormal basis.
    let z = black_box(mat2x2([[0, 0], [0, 0]]));
    let d = z.svd();
    assert!(d.rank(fx(0)) == 0);
    assert!(d.recompose() == z);
    assert!(orth_2x2(d.u) == 0 && orth_2x2(d.v_t) == 0);
}

#[test]
fn test_svd2_api() {
    let (a, _, _) = *oracle::svd2_singular_values_cases().at(0);
    let a = black_box(mat2x2(a));
    let d = Svd2Trait::new(a);
    // The unordered and `try` forms, `sort_by_singular_values` on a sorted decomposition.
    assert!(Svd2Trait::new_unordered(a) == d);
    assert!(a.svd_unordered() == d);
    assert!(Svd2Trait::try_new(a, fx(ONE), 0).unwrap() == d);
    assert!(a.try_svd(fx(ONE), 100).unwrap() == d);
    assert!(a.try_svd_unordered(fx(ONE), 100).unwrap() == d);
    assert!(a.singular_values_unordered() == d.singular_values);
    let mut e = d;
    e.sort_by_singular_values();
    assert!(e == d);
    let (p, u) = a.polar();
    let (p2, u2) = a.try_polar(fx(ONE), 0).unwrap();
    assert!(p2 == p && u2 == u);
    let (p3, u3) = d.to_polar().unwrap();
    assert!(p3 == p && u3 == u);
    assert!(d.pseudo_inverse(fx(-1)).is_none());
    assert!(d.solve(Vector2 { x: fx(ONE), y: fx(ONE) }, fx(-1)).is_none());
    assert!(d.solve(Vector2 { x: fx(ONE), y: fx(ONE) }, fx(EPS)).is_some());
}
/// A `unit` oracle case: the benchmark input.
fn a_bench() -> Matrix2<Fixed> {
    let (a, _, _) = *oracle::svd2_singular_values_cases().at(3);
    mat2x2(a)
}

#[test]
#[inline(never)]
fn bench_svd2_new__baseline() {
    let _a = black_box(a_bench());
    let e = black_box(true);
    assert!(e == e);
}

/// The decomposition: Gram matrix, its eigen decomposition, `M V`, Gram-Schmidt twice.
#[test]
#[inline(never)]
fn bench_svd2_new__eigen_of_gram() {
    let a = black_box(a_bench());
    let e = black_box(true);
    let d = a.svd();
    assert!((d.singular_values.x >= d.singular_values.y) == e);
}

#[test]
#[inline(never)]
fn bench_svd2_singular_values__baseline() {
    let _a = black_box(a_bench());
    let e = black_box(true);
    assert!(e == e);
}

/// The singular values alone (no left vectors).
#[test]
#[inline(never)]
fn bench_svd2_singular_values__eigen_of_gram() {
    let a = black_box(a_bench());
    let e = black_box(true);
    let s = a.singular_values();
    assert!((s.x >= s.y) == e);
}

#[test]
#[inline(never)]
fn bench_svd2_pseudo_inverse__baseline() {
    let _d = black_box(a_bench().svd());
    let e = black_box(true);
    assert!(e == e);
}

/// `V diag(σ⁺) Uᵀ` from a decomposition.
#[test]
#[inline(never)]
fn bench_svd2_pseudo_inverse__scaled_product() {
    let d = black_box(a_bench().svd());
    let e = black_box(true);
    let p = d.pseudo_inverse(fx(EPS)).unwrap();
    assert!((p.m11 == p.m11) == e);
}

#[test]
#[inline(never)]
fn bench_svd2_to_polar__baseline() {
    let _d = black_box(a_bench().svd());
    let e = black_box(true);
    assert!(e == e);
}

/// `(u σ uᵀ, u v_t)` from a decomposition.
#[test]
#[inline(never)]
fn bench_svd2_to_polar__products() {
    let d = black_box(a_bench().svd());
    let e = black_box(true);
    let (p, _u) = d.to_polar().unwrap();
    assert!((p.m11 == p.m11) == e);
}
