// @generated by gen_tests.py — do not edit by hand.

#[derive(Copy, Drop)]
pub struct Mat2F<T> {
    pub m11: T,
    pub m12: T,
    pub m21: T,
    pub m22: T,
}

#[generate_trait]
pub impl Mat2FImpl<T, +Add<T>, +Mul<T>, +Copy<T>, +Drop<T>> of Mat2FTrait<T> {
    fn from_array(data: [T; 4]) -> Mat2F<T> {
        let [m11, m12, m21, m22] = data;
        Mat2F { m11, m12, m21, m22 }
    }
    fn to_array(self: Mat2F<T>) -> [T; 4] {
        [self.m11, self.m12, self.m21, self.m22]
    }
    fn mul_mat(self: Mat2F<T>, rhs: Mat2F<T>) -> Mat2F<T> {
        let (a, b) = (self, rhs);
        Mat2F {
            m11: a.m11 * b.m11 + a.m12 * b.m21,
            m12: a.m11 * b.m12 + a.m12 * b.m22,
            m21: a.m21 * b.m11 + a.m22 * b.m21,
            m22: a.m21 * b.m12 + a.m22 * b.m22,
        }
    }
}

#[derive(Copy, Drop)]
pub struct Mat4F<T> {
    pub m11: T,
    pub m12: T,
    pub m13: T,
    pub m14: T,
    pub m21: T,
    pub m22: T,
    pub m23: T,
    pub m24: T,
    pub m31: T,
    pub m32: T,
    pub m33: T,
    pub m34: T,
    pub m41: T,
    pub m42: T,
    pub m43: T,
    pub m44: T,
}

#[generate_trait]
pub impl Mat4FImpl<T, +Add<T>, +Mul<T>, +Copy<T>, +Drop<T>> of Mat4FTrait<T> {
    fn from_array(data: [T; 16]) -> Mat4F<T> {
        let [m11, m12, m13, m14, m21, m22, m23, m24, m31, m32, m33, m34, m41, m42, m43, m44] = data;
        Mat4F { m11, m12, m13, m14, m21, m22, m23, m24, m31, m32, m33, m34, m41, m42, m43, m44 }
    }
    fn to_array(self: Mat4F<T>) -> [T; 16] {
        [
            self.m11, self.m12, self.m13, self.m14, self.m21, self.m22, self.m23, self.m24,
            self.m31, self.m32, self.m33, self.m34, self.m41, self.m42, self.m43, self.m44,
        ]
    }
    fn mul_mat(self: Mat4F<T>, rhs: Mat4F<T>) -> Mat4F<T> {
        let (a, b) = (self, rhs);
        Mat4F {
            m11: a.m11 * b.m11 + a.m12 * b.m21 + a.m13 * b.m31 + a.m14 * b.m41,
            m12: a.m11 * b.m12 + a.m12 * b.m22 + a.m13 * b.m32 + a.m14 * b.m42,
            m13: a.m11 * b.m13 + a.m12 * b.m23 + a.m13 * b.m33 + a.m14 * b.m43,
            m14: a.m11 * b.m14 + a.m12 * b.m24 + a.m13 * b.m34 + a.m14 * b.m44,
            m21: a.m21 * b.m11 + a.m22 * b.m21 + a.m23 * b.m31 + a.m24 * b.m41,
            m22: a.m21 * b.m12 + a.m22 * b.m22 + a.m23 * b.m32 + a.m24 * b.m42,
            m23: a.m21 * b.m13 + a.m22 * b.m23 + a.m23 * b.m33 + a.m24 * b.m43,
            m24: a.m21 * b.m14 + a.m22 * b.m24 + a.m23 * b.m34 + a.m24 * b.m44,
            m31: a.m31 * b.m11 + a.m32 * b.m21 + a.m33 * b.m31 + a.m34 * b.m41,
            m32: a.m31 * b.m12 + a.m32 * b.m22 + a.m33 * b.m32 + a.m34 * b.m42,
            m33: a.m31 * b.m13 + a.m32 * b.m23 + a.m33 * b.m33 + a.m34 * b.m43,
            m34: a.m31 * b.m14 + a.m32 * b.m24 + a.m33 * b.m34 + a.m34 * b.m44,
            m41: a.m41 * b.m11 + a.m42 * b.m21 + a.m43 * b.m31 + a.m44 * b.m41,
            m42: a.m41 * b.m12 + a.m42 * b.m22 + a.m43 * b.m32 + a.m44 * b.m42,
            m43: a.m41 * b.m13 + a.m42 * b.m23 + a.m43 * b.m33 + a.m44 * b.m43,
            m44: a.m41 * b.m14 + a.m42 * b.m24 + a.m43 * b.m34 + a.m44 * b.m44,
        }
    }
}
