// @generated by gen_tests.py — do not edit by hand.

use crate::fixed::Fixed;
use crate::vec3::Vec3Ops;

#[derive(Copy, Drop)]
pub struct Vec3GenAlways<T> {
    pub x: T,
    pub y: T,
    pub z: T,
}

pub impl Vec3GenAlwaysImpl<
    T, +Add<T>, +Sub<T>, +Mul<T>, +Neg<T>, +PartialEq<T>, +Copy<T>, +Drop<T>,
> of Vec3Ops<Vec3GenAlways<T>, T> {
    #[inline(always)]
    fn new(x: T, y: T, z: T) -> Vec3GenAlways<T> {
        Vec3GenAlways { x, y, z }
    }
    #[inline(always)]
    fn x(self: Vec3GenAlways<T>) -> T {
        self.x
    }
    #[inline(always)]
    fn y(self: Vec3GenAlways<T>) -> T {
        self.y
    }
    #[inline(always)]
    fn z(self: Vec3GenAlways<T>) -> T {
        self.z
    }
    #[inline(always)]
    fn add(self: Vec3GenAlways<T>, rhs: Vec3GenAlways<T>) -> Vec3GenAlways<T> {
        Vec3GenAlways { x: self.x + rhs.x, y: self.y + rhs.y, z: self.z + rhs.z }
    }
    #[inline(always)]
    fn sub(self: Vec3GenAlways<T>, rhs: Vec3GenAlways<T>) -> Vec3GenAlways<T> {
        Vec3GenAlways { x: self.x - rhs.x, y: self.y - rhs.y, z: self.z - rhs.z }
    }
    #[inline(always)]
    fn scale(self: Vec3GenAlways<T>, k: T) -> Vec3GenAlways<T> {
        Vec3GenAlways { x: self.x * k, y: self.y * k, z: self.z * k }
    }
    #[inline(always)]
    fn neg(self: Vec3GenAlways<T>) -> Vec3GenAlways<T> {
        Vec3GenAlways { x: -self.x, y: -self.y, z: -self.z }
    }
    #[inline(always)]
    fn dot(self: Vec3GenAlways<T>, rhs: Vec3GenAlways<T>) -> T {
        self.x * rhs.x + self.y * rhs.y + self.z * rhs.z
    }
    #[inline(always)]
    fn cross(self: Vec3GenAlways<T>, rhs: Vec3GenAlways<T>) -> Vec3GenAlways<T> {
        Vec3GenAlways {
            x: self.y * rhs.z - self.z * rhs.y,
            y: self.z * rhs.x - self.x * rhs.z,
            z: self.x * rhs.y - self.y * rhs.x,
        }
    }
    #[inline(always)]
    fn equals(self: Vec3GenAlways<T>, rhs: Vec3GenAlways<T>) -> bool {
        self.x == rhs.x && self.y == rhs.y && self.z == rhs.z
    }
}


#[derive(Copy, Drop)]
pub struct Vec3FixedAlways {
    pub x: Fixed,
    pub y: Fixed,
    pub z: Fixed,
}

pub impl Vec3FixedAlwaysImpl of Vec3Ops<Vec3FixedAlways, Fixed> {
    #[inline(always)]
    fn new(x: Fixed, y: Fixed, z: Fixed) -> Vec3FixedAlways {
        Vec3FixedAlways { x, y, z }
    }
    #[inline(always)]
    fn x(self: Vec3FixedAlways) -> Fixed {
        self.x
    }
    #[inline(always)]
    fn y(self: Vec3FixedAlways) -> Fixed {
        self.y
    }
    #[inline(always)]
    fn z(self: Vec3FixedAlways) -> Fixed {
        self.z
    }
    #[inline(always)]
    fn add(self: Vec3FixedAlways, rhs: Vec3FixedAlways) -> Vec3FixedAlways {
        Vec3FixedAlways { x: self.x + rhs.x, y: self.y + rhs.y, z: self.z + rhs.z }
    }
    #[inline(always)]
    fn sub(self: Vec3FixedAlways, rhs: Vec3FixedAlways) -> Vec3FixedAlways {
        Vec3FixedAlways { x: self.x - rhs.x, y: self.y - rhs.y, z: self.z - rhs.z }
    }
    #[inline(always)]
    fn scale(self: Vec3FixedAlways, k: Fixed) -> Vec3FixedAlways {
        Vec3FixedAlways { x: self.x * k, y: self.y * k, z: self.z * k }
    }
    #[inline(always)]
    fn neg(self: Vec3FixedAlways) -> Vec3FixedAlways {
        Vec3FixedAlways { x: -self.x, y: -self.y, z: -self.z }
    }
    #[inline(always)]
    fn dot(self: Vec3FixedAlways, rhs: Vec3FixedAlways) -> Fixed {
        self.x * rhs.x + self.y * rhs.y + self.z * rhs.z
    }
    #[inline(always)]
    fn cross(self: Vec3FixedAlways, rhs: Vec3FixedAlways) -> Vec3FixedAlways {
        Vec3FixedAlways {
            x: self.y * rhs.z - self.z * rhs.y,
            y: self.z * rhs.x - self.x * rhs.z,
            z: self.x * rhs.y - self.y * rhs.x,
        }
    }
    #[inline(always)]
    fn equals(self: Vec3FixedAlways, rhs: Vec3FixedAlways) -> bool {
        self.x == rhs.x && self.y == rhs.y && self.z == rhs.z
    }
}


#[derive(Copy, Drop)]
pub struct Vec3GenNever<T> {
    pub x: T,
    pub y: T,
    pub z: T,
}

pub impl Vec3GenNeverImpl<
    T, +Add<T>, +Sub<T>, +Mul<T>, +Neg<T>, +PartialEq<T>, +Copy<T>, +Drop<T>,
> of Vec3Ops<Vec3GenNever<T>, T> {
    fn new(x: T, y: T, z: T) -> Vec3GenNever<T> {
        Vec3GenNever { x, y, z }
    }
    fn x(self: Vec3GenNever<T>) -> T {
        self.x
    }
    fn y(self: Vec3GenNever<T>) -> T {
        self.y
    }
    fn z(self: Vec3GenNever<T>) -> T {
        self.z
    }
    #[inline(never)]
    fn add(self: Vec3GenNever<T>, rhs: Vec3GenNever<T>) -> Vec3GenNever<T> {
        Vec3GenNever { x: self.x + rhs.x, y: self.y + rhs.y, z: self.z + rhs.z }
    }
    #[inline(never)]
    fn sub(self: Vec3GenNever<T>, rhs: Vec3GenNever<T>) -> Vec3GenNever<T> {
        Vec3GenNever { x: self.x - rhs.x, y: self.y - rhs.y, z: self.z - rhs.z }
    }
    #[inline(never)]
    fn scale(self: Vec3GenNever<T>, k: T) -> Vec3GenNever<T> {
        Vec3GenNever { x: self.x * k, y: self.y * k, z: self.z * k }
    }
    #[inline(never)]
    fn neg(self: Vec3GenNever<T>) -> Vec3GenNever<T> {
        Vec3GenNever { x: -self.x, y: -self.y, z: -self.z }
    }
    #[inline(never)]
    fn dot(self: Vec3GenNever<T>, rhs: Vec3GenNever<T>) -> T {
        self.x * rhs.x + self.y * rhs.y + self.z * rhs.z
    }
    #[inline(never)]
    fn cross(self: Vec3GenNever<T>, rhs: Vec3GenNever<T>) -> Vec3GenNever<T> {
        Vec3GenNever {
            x: self.y * rhs.z - self.z * rhs.y,
            y: self.z * rhs.x - self.x * rhs.z,
            z: self.x * rhs.y - self.y * rhs.x,
        }
    }
    #[inline(never)]
    fn equals(self: Vec3GenNever<T>, rhs: Vec3GenNever<T>) -> bool {
        self.x == rhs.x && self.y == rhs.y && self.z == rhs.z
    }
}


#[derive(Copy, Drop)]
pub struct Vec3FixedNever {
    pub x: Fixed,
    pub y: Fixed,
    pub z: Fixed,
}

pub impl Vec3FixedNeverImpl of Vec3Ops<Vec3FixedNever, Fixed> {
    fn new(x: Fixed, y: Fixed, z: Fixed) -> Vec3FixedNever {
        Vec3FixedNever { x, y, z }
    }
    fn x(self: Vec3FixedNever) -> Fixed {
        self.x
    }
    fn y(self: Vec3FixedNever) -> Fixed {
        self.y
    }
    fn z(self: Vec3FixedNever) -> Fixed {
        self.z
    }
    #[inline(never)]
    fn add(self: Vec3FixedNever, rhs: Vec3FixedNever) -> Vec3FixedNever {
        Vec3FixedNever { x: self.x + rhs.x, y: self.y + rhs.y, z: self.z + rhs.z }
    }
    #[inline(never)]
    fn sub(self: Vec3FixedNever, rhs: Vec3FixedNever) -> Vec3FixedNever {
        Vec3FixedNever { x: self.x - rhs.x, y: self.y - rhs.y, z: self.z - rhs.z }
    }
    #[inline(never)]
    fn scale(self: Vec3FixedNever, k: Fixed) -> Vec3FixedNever {
        Vec3FixedNever { x: self.x * k, y: self.y * k, z: self.z * k }
    }
    #[inline(never)]
    fn neg(self: Vec3FixedNever) -> Vec3FixedNever {
        Vec3FixedNever { x: -self.x, y: -self.y, z: -self.z }
    }
    #[inline(never)]
    fn dot(self: Vec3FixedNever, rhs: Vec3FixedNever) -> Fixed {
        self.x * rhs.x + self.y * rhs.y + self.z * rhs.z
    }
    #[inline(never)]
    fn cross(self: Vec3FixedNever, rhs: Vec3FixedNever) -> Vec3FixedNever {
        Vec3FixedNever {
            x: self.y * rhs.z - self.z * rhs.y,
            y: self.z * rhs.x - self.x * rhs.z,
            z: self.x * rhs.y - self.y * rhs.x,
        }
    }
    #[inline(never)]
    fn equals(self: Vec3FixedNever, rhs: Vec3FixedNever) -> bool {
        self.x == rhs.x && self.y == rhs.y && self.z == rhs.z
    }
}


#[derive(Copy, Drop)]
pub struct Vec3FixedDefault {
    pub x: Fixed,
    pub y: Fixed,
    pub z: Fixed,
}

pub impl Vec3FixedDefaultImpl of Vec3Ops<Vec3FixedDefault, Fixed> {
    fn new(x: Fixed, y: Fixed, z: Fixed) -> Vec3FixedDefault {
        Vec3FixedDefault { x, y, z }
    }
    fn x(self: Vec3FixedDefault) -> Fixed {
        self.x
    }
    fn y(self: Vec3FixedDefault) -> Fixed {
        self.y
    }
    fn z(self: Vec3FixedDefault) -> Fixed {
        self.z
    }
    fn add(self: Vec3FixedDefault, rhs: Vec3FixedDefault) -> Vec3FixedDefault {
        Vec3FixedDefault { x: self.x + rhs.x, y: self.y + rhs.y, z: self.z + rhs.z }
    }
    fn sub(self: Vec3FixedDefault, rhs: Vec3FixedDefault) -> Vec3FixedDefault {
        Vec3FixedDefault { x: self.x - rhs.x, y: self.y - rhs.y, z: self.z - rhs.z }
    }
    fn scale(self: Vec3FixedDefault, k: Fixed) -> Vec3FixedDefault {
        Vec3FixedDefault { x: self.x * k, y: self.y * k, z: self.z * k }
    }
    fn neg(self: Vec3FixedDefault) -> Vec3FixedDefault {
        Vec3FixedDefault { x: -self.x, y: -self.y, z: -self.z }
    }
    fn dot(self: Vec3FixedDefault, rhs: Vec3FixedDefault) -> Fixed {
        self.x * rhs.x + self.y * rhs.y + self.z * rhs.z
    }
    fn cross(self: Vec3FixedDefault, rhs: Vec3FixedDefault) -> Vec3FixedDefault {
        Vec3FixedDefault {
            x: self.y * rhs.z - self.z * rhs.y,
            y: self.z * rhs.x - self.x * rhs.z,
            z: self.x * rhs.y - self.y * rhs.x,
        }
    }
    fn equals(self: Vec3FixedDefault, rhs: Vec3FixedDefault) -> bool {
        self.x == rhs.x && self.y == rhs.y && self.z == rhs.z
    }
}
