//! GENERATED by `tools/gen_per_type.py` - do not edit by hand.
//!
//! Per-type bitwise operators: `bitwise_<T>` groups (same input/assertion shape as `arith_<T>`,
//! so net figures are directly comparable).

use core::traits::{BitAnd, BitNot, BitOr, BitXor};

pub fn and<T, +BitAnd<T>>(a: T, b: T) -> T {
    a & b
}
pub fn or<T, +BitOr<T>>(a: T, b: T) -> T {
    a | b
}
pub fn xor<T, +BitXor<T>>(a: T, b: T) -> T {
    a ^ b
}
pub fn not<T, +BitNot<T>>(a: T) -> T {
    ~a
}
/// `a & b`, `a | b` and `a ^ b` at once: the underlying libfunc returns the three results, does
/// the compiler share the builtin call?
pub fn and_or_xor<T, +BitAnd<T>, +BitOr<T>, +BitXor<T>, +Copy<T>, +Drop<T>>(
    a: T, b: T,
) -> (T, T, T) {
    (a & b, a | b, a ^ b)
}

#[cfg(test)]
mod tests {
    use harness::black_box;
    use super::*;

    #[test]
    #[inline(never)]
    fn bench_bitwise_u8__baseline() {
        let a: u8 = black_box(0x23_u8);
        let b: u8 = black_box(0x7_u8);
        let _ = b;
        assert!(a == 0x23_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u8__and() {
        let a: u8 = black_box(0x23_u8);
        let b: u8 = black_box(0x7_u8);
        assert!(and(a, b) == 0x3_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u8__or() {
        let a: u8 = black_box(0x23_u8);
        let b: u8 = black_box(0x7_u8);
        assert!(or(a, b) == 0x27_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u8__xor() {
        let a: u8 = black_box(0x23_u8);
        let b: u8 = black_box(0x7_u8);
        assert!(xor(a, b) == 0x24_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u8__not() {
        let a: u8 = black_box(0x23_u8);
        let b: u8 = black_box(0x7_u8);
        let _ = b;
        assert!(not(a) == 0xdc_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u8__and_or_xor_x3_assert() {
        let a: u8 = black_box(0x23_u8);
        let b: u8 = black_box(0x7_u8);
        let (x, y, z) = and_or_xor(a, b);
        assert!(x == 0x3_u8);
        assert!(y == 0x27_u8);
        assert!(z == 0x24_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u16__baseline() {
        let a: u16 = black_box(0x4d2_u16);
        let b: u16 = black_box(0x32_u16);
        let _ = b;
        assert!(a == 0x4d2_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u16__and() {
        let a: u16 = black_box(0x4d2_u16);
        let b: u16 = black_box(0x32_u16);
        assert!(and(a, b) == 0x12_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u16__or() {
        let a: u16 = black_box(0x4d2_u16);
        let b: u16 = black_box(0x32_u16);
        assert!(or(a, b) == 0x4f2_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u16__xor() {
        let a: u16 = black_box(0x4d2_u16);
        let b: u16 = black_box(0x32_u16);
        assert!(xor(a, b) == 0x4e0_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u16__not() {
        let a: u16 = black_box(0x4d2_u16);
        let b: u16 = black_box(0x32_u16);
        let _ = b;
        assert!(not(a) == 0xfb2d_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u16__and_or_xor_x3_assert() {
        let a: u16 = black_box(0x4d2_u16);
        let b: u16 = black_box(0x32_u16);
        let (x, y, z) = and_or_xor(a, b);
        assert!(x == 0x12_u16);
        assert!(y == 0x4f2_u16);
        assert!(z == 0x4e0_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u32__baseline() {
        let a: u32 = black_box(0x186a0_u32);
        let b: u32 = black_box(0x9c40_u32);
        let _ = b;
        assert!(a == 0x186a0_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u32__and() {
        let a: u32 = black_box(0x186a0_u32);
        let b: u32 = black_box(0x9c40_u32);
        assert!(and(a, b) == 0x8400_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u32__or() {
        let a: u32 = black_box(0x186a0_u32);
        let b: u32 = black_box(0x9c40_u32);
        assert!(or(a, b) == 0x19ee0_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u32__xor() {
        let a: u32 = black_box(0x186a0_u32);
        let b: u32 = black_box(0x9c40_u32);
        assert!(xor(a, b) == 0x11ae0_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u32__not() {
        let a: u32 = black_box(0x186a0_u32);
        let b: u32 = black_box(0x9c40_u32);
        let _ = b;
        assert!(not(a) == 0xfffe795f_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u32__and_or_xor_x3_assert() {
        let a: u32 = black_box(0x186a0_u32);
        let b: u32 = black_box(0x9c40_u32);
        let (x, y, z) = and_or_xor(a, b);
        assert!(x == 0x8400_u32);
        assert!(y == 0x19ee0_u32);
        assert!(z == 0x11ae0_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u64__baseline() {
        let a: u64 = black_box(0x12a05f200_u64);
        let b: u64 = black_box(0xb2d05e00_u64);
        let _ = b;
        assert!(a == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u64__and() {
        let a: u64 = black_box(0x12a05f200_u64);
        let b: u64 = black_box(0xb2d05e00_u64);
        assert!(and(a, b) == 0x22005200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u64__or() {
        let a: u64 = black_box(0x12a05f200_u64);
        let b: u64 = black_box(0xb2d05e00_u64);
        assert!(or(a, b) == 0x1bad5fe00_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u64__xor() {
        let a: u64 = black_box(0x12a05f200_u64);
        let b: u64 = black_box(0xb2d05e00_u64);
        assert!(xor(a, b) == 0x198d5ac00_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u64__not() {
        let a: u64 = black_box(0x12a05f200_u64);
        let b: u64 = black_box(0xb2d05e00_u64);
        let _ = b;
        assert!(not(a) == 0xfffffffed5fa0dff_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u64__and_or_xor_x3_assert() {
        let a: u64 = black_box(0x12a05f200_u64);
        let b: u64 = black_box(0xb2d05e00_u64);
        let (x, y, z) = and_or_xor(a, b);
        assert!(x == 0x22005200_u64);
        assert!(y == 0x1bad5fe00_u64);
        assert!(z == 0x198d5ac00_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u128__baseline() {
        let a: u128 = black_box(0x10000000000000000000003039_u128);
        let b: u128 = black_box(0x8000005_u128);
        let _ = b;
        assert!(a == 0x10000000000000000000003039_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u128__and() {
        let a: u128 = black_box(0x10000000000000000000003039_u128);
        let b: u128 = black_box(0x8000005_u128);
        assert!(and(a, b) == 0x1_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u128__or() {
        let a: u128 = black_box(0x10000000000000000000003039_u128);
        let b: u128 = black_box(0x8000005_u128);
        assert!(or(a, b) == 0x1000000000000000000800303d_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u128__xor() {
        let a: u128 = black_box(0x10000000000000000000003039_u128);
        let b: u128 = black_box(0x8000005_u128);
        assert!(xor(a, b) == 0x1000000000000000000800303c_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u128__not() {
        let a: u128 = black_box(0x10000000000000000000003039_u128);
        let b: u128 = black_box(0x8000005_u128);
        let _ = b;
        assert!(not(a) == 0xffffffefffffffffffffffffffffcfc6_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u128__and_or_xor_x3_assert() {
        let a: u128 = black_box(0x10000000000000000000003039_u128);
        let b: u128 = black_box(0x8000005_u128);
        let (x, y, z) = and_or_xor(a, b);
        assert!(x == 0x1_u128);
        assert!(y == 0x1000000000000000000800303d_u128);
        assert!(z == 0x1000000000000000000800303c_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u256__baseline() {
        let a: u256 = black_box(0x100000000000000000000000010000000000000000000000007_u256);
        let b: u256 = black_box(0x10000000003_u256);
        let _ = b;
        assert!(a == 0x100000000000000000000000010000000000000000000000007_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u256__and() {
        let a: u256 = black_box(0x100000000000000000000000010000000000000000000000007_u256);
        let b: u256 = black_box(0x10000000003_u256);
        assert!(and(a, b) == 0x3_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u256__or() {
        let a: u256 = black_box(0x100000000000000000000000010000000000000000000000007_u256);
        let b: u256 = black_box(0x10000000003_u256);
        assert!(or(a, b) == 0x100000000000000000000000010000000000000010000000007_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u256__xor() {
        let a: u256 = black_box(0x100000000000000000000000010000000000000000000000007_u256);
        let b: u256 = black_box(0x10000000003_u256);
        assert!(xor(a, b) == 0x100000000000000000000000010000000000000010000000004_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u256__not() {
        let a: u256 = black_box(0x100000000000000000000000010000000000000000000000007_u256);
        let b: u256 = black_box(0x10000000003_u256);
        let _ = b;
        assert!(not(a) == 0xfffffffffffffeffffffffffffffffffffffffeffffffffffffffffffffffff8_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_bitwise_u256__and_or_xor_x3_assert() {
        let a: u256 = black_box(0x100000000000000000000000010000000000000000000000007_u256);
        let b: u256 = black_box(0x10000000003_u256);
        let (x, y, z) = and_or_xor(a, b);
        assert!(x == 0x3_u256);
        assert!(y == 0x100000000000000000000000010000000000000010000000007_u256);
        assert!(z == 0x100000000000000000000000010000000000000010000000004_u256);
    }
}
