//! GENERATED by `tools/gen_conversions.py` - do not edit by hand.
//!
//! Integer conversions: `conv_from_<T>` groups. The baseline asserts the black-boxed source value,
//! variants assert the converted value (in the destination type).

pub fn into<S, D, +Into<S, D>>(v: S) -> D {
    v.into()
}
pub fn try_into<S, D, +TryInto<S, D>>(v: S) -> D {
    v.try_into().unwrap()
}

/// bool -> integer: corelib only offers `bool -> felt252`; integers need a branch or a narrowing.
pub fn bool_to_u64_branch(c: bool) -> u64 {
    if c {
        1
    } else {
        0
    }
}
pub fn bool_to_u64_via_felt(c: bool) -> u64 {
    let f: felt252 = c.into();
    f.try_into().unwrap()
}
pub fn bool_to_felt(c: bool) -> felt252 {
    c.into()
}

#[cfg(test)]
mod tests {
    use harness::black_box;
    use super::{bool_to_felt, bool_to_u64_branch, bool_to_u64_via_felt, into, try_into};

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__baseline() {
        let v: u8 = black_box(0xc8_u8);
        assert!(v == 0xc8_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_u16() {
        let v: u8 = black_box(0xc8_u8);
        let r: u16 = into(v);
        assert!(r == 0xc8_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_u32() {
        let v: u8 = black_box(0xc8_u8);
        let r: u32 = into(v);
        assert!(r == 0xc8_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_u64() {
        let v: u8 = black_box(0xc8_u8);
        let r: u64 = into(v);
        assert!(r == 0xc8_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_u128() {
        let v: u8 = black_box(0xc8_u8);
        let r: u128 = into(v);
        assert!(r == 0xc8_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_u256() {
        let v: u8 = black_box(0xc8_u8);
        let r: u256 = into(v);
        assert!(r == 0xc8_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_felt252() {
        let v: u8 = black_box(0xc8_u8);
        let r: felt252 = into(v);
        assert!(r == 0xc8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_i16() {
        let v: u8 = black_box(0xc8_u8);
        let r: i16 = into(v);
        assert!(r == 0xc8_i16);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__into_i128() {
        let v: u8 = black_box(0xc8_u8);
        let r: i128 = into(v);
        assert!(r == 0xc8_i128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u8__try_into_i8() {
        let v: u8 = black_box(0x64_u8);
        let r: i8 = try_into(v);
        assert!(r == 0x64_i8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__baseline() {
        let v: u32 = black_box(0x11170_u32);
        assert!(v == 0x11170_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__into_u64() {
        let v: u32 = black_box(0x11170_u32);
        let r: u64 = into(v);
        assert!(r == 0x11170_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__into_u128() {
        let v: u32 = black_box(0x11170_u32);
        let r: u128 = into(v);
        assert!(r == 0x11170_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__into_u256() {
        let v: u32 = black_box(0x11170_u32);
        let r: u256 = into(v);
        assert!(r == 0x11170_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__into_felt252() {
        let v: u32 = black_box(0x11170_u32);
        let r: felt252 = into(v);
        assert!(r == 0x11170);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__into_i64() {
        let v: u32 = black_box(0x11170_u32);
        let r: i64 = into(v);
        assert!(r == 0x11170_i64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__try_into_u8() {
        let v: u32 = black_box(0xc8_u32);
        let r: u8 = try_into(v);
        assert!(r == 0xc8_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__try_into_u16() {
        let v: u32 = black_box(0xea60_u32);
        let r: u16 = try_into(v);
        assert!(r == 0xea60_u16);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u32__try_into_i32() {
        let v: u32 = black_box(0x11170_u32);
        let r: i32 = try_into(v);
        assert!(r == 0x11170_i32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__baseline() {
        let v: u64 = black_box(0x12a05f200_u64);
        assert!(v == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__into_u128() {
        let v: u64 = black_box(0x12a05f200_u64);
        let r: u128 = into(v);
        assert!(r == 0x12a05f200_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__into_u256() {
        let v: u64 = black_box(0x12a05f200_u64);
        let r: u256 = into(v);
        assert!(r == 0x12a05f200_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__into_felt252() {
        let v: u64 = black_box(0x12a05f200_u64);
        let r: felt252 = into(v);
        assert!(r == 0x12a05f200);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__into_i128() {
        let v: u64 = black_box(0x12a05f200_u64);
        let r: i128 = into(v);
        assert!(r == 0x12a05f200_i128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__try_into_u8() {
        let v: u64 = black_box(0xc8_u64);
        let r: u8 = try_into(v);
        assert!(r == 0xc8_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__try_into_u32() {
        let v: u64 = black_box(0x11170_u64);
        let r: u32 = try_into(v);
        assert!(r == 0x11170_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u64__try_into_i64() {
        let v: u64 = black_box(0x12a05f200_u64);
        let r: i64 = try_into(v);
        assert!(r == 0x12a05f200_i64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__baseline() {
        let v: u128 = black_box(0x10000000000000000000000005_u128);
        assert!(v == 0x10000000000000000000000005_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__into_u256() {
        let v: u128 = black_box(0x10000000000000000000000005_u128);
        let r: u256 = into(v);
        assert!(r == 0x10000000000000000000000005_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__into_felt252() {
        let v: u128 = black_box(0x10000000000000000000000005_u128);
        let r: felt252 = into(v);
        assert!(r == 0x10000000000000000000000005);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__try_into_u8() {
        let v: u128 = black_box(0xc8_u128);
        let r: u8 = try_into(v);
        assert!(r == 0xc8_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__try_into_u32() {
        let v: u128 = black_box(0x11170_u128);
        let r: u32 = try_into(v);
        assert!(r == 0x11170_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__try_into_u64() {
        let v: u128 = black_box(0x12a05f200_u128);
        let r: u64 = try_into(v);
        assert!(r == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u128__try_into_i128() {
        let v: u128 = black_box(0x10000000000000000000000005_u128);
        let r: i128 = try_into(v);
        assert!(r == 0x10000000000000000000000005_i128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u256__baseline() {
        let v: u256 = black_box(0x100000000000000000000000000000000000000000000000005_u256);
        assert!(v == 0x100000000000000000000000000000000000000000000000005_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u256__try_into_felt252() {
        let v: u256 = black_box(0x100000000000000000000000000000000000000000000000005_u256);
        let r: felt252 = try_into(v);
        assert!(r == 0x100000000000000000000000000000000000000000000000005);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u256__try_into_u128() {
        let v: u256 = black_box(0x10000000000000000000000005_u256);
        let r: u128 = try_into(v);
        assert!(r == 0x10000000000000000000000005_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u256__try_into_u64() {
        let v: u256 = black_box(0x12a05f200_u256);
        let r: u64 = try_into(v);
        assert!(r == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_u256__try_into_u8() {
        let v: u256 = black_box(0xc8_u256);
        let r: u8 = try_into(v);
        assert!(r == 0xc8_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__baseline() {
        let v: felt252 = black_box(0x100000000000000000000000000000000000000000000000005);
        assert!(v == 0x100000000000000000000000000000000000000000000000005);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__into_u256() {
        let v: felt252 = black_box(0x100000000000000000000000000000000000000000000000005);
        let r: u256 = into(v);
        assert!(r == 0x100000000000000000000000000000000000000000000000005_u256);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__try_into_u128() {
        let v: felt252 = black_box(0x10000000000000000000000005);
        let r: u128 = try_into(v);
        assert!(r == 0x10000000000000000000000005_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__try_into_u64() {
        let v: felt252 = black_box(0x12a05f200);
        let r: u64 = try_into(v);
        assert!(r == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__try_into_u32() {
        let v: felt252 = black_box(0x11170);
        let r: u32 = try_into(v);
        assert!(r == 0x11170_u32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__try_into_u8() {
        let v: felt252 = black_box(0xc8);
        let r: u8 = try_into(v);
        assert!(r == 0xc8_u8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__try_into_i64() {
        let v: felt252 = black_box(-0x12a05f200);
        let r: i64 = try_into(v);
        assert!(r == -0x12a05f200_i64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_felt252__try_into_i128() {
        let v: felt252 = black_box(-0x10000000000000000000000005);
        let r: i128 = try_into(v);
        assert!(r == -0x10000000000000000000000005_i128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__baseline() {
        let v: i64 = black_box(-0x12a05f200_i64);
        assert!(v == -0x12a05f200_i64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__into_felt252() {
        let v: i64 = black_box(-0x12a05f200_i64);
        let r: felt252 = into(v);
        assert!(r == -0x12a05f200);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__into_i128() {
        let v: i64 = black_box(-0x12a05f200_i64);
        let r: i128 = into(v);
        assert!(r == -0x12a05f200_i128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__try_into_i32() {
        let v: i64 = black_box(-0x11170_i64);
        let r: i32 = try_into(v);
        assert!(r == -0x11170_i32);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__try_into_i8() {
        let v: i64 = black_box(-0x64_i64);
        let r: i8 = try_into(v);
        assert!(r == -0x64_i8);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__try_into_u64() {
        let v: i64 = black_box(0x12a05f200_i64);
        let r: u64 = try_into(v);
        assert!(r == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i64__try_into_u128() {
        let v: i64 = black_box(0x12a05f200_i64);
        let r: u128 = try_into(v);
        assert!(r == 0x12a05f200_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i128__baseline() {
        let v: i128 = black_box(-0x10000000000000000000000005_i128);
        assert!(v == -0x10000000000000000000000005_i128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i128__into_felt252() {
        let v: i128 = black_box(-0x10000000000000000000000005_i128);
        let r: felt252 = into(v);
        assert!(r == -0x10000000000000000000000005);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i128__try_into_i64() {
        let v: i128 = black_box(-0x12a05f200_i128);
        let r: i64 = try_into(v);
        assert!(r == -0x12a05f200_i64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i128__try_into_u128() {
        let v: i128 = black_box(0x10000000000000000000000005_i128);
        let r: u128 = try_into(v);
        assert!(r == 0x10000000000000000000000005_u128);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_i128__try_into_u64() {
        let v: i128 = black_box(0x12a05f200_i128);
        let r: u64 = try_into(v);
        assert!(r == 0x12a05f200_u64);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_bool__baseline() {
        let c = black_box(true);
        assert!(c);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_bool__into_felt252() {
        let c = black_box(true);
        assert!(bool_to_felt(c) == 1);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_bool__if_else_u64() {
        let c = black_box(true);
        assert!(bool_to_u64_branch(c) == 1);
    }

    #[test]
    #[inline(never)]
    fn bench_conv_from_bool__into_felt252_try_into_u64() {
        let c = black_box(true);
        assert!(bool_to_u64_via_felt(c) == 1);
    }
}
